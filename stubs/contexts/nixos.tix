# Auto-generated NixOS option type stubs.
# Generated by: tix gen-stubs nixos
#
# Re-generate with: tix gen-stubs nixos -o <this-file>
#
# Use in tix.toml as a context stub to get typed config:
#
#   [context.nixos]
#   paths = ["modules/**/*.nix"]
#   stubs = ["./path/to/this-file.tix"]

type NixosConfig = {
  appstream: {
    enable: bool,
    ...
  },
  assertions: [{ ... }],
  boot: {
    bcache: {
      enable: bool,
      ...
    },
    bcachefs: {
      modulePackage: Derivation,
      package: Derivation,
      ...
    },
    binfmt: {
      addEmulatedSystemsToNixSandbox: bool,
      emulatedSystems: [string],
      preferStaticEmulators: bool,
      registrations: { _: { ... } },
      ...
    },
    binfmtMiscRegistrations: { _: { ... } },
    blacklistedKernelModules: { _: bool },
    bootMount: { ... },
    bootspec: {
      enable: bool,
      enableValidation: bool,
      extensions: { _: { ... } },
      filename: { ... },
      package: Derivation,
      validator: { ... },
      writer: { ... },
      ...
    },
    cleanTmpDir: bool,
    consoleLogLevel: int,
    copyKernels: bool,
    crashDump: {
      enable: bool,
      kernelParams: [string],
      reservedMemory: string,
      ...
    },
    devShmSize: string,
    devSize: string,
    earlyVconsoleSetup: bool,
    enableContainers: bool,
    extraGrubEntries: string,
    extraGrubEntriesBeforeNixos: bool,
    extraModprobeConfig: string,
    extraModulePackages: [Derivation],
    extraSystemdUnitPaths: [string],
    extraTTYs: { ... },
    growPartition: bool,
    grubDevice: string,
    grubSplashImage: path | null,
    hardwareScan: bool,
    initrd: {
      allowMissingModules: bool,
      availableKernelModules: { _: bool },
      checkJournalingFS: bool,
      clevis: {
        devices: { ... },
        enable: bool,
        package: Derivation,
        useTang: bool,
        ...
      },
      compressor: string | { ... },
      compressorArgs: [string] | null,
      enable: bool,
      extraFiles: { _: { ... } },
      extraFirmwarePaths: [string],
      extraUdevRulesCommands: string,
      extraUtilsCommands: string,
      extraUtilsCommandsTest: string,
      includeDefaultModules: bool,
      kernelModules: { _: bool },
      luks: {
        cryptoModules: { ... },
        devices: { ... },
        enable: { ... },
        fido2Support: bool,
        forceLuksSupportInInitrd: bool,
        gpgSupport: bool,
        mitigateDMAAttacks: bool,
        reusePassphrases: bool,
        yubikeySupport: bool,
        ...
      },
      mdadmConf: string,
      network: {
        enable: bool,
        flushBeforeStage2: bool,
        ifstate: { ... },
        openvpn: { ... },
        postCommands: string,
        ssh: { ... },
        udhcpc: { ... },
        ...
      },
      nix-store-veritysetup: {
        enable: bool,
        ...
      },
      osRelease: { ... },
      postDeviceCommands: string,
      postMountCommands: string,
      postResumeCommands: string,
      preDeviceCommands: string,
      preFailCommands: string,
      preLVMCommands: string,
      prepend: [string],
      secrets: { _: { ... } },
      services: {
        bcache: { ... },
        lvm: { ... },
        resolved: { ... },
        swraid: { ... },
        udev: { ... },
        ...
      },
      stage1Greeting: string,
      supportedFilesystems: { _: bool },
      systemd: {
        additionalUpstreamUnits: { ... },
        automounts: { ... },
        contents: { ... },
        dbus: { ... },
        dmVerity: { ... },
        emergencyAccess: { ... },
        enable: bool,
        enableTpm2: bool,
        extraBin: { ... },
        extraConfig: { ... },
        fido2: { ... },
        groups: { ... },
        initrdBin: { ... },
        managerEnvironment: { ... },
        mounts: { ... },
        network: { ... },
        package: Derivation,
        packages: { ... },
        paths: { ... },
        repart: { ... },
        root: string,
        services: { ... },
        settings: { ... },
        shell: { ... },
        slices: { ... },
        sockets: { ... },
        storePaths: { ... },
        strip: { ... },
        suppressedStorePaths: { ... },
        suppressedUnits: { ... },
        targets: { ... },
        timers: { ... },
        tmpfiles: { ... },
        tpm2: { ... },
        units: { ... },
        users: { ... },
        ...
      },
      unl0kr: {
        allowVendorDrivers: bool,
        enable: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      verbose: bool,
      ...
    },
    isContainer: bool,
    isNspawnContainer: bool,
    iscsi-initiator: {
      discoverPortal: string | null,
      extraConfig: string | null,
      extraConfigFile: string | null,
      extraIscsiCommands: string,
      logLevel: int,
      loginAll: bool,
      name: string | null,
      target: string | null,
      ...
    },
    kernel: {
      enable: bool,
      features: { ... },
      randstructSeed: string,
      sysctl: { ... },
      sysfs: { ... },
      ...
    },
    kernelModules: { _: bool },
    kernelPackages: { ... },
    kernelParams: [{ ... }],
    kernelPatches: [{ ... }],
    kexec: {
      enable: bool,
      ...
    },
    loader: {
      efi: {
        canTouchEfiVariables: bool,
        efiSysMountPoint: string,
        ...
      },
      external: {
        enable: bool,
        installHook: path,
        ...
      },
      generationsDir: {
        copyKernels: bool,
        enable: bool,
        ...
      },
      generic-extlinux-compatible: {
        configurationLimit: int,
        enable: bool,
        mirroredBoots: { ... },
        populateCmd: string,
        useGenerationDeviceTree: bool,
        ...
      },
      grub: {
        backgroundColor: { ... },
        bootDevice: { ... },
        configurationLimit: int,
        configurationName: string,
        copyKernels: bool,
        default: { ... },
        device: string,
        devices: { ... },
        efiInstallAsRemovable: bool,
        efiSupport: bool,
        enable: bool,
        enableCryptodisk: bool,
        entryOptions: { ... },
        extraConfig: string,
        extraEntries: string,
        extraEntriesBeforeNixOS: bool,
        extraFiles: { ... },
        extraGrubInstallArgs: { ... },
        extraInitrd: { ... },
        extraInstallCommands: string,
        extraPerEntryConfig: string,
        extraPrepareConfig: string,
        font: { ... },
        fontSize: { ... },
        forceInstall: bool,
        forcei686: bool,
        fsIdentifier: string,
        gfxmodeBios: string,
        gfxmodeEfi: string,
        gfxpayloadBios: string,
        gfxpayloadEfi: string,
        ipxe: { ... },
        memtest86: { ... },
        mirroredBoots: { ... },
        splashImage: { ... },
        splashMode: string,
        storePath: string,
        subEntryOptions: { ... },
        theme: { ... },
        timeout: { ... },
        timeoutStyle: string,
        trustedBoot: { ... },
        useOSProber: bool,
        users: { ... },
        version: int,
        zfsPackage: Derivation,
        zfsSupport: bool,
        ...
      },
      gummiboot: {
        enable: bool,
        timeout: { ... },
        ...
      },
      initScript: {
        enable: bool,
        ...
      },
      limine: {
        additionalFiles: { ... },
        biosDevice: string,
        biosSupport: bool,
        efiInstallAsRemovable: bool,
        efiSupport: bool,
        enable: bool,
        enableEditor: bool,
        enrollConfig: bool,
        extraConfig: string,
        extraEntries: string,
        force: bool,
        forceMbr: bool,
        maxGenerations: { ... },
        package: Derivation,
        panicOnChecksumMismatch: bool,
        partitionIndex: { ... },
        resolution: { ... },
        secureBoot: { ... },
        style: { ... },
        validateChecksums: bool,
        ...
      },
      raspberryPi: { ... },
      refind: {
        additionalFiles: { ... },
        efiInstallAsRemovable: bool,
        enable: bool,
        extraConfig: string,
        maxGenerations: { ... },
        package: Derivation,
        ...
      },
      supportsInitrdSecrets: bool,
      systemd-boot: {
        configurationLimit: { ... },
        consoleMode: string,
        editor: bool,
        edk2-uefi-shell: { ... },
        enable: bool,
        extraEntries: { ... },
        extraFiles: { ... },
        extraInstallCommands: string,
        graceful: bool,
        installDeviceTree: { ... },
        memtest86: { ... },
        netbootxyz: { ... },
        rebootForBitlocker: bool,
        sortKey: string,
        windows: { ... },
        xbootldrMountPoint: { ... },
        ...
      },
      timeout: int | null,
      ...
    },
    modprobeConfig: {
      enable: bool,
      useUbuntuModuleBlacklist: bool,
      ...
    },
    nixStoreMountOpts: [string],
    plymouth: {
      enable: bool,
      extraConfig: string,
      font: path,
      logo: path,
      package: Derivation,
      theme: string,
      themePackages: [Derivation],
      tpm2-totp: {
        enable: bool,
        package: Derivation,
        ...
      },
      ...
    },
    postBootCommands: string,
    readOnlyNixStore: { ... },
    resumeDevice: string,
    runSize: string,
    specialFileSystems: { _: { ... } },
    stage2Greeting: string,
    supportedFilesystems: { _: bool },
    swraid: {
      enable: bool,
      mdadmConf: string,
      ...
    },
    systemd: {
      services: { _: { ... } },
      sockets: { _: { ... } },
      targets: { _: { ... } },
      ...
    },
    systemdExecutable: string,
    tmp: {
      cleanOnBoot: bool,
      tmpfsHugeMemoryPages: string,
      tmpfsSize: string | { ... },
      useTmpfs: bool,
      useZram: bool,
      zramSettings: {
        compression-algorithm: string,
        fs-type: string,
        options: string,
        zram-size: string,
        ...
      },
      ...
    },
    tmpOnTmpfs: bool,
    tmpOnTmpfsSize: string | { ... },
    uki: {
      configFile: path,
      name: string,
      settings: { _: { ... } },
      tries: { ... } | null,
      version: string | null,
      ...
    },
    uvesafb: {
      enable: bool,
      gfx-mode: string,
      v86d: {
        package: Derivation,
        ...
      },
      ...
    },
    vesa: bool,
    zfs: {
      allowHibernation: bool,
      devNodes: path,
      enableLegacyCrypto: { ... },
      enableUnstable: { ... },
      enabled: bool,
      extraPools: [string],
      forceImportAll: bool,
      forceImportRoot: bool,
      modulePackage: Derivation,
      package: Derivation,
      passwordTimeout: int,
      pools: { _: { ... } },
      removeLinuxDRM: bool,
      requestEncryptionCredentials: bool | { ... },
      useKeyringForCredentials: bool,
      ...
    },
    ...
  },
  console: {
    colors: [{ ... }],
    earlySetup: bool,
    enable: bool,
    extraTTYs: { ... },
    font: string | path | null,
    keyMap: string | path,
    packages: [Derivation],
    useXkbConfig: bool,
    ...
  },
  containers: { _: {
  additionalCapabilities: { ... },
  allowedDevices: { ... },
  autoStart: bool,
  bindMounts: { ... },
  config: { ... },
  enableTun: bool,
  ephemeral: bool,
  extraFlags: { ... },
  extraVeths: { ... },
  flake: { ... },
  forwardPorts: { ... },
  hostAddress: { ... },
  hostAddress6: { ... },
  hostBridge: { ... },
  interfaces: { ... },
  localAddress: { ... },
  localAddress6: { ... },
  macvlans: { ... },
  networkNamespace: { ... },
  nixpkgs: path,
  path: path,
  pkgs: { ... },
  privateNetwork: bool,
  privateUsers: { ... },
  restartIfChanged: bool,
  specialArgs: { ... },
  timeoutStartSec: string,
  tmpfs: { ... },
  ...
} },
  docker-containers: { ... },
  documentation: {
    dev: {
      enable: bool,
      ...
    },
    doc: {
      enable: bool,
      ...
    },
    enable: bool,
    info: {
      enable: bool,
      ...
    },
    man: {
      cache: {
        enable: bool,
        generateAtRuntime: bool,
        ...
      },
      enable: bool,
      generateCaches: bool,
      man-db: {
        enable: bool,
        manualPages: path,
        package: Derivation,
        skipPackages: { ... },
        ...
      },
      mandoc: {
        cachePath: { ... },
        enable: bool,
        extraConfig: string,
        manPath: { ... },
        package: Derivation,
        settings: { ... },
        ...
      },
      manualPages: path,
      ...
    },
    nixos: {
      checkRedirects: bool,
      enable: bool,
      extraModuleSources: [{ ... }],
      extraModules: [{ ... }],
      includeAllModules: bool,
      options: {
        allowDocBook: { ... },
        splitBuild: bool,
        warningsAreErrors: bool,
        ...
      },
      ...
    },
    ...
  },
  dysnomia: { ... },
  ec2: {
    efi: { ... },
    hvm: { ... },
    zfs: {
      datasets: { _: { ... } },
      enable: { ... },
      ...
    },
    ...
  },
  environment: {
    binsh: path | null,
    blcr: {
      enable: { ... },
      ...
    },
    budgie: {
      excludePackages: [Derivation],
      ...
    },
    checkConfigurationOptions: bool,
    cinnamon: {
      excludePackages: [Derivation],
      ...
    },
    corePackages: [Derivation],
    cosmic: {
      excludePackages: [Derivation],
      ...
    },
    debuginfodServers: [string],
    defaultPackages: [Derivation],
    enableAllTerminfo: bool,
    enableDebugInfo: bool,
    enlightenment: {
      excludePackages: [Derivation],
      ...
    },
    etc: { _: { ... } },
    extraInit: string,
    extraOutputsToInstall: [string],
    extraSetup: string,
    freetds: { _: string },
    gnome: {
      excludePackages: [Derivation],
      ...
    },
    homeBinInPath: bool,
    interactiveShellInit: string,
    ldso: path | null,
    ldso32: path | null,
    localBinInPath: bool,
    loginShellInit: string,
    lxqt: {
      excludePackages: [Derivation],
      ...
    },
    mate: {
      excludePackages: [Derivation],
      ...
    },
    memoryAllocator: {
      provider: string,
      ...
    },
    noXlibs: { ... },
    pantheon: {
      excludePackages: [Derivation],
      ...
    },
    pathsToLink: [string],
    plasma6: {
      excludePackages: [Derivation],
      ...
    },
    profileRelativeEnvVars: { _: [string] },
    profileRelativeSessionVariables: { _: [string] },
    profiles: [string],
    sessionVariables: { _: { ... } | path | null },
    shell: {
      enable: bool,
      ...
    },
    shellAliases: { _: string | path | null },
    shellInit: string,
    shells: [Derivation | path],
    stub-ld: {
      enable: bool,
      ...
    },
    systemPackages: [Derivation],
    unixODBCDrivers: [Derivation],
    usrbinenv: path | null,
    variables: { _: { ... } | path | null },
    wordlist: {
      enable: bool,
      lists: { _: { ... } },
      ...
    },
    wvdial: {
      dialerDefaults: string,
      pppDefaults: string,
      ...
    },
    xfce: {
      excludePackages: [Derivation],
      ...
    },
    ...
  },
  fileSystems: { _: {
  autoFormat: bool,
  autoResize: bool,
  depends: { ... },
  device: { ... },
  enable: bool,
  encrypted: { ... },
  formatOptions: { ... },
  fsType: string,
  label: { ... },
  mountPoint: string,
  neededForBoot: bool,
  noCheck: bool,
  options: { ... },
  overlay: { ... },
  stratis: { ... },
  ...
} },
  fonts: {
    enableCoreFonts: { ... },
    enableDefaultFonts: bool,
    enableDefaultPackages: bool,
    enableFontDir: bool,
    enableGhostscriptFonts: bool,
    fontDir: {
      decompressFonts: bool,
      enable: bool,
      ...
    },
    fontconfig: {
      allowBitmaps: bool,
      allowType1: bool,
      antialias: bool,
      cache32Bit: bool,
      confPackages: [path],
      defaultFonts: {
        emoji: { ... },
        monospace: { ... },
        sansSerif: { ... },
        serif: { ... },
        ...
      },
      dpi: { ... },
      enable: bool,
      forceAutohint: { ... },
      hinting: {
        autohint: bool,
        enable: bool,
        style: string,
        ...
      },
      includeUserConf: bool,
      localConf: string,
      penultimate: { ... },
      renderMonoTTFAsBitmap: { ... },
      subpixel: {
        lcdfilter: string,
        rgba: string,
        ...
      },
      ultimate: {
        allowBitmaps: bool,
        allowType1: bool,
        enable: { ... },
        forceAutohint: { ... },
        preset: { ... },
        renderMonoTTFAsBitmap: { ... },
        substitutions: { ... },
        useEmbeddedBitmaps: bool,
        ...
      },
      useEmbeddedBitmaps: bool,
      ...
    },
    fonts: [path],
    optimizeForVeryHighDPI: { ... },
    packages: [path],
    ...
  },
  gtk: {
    iconCache: {
      enable: bool,
      ...
    },
    ...
  },
  hardware: {
    acpilight: {
      enable: bool,
      ...
    },
    alsa: {
      cardAliases: { _: { ... } },
      config: string,
      controls: { _: { ... } },
      defaultDevice: {
        capture: string,
        playback: string,
        ...
      },
      deviceAliases: { _: string },
      enable: bool,
      enableBluetooth: bool,
      enableOSSEmulation: bool,
      enablePersistence: bool,
      enableRecorder: bool,
      plugins: [Derivation],
      ...
    },
    amdgpu: {
      amdvlk: { ... },
      initrd: {
        enable: bool,
        ...
      },
      legacySupport: {
        enable: bool,
        ...
      },
      opencl: {
        enable: bool,
        ...
      },
      overdrive: {
        enable: bool,
        ppfeaturemask: string,
        ...
      },
      ...
    },
    apple: {
      touchBar: {
        enable: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      ...
    },
    bladeRF: {
      enable: bool,
      ...
    },
    block: {
      defaultScheduler: { ... } | null,
      defaultSchedulerExclude: { ... } | null,
      defaultSchedulerRotational: { ... } | null,
      scheduler: { _: { ... } },
      ...
    },
    bluetooth: {
      config: { _: { ... } },
      disabledPlugins: [string],
      enable: bool,
      extraConfig: { ... },
      hsphfpd: {
        enable: bool,
        ...
      },
      input: { _: { ... } },
      network: { _: { ... } },
      package: Derivation,
      powerOnBoot: bool,
      settings: { _: { ... } },
      ...
    },
    brightnessctl: { ... },
    brillo: {
      enable: bool,
      ...
    },
    bumblebee: {
      connectDisplay: bool,
      driver: string,
      enable: bool,
      group: string,
      pmMethod: string,
      ...
    },
    ckb: {
      enable: bool,
      package: Derivation,
      ...
    },
    ckb-next: {
      enable: bool,
      gid: int | null,
      package: Derivation,
      ...
    },
    coral: {
      pcie: {
        enable: bool,
        ...
      },
      usb: {
        enable: bool,
        ...
      },
      ...
    },
    cpu: {
      amd: {
        microcodePackage: Derivation,
        ryzen-smu: { ... },
        sev: { ... },
        sevGuest: { ... },
        updateMicrocode: bool,
        ...
      },
      intel: {
        microcodePackage: Derivation,
        sgx: { ... },
        updateMicrocode: bool,
        ...
      },
      x86: {
        msr: { ... },
        ...
      },
      ...
    },
    decklink: {
      enable: bool,
      ...
    },
    deviceTree: {
      base: { ... },
      dtbSource: path,
      dtboBuildExtraIncludePaths: [path],
      dtboBuildExtraPreprocessorFlags: [string],
      enable: bool,
      filter: string | null,
      kernelPackage: path,
      name: string | null,
      overlays: [{ ... }],
      package: path | null,
      ...
    },
    digitalbitbox: {
      enable: bool,
      package: Derivation,
      ...
    },
    display: {
      edid: {
        enable: bool,
        linuxhw: { ... },
        modelines: { ... },
        packages: { ... },
        ...
      },
      outputs: { _: { ... } },
      ...
    },
    enableAllFirmware: bool,
    enableAllHardware: bool,
    enableKSM: bool,
    enableRedistributableFirmware: bool,
    facetimehd: {
      enable: bool,
      withCalibration: bool,
      ...
    },
    facter: {
      debug: {
        nix-diff: Derivation,
        nvd: Derivation,
        ...
      },
      detected: {
        bluetooth: { ... },
        boot: { ... },
        camera: { ... },
        dhcp: { ... },
        fingerprint: { ... },
        graphics: { ... },
        networking: { ... },
        uefi: { ... },
        virtualisation: { ... },
        ...
      },
      enable: bool,
      report: { _: { ... } },
      reportPath: path | null,
      ...
    },
    fancontrol: {
      config: string,
      enable: bool,
      ...
    },
    firmware: [Derivation],
    firmwareCompression: string,
    flipperzero: {
      enable: bool,
      ...
    },
    flirc: {
      enable: bool,
      ...
    },
    fw-fanctrl: {
      config: { ... },
      disableBatteryTempCheck: bool,
      ectoolPackage: Derivation,
      enable: bool,
      package: Derivation,
      ...
    },
    gkraken: {
      enable: { ... },
      ...
    },
    glasgow: {
      enable: bool,
      ...
    },
    gpgSmartcards: {
      enable: bool,
      ...
    },
    graphics: {
      enable: bool,
      enable32Bit: bool,
      extraPackages: [Derivation],
      extraPackages32: [Derivation],
      package: Derivation,
      package32: Derivation,
      ...
    },
    hackrf: {
      enable: bool,
      ...
    },
    hid-fanatecff: {
      enable: bool,
      ...
    },
    i2c: {
      enable: bool,
      group: string,
      ...
    },
    infiniband: {
      enable: bool,
      guids: [string],
      ...
    },
    inputmodule: {
      enable: bool,
      ...
    },
    intel-gpu-tools: {
      enable: bool,
      ...
    },
    ipu6: {
      enable: bool,
      platform: string,
      ...
    },
    keyboard: {
      qmk: {
        enable: bool,
        keychronSupport: bool,
        ...
      },
      teck: {
        enable: bool,
        ...
      },
      uhk: {
        enable: bool,
        ...
      },
      zsa: {
        enable: bool,
        ...
      },
      ...
    },
    kryoflux: {
      enable: bool,
      package: Derivation,
      ...
    },
    ksm: {
      enable: bool,
      sleep: int | null,
      ...
    },
    ledger: {
      enable: bool,
      ...
    },
    libftdi: {
      enable: bool,
      package: Derivation,
      ...
    },
    libjaylink: {
      enable: bool,
      package: Derivation,
      ...
    },
    logitech: {
      enable: bool,
      enableGraphical: bool,
      lcd: {
        devices: { ... },
        enable: bool,
        startWhenNeeded: bool,
        ...
      },
      wireless: {
        enable: bool,
        enableGraphical: bool,
        ...
      },
      ...
    },
    mcelog: {
      enable: bool,
      ...
    },
    mwProCapture: {
      enable: bool,
      ...
    },
    new-lg4ff: {
      enable: bool,
      ...
    },
    nfc-nci: {
      enable: bool,
      enableIFD: bool,
      settings: { ... },
      ...
    },
    nitrokey: {
      enable: bool,
      ...
    },
    nvidia: {
      datacenter: {
        enable: bool,
        settings: { ... },
        ...
      },
      dynamicBoost: {
        enable: bool,
        ...
      },
      enabled: bool,
      forceFullCompositionPipeline: bool,
      gsp: {
        enable: bool,
        ...
      },
      modesetting: {
        enable: bool,
        ...
      },
      nvidiaPersistenced: bool,
      nvidiaSettings: bool,
      open: bool | null,
      package: { ... },
      powerManagement: {
        enable: bool,
        finegrained: bool,
        kernelSuspendNotifier: bool,
        ...
      },
      prime: {
        allowExternalGpu: bool,
        amdgpuBusId: { ... },
        intelBusId: { ... },
        nvidiaBusId: { ... },
        offload: { ... },
        reverseSync: { ... },
        sync: { ... },
        ...
      },
      videoAcceleration: bool,
      ...
    },
    nvidia-container-toolkit: {
      csv-files: [path],
      device-name-strategy: string,
      disable-hooks: [string],
      discovery-mode: string,
      enable: bool,
      enable-hooks: [string],
      extraArgs: [string],
      mount-nvidia-docker-1-directories: bool,
      mount-nvidia-executables: bool,
      mounts: [{ ... }],
      package: Derivation,
      suppressNvidiaDriverAssertion: bool,
      ...
    },
    nvidiaOptimus: {
      disable: bool,
      ...
    },
    onlykey: {
      enable: bool,
      ...
    },
    opengl: {
      driSupport: { ... },
      driSupport32Bit: bool,
      enable: bool,
      extraPackages: [Derivation],
      extraPackages32: [Derivation],
      package: Derivation,
      package32: Derivation,
      s3tcSupport: { ... },
      ...
    },
    openrazer: {
      batteryNotifier: { ... },
      devicesOffOnScreensaver: bool,
      enable: bool,
      keyStatistics: bool,
      mouseBatteryNotifier: { ... },
      syncEffectsEnabled: bool,
      users: [string],
      verboseLogging: bool,
      ...
    },
    opentabletdriver: {
      blacklistedKernelModules: [string],
      daemon: {
        enable: bool,
        ...
      },
      enable: bool,
      package: Derivation,
      ...
    },
    parallels: {
      autoMountShares: { ... },
      enable: bool,
      package: Derivation,
      ...
    },
    pcmcia: {
      config: path | null,
      enable: bool,
      firmware: [path],
      ...
    },
    printers: {
      ensureDefaultPrinter: { ... } | null,
      ensurePrinters: [{ ... }],
      ...
    },
    pulseaudio: { ... },
    raid: {
      HPSmartArray: {
        enable: bool,
        ...
      },
      ...
    },
    rasdaemon: {
      config: string,
      enable: bool,
      extraModules: [string],
      labels: string,
      mainboard: string,
      package: Derivation,
      record: bool,
      testing: bool,
      ...
    },
    rtl-sdr: {
      enable: bool,
      package: Derivation,
      ...
    },
    saleae-logic: {
      enable: bool,
      package: Derivation,
      ...
    },
    sane: {
      backends-package: Derivation,
      brscan4: {
        enable: bool,
        netDevices: { ... },
        ...
      },
      brscan5: {
        enable: bool,
        netDevices: { ... },
        ...
      },
      configDir: string,
      disabledDefaultBackends: [string],
      drivers: {
        scanSnap: { ... },
        ...
      },
      dsseries: {
        enable: bool,
        ...
      },
      enable: bool,
      extraBackends: [path],
      netConf: string,
      openFirewall: bool,
      snapshot: bool,
      ...
    },
    sata: {
      timeout: {
        deciSeconds: int,
        drives: { ... },
        enable: bool,
        ...
      },
      ...
    },
    sensor: {
      hddtemp: {
        dbEntries: { ... },
        drives: { ... },
        enable: bool,
        extraArgs: { ... },
        unit: string,
        ...
      },
      iio: {
        enable: bool,
        package: Derivation,
        ...
      },
      ...
    },
    sheep_net: {
      enable: bool,
      ...
    },
    spacenavd: {
      enable: bool,
      ...
    },
    steam-hardware: {
      enable: bool,
      ...
    },
    system76: {
      enableAll: bool,
      firmware-daemon: {
        enable: bool,
        ...
      },
      kernel-modules: {
        enable: bool,
        ...
      },
      power-daemon: {
        enable: bool,
        ...
      },
      ...
    },
    tenstorrent: {
      enable: bool,
      ...
    },
    trackpoint: {
      device: string,
      draghys: int,
      drift_time: int,
      emulateWheel: bool,
      enable: bool,
      ext_dev: bool,
      fakeButtons: bool,
      inertia: int,
      jenks: int,
      mindrag: int,
      press_to_select: bool,
      reach: int,
      sensitivity: int,
      skipback: bool,
      speed: int,
      thresh: int,
      upthresh: int,
      ztime: int,
      ...
    },
    tuxedo-drivers: {
      enable: bool,
      settings: {
        charging-priority: { ... },
        charging-profile: { ... },
        fn-lock: { ... },
        ...
      },
      ...
    },
    tuxedo-keyboard: { ... },
    tuxedo-rs: {
      enable: bool,
      tailor-gui: {
        enable: bool,
        ...
      },
      ...
    },
    u2f: { ... },
    ubertooth: {
      enable: bool,
      group: string,
      ...
    },
    uinput: {
      enable: bool,
      ...
    },
    uni-sync: {
      devices: [{ ... }],
      enable: bool,
      package: Derivation,
      ...
    },
    usb-modeswitch: {
      enable: bool,
      ...
    },
    usbStorage: {
      manageShutdown: bool,
      manageStartStop: bool,
      ...
    },
    usbWwan: { ... },
    video: {
      hidpi: {
        enable: { ... },
        ...
      },
      ...
    },
    wirelessRegulatoryDatabase: bool,
    wooting: {
      enable: bool,
      ...
    },
    xone: {
      enable: bool,
      ...
    },
    xow: { ... },
    xpad-noone: {
      enable: bool,
      ...
    },
    xpadneo: {
      enable: bool,
      quirks: { _: { ... } },
      rumbleAttenuation: { ... },
      settings: { _: { ... } },
      ...
    },
    ...
  },
  i18n: {
    consoleColors: [{ ... }],
    consoleFont: string | path | null,
    consoleKeyMap: string | path,
    consolePackages: [Derivation],
    consoleUseXkbConfig: bool,
    defaultCharset: string,
    defaultLocale: string,
    extraLocaleSettings: { _: string },
    extraLocales: [string] | string,
    glibcLocales: path,
    inputMethod: {
      enable: bool,
      enableGtk2: bool,
      enableGtk3: bool,
      enabled: string | null,
      fcitx: { ... },
      fcitx5: {
        addons: { ... },
        enableRimeData: { ... },
        ignoreUserConfig: bool,
        plasma6Support: { ... },
        quickPhrase: { ... },
        quickPhraseFiles: { ... },
        settings: { ... },
        waylandFrontend: bool,
        ...
      },
      ibus: {
        engines: { ... },
        panel: { ... },
        waylandFrontend: bool,
        ...
      },
      kime: {
        config: { ... },
        daemonModules: { ... },
        extraConfig: string,
        iconColor: string,
        ...
      },
      package: path | null,
      type: string | null,
      uim: {
        toolbar: string,
        ...
      },
      ...
    },
    localeCharsets: { _: string },
    supportedLocales: [string],
    ...
  },
  ids: {
    gids: { _: { ... } },
    uids: { _: { ... } },
    ...
  },
  image: {
    modules: { _: { ... } },
    ...
  },
  isSpecialisation: bool,
  jobs: { _: {
  after: { ... },
  aliases: { ... },
  before: { ... },
  bindsTo: { ... },
  confinement: { ... },
  conflicts: { ... },
  description: { ... },
  documentation: { ... },
  enable: bool,
  enableDefaultPath: bool,
  enableStrictShellChecks: bool,
  environment: { ... },
  jobScripts: { ... },
  name: string,
  notSocketActivated: bool,
  onFailure: { ... },
  onSuccess: { ... },
  overrideStrategy: string,
  partOf: { ... },
  path: { ... },
  postStart: string,
  postStop: string,
  preStart: string,
  preStop: string,
  reload: string,
  reloadIfChanged: bool,
  reloadTriggers: { ... },
  requiredBy: { ... },
  requires: { ... },
  requisite: { ... },
  restartIfChanged: bool,
  restartTriggers: { ... },
  runner: { ... },
  script: string,
  scriptArgs: string,
  serviceConfig: { ... },
  startAt: { ... },
  startLimitBurst: int,
  startLimitIntervalSec: int,
  stopIfChanged: bool,
  unitConfig: { ... },
  upheldBy: { ... },
  upholds: { ... },
  wantedBy: { ... },
  wants: { ... },
  ...
} },
  krb5: {
    appdefaults: { ... },
    capaths: { ... },
    config: { ... },
    domain_realm: { ... },
    extraConfig: { ... },
    kerberos: { ... },
    libdefaults: { ... },
    plugins: { ... },
    realms: { ... },
    ...
  },
  lib: { _: { ... } },
  location: {
    latitude: float,
    longitude: float,
    provider: string,
    ...
  },
  meta: {
    buildDocsInSandbox: bool,
    doc: path,
    maintainers: [{ ... }],
    teams: [{ ... }],
    ...
  },
  minifyStaticFiles: {
    script: {
      package: { ... },
      ...
    },
    style: {
      package: { ... },
      ...
    },
    svg: {
      package: { ... },
      ...
    },
    ...
  },
  nesting: {
    children: { ... },
    clone: { ... },
    ...
  },
  networking: {
    bonds: { _: { ... } },
    bridges: { _: { ... } },
    connman: { ... },
    defaultGateway: {
  address: string,
  interface: { ... },
  metric: { ... },
  source: { ... },
  ...
},
    defaultGateway6: {
  address: string,
  interface: { ... },
  metric: { ... },
  source: { ... },
  ...
},
    defaultGatewayWindowSize: int | null,
    dhcpcd: {
      IPv6rs: bool | null,
      allowInterfaces: [string] | null,
      allowSetuid: bool,
      denyInterfaces: [string],
      enable: bool,
      extraConfig: string,
      persistent: bool,
      runHook: string,
      setHostname: bool,
      wait: string,
      ...
    },
    dnsExtensionMechanism: bool,
    dnsSingleRequest: bool,
    domain: string | null,
    enableB43Firmware: bool,
    enableIPv6: bool,
    enableIntel2100BGFirmware: bool,
    enableIntel2200BGFirmware: bool,
    enableIntel3945ABGFirmware: bool,
    enableRT73Firmware: bool,
    enableRTL8192cFirmware: bool,
    enableRalinkFirmware: bool,
    extraHosts: string,
    extraResolvconfConf: string,
    firewall: {
      allInterfaces: { _: { ... } },
      allowPing: bool,
      allowedTCPPortRanges: [{ ... }],
      allowedTCPPorts: [{ ... }],
      allowedUDPPortRanges: [{ ... }],
      allowedUDPPorts: [{ ... }],
      autoLoadConntrackHelpers: bool,
      backend: string,
      checkReversePath: bool | string,
      connectionTrackingModules: [string],
      enable: bool,
      extraCommands: string,
      extraForwardRules: string,
      extraInputRules: string,
      extraPackages: [Derivation],
      extraReversePathFilterRules: string,
      extraStopCommands: string,
      filterForward: bool,
      interfaces: { _: { ... } },
      logRefusedConnections: bool,
      logRefusedPackets: bool,
      logRefusedUnicastsOnly: bool,
      logReversePathDrops: bool,
      package: Derivation,
      pingLimit: string | null,
      rejectPackets: bool,
      trustedInterfaces: [string],
      ...
    },
    fooOverUDP: { _: { ... } },
    fqdn: string,
    fqdnOrHostName: string,
    getaddrinfo: {
      enable: bool,
      label: { _: int },
      precedence: { _: int },
      reload: bool,
      scopev4: { _: int },
      ...
    },
    greTunnels: { _: { ... } },
    hostConf: { ... },
    hostFiles: [path],
    hostId: string | null,
    hostName: { ... },
    hosts: { _: [string] },
    ifstate: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    interfaces: { _: { ... } },
    ipips: { _: { ... } },
    iproute2: {
      enable: bool,
      rttablesExtraConfig: string,
      ...
    },
    ipvlans: { _: { ... } },
    jool: {
      enable: bool,
      nat64: { _: { ... } },
      siit: { _: { ... } },
      ...
    },
    localCommands: string,
    macvlans: { _: { ... } },
    modemmanager: {
      enable: bool,
      fccUnlockScripts: [{ ... }],
      package: Derivation,
      ...
    },
    nameservers: [string],
    nat: {
      dmzHost: string | null,
      enable: bool,
      enableIPv6: bool,
      externalIP: string | null,
      externalIPv6: string | null,
      externalInterface: string | null,
      extraCommands: string,
      extraStopCommands: string,
      forwardPorts: [{ ... }],
      internalIPs: [string],
      internalIPv6s: [string],
      internalInterfaces: [string],
      ...
    },
    networkmanager: {
      appendNameservers: [string],
      connectionConfig: { _: { ... } },
      dhcp: string,
      dispatcherScripts: [{ ... }],
      dns: string,
      dynamicHosts: { ... },
      enable: bool,
      enableDefaultPlugins: { ... },
      enableFccUnlock: { ... },
      enableStrongSwan: { ... },
      ensureProfiles: {
        environmentFiles: { ... },
        profiles: { ... },
        secrets: { ... },
        ...
      },
      ethernet: {
        macAddress: { ... },
        ...
      },
      extraConfig: { ... },
      fccUnlockScripts: [{ ... }],
      firewallBackend: { ... },
      insertNameservers: [string],
      logLevel: string,
      package: Derivation,
      packages: [Derivation],
      plugins: [Derivation],
      settings: { _: { ... } },
      unmanaged: [string],
      useDnsmasq: string,
      wifi: {
        backend: string,
        macAddress: { ... },
        powersave: { ... },
        scanRandMacAddress: bool,
        ...
      },
      ...
    },
    nftables: {
      checkRuleset: bool,
      checkRulesetRedirects: { _: path },
      enable: bool,
      extraDeletions: string,
      flattenRulesetFile: bool,
      flushRuleset: bool,
      preCheckRuleset: string,
      ruleset: string,
      rulesetFile: path | null,
      tables: { _: { ... } },
      ...
    },
    openconnect: {
      interfaces: { _: { ... } },
      package: Derivation,
      ...
    },
    proxy: {
      allProxy: string | null,
      default: string | null,
      envVars: { ... },
      ftpProxy: string | null,
      httpProxy: string | null,
      httpsProxy: string | null,
      noProxy: string | null,
      rsyncProxy: string | null,
      ...
    },
    resolvconf: {
      dnsExtensionMechanism: bool,
      dnsSingleRequest: bool,
      enable: bool,
      extraConfig: string,
      extraOptions: [string],
      package: Derivation,
      subscriberFiles: [path],
      useHostResolvConf: { ... },
      useLocalResolver: bool,
      ...
    },
    resolvconfOptions: [string],
    rxe: {
      enable: bool,
      interfaces: [string],
      ...
    },
    search: [string],
    sits: { _: { ... } },
    stevenblack: {
      block: [string],
      enable: bool,
      package: Derivation,
      whitelist: [{ ... }],
      ...
    },
    supplicant: { _: { ... } },
    tcpcrypt: {
      enable: bool,
      ...
    },
    tempAddresses: string,
    timeServers: [string],
    ucarp: {
      addr: string,
      advBase: { ... },
      advSkew: { ... },
      deadratio: { ... },
      downscript: path,
      enable: bool,
      extraParam: string | null,
      ignoreIfState: bool,
      interface: string,
      neutral: bool,
      noMcast: bool,
      package: Derivation,
      passwordFile: string,
      preempt: bool,
      shutdown: bool,
      srcIp: string,
      upscript: path,
      vhId: { ... },
      ...
    },
    useDHCP: bool,
    useHostResolvConf: bool,
    useNetworkd: bool,
    usePredictableInterfaceNames: bool,
    vlans: { _: { ... } },
    vpnc: { ... },
    vswitches: { _: { ... } },
    wg-quick: {
      interfaces: { _: { ... } },
      ...
    },
    wicd: { ... },
    wireguard: {
      enable: bool,
      interfaces: { _: { ... } },
      useNetworkd: bool,
      ...
    },
    wireless: {
      allowAuxiliaryImperativeNetworks: bool,
      athUserRegulatoryDomain: bool,
      autoDetectInterfaces: bool,
      dbusControlled: bool,
      driver: string,
      enable: bool,
      enableHardening: { ... },
      environmentFile: { ... },
      extraConfig: string,
      extraConfigFiles: [path],
      fallbackToWPA2: bool,
      interfaces: [string],
      iwd: {
        enable: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      networks: { _: { ... } },
      scanOnLowSignal: bool,
      secretsFile: path | null,
      userControlled: bool,
      ...
    },
    wlanInterfaces: { _: { ... } },
    ...
  },
  nix: {
    allowedUsers: { ... },
    autoOptimiseStore: { ... },
    binaryCachePublicKeys: { ... },
    binaryCaches: { ... },
    buildCores: { ... },
    buildMachines: [{ ... }],
    channel: {
      enable: bool,
      ...
    },
    checkAllErrors: bool,
    checkConfig: bool,
    chrootDirs: { ... },
    daemonCPUSchedPolicy: string,
    daemonGroup: string,
    daemonIONiceLevel: int,
    daemonIOSchedClass: string,
    daemonIOSchedPriority: int,
    daemonNiceLevel: { ... },
    daemonUser: string,
    distributedBuilds: bool,
    enable: bool,
    envVars: { ... },
    extraOptions: string,
    firewall: {
      allowLoopback: { ... },
      allowNonTCPUDP: bool,
      allowPrivateNetworks: { ... },
      allowedTCPPorts: [{ ... }],
      allowedUDPPorts: [{ ... }],
      enable: bool,
      extraNftablesRules: [{ ... }],
      ...
    },
    gc: {
      automatic: bool,
      dates: { ... },
      options: { ... },
      persistent: bool,
      randomizedDelaySec: { ... },
      ...
    },
    maxJobs: { ... },
    nixPath: [string],
    nrBuildUsers: int,
    optimise: {
      automatic: bool,
      dates: { ... },
      persistent: bool,
      randomizedDelaySec: { ... },
      ...
    },
    package: Derivation,
    readOnlyStore: { ... },
    registry: { _: { ... } },
    requireSignedBinaryCaches: { ... },
    sandboxPaths: { ... },
    settings: {
  allowed-users: { ... },
  auto-optimise-store: bool,
  cores: int,
  extra-sandbox-paths: { ... },
  max-jobs: { ... },
  require-sigs: bool,
  sandbox: { ... },
  substituters: { ... },
  system-features: { ... },
  trusted-public-keys: { ... },
  trusted-substituters: { ... },
  trusted-users: { ... },
  ...
},
    sshServe: {
      enable: bool,
      keys: [string],
      protocol: string,
      trusted: bool,
      write: bool,
      ...
    },
    systemFeatures: { ... },
    trustedBinaryCaches: { ... },
    trustedUsers: { ... },
    useChroot: { ... },
    useSandbox: { ... },
    ...
  },
  nixops: {
    enableDeprecatedAutoLuks: bool,
    ...
  },
  nixpkgs: {
    buildPlatform: string | { ... },
    config: { ... },
    crossSystem: { ... } | null,
    flake: {
      setFlakeRegistry: bool,
      setNixPath: bool,
      source: string | path | null,
      ...
    },
    hostPlatform: string | { ... },
    initialSystem: { ... },
    localSystem: { ... },
    overlays: [{ ... }],
    pkgs: { ... },
    system: string,
    ...
  },
  oci: {
    diskSize: string | { ... },
    efi: { ... },
    ...
  },
  openstack: {
    efi: { ... },
    zfs: {
      datasets: { _: { ... } },
      enable: { ... },
      ...
    },
    ...
  },
  passthru: { ... },
  power: {
    ups: {
      enable: bool,
      maxStartDelay: int,
      mode: string,
      openFirewall: bool,
      package: Derivation,
      schedulerRules: string,
      ups: { _: { ... } },
      upsd: { ... },
      upsmon: { ... },
      users: { _: { ... } },
      ...
    },
    ...
  },
  powerManagement: {
    bootCommands: string,
    cpuFreqGovernor: string | null,
    cpufreq: {
      max: { ... } | null,
      min: { ... } | null,
      ...
    },
    enable: bool,
    powerDownCommands: string,
    powerUpCommands: string,
    powertop: {
      enable: bool,
      postStart: string,
      preStart: string,
      ...
    },
    resumeCommands: string,
    scsiLinkPolicy: string | null,
    ...
  },
  programs: {
    _1password: {
      enable: bool,
      gid: { ... },
      package: Derivation,
      ...
    },
    _1password-gui: {
      enable: bool,
      gid: { ... },
      package: Derivation,
      polkitPolicyOwners: [string],
      ...
    },
    adb: { ... },
    alvr: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      ...
    },
    amnezia-vpn: {
      enable: bool,
      package: Derivation,
      ...
    },
    appgate-sdp: {
      enable: bool,
      ...
    },
    appimage: {
      binfmt: bool,
      enable: bool,
      package: Derivation,
      ...
    },
    arp-scan: {
      enable: bool,
      ...
    },
    atop: {
      atopRotateTimer: {
        enable: bool,
        ...
      },
      atopService: {
        enable: bool,
        ...
      },
      atopacctService: {
        enable: bool,
        ...
      },
      atopgpu: {
        enable: bool,
        ...
      },
      enable: bool,
      netatop: {
        enable: bool,
        package: Derivation,
        ...
      },
      package: Derivation,
      settings: { ... },
      setuidWrapper: {
        enable: bool,
        ...
      },
      ...
    },
    ausweisapp: {
      enable: bool,
      openFirewall: bool,
      ...
    },
    autoenv: {
      enable: bool,
      package: Derivation,
      ...
    },
    autojump: {
      enable: bool,
      ...
    },
    bandwhich: {
      enable: bool,
      ...
    },
    bash: {
      blesh: {
        enable: bool,
        ...
      },
      completion: {
        enable: bool,
        package: Derivation,
        ...
      },
      enable: bool,
      enableCompletion: bool,
      enableLsColors: bool,
      interactiveShellInit: string,
      loginShellInit: string,
      logout: string,
      lsColorsFile: path | null,
      promptInit: string,
      promptPluginInit: string,
      shellAliases: { _: { ... } },
      shellInit: string,
      undistractMe: {
        enable: bool,
        playSound: bool,
        timeout: int,
        ...
      },
      vteIntegration: bool,
      ...
    },
    bash-my-aws: {
      enable: bool,
      ...
    },
    bat: {
      enable: bool,
      extraPackages: [Derivation],
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    bazecor: {
      enable: bool,
      package: Derivation,
      ...
    },
    bcc: {
      enable: bool,
      ...
    },
    benchexec: {
      enable: bool,
      package: Derivation,
      users: [{ ... }],
      ...
    },
    browserpass: {
      enable: bool,
      ...
    },
    calls: {
      enable: bool,
      ...
    },
    captive-browser: {
      bindInterface: bool,
      browser: string,
      dhcp-dns: string,
      enable: bool,
      interface: string,
      package: Derivation,
      socks5-addr: string,
      ...
    },
    cardboard: { ... },
    ccache: {
      cacheDir: path,
      enable: bool,
      group: string,
      owner: string,
      packageNames: [string],
      trace: bool,
      ...
    },
    cdemu: {
      enable: bool,
      group: string,
      gui: bool,
      image-analyzer: bool,
      ...
    },
    cfs-zen-tweaks: {
      enable: bool,
      ...
    },
    chromium: {
      defaultSearchProviderEnabled: bool | null,
      defaultSearchProviderSearchURL: string | null,
      defaultSearchProviderSuggestURL: string | null,
      enable: bool,
      enablePlasmaBrowserIntegration: bool,
      extensions: [string] | null,
      extraOpts: { ... },
      homepageLocation: string | null,
      initialPrefs: { ... },
      plasmaBrowserIntegrationPackage: Derivation,
      ...
    },
    chrysalis: {
      enable: bool,
      package: Derivation,
      ...
    },
    clash-verge: {
      autoStart: bool,
      enable: bool,
      group: string,
      package: Derivation,
      serviceMode: bool,
      tunMode: bool,
      ...
    },
    cnping: {
      enable: bool,
      ...
    },
    command-not-found: {
      dbPath: path,
      enable: bool,
      ...
    },
    coolercontrol: {
      enable: bool,
      nvidiaSupport: { ... },
      ...
    },
    corectrl: {
      enable: bool,
      gpuOverclock: {
        enable: bool,
        ppfeaturemask: string,
        ...
      },
      package: Derivation,
      ...
    },
    corefreq: {
      enable: bool,
      package: Derivation,
      ...
    },
    cpu-energy-meter: {
      enable: bool,
      package: Derivation,
      ...
    },
    criu: {
      enable: bool,
      ...
    },
    dconf: {
      enable: bool,
      packages: [Derivation],
      profiles: { _: { ... } },
      ...
    },
    digitalbitbox: {
      enable: bool,
      package: Derivation,
      ...
    },
    direnv: {
      angrr: {
        autoUse: bool,
        enable: bool,
        ...
      },
      direnvrcExtra: string,
      enable: bool,
      enableBashIntegration: bool,
      enableFishIntegration: bool,
      enableXonshIntegration: bool,
      enableZshIntegration: bool,
      finalPackage: { ... },
      loadInNixShell: bool,
      nix-direnv: {
        enable: bool,
        package: Derivation,
        ...
      },
      package: Derivation,
      settings: { ... },
      silent: bool,
      ...
    },
    dmrconfig: {
      enable: bool,
      package: Derivation,
      ...
    },
    dms-shell: {
      enable: bool,
      enableAudioWavelength: bool,
      enableBrightnessControl: { ... },
      enableCalendarEvents: bool,
      enableClipboard: { ... },
      enableClipboardPaste: bool,
      enableColorPicker: { ... },
      enableDynamicTheming: bool,
      enableSystemMonitoring: bool,
      enableSystemSound: { ... },
      enableVPN: bool,
      package: Derivation,
      plugins: { _: { ... } },
      quickshell: {
        package: Derivation,
        ...
      },
      systemd: {
        enable: bool,
        restartIfChanged: bool,
        target: string,
        ...
      },
      ...
    },
    droidcam: {
      enable: bool,
      ...
    },
    dsearch: {
      enable: bool,
      package: Derivation,
      systemd: {
        enable: bool,
        target: string,
        ...
      },
      ...
    },
    dublin-traceroute: {
      enable: bool,
      package: Derivation,
      ...
    },
    dwl: {
      enable: bool,
      extraSessionCommands: string,
      package: Derivation,
      ...
    },
    ecryptfs: { ... },
    ente-auth: {
      enable: bool,
      package: Derivation,
      ...
    },
    envision: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      ...
    },
    evince: {
      enable: bool,
      package: Derivation,
      ...
    },
    evolution: {
      enable: bool,
      plugins: [Derivation],
      ...
    },
    extra-container: {
      enable: bool,
      ...
    },
    fcast-receiver: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      ...
    },
    feedbackd: {
      enable: bool,
      package: Derivation,
      theme-package: Derivation | null,
      ...
    },
    file-roller: { ... },
    firefox: {
      autoConfig: string,
      autoConfigFiles: [path],
      enable: bool,
      finalPackage: Derivation,
      languagePacks: [string],
      nativeMessagingHosts: {
        browserpass: { ... },
        bukubrow: { ... },
        euwebid: { ... },
        ff2mpv: { ... },
        fxCast: { ... },
        gsconnect: { ... },
        jabref: { ... },
        packages: { ... },
        passff: { ... },
        tridactyl: { ... },
        ugetIntegrator: { ... },
        ...
      },
      package: Derivation,
      policies: { ... },
      preferences: { _: { ... } },
      preferencesStatus: string,
      wrapperConfig: { ... },
      ...
    },
    firejail: {
      enable: bool,
      wrappedBinaries: { _: { ... } },
      ...
    },
    fish: {
      enable: bool,
      extraCompletionPackages: [Derivation],
      generateCompletions: bool,
      interactiveShellInit: string,
      loginShellInit: string,
      package: Derivation,
      promptInit: string,
      shellAbbrs: { _: string },
      shellAliases: { _: { ... } },
      shellInit: string,
      useBabelfish: bool,
      vendor: {
        completions: { ... },
        config: { ... },
        functions: { ... },
        ...
      },
      ...
    },
    flashprog: {
      enable: bool,
      package: Derivation,
      ...
    },
    flashrom: {
      enable: bool,
      package: Derivation,
      ...
    },
    flexoptix-app: {
      enable: bool,
      package: Derivation,
      ...
    },
    foot: {
      enable: bool,
      enableBashIntegration: bool,
      enableFishIntegration: bool,
      enableZshIntegration: bool,
      package: Derivation,
      settings: { _: { ... } },
      theme: string | null,
      xdg: {
        serverAutostart: bool,
        ...
      },
      ...
    },
    fuse: {
      enable: bool,
      mountMax: { ... },
      userAllowOther: bool,
      ...
    },
    fzf: {
      fuzzyCompletion: bool,
      keybindings: bool,
      ...
    },
    gamemode: {
      enable: bool,
      enableRenice: bool,
      settings: { _: { ... } },
      ...
    },
    gamescope: {
      args: [string],
      capSysNice: bool,
      enable: bool,
      env: { _: string },
      package: Derivation,
      ...
    },
    gdk-pixbuf: {
      modulePackages: [Derivation],
      ...
    },
    geary: {
      enable: bool,
      ...
    },
    ghidra: {
      enable: bool,
      gdb: bool,
      package: Derivation,
      ...
    },
    git: {
      attributes: string,
      config: { ... },
      enable: bool,
      lfs: {
        enable: bool,
        enablePureSSHTransfer: bool,
        package: Derivation,
        ...
      },
      package: Derivation,
      prompt: {
        enable: bool,
        ...
      },
      ...
    },
    git-worktree-switcher: {
      enable: bool,
      package: Derivation,
      ...
    },
    gnome-disks: {
      enable: bool,
      ...
    },
    gnome-documents: { ... },
    gnome-terminal: {
      enable: bool,
      ...
    },
    gnupg: {
      agent: {
        enable: bool,
        enableBrowserSocket: bool,
        enableExtraSocket: bool,
        enableSSHSupport: bool,
        pinentryFlavor: { ... },
        pinentryPackage: { ... },
        settings: { ... },
        ...
      },
      dirmngr: {
        enable: bool,
        ...
      },
      package: Derivation,
      ...
    },
    goldwarden: { ... },
    gpaste: {
      enable: bool,
      ...
    },
    gphoto2: {
      enable: bool,
      ...
    },
    gpu-screen-recorder: {
      enable: bool,
      package: Derivation,
      ...
    },
    gtklock: {
      config: { _: { ... } },
      enable: bool,
      modules: [Derivation],
      package: Derivation,
      style: string | null,
      ...
    },
    haguichi: {
      enable: bool,
      ...
    },
    hamster: {
      enable: bool,
      ...
    },
    htop: {
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    hyprland: {
      enable: bool,
      enableNvidiaPatches: { ... },
      nvidiaPatches: { ... },
      package: Derivation,
      portalPackage: Derivation,
      systemd: {
        setPath: { ... },
        ...
      },
      withUWSM: bool,
      xwayland: {
        enable: bool,
        hidpi: { ... },
        ...
      },
      ...
    },
    hyprlock: {
      enable: bool,
      package: Derivation,
      ...
    },
    i3lock: {
      enable: bool,
      package: Derivation,
      u2fSupport: bool,
      ...
    },
    iay: {
      enable: bool,
      minimalPrompt: bool,
      package: Derivation,
      ...
    },
    ibus: {
      plugins: [{ ... }],
      ...
    },
    idescriptor: {
      enable: bool,
      package: Derivation,
      users: [string],
      ...
    },
    iftop: {
      enable: bool,
      ...
    },
    iio-hyprland: {
      enable: bool,
      package: Derivation,
      ...
    },
    immersed: {
      enable: bool,
      package: Derivation,
      ...
    },
    immersed-vr: { ... },
    info: {
      enable: bool,
      ...
    },
    iotop: {
      enable: bool,
      package: Derivation,
      ...
    },
    java: {
      binfmt: bool,
      enable: bool,
      package: Derivation,
      ...
    },
    joycond-cemuhook: {
      enable: bool,
      ...
    },
    k3b: {
      enable: bool,
      ...
    },
    k40-whisperer: {
      enable: bool,
      group: string,
      package: Derivation,
      ...
    },
    kbdlight: {
      enable: bool,
      ...
    },
    kclock: {
      enable: bool,
      ...
    },
    kde-pim: {
      enable: bool,
      kmail: bool,
      kontact: bool,
      merkuro: bool,
      ...
    },
    kdeconnect: {
      enable: bool,
      package: Derivation | null,
      ...
    },
    kubeswitch: {
      commandName: string,
      enable: bool,
      package: Derivation,
      ...
    },
    labwc: {
      enable: bool,
      package: Derivation,
      ...
    },
    ladybird: {
      enable: bool,
      ...
    },
    lazygit: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    less: {
      clearDefaultCommands: bool,
      commands: { _: string },
      configFile: path | null,
      enable: bool,
      envVariables: { _: string },
      lessclose: string | null,
      lessopen: string | null,
      lineEditingKeys: { _: string },
      package: Derivation,
      ...
    },
    liboping: {
      enable: bool,
      ...
    },
    light: { ... },
    localsend: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      ...
    },
    man: {
      enable: bool,
      ...
    },
    mangowc: {
      enable: bool,
      package: Derivation,
      ...
    },
    mdevctl: {
      enable: bool,
      ...
    },
    mepo: {
      enable: bool,
      locationBackends: {
        geoclue: bool,
        gpsd: bool,
        ...
      },
      ...
    },
    mininet: {
      enable: bool,
      ...
    },
    minipro: {
      enable: bool,
      package: Derivation,
      ...
    },
    miriway: {
      config: string,
      enable: bool,
      ...
    },
    mosh: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      withUtempter: bool,
      ...
    },
    mouse-actions: {
      autorun: bool,
      enable: bool,
      package: Derivation,
      ...
    },
    msmtp: {
      accounts: { _: { ... } },
      defaults: { ... },
      enable: bool,
      extraConfig: string,
      package: Derivation,
      setSendmail: bool,
      ...
    },
    mtr: {
      enable: bool,
      package: Derivation,
      ...
    },
    nano: {
      enable: bool,
      nanorc: string,
      package: Derivation,
      syntaxHighlight: bool,
      ...
    },
    nautilus-open-any-terminal: {
      enable: bool,
      terminal: string | null,
      ...
    },
    nbd: {
      enable: bool,
      ...
    },
    nekoray: { ... },
    neovim: {
      configure: { ... },
      defaultEditor: bool,
      enable: bool,
      finalPackage: Derivation,
      package: Derivation,
      runtime: { _: { ... } },
      viAlias: bool,
      vimAlias: bool,
      withNodeJs: bool,
      withPython3: bool,
      withRuby: bool,
      ...
    },
    nethoscope: {
      enable: bool,
      ...
    },
    nexttrace: {
      enable: bool,
      package: Derivation,
      ...
    },
    nh: {
      clean: {
        dates: { ... },
        enable: bool,
        extraArgs: { ... },
        ...
      },
      enable: bool,
      flake: string | null,
      package: Derivation,
      ...
    },
    niri: {
      enable: bool,
      package: Derivation,
      useNautilus: bool,
      ...
    },
    nix-index: {
      enable: bool,
      enableBashIntegration: bool,
      enableFishIntegration: bool,
      enableZshIntegration: bool,
      package: Derivation,
      ...
    },
    nix-ld: {
      enable: bool,
      libraries: [Derivation],
      package: Derivation,
      ...
    },
    nix-required-mounts: {
      allowedPatterns: { _: { ... } },
      enable: bool,
      extraWrapperArgs: [string],
      package: Derivation,
      presets: {
        nvidia-gpu: { ... },
        ...
      },
      ...
    },
    nixbit: {
      enable: bool,
      forceAutostart: bool,
      package: Derivation,
      repository: string,
      ...
    },
    nm-applet: {
      enable: bool,
      indicator: bool,
      package: Derivation,
      ...
    },
    nncp: {
      enable: bool,
      group: string,
      package: Derivation,
      secrets: [string],
      settings: { ... },
      ...
    },
    noisetorch: {
      enable: bool,
      package: Derivation,
      ...
    },
    npm: {
      enable: bool,
      npmrc: string,
      package: Derivation,
      ...
    },
    ns-usbloader: {
      enable: bool,
      ...
    },
    oblogout: { ... },
    obs-studio: {
      enable: bool,
      enableVirtualCamera: bool,
      finalPackage: Derivation | null,
      package: Derivation | null,
      plugins: [Derivation],
      ...
    },
    oddjobd: {
      enable: bool,
      package: Derivation,
      ...
    },
    opengamepadui: {
      args: [string],
      enable: bool,
      extraPackages: [Derivation],
      fontPackages: [Derivation],
      gamescopeSession: { ... },
      inputplumber: {
        enable: bool,
        ...
      },
      package: Derivation,
      powerstation: {
        enable: bool,
        ...
      },
      ...
    },
    openvpn3: {
      enable: bool,
      log-service: { ... },
      netcfg: { ... },
      package: Derivation,
      ...
    },
    pantheon-tweaks: { ... },
    partition-manager: {
      enable: bool,
      package: Derivation,
      ...
    },
    pay-respects: {
      aiIntegration: bool | { ... },
      alias: string,
      enable: bool,
      package: Derivation,
      runtimeRules: [{ ... }],
      ...
    },
    plotinus: {
      enable: bool,
      ...
    },
    pmount: {
      enable: bool,
      package: Derivation,
      ...
    },
    pqos-wrapper: {
      enable: bool,
      package: Derivation,
      ...
    },
    projecteur: {
      enable: bool,
      package: Derivation,
      ...
    },
    proxychains: {
      chain: {
        length: { ... },
        type: string,
        ...
      },
      enable: bool,
      localnet: string,
      package: Derivation,
      proxies: { _: { ... } },
      proxyDNS: bool,
      quietMode: bool,
      remoteDNSSubnet: string,
      tcpConnectTimeOut: int,
      tcpReadTimeOut: int,
      ...
    },
    pulseview: {
      enable: bool,
      ...
    },
    qdmr: {
      enable: bool,
      package: Derivation,
      ...
    },
    qgroundcontrol: {
      blacklistModemManagerFromTTYUSB: bool,
      enable: bool,
      package: Derivation,
      ...
    },
    qt5ct: {
      enable: { ... },
      ...
    },
    quark-goldleaf: {
      enable: bool,
      ...
    },
    regreet: {
      cageArgs: [string],
      cursorTheme: {
        name: string,
        package: Derivation,
        ...
      },
      enable: bool,
      extraCss: path | string,
      font: {
        name: string,
        package: Derivation,
        size: { ... },
        ...
      },
      iconTheme: {
        name: string,
        package: Derivation,
        ...
      },
      package: Derivation,
      settings: { ... },
      theme: {
        name: string,
        package: Derivation,
        ...
      },
      ...
    },
    river: {
      enable: bool,
      extraPackages: [Derivation],
      package: Derivation | null,
      xwayland: {
        enable: bool,
        ...
      },
      ...
    },
    river-classic: {
      enable: bool,
      extraPackages: [Derivation],
      package: Derivation | null,
      xwayland: {
        enable: bool,
        ...
      },
      ...
    },
    rog-control-center: {
      autoStart: bool,
      enable: bool,
      ...
    },
    rush: {
      enable: bool,
      global: string,
      package: Derivation,
      rules: { _: string },
      shell: Derivation | path,
      wrap: bool,
      ...
    },
    rust-motd: {
      enable: bool,
      enableMotdInSSHD: bool,
      order: [string],
      refreshInterval: string,
      settings: { _: { ... } },
      ...
    },
    ryzen-monitor-ng: {
      enable: bool,
      package: Derivation,
      ...
    },
    schroot: {
      enable: bool,
      package: Derivation,
      profiles: { _: { ... } },
      settings: { _: { ... } },
      ...
    },
    screen: {
      enable: bool,
      package: Derivation,
      screenrc: string,
      ...
    },
    seahorse: {
      enable: bool,
      ...
    },
    sedutil: {
      enable: bool,
      ...
    },
    sharing: {
      enable: bool,
      ...
    },
    singularity: {
      enable: bool,
      enableExternalLocalStateDir: bool,
      enableFakeroot: bool,
      enableSuid: bool,
      package: Derivation,
      packageOverriden: Derivation | null,
      systemBinPaths: [path],
      ...
    },
    skim: {
      enable: bool,
      fuzzyCompletion: { ... },
      keybindings: bool,
      package: Derivation,
      ...
    },
    slock: {
      enable: bool,
      package: Derivation,
      ...
    },
    sniffnet: {
      enable: bool,
      ...
    },
    soundmodem: {
      enable: bool,
      package: Derivation,
      ...
    },
    spacefm: { ... },
    ssh: {
      agentPKCS11Whitelist: string | null,
      agentTimeout: string | null,
      askPassword: string,
      ciphers: [string] | null,
      enableAskPassword: bool,
      extraConfig: string,
      forwardX11: bool | null,
      hostKeyAlgorithms: [string],
      kexAlgorithms: [string] | null,
      knownHosts: { _: { ... } },
      knownHostsFiles: [path],
      macs: [string] | null,
      package: Derivation,
      pubkeyAcceptedKeyTypes: [string],
      setXAuthLocation: bool,
      startAgent: bool,
      systemd-ssh-proxy: {
        enable: bool,
        ...
      },
      ...
    },
    starship: {
      enable: bool,
      interactiveOnly: bool,
      package: Derivation,
      presets: [string],
      settings: { ... },
      transientPrompt: {
        enable: bool,
        left: { ... },
        right: { ... },
        ...
      },
      ...
    },
    steam: {
      dedicatedServer: {
        openFirewall: bool,
        ...
      },
      enable: bool,
      extest: {
        enable: bool,
        ...
      },
      extraCompatPackages: [Derivation],
      extraPackages: [Derivation],
      fontPackages: [Derivation],
      gamescopeSession: { ... },
      localNetworkGameTransfers: {
        openFirewall: bool,
        ...
      },
      package: Derivation,
      protontricks: {
        enable: bool,
        package: Derivation,
        ...
      },
      remotePlay: {
        openFirewall: bool,
        ...
      },
      ...
    },
    streamcontroller: {
      enable: bool,
      package: Derivation,
      ...
    },
    streamdeck-ui: {
      autoStart: bool,
      enable: bool,
      package: Derivation,
      ...
    },
    sway: {
      enable: bool,
      extraOptions: [string],
      extraPackages: [Derivation],
      extraSessionCommands: string,
      package: Derivation | null,
      wrapperFeatures: {
        base: bool,
        gtk: bool,
        ...
      },
      xwayland: {
        enable: bool,
        ...
      },
      ...
    },
    sysdig: {
      enable: bool,
      ...
    },
    system-config-printer: {
      enable: bool,
      ...
    },
    systemtap: {
      enable: bool,
      ...
    },
    tcpdump: {
      enable: bool,
      ...
    },
    television: {
      enable: bool,
      enableBashIntegration: bool,
      enableFishIntegration: bool,
      enableZshIntegration: bool,
      package: Derivation,
      ...
    },
    thefuck: { ... },
    throne: {
      enable: bool,
      package: Derivation,
      tunMode: {
        enable: bool,
        setuid: bool,
        ...
      },
      ...
    },
    thunar: {
      enable: bool,
      plugins: [Derivation],
      ...
    },
    thunderbird: {
      enable: bool,
      package: Derivation,
      policies: { ... },
      preferences: { _: { ... } },
      preferencesStatus: string,
      ...
    },
    tilp2: { ... },
    tmux: {
      aggressiveResize: bool,
      baseIndex: int,
      clock24: bool,
      customPaneNavigationAndResize: bool,
      enable: bool,
      escapeTime: int,
      extraConfig: string,
      extraConfigBeforePlugins: string,
      extraTmuxConf: string,
      historyLimit: int,
      keyMode: string,
      newSession: bool,
      package: Derivation,
      plugins: [Derivation],
      resizeAmount: int,
      reverseSplit: bool,
      secureSocket: bool,
      shortcut: string,
      terminal: string,
      withUtempter: bool,
      ...
    },
    traceroute: {
      enable: bool,
      ...
    },
    trippy: {
      enable: bool,
      ...
    },
    tsmClient: {
      defaultServername: { ... } | null,
      dsmSysText: string,
      enable: bool,
      package: Derivation,
      servers: { _: { ... } },
      wrappedPackage: Derivation,
      ...
    },
    turbovnc: {
      ensureHeadlessSoftwareOpenGL: bool,
      ...
    },
    tuxclocker: {
      enable: bool,
      enableAMD: bool,
      enabledNVIDIADevices: [int],
      useUnfree: bool,
      ...
    },
    udevil: {
      enable: bool,
      ...
    },
    unity3d: {
      enable: { ... },
      ...
    },
    usbtop: {
      enable: bool,
      ...
    },
    uwsm: {
      enable: bool,
      package: Derivation,
      waylandCompositors: { _: { ... } },
      ...
    },
    vim: {
      defaultEditor: bool,
      enable: bool,
      package: Derivation,
      ...
    },
    virt-manager: {
      enable: bool,
      package: Derivation,
      ...
    },
    vivid: {
      enable: bool,
      package: Derivation,
      theme: string,
      ...
    },
    vscode: {
      defaultEditor: bool,
      enable: bool,
      extensions: [Derivation],
      finalPackage: Derivation,
      package: Derivation,
      ...
    },
    wavemon: {
      enable: bool,
      ...
    },
    way-cooler: { ... },
    waybar: {
      enable: bool,
      package: Derivation,
      systemd: {
        target: string,
        ...
      },
      ...
    },
    wayfire: {
      enable: bool,
      package: Derivation,
      plugins: [Derivation],
      xwayland: {
        enable: bool,
        ...
      },
      ...
    },
    wayland: {
      miracle-wm: {
        enable: bool,
        ...
      },
      ...
    },
    wayvnc: {
      enable: bool,
      package: Derivation,
      ...
    },
    weylus: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      users: [string],
      ...
    },
    winbox: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      ...
    },
    wireshark: {
      dumpcap: {
        enable: bool,
        ...
      },
      enable: bool,
      package: Derivation,
      usbmon: {
        enable: bool,
        ...
      },
      ...
    },
    wshowkeys: {
      enable: bool,
      package: Derivation,
      ...
    },
    x2goserver: { ... },
    xastir: {
      enable: bool,
      ...
    },
    xfconf: {
      enable: bool,
      ...
    },
    xfs_quota: {
      projects: { _: { ... } },
      ...
    },
    xonsh: {
      bashCompletion: {
        enable: bool,
        package: Derivation,
        ...
      },
      config: string,
      enable: bool,
      extraPackages: { ... } -> { ... },
      package: Derivation,
      ...
    },
    xppen: {
      enable: bool,
      package: Derivation,
      ...
    },
    xscreensaver: {
      enable: bool,
      package: Derivation,
      sonar: {
        enable: bool,
        ...
      },
      ...
    },
    xss-lock: {
      enable: bool,
      extraOptions: [string],
      lockerCommand: string,
      ...
    },
    xwayland: {
      defaultFontPath: string,
      enable: bool,
      package: path,
      ...
    },
    yabar: { ... },
    yazi: {
      enable: bool,
      flavors: { _: { ... } },
      initLua: path | null,
      package: Derivation,
      plugins: { _: { ... } },
      settings: { ... },
      ...
    },
    ydotool: {
      enable: bool,
      group: string,
      ...
    },
    yubikey-manager: {
      enable: bool,
      package: Derivation,
      ...
    },
    yubikey-touch-detector: {
      enable: bool,
      libnotify: bool,
      unixSocket: bool,
      verbose: bool,
      ...
    },
    zmap: {
      enable: bool,
      ...
    },
    zoom-us: {
      enable: bool,
      package: Derivation,
      ...
    },
    zoxide: {
      enable: bool,
      enableBashIntegration: bool,
      enableFishIntegration: bool,
      enableXonshIntegration: bool,
      enableZshIntegration: bool,
      flags: [string],
      package: Derivation,
      ...
    },
    zsh: {
      autosuggestions: {
        async: bool,
        enable: bool,
        extraConfig: { ... },
        highlightStyle: string,
        strategy: { ... },
        ...
      },
      enable: bool,
      enableAutosuggestions: bool,
      enableBashCompletion: bool,
      enableCompletion: bool,
      enableGlobalCompInit: bool,
      enableLsColors: bool,
      enableSyntaxHighlighting: bool,
      histFile: string,
      histSize: int,
      interactiveShellInit: string,
      loginShellInit: string,
      oh-my-zsh: {
        custom: { ... },
        enable: bool,
        plugins: { ... },
        theme: string,
        ...
      },
      ohMyZsh: {
        cacheDir: string,
        custom: { ... },
        customPkgs: { ... },
        enable: bool,
        package: Derivation,
        plugins: { ... },
        preLoaded: string,
        theme: string,
        ...
      },
      promptInit: string,
      setOptions: [string],
      shellAliases: { _: { ... } },
      shellInit: string,
      syntax-highlighting: {
        enable: bool,
        highlighters: { ... },
        patterns: { ... },
        ...
      },
      syntaxHighlighting: {
        enable: bool,
        highlighters: { ... },
        patterns: { ... },
        styles: { ... },
        ...
      },
      vteIntegration: bool,
      zsh-autoenv: {
        enable: bool,
        package: Derivation,
        ...
      },
      ...
    },
    ...
  },
  qt: {
    enable: bool,
    platformTheme: string | null,
    style: string | null,
    ...
  },
  qt5: {
    enable: bool,
    platformTheme: string | null,
    style: string | null,
    ...
  },
  security: {
    acme: {
      acceptTerms: bool,
      activationDelay: { ... },
      certs: { _: { ... } },
      defaults: { ... },
      directory: { ... },
      email: { ... },
      enableDebugLogs: { ... },
      maxConcurrentRenewals: int,
      preDelay: { ... },
      preliminarySelfsigned: { ... },
      production: { ... },
      renewInterval: { ... },
      server: { ... },
      useRoot: bool,
      validMin: { ... },
      validMinDays: { ... },
      ...
    },
    agnos: {
      enable: bool,
      generateKeys: {
        enable: bool,
        keySize: int,
        ...
      },
      group: string,
      persistent: bool,
      server: string | null,
      serverCa: path | null,
      settings: { ... },
      startAt: string | { ... },
      temporarilyOpenFirewall: bool,
      user: string,
      ...
    },
    allowSimultaneousMultithreading: bool,
    allowUserNamespaces: bool,
    apparmor: {
      confineSUIDApplications: { ... },
      enable: bool,
      enableCache: bool,
      includes: { _: string },
      killUnconfinedConfinables: bool,
      packages: [Derivation],
      policies: { _: { ... } },
      profiles: { ... },
      ...
    },
    audit: {
      backlogLimit: int,
      enable: string,
      failureMode: string,
      package: Derivation,
      rateLimit: int,
      rules: [string],
      ...
    },
    auditd: {
      enable: bool,
      package: Derivation,
      plugins: { _: { ... } },
      settings: { ... },
      ...
    },
    chromiumSuidSandbox: {
      enable: bool,
      ...
    },
    dhparams: {
      defaultBitSize: { ... },
      enable: bool,
      params: { _: { ... } },
      path: string,
      stateful: bool,
      ...
    },
    doas: {
      enable: bool,
      extraConfig: string,
      extraRules: [{ ... }],
      package: Derivation,
      wheelNeedsPassword: bool,
      ...
    },
    duosec: {
      acceptEnvFactor: bool,
      allowTcpForwarding: bool,
      autopush: bool,
      failmode: string,
      fallbackLocalIP: bool,
      group: string,
      groups: string,
      host: string,
      ikey: string,
      integrationKey: string,
      motd: bool,
      pam: {
        enable: bool,
        ...
      },
      prompts: string,
      pushinfo: bool,
      secretKeyFile: path | null,
      skey: { ... },
      ssh: {
        enable: bool,
        ...
      },
      ...
    },
    enableWrappers: bool,
    forcePageTableIsolation: bool,
    googleOsLogin: {
      enable: bool,
      ...
    },
    hideProcessInformation: { ... },
    initialRootPassword: { ... },
    ipa: {
      basedn: string,
      cacheCredentials: bool,
      certificate: Derivation,
      chromiumSupport: bool,
      domain: string,
      dyndns: {
        enable: bool,
        interface: string,
        ...
      },
      enable: bool,
      ifpAllowedUids: [string],
      ipaHostname: string,
      offlinePasswords: bool,
      realm: string,
      server: string,
      shells: [Derivation],
      useAsTimeserver: bool,
      ...
    },
    isolate: {
      boxRoot: path,
      cgRoot: string,
      enable: bool,
      extraConfig: string,
      firstGid: { ... },
      firstUid: { ... },
      lockRoot: path,
      numBoxes: { ... },
      package: Derivation,
      restrictedInit: bool,
      ...
    },
    klogd: {
      enable: { ... },
      ...
    },
    krb5: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    lockKernelModules: bool,
    loginDefs: {
      chfnRestrict: string | null,
      package: Derivation,
      settings: { ... },
      ...
    },
    lsm: [string],
    pam: {
      dp9ik: {
        authserver: { ... },
        control: string,
        enable: bool,
        ...
      },
      enableEcryptfs: { ... },
      enableFscrypt: bool,
      enableOTPW: bool,
      enableSSHAgentAuth: bool,
      enableU2F: bool,
      enableUMask: bool,
      howdy: {
        control: string,
        enable: bool,
        ...
      },
      krb5: {
        enable: bool,
        ...
      },
      loginLimits: [{ ... }],
      makeHomeDir: {
        skelDirectory: string,
        umask: string,
        ...
      },
      mount: {
        additionalSearchPaths: { ... },
        createMountPoints: bool,
        cryptMountOptions: { ... },
        debugLevel: int,
        enable: bool,
        extraVolumes: { ... },
        fuseMountOptions: { ... },
        logoutHup: bool,
        logoutKill: bool,
        logoutTerm: bool,
        logoutWait: int,
        removeCreatedMountPoints: bool,
        ...
      },
      oath: {
        digits: string,
        enable: bool,
        usersFile: path,
        window: int,
        ...
      },
      p11: {
        control: string,
        enable: bool,
        ...
      },
      package: Derivation,
      rssh: {
        enable: bool,
        settings: { ... },
        ...
      },
      services: { _: { ... } },
      sshAgentAuth: {
        authorizedKeysFiles: { ... },
        enable: bool,
        ...
      },
      u2f: {
        appId: { ... },
        authFile: { ... },
        control: string,
        cue: { ... },
        debug: { ... },
        enable: bool,
        interactive: { ... },
        origin: { ... },
        settings: { ... },
        ...
      },
      ussh: {
        authorizedPrincipals: { ... },
        authorizedPrincipalsFile: { ... },
        caFile: { ... },
        control: string,
        enable: bool,
        group: { ... },
        ...
      },
      yubico: {
        challengeResponsePath: { ... },
        control: string,
        debug: bool,
        enable: bool,
        id: string,
        mode: string,
        ...
      },
      zfs: {
        enable: bool,
        homes: string,
        mountRecursively: bool,
        noUnmount: bool,
        ...
      },
      ...
    },
    pki: {
      caBundle: path,
      caCertificateBlacklist: [string],
      certificateFiles: [path],
      certificates: [string],
      installCACerts: bool,
      useCompatibleBundle: bool,
      ...
    },
    please: {
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      wheelNeedsPassword: bool,
      ...
    },
    polkit: {
      adminIdentities: [string],
      debug: bool,
      enable: bool,
      extraConfig: string,
      package: Derivation,
      ...
    },
    protectKernelImage: bool,
    rngd: { ... },
    rtkit: {
      args: [string],
      enable: bool,
      package: Derivation,
      ...
    },
    run0: {
      enableSudoAlias: bool,
      wheelNeedsPassword: bool,
      ...
    },
    setuidOwners: { ... },
    setuidPrograms: { ... },
    shadow: {
      enable: bool,
      su: {
        package: Derivation,
        ...
      },
      ...
    },
    soteria: {
      enable: bool,
      package: Derivation,
      ...
    },
    sudo: {
      configFile: string,
      defaultOptions: [string],
      enable: bool,
      execWheelOnly: bool,
      extraConfig: string,
      extraRules: [{ ... }],
      keepTerminfo: bool,
      package: Derivation,
      wheelNeedsPassword: bool,
      ...
    },
    sudo-rs: {
      configFile: string,
      defaultOptions: [string],
      enable: bool,
      execWheelOnly: bool,
      extraConfig: string,
      extraRules: [{ ... }],
      package: Derivation,
      wheelNeedsPassword: bool,
      ...
    },
    tpm2: {
      abrmd: {
        enable: bool,
        package: Derivation,
        ...
      },
      applyUdevRules: bool,
      enable: bool,
      fapi: {
        ekCertLess: { ... },
        ekFingerprint: { ... },
        firmwareLogFile: string,
        imaLogFile: string,
        logDir: string,
        profileDir: string,
        profileName: string,
        systemDir: string,
        systemPcrs: { ... },
        tcti: string,
        userDir: string,
        ...
      },
      pkcs11: {
        enable: bool,
        package: Derivation,
        ...
      },
      tctiEnvironment: {
        deviceConf: string,
        enable: bool,
        interface: string,
        tabrmdConf: string,
        ...
      },
      tssGroup: string | null,
      tssUser: string | null,
      ...
    },
    unprivilegedUsernsClone: bool,
    virtualisation: {
      flushL1DataCache: string | null,
      ...
    },
    virtualization: {
      flushL1DataCache: string | null,
      ...
    },
    wrapperDir: path,
    wrapperDirSize: string,
    wrappers: { _: { ... } },
    ...
  },
  services: {
    SystemdJournal2Gelf: {
      enable: bool,
      extraOptions: string,
      graylogServer: string,
      package: Derivation,
      ...
    },
    _3proxy: {
      confFile: path,
      denyPrivate: bool,
      enable: bool,
      extraConfig: string | null,
      privateRanges: [string],
      resolution: { ... },
      services: [{ ... }],
      usersFile: path | null,
      ...
    },
    a2boot: {
      enable: bool,
      ...
    },
    accounts-daemon: {
      enable: bool,
      ...
    },
    acme-dns: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    acpid: {
      acEventCommands: string,
      enable: bool,
      handlers: { _: { ... } },
      lidEventCommands: string,
      logEvents: bool,
      powerEventCommands: string,
      ...
    },
    activemq: {
      baseDir: string,
      configurationDir: string,
      configurationURI: string,
      enable: bool,
      extraJavaOptions: string,
      javaProperties: { ... },
      ...
    },
    actkbd: {
      bindings: [{ ... }],
      enable: bool,
      extraConfig: string,
      ...
    },
    actual: {
      enable: bool,
      group: string | null,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string | null,
      ...
    },
    adguardhome: {
      allowDHCP: bool,
      enable: bool,
      extraArgs: [string],
      host: string,
      mutableSettings: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    aerospike: {
      enable: bool,
      extraConfig: string,
      networkConfig: string,
      package: Derivation,
      workDir: string,
      ...
    },
    aesmd: {
      debug: { ... },
      enable: bool,
      environment: { _: string },
      package: Derivation,
      quoteProviderLibrary: path | null,
      settings: { ... },
      ...
    },
    agate: {
      addresses: [string],
      certificatesDir: path,
      contentDir: path,
      enable: bool,
      extraArgs: [string],
      hostnames: [string],
      language: string | null,
      onlyTls_1_3: bool,
      package: Derivation,
      ...
    },
    agorakit: {
      appKeyFile: path,
      appURL: string,
      config: { _: { ... } },
      dataDir: path,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        user: string,
        ...
      },
      enable: bool,
      group: string,
      hostName: string,
      mail: {
        driver: string,
        encryption: { ... },
        from: string,
        fromName: string,
        host: string,
        passwordFile: { ... },
        port: { ... },
        user: { ... },
        ...
      },
      maxUploadSize: string,
      nginx: { ... },
      phpPackage: Derivation,
      poolConfig: { _: { ... } },
      user: string,
      ...
    },
    airsonic: {
      contextPath: path,
      enable: bool,
      home: path,
      jre: Derivation,
      jvmOptions: [string],
      listenAddress: string,
      maxMemory: int,
      port: { ... },
      transcoders: [path],
      user: string,
      virtualHost: string | null,
      war: path,
      ...
    },
    akkoma: {
      config: { ... },
      dist: {
        address: { ... },
        cookie: { ... },
        epmdPort: { ... },
        extraFlags: { ... },
        portMax: { ... },
        portMin: { ... },
        ...
      },
      enable: bool,
      extraPackages: [Derivation],
      extraStatic: { _: path },
      frontends: { _: { ... } },
      group: string,
      initDb: {
        enable: bool,
        password: { ... },
        username: string,
        ...
      },
      initSecrets: bool,
      installWrapper: bool,
      nginx: { ... },
      package: Derivation,
      user: string,
      ...
    },
    alerta: {
      authenticationRequired: bool,
      bind: string,
      corsOrigins: [string],
      databaseName: string,
      databaseUrl: string,
      enable: bool,
      extraConfig: string,
      logDir: path,
      port: { ... },
      signupEnabled: bool,
      ...
    },
    alice-lg: {
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    alloy: {
      configPath: path,
      enable: bool,
      environmentFile: path | null,
      extraFlags: [string],
      package: Derivation,
      ...
    },
    alps: {
      args: [string],
      bindIP: string,
      enable: bool,
      imaps: {
        host: string,
        port: { ... },
        ...
      },
      package: Derivation,
      port: { ... },
      smtps: {
        host: string,
        port: { ... },
        ...
      },
      theme: string,
      ...
    },
    amazon-cloudwatch-agent: {
      commonConfiguration: { ... },
      commonConfigurationFile: path,
      configuration: { ... },
      configurationFile: path,
      enable: bool,
      mode: string,
      package: Derivation,
      user: string,
      ...
    },
    amazon-ssm-agent: {
      enable: bool,
      package: Derivation,
      ...
    },
    amule: {
      ExternalConnectPasswordFile: path | null,
      WebServerPasswordFile: path | null,
      amuleWebPackage: Derivation,
      dataDir: path,
      enable: bool,
      extraArgs: [string],
      group: string,
      openExternalConnectPort: bool,
      openPeerPorts: bool,
      openWebServerPort: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    ananicy: {
      enable: bool,
      extraCgroups: [{ ... }],
      extraRules: [{ ... }],
      extraTypes: [{ ... }],
      package: Derivation,
      rulesProvider: Derivation,
      settings: { _: { ... } },
      ...
    },
    anbox: { ... },
    angrr: {
      configFile: path | null,
      enable: bool,
      enableNixGcIntegration: bool,
      extraArgs: [string],
      logLevel: string,
      ownedOnly: { ... },
      package: Derivation,
      period: { ... },
      removeRoot: { ... },
      settings: { ... },
      timer: {
        dates: string,
        enable: bool,
        ...
      },
      ...
    },
    anki-sync-server: {
      address: string,
      baseDirectory: string,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      users: [{ ... }],
      ...
    },
    ankisyncd: { ... },
    antennas: { ... },
    anubis: {
      defaultOptions: { ... },
      instances: { _: { ... } },
      package: Derivation,
      ...
    },
    anuko-time-tracker: {
      dataDir: string,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        user: string,
        ...
      },
      enable: bool,
      hostname: string,
      nginx: { ... },
      package: Derivation,
      poolConfig: { _: { ... } },
      settings: {
        defaultCurrency: string,
        defaultLanguage: string,
        email: { ... },
        emailRequired: bool,
        exportDecimalDuration: bool,
        forumLink: string,
        helpLink: string,
        multiorgMode: bool,
        reportFooter: bool,
        weekendStartDay: int,
        ...
      },
      user: string,
      ...
    },
    apache-kafka: {
      brokerId: { ... },
      clusterId: string | null,
      configFiles: {
        log4jProperties: path,
        serverProperties: path,
        ...
      },
      enable: bool,
      extraProperties: { ... },
      formatLogDirs: bool,
      formatLogDirsIgnoreFormatted: bool,
      hostname: { ... },
      jre: Derivation,
      jvmOptions: [string],
      log4jProperties: string,
      logDirs: { ... },
      package: Derivation,
      port: { ... },
      serverProperties: { ... },
      settings: { ... },
      zookeeper: { ... },
      ...
    },
    apcupsd: {
      configText: string,
      enable: bool,
      hooks: { _: string },
      ...
    },
    arbtt: {
      enable: bool,
      logFile: string,
      package: Derivation,
      sampleRate: int,
      ...
    },
    archisteamfarm: {
      bots: { _: { ... } },
      dataDir: path,
      enable: bool,
      ipcPasswordFile: path | null,
      ipcSettings: { ... },
      package: Derivation,
      settings: { ... },
      web-ui: { ... },
      ...
    },
    aria2: {
      downloadDir: { ... },
      downloadDirPermission: string,
      enable: bool,
      extraArguments: { ... },
      listenPortRange: { ... },
      openPorts: bool,
      rpcListenPort: { ... },
      rpcSecret: { ... },
      rpcSecretFile: path,
      serviceUMask: string,
      settings: { ... },
      ...
    },
    armagetronad: {
      servers: { _: { ... } },
      ...
    },
    arsenik: {
      anglemod: bool,
      enable: bool,
      hold_timeout: int,
      hrm: bool,
      lafayette: bool,
      layout: string,
      long_hold_timeout: int,
      lt: bool,
      mac: bool,
      num: bool,
      package: Derivation,
      run: string,
      tap_timeout: int,
      vim: bool,
      wide: bool,
      ...
    },
    artalk: {
      allowModify: bool,
      configFile: string,
      enable: bool,
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      workdir: string,
      ...
    },
    asterisk: {
      confFiles: { _: string },
      enable: bool,
      extraArguments: [string],
      extraConfig: string,
      package: Derivation,
      useTheseDefaultConfFiles: [string],
      ...
    },
    asusd: {
      animeConfig: { ... },
      asusdConfig: { ... },
      auraConfig: { ... },
      auraConfigs: { _: { ... } },
      enable: bool,
      enableUserService: { ... },
      fanCurvesConfig: { ... },
      package: Derivation,
      profileConfig: { ... },
      userLedModesConfig: { ... },
      ...
    },
    atalkd: {
      configFile: path | null,
      enable: bool,
      interfaces: { _: { ... } },
      ...
    },
    atd: {
      allowEveryone: bool,
      enable: bool,
      ...
    },
    atftpd: {
      enable: bool,
      extraOptions: [string],
      root: path,
      ...
    },
    athens: {
      basicAuthPass: string | null,
      basicAuthUser: string | null,
      cloudRuntime: string,
      downloadMode: string | { ... },
      downloadURL: string,
      enable: bool,
      enablePprof: bool,
      extraConfig: { ... },
      filterFile: path | null,
      forceSSL: bool,
      githubToken: string | null,
      globalEndpoint: string,
      goBinary: Derivation,
      goBinaryEnvVars: { ... },
      goEnv: string,
      goGetDir: path | null,
      goGetWorkers: int,
      hgrcPath: path | null,
      index: {
        mysql: { ... },
        postgres: { ... },
        ...
      },
      indexType: string,
      logLevel: string | null,
      netrcPath: path | null,
      networkMode: string,
      noSumPatterns: [string],
      package: Derivation,
      pathPrefix: string | null,
      port: { ... },
      pprofPort: { ... },
      protocolWorkers: int,
      robotsFile: path | null,
      shutdownTimeout: int,
      singleFlight: {
        etcd: { ... },
        redis: { ... },
        redisSentinel: { ... },
        ...
      },
      singleFlightType: string,
      statsExporter: string | null,
      storage: {
        azureblob: { ... },
        cdn: { ... },
        disk: { ... },
        external: { ... },
        gcp: { ... },
        minio: { ... },
        mongo: { ... },
        s3: { ... },
        ...
      },
      storageType: string,
      sumDBs: [string],
      timeout: int,
      tlsCertFile: path | null,
      tlsKeyFile: path | null,
      traceExporter: string | null,
      traceExporterURL: string | null,
      unixSocket: path | null,
      validatorHook: string | null,
      ...
    },
    atticd: {
      enable: bool,
      environmentFile: path | null,
      group: string,
      mode: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    atuin: {
      database: {
        createLocally: bool,
        uri: { ... },
        ...
      },
      enable: bool,
      host: string,
      maxHistoryLength: int,
      openFirewall: bool,
      openRegistration: bool,
      package: Derivation,
      path: string,
      port: { ... },
      ...
    },
    audiobookshelf: {
      dataDir: string,
      enable: bool,
      group: string,
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    authelia: {
      instances: { _: { ... } },
      ...
    },
    auto-cpufreq: {
      enable: bool,
      settings: { ... },
      ...
    },
    auto-epp: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    autobrr: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      secretFile: path,
      settings: { ... },
      ...
    },
    autofs: {
      autoMaster: string,
      debug: bool,
      enable: bool,
      timeout: int,
      ...
    },
    automatic-timezoned: {
      enable: bool,
      package: Derivation,
      ...
    },
    automx2: {
      domain: string,
      enable: bool,
      package: Derivation,
      port: { ... },
      settings: { ... } | null,
      ...
    },
    automysqlbackup: {
      calendar: string,
      config: { _: { ... } },
      enable: bool,
      settings: { _: { ... } },
      ...
    },
    autorandr: {
      defaultTarget: string,
      enable: bool,
      hooks: { ... },
      ignoreLid: bool,
      matchEdid: bool,
      profiles: { _: { ... } },
      ...
    },
    autossh: {
      sessions: [{ ... }],
      ...
    },
    autossh-ng: {
      sessions: { _: { ... } },
      ...
    },
    autosuspend: {
      checks: { _: { ... } },
      enable: bool,
      package: Derivation,
      settings: { ... },
      wakeups: { _: { ... } },
      ...
    },
    autotierfs: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    avahi: {
      allowInterfaces: [string] | null,
      allowPointToPoint: bool,
      browseDomains: [string],
      cacheEntriesMax: int | null,
      debug: bool,
      denyInterfaces: [string] | null,
      domainName: string,
      enable: bool,
      extraConfig: string,
      extraServiceFiles: { _: { ... } },
      hostName: string,
      interfaces: [string] | null,
      ipv4: bool,
      ipv6: bool,
      nssmdns: bool,
      nssmdns4: bool,
      nssmdns6: bool,
      openFirewall: bool,
      package: Derivation,
      publish: {
        addresses: bool,
        domain: bool,
        enable: bool,
        hinfo: bool,
        userServices: bool,
        workstation: bool,
        ...
      },
      reflector: bool,
      wideArea: bool,
      ...
    },
    awstats: {
      configs: { _: { ... } },
      dataDir: path,
      enable: bool,
      service: {
        enable: { ... },
        urlPrefix: { ... },
        ...
      },
      updateAt: string | null,
      vardir: path,
      ...
    },
    ax25: {
      axlisten: {
        config: string,
        enable: bool,
        package: Derivation,
        ...
      },
      axports: { _: { ... } },
      ...
    },
    ayatana-indicators: {
      enable: bool,
      packages: [Derivation],
      ...
    },
    babeld: {
      enable: bool,
      extraConfig: string,
      interfaceDefaults: { _: { ... } },
      interfaces: { _: { ... } },
      ...
    },
    bacula-dir: {
      enable: bool,
      extraConfig: string,
      extraDirectorConfig: string,
      extraMessagesConfig: string,
      name: string,
      password: string,
      port: { ... },
      tls: { ... },
      ...
    },
    bacula-fd: {
      director: { _: { ... } },
      enable: bool,
      extraClientConfig: string,
      extraMessagesConfig: string,
      name: string,
      port: { ... },
      tls: { ... },
      ...
    },
    bacula-sd: {
      autochanger: { _: { ... } },
      device: { _: { ... } },
      director: { _: { ... } },
      enable: bool,
      extraMessagesConfig: string,
      extraStorageConfig: string,
      name: string,
      port: { ... },
      tls: { ... },
      ...
    },
    baget: {
      enable: { ... },
      ...
    },
    baikal: {
      enable: bool,
      group: string,
      package: Derivation,
      phpPackage: Derivation,
      pool: string,
      user: string,
      virtualHost: string | null,
      ...
    },
    bamf: {
      enable: bool,
      ...
    },
    bazarr: {
      dataDir: string,
      enable: bool,
      group: string,
      listenPort: { ... },
      openFirewall: bool,
      package: Derivation,
      user: string,
      ...
    },
    bcachefs: {
      autoScrub: {
        enable: bool,
        fileSystems: { ... },
        interval: string,
        ...
      },
      ...
    },
    bcg: {
      automaticRemoveKitFromNames: bool,
      automaticRenameGenericNodes: bool,
      automaticRenameKitNodes: bool,
      automaticRenameNodes: bool,
      baseTopicPrefix: string,
      device: string,
      enable: bool,
      environmentFiles: [path],
      mqtt: {
        cafile: { ... },
        certfile: { ... },
        host: string,
        keyfile: { ... },
        password: { ... },
        port: { ... },
        username: { ... },
        ...
      },
      name: string | null,
      package: Derivation,
      qosNodeMessages: int,
      rename: { _: string },
      retainNodeMessages: bool,
      verbose: string,
      ...
    },
    beanstalkd: {
      enable: bool,
      listen: {
        address: string,
        port: { ... },
        ...
      },
      openFirewall: bool,
      ...
    },
    bee: {
      daemonNiceLevel: int,
      enable: bool,
      group: string,
      package: Derivation,
      settings: { ... } | null,
      user: string,
      ...
    },
    beegfs: { ... },
    beegfsEnable: { ... },
    beesd: {
      filesystems: { _: { ... } },
      ...
    },
    below: {
      cgroupFilterOut: string | null,
      collect: {
        diskStats: bool,
        exitStats: bool,
        ioStats: bool,
        ...
      },
      compression: {
        enable: bool,
        ...
      },
      dirs: {
        log: { ... },
        store: { ... },
        ...
      },
      enable: bool,
      retention: {
        size: { ... },
        time: { ... },
        ...
      },
      ...
    },
    bentopdf: {
      caddy: {
        enable: bool,
        virtualHost: { ... },
        ...
      },
      domain: string,
      enable: bool,
      nginx: {
        enable: bool,
        virtualHost: { ... },
        ...
      },
      package: Derivation,
      ...
    },
    bepasty: {
      enable: bool,
      servers: { _: { ... } },
      ...
    },
    beszel: {
      agent: {
        enable: bool,
        environment: { ... },
        environmentFile: { ... },
        extraPath: { ... },
        openFirewall: bool,
        package: Derivation,
        smartmon: { ... },
        ...
      },
      hub: {
        dataDir: path,
        enable: bool,
        environment: { ... },
        environmentFile: { ... },
        host: string,
        package: Derivation,
        port: { ... },
        ...
      },
      ...
    },
    biboumi: {
      credentialsFile: path,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    bind: {
      blockedNetworks: [string],
      cacheNetworks: [string],
      configFile: path,
      directory: string,
      enable: bool,
      extraArgs: [string],
      extraConfig: string,
      extraOptions: string,
      forward: string,
      forwarders: [string],
      ipv4Only: bool,
      listenOn: [string],
      listenOnIpv6: [string],
      listenOnIpv6Port: { ... },
      listenOnPort: { ... },
      package: Derivation,
      zones: { _: { ... } },
      ...
    },
    bird: {
      autoReload: bool,
      checkConfig: bool,
      config: string,
      enable: bool,
      package: Derivation,
      preCheckConfig: string,
      ...
    },
    bird-lg: {
      frontend: {
        bgpMapInfo: { ... },
        dnsInterface: string,
        domain: string,
        enable: bool,
        extraArgs: { ... },
        listenAddress: { ... },
        listenAddresses: { ... },
        nameFilter: string,
        navbar: { ... },
        netSpecificMode: string,
        protocolFilter: { ... },
        proxyPort: { ... },
        servers: { ... },
        timeout: int,
        titleBrand: string,
        whois: string,
        ...
      },
      group: string,
      package: Derivation,
      proxy: {
        allowedIPs: { ... },
        birdSocket: string,
        enable: bool,
        extraArgs: { ... },
        listenAddress: { ... },
        listenAddresses: { ... },
        traceroute: { ... },
        ...
      },
      user: string,
      ...
    },
    bird2: { ... },
    bird6: { ... },
    birdwatcher: {
      enable: bool,
      flags: [string],
      package: Derivation,
      settings: string,
      ...
    },
    bitbox-bridge: {
      enable: bool,
      package: Derivation,
      port: { ... },
      runOnMount: bool,
      ...
    },
    bitcoind: { _: { ... } },
    bitlbee: {
      authBackend: string,
      authMode: string,
      configDir: path,
      enable: bool,
      extraDefaults: string,
      extraSettings: string,
      hostName: string,
      interface: string,
      libpurple_plugins: [Derivation],
      plugins: [Derivation],
      portNumber: { ... },
      protocols: string,
      ...
    },
    bitmagnet: {
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      useLocalPostgresDB: bool,
      user: string,
      ...
    },
    bitwarden-directory-connector-cli: {
      domain: string,
      enable: bool,
      interval: string,
      ldap: { ... },
      package: Derivation,
      secrets: {
        bitwarden: { ... },
        ldap: string,
        ...
      },
      sync: { ... },
      user: string,
      ...
    },
    bitwarden_rs: { ... },
    blackfire-agent: {
      enable: bool,
      settings: { ... },
      ...
    },
    blendfarm: {
      basicSecurityPasswordFile: string | null,
      blenderPackage: Derivation,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      serverConfig: { ... },
      user: string,
      ...
    },
    blockbook-frontend: { _: { ... } },
    blocky: {
      enable: bool,
      enableConfigCheck: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    bloop: {
      extraOptions: [string],
      install: bool,
      ...
    },
    blueman: {
      enable: bool,
      ...
    },
    bluemap: {
      addons: { _: path },
      coreSettings: { ... },
      defaultWorld: path,
      enable: bool,
      enableNginx: bool,
      enableRender: bool,
      eula: bool,
      host: string,
      maps: { _: { ... } },
      onCalendar: string,
      packs: { _: path },
      resourcepacks: { _: path },
      storage: { _: { ... } },
      webRoot: path,
      webappSettings: { ... },
      webserverSettings: { ... },
      ...
    },
    bluesky-pds: {
      enable: bool,
      environmentFiles: [path],
      goat: {
        enable: bool,
        ...
      },
      package: Derivation,
      pdsadmin: {
        enable: bool,
        ...
      },
      settings: { ... },
      ...
    },
    boinc: {
      allowRemoteGuiRpc: bool,
      dataDir: path,
      enable: bool,
      extraEnvPackages: [Derivation],
      package: Derivation,
      ...
    },
    bonsaid: {
      configFile: path,
      enable: bool,
      extraFlags: [string],
      package: Derivation,
      settings: [{ ... }],
      ...
    },
    bookstack: {
      appKeyFile: { ... },
      appURL: { ... },
      cacheDir: { ... },
      config: { ... },
      dataDir: path,
      database: {
        createLocally: { ... },
        host: { ... },
        name: { ... },
        passwordFile: { ... },
        port: { ... },
        user: { ... },
        ...
      },
      enable: bool,
      extraConfig: { ... },
      group: string,
      hostname: string,
      mail: {
        driver: { ... },
        encryption: { ... },
        from: { ... },
        fromName: { ... },
        host: { ... },
        name: { ... },
        passwordFile: { ... },
        port: { ... },
        user: { ... },
        ...
      },
      maxUploadSize: string,
      nginx: { ... },
      package: Derivation,
      poolConfig: { _: { ... } },
      settings: { ... },
      user: string,
      ...
    },
    borgbackup: {
      jobs: { _: { ... } },
      package: Derivation,
      repos: { _: { ... } },
      ...
    },
    borgmatic: {
      configurations: { _: { ... } },
      enable: bool,
      enableConfigCheck: bool,
      settings: { ... },
      ...
    },
    bosun: {
      checkFrequency: string,
      enable: bool,
      extraConfig: string,
      group: string,
      influxHost: string | null,
      ledisDir: path,
      listenAddress: string,
      opentsdbHost: string | null,
      package: Derivation,
      stateFile: path,
      user: string,
      ...
    },
    botamusique: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    bpftune: {
      enable: bool,
      package: Derivation,
      ...
    },
    brltty: {
      enable: bool,
      ...
    },
    broadcast-box: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      web: {
        host: string,
        openFirewall: bool,
        port: { ... },
        ...
      },
      ...
    },
    btrbk: {
      extraPackages: [Derivation],
      instances: { _: { ... } },
      ioSchedulingClass: string,
      niceness: { ... },
      sshAccess: [{ ... }],
      ...
    },
    btrfs: {
      autoScrub: {
        enable: bool,
        fileSystems: { ... },
        interval: string,
        limit: { ... },
        ...
      },
      ...
    },
    buffyboard: {
      configFile: path,
      enable: bool,
      extraFlags: [string],
      package: Derivation,
      settings: { ... },
      ...
    },
    buildbot-master: {
      bpPort: string | { ... },
      buildbotDir: path,
      buildbotUrl: string,
      builders: [string],
      changeSource: [string],
      configurators: [string],
      dbUrl: string,
      enable: bool,
      extraConfig: string,
      extraGroups: [string],
      extraImports: string,
      factorySteps: [string],
      group: string,
      home: path,
      listenAddress: string,
      masterCfg: path,
      package: Derivation,
      packages: [Derivation],
      pbPort: string | { ... },
      port: { ... },
      pythonPackages: { ... } -> { ... },
      reporters: [string],
      schedulers: [string],
      status: { ... },
      title: string,
      titleUrl: string,
      user: string,
      workers: [string],
      ...
    },
    buildbot-worker: {
      adminMessage: string | null,
      buildbotDir: path,
      enable: bool,
      extraGroups: [string],
      group: string,
      home: path,
      hostMessage: string | null,
      keepalive: int,
      masterUrl: string,
      package: Derivation,
      packages: [Derivation],
      user: string,
      workerPass: string,
      workerPassFile: path,
      workerUser: string,
      ...
    },
    buildkite-agents: { _: { ... } },
    byedpi: {
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      ...
    },
    c2fmzq-server: {
      bindIP: string,
      enable: bool,
      package: Derivation,
      passphraseFile: string,
      port: { ... },
      settings: { ... },
      ...
    },
    cachefilesd: {
      cacheDir: string,
      enable: bool,
      extraConfig: string,
      ...
    },
    cachix-agent: {
      credentialsFile: path,
      enable: bool,
      host: string | null,
      name: string,
      package: Derivation,
      profile: string | null,
      verbose: bool,
      ...
    },
    cachix-watch-store: {
      cacheName: string,
      cachixTokenFile: path,
      compressionLevel: { ... } | null,
      enable: bool,
      host: string | null,
      jobs: { ... } | null,
      package: Derivation,
      signingKeyFile: path | null,
      verbose: bool,
      ...
    },
    caddy: {
      acmeCA: string | null,
      adapter: string | null,
      agree: { ... },
      ca: string | null,
      config: string,
      configFile: path,
      dataDir: path,
      email: string | null,
      enable: bool,
      enableReload: bool,
      environmentFile: path | null,
      extraConfig: string,
      globalConfig: string,
      group: string,
      httpPort: { ... } | null,
      httpsPort: { ... } | null,
      logDir: path,
      logFormat: string,
      openFirewall: bool,
      package: Derivation,
      resume: bool,
      settings: { ... },
      user: string,
      virtualHosts: { _: { ... } },
      ...
    },
    cadvisor: {
      enable: bool,
      extraOptions: [string],
      listenAddress: string,
      port: { ... },
      storageDriver: string | null,
      storageDriverDb: string,
      storageDriverHost: string,
      storageDriverPassword: string,
      storageDriverPasswordFile: string,
      storageDriverSecure: bool,
      storageDriverUser: string,
      ...
    },
    cage: {
      enable: bool,
      environment: { _: string },
      extraArguments: [string],
      package: Derivation,
      program: path,
      user: string,
      ...
    },
    calibre-server: {
      auth: {
        enable: bool,
        mode: string,
        userDb: { ... },
        ...
      },
      enable: bool,
      extraFlags: [string],
      group: string,
      host: string,
      libraries: [path],
      libraryDir: { ... },
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    calibre-web: {
      dataDir: string,
      enable: bool,
      group: string,
      listen: {
        ip: string,
        port: { ... },
        ...
      },
      openFirewall: bool,
      options: {
        calibreLibrary: { ... },
        enableBookConversion: bool,
        enableBookUploading: bool,
        enableKepubify: bool,
        reverseProxyAuth: { ... },
        ...
      },
      package: Derivation,
      user: string,
      ...
    },
    canaille: {
      enable: bool,
      jwtPrivateKeyFile: path | null,
      ldapBindPasswordFile: path | null,
      package: Derivation,
      secretKeyFile: path,
      settings: { ... },
      smtpPasswordFile: path | null,
      ...
    },
    canto-daemon: {
      enable: bool,
      ...
    },
    cassandra: {
      allowClients: bool,
      clusterName: string,
      enable: bool,
      extraConfig: { ... },
      extraEnvSh: string,
      fullRepairInterval: string | null,
      fullRepairOptions: [string],
      group: string,
      heapNewSize: string | null,
      homeDir: path,
      incrementalRepairInterval: string | null,
      incrementalRepairOptions: [string],
      jmxPort: { ... },
      jmxRoles: [{ ... }],
      jmxRolesFile: path | null,
      jvmOpts: [string],
      listenAddress: string | null,
      listenInterface: string | null,
      logbackConfig: string,
      mallocArenaMax: int | null,
      maxHeapSize: string | null,
      package: Derivation,
      remoteJmx: bool,
      rpcAddress: string | null,
      rpcInterface: string | null,
      seedAddresses: [string],
      user: string,
      ...
    },
    castopod: {
      configureNginx: bool,
      dataDir: path,
      database: {
        createLocally: bool,
        hostname: string,
        name: string,
        passwordFile: { ... },
        user: string,
        ...
      },
      enable: bool,
      environmentFile: path | null,
      localDomain: string,
      maxUploadSize: string,
      package: Derivation,
      poolSettings: { _: { ... } },
      settings: { _: { ... } },
      ...
    },
    cato-client: {
      enable: bool,
      package: Derivation,
      ...
    },
    centrifugo: {
      credentials: { _: path },
      enable: bool,
      environmentFiles: [path],
      extraGroups: [string],
      package: Derivation,
      settings: { ... },
      ...
    },
    ceph: {
      client: {
        enable: bool,
        extraConfig: { ... },
        ...
      },
      enable: bool,
      extraConfig: { _: string },
      global: {
        authClientRequired: string,
        authClusterRequired: string,
        authServiceRequired: string,
        clusterName: string,
        clusterNetwork: { ... },
        fsid: string,
        maxOpenFiles: int,
        mgrModulePath: path,
        monHost: { ... },
        monInitialMembers: { ... },
        publicNetwork: { ... },
        rgwMimeTypesFile: { ... },
        ...
      },
      mds: {
        daemons: { ... },
        enable: bool,
        extraConfig: { ... },
        package: Derivation,
        ...
      },
      mgr: {
        daemons: { ... },
        enable: bool,
        extraConfig: { ... },
        package: Derivation,
        ...
      },
      mon: {
        daemons: { ... },
        enable: bool,
        extraConfig: { ... },
        package: Derivation,
        ...
      },
      osd: {
        daemons: { ... },
        enable: bool,
        extraConfig: { ... },
        package: Derivation,
        ...
      },
      rgw: {
        daemons: { ... },
        enable: bool,
        package: Derivation,
        ...
      },
      ...
    },
    certmgr: {
      defaultRemote: string,
      enable: bool,
      metricsAddress: string,
      metricsPort: { ... },
      package: Derivation,
      renewInterval: string,
      specs: { _: { ... } },
      svcManager: string,
      validMin: string,
      ...
    },
    certspotter: {
      emailRecipients: [string],
      enable: bool,
      extraFlags: [string],
      hooks: [path],
      package: Derivation,
      sendmailPath: path | null,
      startAtEnd: bool,
      watchlist: [string],
      ...
    },
    cfdyndns: {
      apiTokenFile: string | null,
      apikey: { ... },
      apikeyFile: string | null,
      email: string,
      enable: bool,
      records: [string],
      ...
    },
    cfssl: {
      address: string,
      ca: string,
      caBundle: path | null,
      caKey: string,
      configFile: string | null,
      dataDir: path,
      dbConfig: path | null,
      disable: string | null,
      enable: bool,
      intBundle: path | null,
      intDir: path | null,
      logLevel: { ... },
      metadata: path | null,
      mutualTlsCa: path | null,
      mutualTlsClientCert: path | null,
      mutualTlsClientKey: path | null,
      mutualTlsCn: string | null,
      port: { ... },
      remote: string | null,
      responder: path | null,
      responderKey: string | null,
      tlsCert: path | null,
      tlsKey: string | null,
      tlsRemoteCa: path | null,
      ...
    },
    cgit: { _: { ... } },
    cgmanager: {
      enable: { ... },
      ...
    },
    cgminer: {
      config: { _: { ... } },
      enable: bool,
      hardware: [{ ... }],
      package: Derivation,
      pools: [{ ... }],
      user: string,
      ...
    },
    changedetection-io: {
      baseURL: string | null,
      behindProxy: bool,
      chromePort: { ... },
      datastorePath: string,
      enable: bool,
      environmentFile: path | null,
      group: string,
      listenAddress: string,
      package: Derivation,
      playwrightSupport: bool,
      port: { ... },
      user: string,
      webDriverSupport: bool,
      ...
    },
    charybdis: { ... },
    chatgpt-retrieval-plugin: { ... },
    chhoto-url: {
      enable: bool,
      environmentFiles: [path],
      package: Derivation,
      settings: { ... },
      ...
    },
    chisel-server: {
      authfile: path | null,
      backend: string | null,
      enable: bool,
      host: string | null,
      keepalive: string | null,
      port: { ... } | null,
      reverse: bool,
      socks5: bool,
      ...
    },
    chromadb: {
      dbpath: string,
      enable: bool,
      host: string,
      logFile: { ... },
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    chronos: { ... },
    chrony: {
      autotrimThreshold: { ... },
      directory: string,
      dispatcherScript: bool,
      enable: bool,
      enableMemoryLocking: bool,
      enableNTS: bool,
      enableRTCTrimming: bool,
      extraConfig: string,
      extraFlags: [string],
      initstepslew: {
        enabled: bool,
        threshold: { ... },
        ...
      },
      makestep: {
        enable: bool,
        limit: { ... },
        threshold: { ... },
        ...
      },
      package: Derivation,
      serverOption: string,
      servers: [string],
      ...
    },
    cinnamon: {
      apps: {
        enable: bool,
        ...
      },
      ...
    },
    cjdns: {
      ETHInterface: {
        beacon: int,
        bind: string,
        connectTo: { ... },
        ...
      },
      UDPInterface: {
        bind: string,
        connectTo: { ... },
        ...
      },
      addExtraHosts: bool,
      admin: {
        bind: string,
        ...
      },
      authorizedPasswords: [string],
      confFile: path | null,
      enable: bool,
      extraConfig: { ... },
      ...
    },
    clamav: {
      clamonacc: {
        enable: bool,
        ...
      },
      daemon: {
        enable: bool,
        extraConfig: { ... },
        settings: { ... },
        ...
      },
      fangfrisch: {
        enable: bool,
        interval: string,
        settings: { ... },
        ...
      },
      package: Derivation,
      scanner: {
        enable: bool,
        interval: string,
        scanDirectories: { ... },
        ...
      },
      updater: {
        config: { ... },
        enable: bool,
        extraConfig: { ... },
        frequency: int,
        interval: string,
        settings: { ... },
        ...
      },
      ...
    },
    clamsmtp: { ... },
    clatd: {
      enable: bool,
      enableNetworkManagerIntegration: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    clickhouse: {
      enable: bool,
      extraServerConfig: string,
      extraUsersConfig: string,
      package: Derivation,
      serverConfig: { ... },
      usersConfig: { ... },
      ...
    },
    clight: {
      enable: bool,
      settings: { _: { ... } },
      temperature: {
        day: int,
        night: int,
        ...
      },
      ...
    },
    clipcat: {
      enable: bool,
      package: Derivation,
      ...
    },
    clipmenu: {
      enable: bool,
      package: Derivation,
      ...
    },
    cloud-init: {
      btrfs: {
        enable: bool,
        ...
      },
      config: string,
      enable: bool,
      ext4: {
        enable: bool,
        ...
      },
      extraPackages: [Derivation],
      network: {
        enable: bool,
        ...
      },
      settings: { ... },
      xfs: {
        enable: bool,
        ...
      },
      ...
    },
    cloudflare-ddns: {
      cacheExpiration: string,
      credentialsFile: path,
      deleteOnStop: bool,
      detectionTimeout: string,
      domains: [string],
      enable: bool,
      group: string,
      healthchecks: string | null,
      ip4Domains: [string] | null,
      ip6Domains: [string] | null,
      package: Derivation,
      provider: {
        ipv4: string,
        ipv6: string,
        ...
      },
      proxied: string,
      recordComment: string,
      shoutrrr: [string] | null,
      ttl: { ... },
      updateCron: string,
      updateOnStart: bool,
      updateTimeout: string,
      uptimeKuma: string | null,
      user: string,
      wafListDescription: string,
      wafLists: [string],
      ...
    },
    cloudflare-dyndns: {
      apiTokenFile: path,
      deleteMissing: bool,
      domains: [string],
      enable: bool,
      frequency: string | null,
      ipv4: bool,
      ipv6: bool,
      package: Derivation,
      proxied: bool,
      ...
    },
    cloudflare-warp: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      rootDir: string,
      udpPort: { ... },
      ...
    },
    cloudflared: {
      certificateFile: path | null,
      enable: bool,
      group: { ... },
      package: Derivation,
      tunnels: { _: { ... } },
      user: { ... },
      ...
    },
    cloudlog: {
      baseUrl: string,
      dataDir: string,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        user: string,
        ...
      },
      enable: bool,
      extraConfig: string,
      poolConfig: { _: { ... } },
      update-clublog-scp: {
        enable: bool,
        interval: string,
        ...
      },
      update-dok: {
        enable: bool,
        interval: string,
        ...
      },
      update-lotw-users: {
        enable: bool,
        interval: string,
        ...
      },
      update-sota: {
        enable: bool,
        interval: string,
        ...
      },
      update-wwff: {
        enable: bool,
        interval: string,
        ...
      },
      upload-clublog: {
        enable: bool,
        interval: string,
        ...
      },
      upload-lotw: {
        enable: bool,
        interval: string,
        ...
      },
      upload-qrz: {
        enable: bool,
        interval: string,
        ...
      },
      user: string,
      virtualHost: string | null,
      ...
    },
    cntlm: {
      configText: string,
      domain: string,
      enable: bool,
      extraConfig: string,
      netbios_hostname: string,
      noproxy: [string],
      password: string,
      port: [{ ... }],
      proxy: [string],
      username: string,
      ...
    },
    cockpit: {
      allowed-origins: [string],
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      plugins: [Derivation],
      port: { ... },
      settings: { _: { ... } },
      showBanner: bool,
      ...
    },
    cockroachdb: {
      cache: string,
      certsDir: path | null,
      enable: bool,
      extraArgs: [string],
      group: string,
      http: {
        address: string,
        port: { ... },
        ...
      },
      insecure: bool,
      join: string | null,
      listen: {
        address: string,
        port: { ... },
        ...
      },
      locality: string | null,
      maxSqlMemory: string,
      openPorts: bool,
      package: Derivation,
      user: string,
      ...
    },
    cocoon: {
      enable: bool,
      environmentFiles: [path],
      package: Derivation,
      settings: { ... },
      ...
    },
    code-server: {
      auth: string,
      disableFileDownloads: bool,
      disableGettingStartedOverride: bool,
      disableTelemetry: bool,
      disableUpdateCheck: bool,
      disableWorkspaceTrust: bool,
      enable: bool,
      extensionsDir: string | null,
      extraArguments: [string],
      extraEnvironment: { _: string },
      extraGroups: [string],
      extraPackages: [Derivation],
      group: string,
      hashedPassword: string,
      host: string,
      package: Derivation,
      port: { ... },
      proxyDomain: string | null,
      socket: string | null,
      socketMode: string | null,
      user: string,
      userDataDir: string | null,
      ...
    },
    coder: {
      accessUrl: string | null,
      database: {
        createLocally: bool,
        database: string,
        host: string,
        password: { ... },
        sslmode: { ... },
        username: string,
        ...
      },
      enable: bool,
      environment: {
        extra: { ... },
        file: { ... },
        ...
      },
      group: string,
      homeDir: string,
      listenAddress: string,
      package: Derivation,
      tlsCert: path | null,
      tlsKey: path | null,
      user: string,
      wildcardAccessUrl: string | null,
      ...
    },
    codimd: { ... },
    collabora-online: {
      aliasGroups: [{ ... }],
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      port: { ... },
      settings: { _: { ... } },
      ...
    },
    collectd: {
      autoLoadPlugin: bool,
      buildMinimalPackage: bool,
      dataDir: path,
      enable: bool,
      extraConfig: string,
      include: [string],
      package: Derivation,
      plugins: { _: string },
      user: string | null,
      validateConfig: bool,
      ...
    },
    colord: {
      enable: bool,
      ...
    },
    commafeed: {
      enable: bool,
      environment: { _: { ... } },
      environmentFile: path | null,
      group: string,
      package: Derivation,
      stateDir: path,
      user: string,
      ...
    },
    compton: { ... },
    conduwuit: { ... },
    confd: {
      backend: string,
      confDir: path,
      enable: bool,
      interval: int,
      logLevel: string,
      nodes: [string],
      package: Derivation,
      prefix: path,
      watch: bool,
      ...
    },
    confluence: { ... },
    conman: {
      config: string | null,
      configFile: path | null,
      enable: bool,
      package: Derivation,
      ...
    },
    connman: {
      enable: bool,
      enableVPN: bool,
      extraConfig: string,
      extraFlags: [string],
      networkInterfaceBlacklist: [string],
      package: Derivation,
      wifi: {
        backend: string,
        ...
      },
      ...
    },
    consul: {
      alerts: {
        consulAddr: string,
        enable: bool,
        listenAddr: string,
        package: Derivation,
        watchChecks: bool,
        watchEvents: bool,
        ...
      },
      dropPrivileges: bool,
      enable: bool,
      extraConfig: { _: { ... } },
      extraConfigFiles: [string],
      forceAddrFamily: string,
      forceIpv4: bool | null,
      interface: {
        advertise: { ... },
        bind: { ... },
        ...
      },
      leaveOnStop: bool,
      package: Derivation,
      webUi: bool,
      ...
    },
    consul-template: {
      instances: { _: { ... } },
      ...
    },
    convos: {
      enable: bool,
      listenAddress: string,
      listenPort: { ... },
      reverseProxy: bool,
      ...
    },
    cook-cli: {
      autoStart: bool,
      basePath: string,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    coredns: {
      config: string,
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      ...
    },
    corerad: {
      configFile: path,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    corosync: {
      clusterName: string,
      enable: bool,
      extraOptions: [string],
      nodelist: [{ ... }],
      package: Derivation,
      ...
    },
    corteza: {
      address: string,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      user: string,
      ...
    },
    coturn: {
      alt-listening-port: { ... },
      alt-tls-listening-port: { ... },
      cert: string | null,
      cli-ip: string,
      cli-password: string | null,
      cli-port: { ... },
      dh-file: string | null,
      enable: bool,
      extraConfig: string,
      listening-ips: [string],
      listening-port: { ... },
      lt-cred-mech: bool,
      max-port: { ... },
      min-port: { ... },
      no-auth: bool,
      no-cli: bool,
      no-dtls: bool,
      no-tcp: bool,
      no-tcp-relay: bool,
      no-tls: bool,
      no-udp: bool,
      no-udp-relay: bool,
      pkey: string | null,
      realm: string,
      relay-ips: [string],
      secure-stun: bool,
      static-auth-secret: string | null,
      static-auth-secret-file: string | null,
      tls-listening-port: { ... },
      use-auth-secret: bool,
      ...
    },
    couchdb: {
      adminPass: string | null,
      adminUser: string,
      argsFile: path,
      bindAddress: string,
      configFile: path,
      databaseDir: path,
      enable: bool,
      extraConfig: { ... },
      extraConfigFiles: [path],
      group: string,
      logFile: path,
      package: Derivation,
      port: { ... },
      uriFile: path,
      user: string,
      viewIndexDir: path,
      ...
    },
    couchpotato: { ... },
    cpuminer-cryptonight: {
      enable: bool,
      pass: string,
      threads: { ... },
      url: string,
      user: string,
      ...
    },
    cpupower-gui: {
      enable: bool,
      ...
    },
    crab-hole: {
      configFile: path,
      enable: bool,
      package: Derivation,
      settings: { ... },
      supplementaryGroups: [string],
      ...
    },
    crabfit: { ... },
    create_ap: {
      enable: bool,
      settings: { _: { ... } },
      ...
    },
    croc: {
      debug: bool,
      enable: bool,
      openFirewall: bool,
      pass: path | string,
      ports: [{ ... }],
      ...
    },
    cron: {
      cronFiles: [path],
      enable: bool,
      mailto: string | null,
      systemCronJobs: [string],
      ...
    },
    cross-seed: {
      configDir: path,
      enable: bool,
      group: string,
      package: Derivation,
      settings: { ... },
      settingsFile: path | null,
      useGenConfigDefaults: bool,
      user: string,
      ...
    },
    crossfire-server: {
      configFiles: { _: string },
      dataDir: string,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      stateDir: string,
      ...
    },
    crossmacro: {
      daemonPackage: Derivation,
      enable: bool,
      package: Derivation,
      users: [string],
      ...
    },
    crowd: { ... },
    crowdsec: {
      autoUpdateService: bool,
      enable: bool,
      group: string,
      hub: { ... },
      localConfig: { ... },
      name: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    crowdsec-firewall-bouncer: {
      createRulesets: bool,
      enable: bool,
      package: Derivation,
      registerBouncer: {
        bouncerName: string,
        enable: bool,
        ...
      },
      secrets: {
        apiKeyPath: { ... },
        ...
      },
      settings: { ... },
      ...
    },
    cryptpad: {
      configureNginx: bool,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    cyrus-imap: {
      cyrusConfigFile: path | null,
      cyrusSettings: { ... },
      debug: bool,
      enable: bool,
      group: string | null,
      imapdConfigFile: path | null,
      imapdSettings: { ... },
      listenQueue: int,
      sslCACert: { ... },
      sslServerCert: { ... },
      sslServerKey: { ... },
      tmpDBDir: path,
      user: string | null,
      ...
    },
    dae: {
      assets: [path],
      assetsPath: string,
      config: string | null,
      configFile: path | null,
      disableTxChecksumIpGeneric: bool,
      enable: bool,
      openFirewall: { ... },
      package: Derivation,
      ...
    },
    dante: {
      config: string,
      enable: bool,
      ...
    },
    darkhttpd: {
      address: string,
      enable: bool,
      extraArgs: [string],
      hideServerId: bool,
      port: { ... },
      rootDir: path,
      ...
    },
    das_watchdog: {
      enable: bool,
      ...
    },
    dashy: {
      enable: bool,
      finalDrv: Derivation,
      package: Derivation,
      settings: { ... },
      virtualHost: {
        domain: string,
        enableNginx: bool,
        ...
      },
      ...
    },
    datadog-agent: {
      apiKeyFile: path,
      checks: { _: { ... } },
      ddUrl: string | null,
      diskCheck: { ... },
      enable: bool,
      enableLiveProcessCollection: bool,
      enableTraceAgent: bool,
      extraConfig: { ... },
      extraIntegrations: { ... },
      hostname: string | null,
      logLevel: string | null,
      networkCheck: { ... },
      package: Derivation,
      processAgentPackage: Derivation,
      site: string | null,
      tags: [string] | null,
      ...
    },
    davfs2: {
      davGroup: string,
      davUser: string,
      enable: bool,
      extraConfig: { ... },
      settings: { ... },
      ...
    },
    davis: {
      adminLogin: string,
      adminPasswordFile: path,
      appSecretFile: path,
      config: { _: { ... } },
      dataDir: path,
      database: {
        createLocally: bool,
        driver: string,
        name: { ... },
        urlFile: { ... },
        ...
      },
      enable: bool,
      group: string,
      hostname: string,
      mail: {
        dsn: { ... },
        dsnFile: { ... },
        inviteFromAddress: { ... },
        ...
      },
      nginx: { ... },
      package: Derivation,
      poolConfig: { _: { ... } },
      user: string,
      ...
    },
    davmail: {
      config: { ... } | bool,
      enable: bool,
      url: string,
      ...
    },
    dawarich: {
      automaticMigrations: bool,
      configureNginx: bool,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        user: string,
        ...
      },
      enable: bool,
      environment: { _: { ... } },
      extraEnvFiles: [path],
      group: string,
      localDomain: string,
      package: Derivation,
      redis: {
        createLocally: bool,
        host: string,
        port: { ... },
        ...
      },
      secretKeyBaseFile: string | null,
      sidekiqProcesses: { _: { ... } },
      sidekiqThreads: int,
      smtp: {
        fromAddress: { ... },
        host: { ... },
        passwordFile: { ... },
        port: { ... },
        user: { ... },
        ...
      },
      user: string,
      webPort: { ... },
      ...
    },
    db-rest: {
      enable: bool,
      group: string,
      host: string,
      package: Derivation,
      port: { ... },
      redis: {
        createLocally: bool,
        enable: bool,
        host: { ... },
        passwordFile: { ... },
        port: { ... },
        useSSL: bool,
        user: { ... },
        ...
      },
      user: string,
      ...
    },
    dbus: {
      apparmor: string,
      brokerPackage: Derivation,
      dbusPackage: Derivation,
      enable: bool,
      implementation: string,
      packages: [path],
      ...
    },
    dd-agent: { ... },
    ddccontrol: {
      enable: bool,
      package: Derivation,
      ...
    },
    ddclient: {
      configFile: path | null,
      domain: { ... },
      domains: [string],
      enable: bool,
      extraConfig: string,
      homeDir: { ... },
      interval: string,
      ipv6: { ... },
      package: Derivation,
      password: { ... },
      passwordFile: string | null,
      protocol: string,
      quiet: bool,
      script: string,
      secretsFile: string | null,
      server: string,
      ssl: bool,
      use: string,
      username: string,
      usev4: string,
      usev6: string,
      verbose: bool,
      zone: string,
      ...
    },
    ddns-updater: {
      enable: bool,
      environment: { _: string },
      package: Derivation,
      ...
    },
    deconz: {
      allowRebootSystem: bool,
      allowRestartService: bool,
      allowSetSystemTime: bool,
      device: string | null,
      enable: bool,
      extraArgs: [string],
      httpPort: { ... },
      listenAddress: string,
      openFirewall: bool,
      package: Derivation,
      wsPort: { ... },
      ...
    },
    deepin: { ... },
    deluge: {
      authFile: path,
      config: { ... },
      dataDir: path,
      declarative: bool,
      enable: bool,
      extraPackages: [Derivation],
      group: string,
      openFilesLimit: int | string,
      openFirewall: bool,
      package: Derivation,
      user: string,
      web: {
        enable: bool,
        openFirewall: bool,
        port: { ... },
        ...
      },
      ...
    },
    dendrite: {
      enable: bool,
      environmentFile: path | null,
      httpPort: { ... } | null,
      httpsPort: { ... } | null,
      loadCredential: [string],
      openRegistration: bool,
      settings: { ... },
      tlsCert: path | null,
      tlsKey: path | null,
      ...
    },
    dependency-track: {
      database: {
        createLocally: bool,
        databaseName: string,
        passwordFile: path,
        type: string,
        username: string,
        ...
      },
      enable: bool,
      frontend: {
        baseUrl: string,
        ...
      },
      javaArgs: [string],
      ldap: {
        bindPasswordFile: path,
        ...
      },
      logLevel: string,
      nginx: {
        domain: string,
        enable: bool,
        ...
      },
      oidc: {
        clientId: string,
        enable: bool,
        flow: string,
        issuer: string,
        loginButtonText: string,
        scope: string,
        teamSynchronization: bool,
        teams: { ... },
        userProvisioning: bool,
        usernameClaim: string,
        ...
      },
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    desktopManager: {
      budgie: {
        enable: bool,
        extraGSettingsOverridePackages: { ... },
        extraGSettingsOverrides: string,
        extraPlugins: { ... },
        sessionPath: { ... },
        ...
      },
      cosmic: {
        enable: bool,
        showExcludedPkgsWarning: bool,
        xwayland: { ... },
        ...
      },
      gnome: {
        debug: bool,
        enable: bool,
        extraGSettingsOverridePackages: { ... },
        extraGSettingsOverrides: string,
        favoriteAppsOverride: string,
        flashback: { ... },
        sessionPath: { ... },
        ...
      },
      lomiri: {
        basics: bool,
        enable: bool,
        ...
      },
      pantheon: {
        debug: bool,
        enable: bool,
        extraGSettingsOverridePackages: { ... },
        extraGSettingsOverrides: string,
        extraSwitchboardPlugs: { ... },
        extraWingpanelIndicators: { ... },
        sessionPath: { ... },
        ...
      },
      plasma6: {
        enable: bool,
        enableQt5Integration: bool,
        notoPackage: Derivation,
        ...
      },
      ...
    },
    devmon: {
      enable: bool,
      ...
    },
    devpi-server: {
      enable: bool,
      extraPackages: { ... } -> { ... },
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      primaryUrl: string,
      replica: bool,
      secretFile: path | null,
      ...
    },
    dex: {
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      settings: { ... },
      ...
    },
    deye-dummycloud: {
      enable: bool,
      mqttBrokerUrl: string,
      mqttPassword: string,
      mqttUsername: string,
      ...
    },
    dgraph: {
      alpha: {
        host: string,
        port: { ... },
        ...
      },
      enable: bool,
      package: Derivation,
      settings: { ... },
      zero: {
        host: string,
        port: { ... },
        ...
      },
      ...
    },
    dhcpd4: { ... },
    dhcpd6: { ... },
    dictd: {
      DBs: [Derivation],
      enable: bool,
      ...
    },
    diod: {
      allsquash: bool,
      authRequired: bool,
      enable: bool,
      exportall: bool,
      exportopts: [string],
      exports: [string],
      extraConfig: string,
      listen: [string],
      logdest: string,
      nwthreads: int,
      squashuser: string,
      statfsPassthru: bool,
      userdb: bool,
      ...
    },
    discourse: {
      admin: {
        email: string,
        fullName: string,
        passwordFile: path,
        skipCreate: bool,
        username: string,
        ...
      },
      backendSettings: { _: { ... } },
      database: {
        createLocally: bool,
        host: { ... },
        ignorePostgresqlVersion: bool,
        name: string,
        passwordFile: { ... },
        pool: int,
        username: string,
        ...
      },
      enable: bool,
      enableACME: bool,
      hostname: string,
      mail: {
        contactEmailAddress: string,
        incoming: { ... },
        notificationEmailAddress: string,
        outgoing: { ... },
        ...
      },
      nginx: {
        enable: bool,
        ...
      },
      package: Derivation,
      plugins: [Derivation],
      redis: {
        dbNumber: int,
        host: string,
        passwordFile: { ... },
        useSSL: bool,
        ...
      },
      secretKeyBaseFile: path | null,
      sidekiqProcesses: int,
      siteSettings: { ... },
      sslCertificate: path | null,
      sslCertificateKey: path | null,
      unicornTimeout: int,
      ...
    },
    disnix: {
      enable: bool,
      enableMultiUser: bool,
      enableProfilePath: bool,
      package: Derivation,
      profiles: [string],
      useWebServiceInterface: bool,
      ...
    },
    displayManager: {
      autoLogin: { ... },
      cosmic-greeter: {
        enable: bool,
        package: Derivation,
        ...
      },
      defaultSession: string | null,
      dms-greeter: {
        compositor: { ... },
        configFiles: { ... },
        configHome: { ... },
        enable: bool,
        logs: { ... },
        package: Derivation,
        quickshell: { ... },
        ...
      },
      enable: bool,
      environment: { _: { ... } },
      execCmd: string | null,
      gdm: {
        autoLogin: { ... },
        autoSuspend: bool,
        banner: { ... },
        debug: bool,
        enable: bool,
        settings: { ... },
        wayland: bool,
        ...
      },
      generic: {
        enable: bool,
        environment: { ... },
        execCmd: { ... },
        preStart: string,
        ...
      },
      hiddenUsers: [string],
      lemurs: {
        enable: bool,
        package: Derivation,
        settings: { ... },
        vt: { ... },
        ...
      },
      logToFile: bool,
      logToJournal: bool,
      ly: {
        enable: bool,
        package: Derivation,
        settings: { ... },
        x11Support: bool,
        ...
      },
      plasma-login-manager: {
        enable: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      preStart: string,
      sddm: {
        autoLogin: { ... },
        autoNumlock: bool,
        enable: bool,
        enableHidpi: bool,
        extraConfig: { ... },
        extraPackages: { ... },
        package: Derivation,
        settings: { ... },
        setupScript: string,
        stopScript: string,
        theme: string,
        themes: { ... },
        wayland: { ... },
        ...
      },
      sessionData: { ... },
      sessionPackages: [Derivation],
      ...
    },
    distccd: {
      allowedClients: [string],
      enable: bool,
      jobTimeout: int | null,
      logLevel: string | null,
      maxJobs: int | null,
      nice: { ... } | null,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      stats: {
        enable: bool,
        port: { ... },
        ...
      },
      zeroconf: bool,
      ...
    },
    dkimproxy-out: {
      domains: [string],
      enable: bool,
      keySize: int,
      listen: string,
      relay: string,
      selector: string,
      ...
    },
    dleyna: {
      enable: bool,
      ...
    },
    dleyna-renderer: { ... },
    dleyna-server: { ... },
    dnscache: {
      clientIps: [string],
      domainServers: { _: { ... } },
      enable: bool,
      forwardOnly: bool,
      ip: string,
      ...
    },
    dnscrypt-proxy: {
      configFile: path,
      enable: bool,
      package: Derivation,
      settings: { ... },
      upstreamDefaults: bool,
      ...
    },
    dnscrypt-proxy2: { ... },
    dnscrypt-wrapper: { ... },
    dnsdist: {
      dnscrypt: {
        certLifetime: { ... },
        enable: bool,
        listenAddress: string,
        listenPort: { ... },
        providerKey: { ... },
        providerName: string,
        ...
      },
      enable: bool,
      extraConfig: string,
      listenAddress: string,
      listenPort: { ... },
      ...
    },
    dnsmasq: {
      alwaysKeepRunning: bool,
      configFile: Derivation,
      enable: bool,
      extraConfig: { ... },
      package: Derivation,
      resolveLocalQueries: bool,
      servers: { ... },
      settings: { ... },
      ...
    },
    dnsproxy: {
      enable: bool,
      flags: [string],
      package: Derivation,
      settings: { ... },
      ...
    },
    do-agent: {
      enable: bool,
      ...
    },
    dockerRegistry: {
      configFile: path,
      enable: bool,
      enableDelete: bool,
      enableGarbageCollect: bool,
      enableRedisCache: bool,
      extraConfig: { ... },
      garbageCollectDates: string,
      listenAddress: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      redisPassword: string,
      redisUrl: string,
      storagePath: path | null,
      ...
    },
    docling-serve: {
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      stateDir: path,
      ...
    },
    documize: {
      cert: string | null,
      db: string,
      dbtype: string,
      enable: bool,
      forcesslport: { ... } | null,
      key: string | null,
      location: string | null,
      offline: bool,
      package: Derivation,
      port: { ... },
      salt: string | null,
      stateDirectoryName: string,
      ...
    },
    docuseal: {
      enable: bool,
      extraConfig: { ... },
      extraEnvFiles: [path],
      host: string,
      package: Derivation,
      port: { ... },
      redis: {
        createLocally: bool,
        host: string,
        name: string,
        port: { ... },
        ...
      },
      secretKeyBaseFile: path,
      ...
    },
    docuum: {
      deletionChunkSize: int,
      enable: bool,
      keep: [string],
      minAge: string | null,
      threshold: string,
      ...
    },
    doh-proxy-rust: {
      enable: bool,
      flags: [string],
      ...
    },
    doh-server: {
      configFile: path,
      enable: bool,
      package: Derivation,
      settings: { ... },
      useACMEHost: string | null,
      ...
    },
    dokuwiki: {
      sites: { _: { ... } },
      webserver: string,
      ...
    },
    dolibarr: {
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        type: string,
        user: string,
        ...
      },
      domain: string,
      enable: bool,
      group: string,
      h2o: { ... },
      nginx: { ... },
      package: Derivation,
      poolConfig: { _: { ... } },
      settings: { _: { ... } },
      stateDir: string,
      user: string,
      ...
    },
    domoticz: {
      bind: string,
      enable: bool,
      port: { ... },
      ...
    },
    dovecot2: {
      configFile: path | null,
      createMailUser: bool,
      enable: bool,
      enableDHE: bool,
      enableImap: bool,
      enableLmtp: bool,
      enablePAM: bool,
      enablePop3: bool,
      enableQuota: bool,
      extraConfig: string,
      group: string,
      imapsieve: {
        mailbox: { ... },
        ...
      },
      mailGroup: string | null,
      mailLocation: string,
      mailPlugins: { ... },
      mailUser: string | null,
      mailboxes: { _: { ... } },
      modules: { ... },
      package: { ... },
      pluginSettings: { _: { ... } },
      protocols: [string],
      quotaGlobalPerUser: string,
      quotaPort: string,
      showPAMFailure: bool,
      sieve: {
        extensions: { ... },
        globalExtensions: { ... },
        pipeBins: { ... },
        plugins: { ... },
        scripts: { ... },
        ...
      },
      sieveScripts: { _: path },
      sslCACert: string | null,
      sslServerCert: string | null,
      sslServerKey: string | null,
      user: string,
      ...
    },
    dragonflydb: {
      bind: string | null,
      cacheMode: bool | null,
      dbNum: { ... } | null,
      enable: bool,
      keysOutputLimit: { ... },
      maxMemory: { ... } | null,
      memcachePort: { ... } | null,
      port: { ... },
      requirePass: string | null,
      user: string,
      ...
    },
    drasl: {
      enable: bool,
      enableDebug: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    draupnir: {
      accessTokenFile: path | null,
      dataPath: { ... },
      enable: bool,
      homeserverUrl: { ... },
      managementRoom: { ... },
      package: Derivation,
      pantalaimon: { ... },
      secrets: {
        accessToken: { ... },
        pantalaimon: { ... },
        web: { ... },
        ...
      },
      settings: { ... },
      ...
    },
    drbd: {
      config: string,
      enable: bool,
      ...
    },
    druid: {
      broker: {
        config: { ... },
        enable: bool,
        internalConfig: { ... },
        jdk: Derivation,
        jvmArgs: string,
        openFirewall: bool,
        restartIfChanged: bool,
        ...
      },
      commonConfig: { _: { ... } },
      commonTmpDirs: [string],
      coordinator: {
        config: { ... },
        enable: bool,
        internalConfig: { ... },
        jdk: Derivation,
        jvmArgs: string,
        openFirewall: bool,
        restartIfChanged: bool,
        ...
      },
      extraClassPaths: [string],
      extraConfDirs: [path],
      historical: {
        config: { ... },
        enable: bool,
        internalConfig: { ... },
        jdk: Derivation,
        jvmArgs: string,
        openFirewall: bool,
        restartIfChanged: bool,
        segmentLocations: { ... },
        ...
      },
      log4j: path,
      middleManager: {
        config: { ... },
        enable: bool,
        internalConfig: { ... },
        jdk: Derivation,
        jvmArgs: string,
        openFirewall: bool,
        restartIfChanged: bool,
        ...
      },
      overlord: {
        config: { ... },
        enable: bool,
        internalConfig: { ... },
        jdk: Derivation,
        jvmArgs: string,
        openFirewall: bool,
        restartIfChanged: bool,
        ...
      },
      package: Derivation,
      router: {
        config: { ... },
        enable: bool,
        internalConfig: { ... },
        jdk: Derivation,
        jvmArgs: string,
        openFirewall: bool,
        restartIfChanged: bool,
        ...
      },
      ...
    },
    drupal: {
      enable: bool,
      package: Derivation,
      sites: { _: { ... } },
      webserver: string,
      ...
    },
    dsnet: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    dspam: {
      domainSocket: path | null,
      enable: bool,
      extraConfig: string,
      group: string,
      maintenanceInterval: string | null,
      storageDriver: string,
      user: string,
      ...
    },
    duckdns: {
      domains: [string] | null,
      domainsFile: path | null,
      enable: bool,
      tokenFile: path,
      ...
    },
    duckling: {
      enable: bool,
      port: { ... },
      ...
    },
    dump1090-fa: {
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      ...
    },
    dunst: {
      enable: bool,
      enableWayland: bool,
      enableX11: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    duplicati: {
      dataDir: string,
      enable: bool,
      interface: string,
      package: Derivation,
      parameters: string,
      parametersFile: path | null,
      port: { ... },
      user: string,
      ...
    },
    duplicity: {
      cleanup: {
        maxAge: { ... },
        maxFull: { ... },
        maxIncr: { ... },
        ...
      },
      enable: bool,
      exclude: [string],
      excludeFileList: path | null,
      extraFlags: [string],
      frequency: string | null,
      fullIfOlderThan: string,
      include: [string],
      includeFileList: path | null,
      root: path,
      secretFile: path | null,
      targetUrl: string,
      ...
    },
    dwm-status: {
      enable: bool,
      extraConfig: { ... },
      order: { ... },
      package: Derivation,
      settings: { ... },
      ...
    },
    dysnomia: {
      components: { _: { ... } },
      containers: { _: { ... } },
      enable: bool,
      enableAuthentication: bool,
      enableLegacyModules: bool,
      extraContainerPaths: [path],
      extraContainerProperties: { ... },
      extraModulePaths: [path],
      package: path,
      properties: { ... },
      ...
    },
    e-imzo: {
      enable: bool,
      package: Derivation,
      ...
    },
    earlyoom: {
      enable: bool,
      enableDebugInfo: bool,
      enableNotifications: bool,
      extraArgs: [string],
      freeMemKillThreshold: { ... } | null,
      freeMemThreshold: { ... },
      freeSwapKillThreshold: { ... } | null,
      freeSwapThreshold: { ... },
      ignoreOOMScoreAdjust: { ... },
      killHook: path | null,
      notificationsCommand: { ... },
      package: Derivation,
      reportInterval: int,
      useKernelOOMKiller: { ... },
      ...
    },
    easytier: {
      allowSystemForward: bool,
      enable: bool,
      instances: { _: { ... } },
      package: Derivation,
      ...
    },
    ebusd: {
      configpath: string,
      device: string,
      enable: bool,
      extraArguments: [string],
      logs: {
        all: string,
        bus: string,
        device: string,
        main: string,
        network: string,
        other: string,
        update: string,
        ...
      },
      mqtt: {
        enable: bool,
        home-assistant: bool,
        host: string,
        password: string,
        port: { ... },
        retain: bool,
        user: string,
        ...
      },
      package: Derivation,
      port: { ... },
      readonly: bool,
      scanconfig: string,
      ...
    },
    echoip: {
      enable: bool,
      enablePortLookup: bool,
      enableReverseHostnameLookups: bool,
      extraArgs: [string],
      listenAddress: string,
      package: Derivation,
      remoteIpHeader: string | null,
      virtualHost: string | null,
      ...
    },
    ecs-agent: {
      enable: bool,
      extra-environment: { _: string },
      package: Derivation,
      ...
    },
    eg25-manager: {
      enable: bool,
      package: Derivation,
      ...
    },
    eintopf: { ... },
    ejabberd: {
      configFile: path | null,
      ctlConfig: string,
      enable: bool,
      group: string,
      imagemagick: bool,
      loadDumps: [path],
      logsDir: path,
      package: Derivation,
      spoolDir: path,
      user: string,
      ...
    },
    elasticsearch: {
      cluster_name: string,
      dataDir: path,
      enable: bool,
      extraCmdLineOptions: [string],
      extraConf: string,
      extraJavaOptions: [string],
      listenAddress: string,
      logging: string,
      package: Derivation,
      plugins: [Derivation],
      port: { ... },
      restartIfChanged: bool,
      single_node: bool,
      tcp_port: { ... },
      ...
    },
    elasticsearch-curator: {
      actionYAML: string,
      enable: bool,
      hosts: [string],
      interval: string,
      port: { ... },
      ...
    },
    elephant: {
      enable: bool,
      package: Derivation,
      ...
    },
    emacs: {
      defaultEditor: bool,
      enable: bool,
      install: bool,
      package: Derivation,
      startWithGraphical: bool,
      ...
    },
    endlessh: {
      enable: bool,
      extraOptions: [string],
      openFirewall: bool,
      port: { ... },
      ...
    },
    endlessh-go: {
      enable: bool,
      extraOptions: [string],
      listenAddress: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      prometheus: {
        enable: bool,
        listenAddress: string,
        port: { ... },
        ...
      },
      ...
    },
    engelsystem: {
      config: { ... },
      createDatabase: bool,
      domain: string,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    ente: {
      api: {
        domain: string,
        enable: bool,
        enableLocalDB: bool,
        group: string,
        nginx: { ... },
        package: Derivation,
        settings: { ... },
        user: string,
        ...
      },
      web: {
        domains: { ... },
        enable: bool,
        package: Derivation,
        ...
      },
      ...
    },
    envfs: {
      enable: bool,
      extraFallbackPathCommands: string,
      package: Derivation,
      ...
    },
    envoy: {
      enable: bool,
      package: Derivation,
      requireValidConfig: bool,
      settings: { ... },
      ...
    },
    epgstation: {
      basicAuth: { ... },
      clientSocketioPort: { ... },
      database: {
        name: string,
        passwordFile: path,
        ...
      },
      enable: bool,
      ffmpeg: Derivation,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      socketioPort: { ... },
      usePreconfiguredStreaming: bool,
      ...
    },
    epmd: {
      enable: bool,
      listenStream: string,
      package: Derivation,
      ...
    },
    ergo: {
      api: {
        keyHash: { ... },
        listen: { ... },
        ...
      },
      dataDir: path,
      enable: bool,
      group: string,
      listen: {
        ip: string,
        port: { ... },
        ...
      },
      openFirewall: bool,
      testnet: bool,
      user: string,
      ...
    },
    ergochat: {
      configFile: path,
      enable: bool,
      openFilesLimit: int,
      settings: { ... },
      ...
    },
    erigon: {
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      secretJwtPath: path,
      settings: { ... },
      ...
    },
    errbot: {
      instances: { _: { ... } },
      ...
    },
    ersatztv: {
      baseUrl: string,
      enable: bool,
      environment: { _: { ... } },
      group: string,
      openFirewall: bool,
      package: Derivation,
      user: string,
      ...
    },
    esdm: {
      cuseRandomEnable: { ... },
      cuseUrandomEnable: { ... },
      enable: bool,
      enableLinuxCompatServices: bool,
      package: Derivation,
      procEnable: { ... },
      verbose: { ... },
      ...
    },
    espanso: {
      enable: bool,
      package: Derivation,
      ...
    },
    esphome: {
      address: string,
      allowedDevices: [string],
      enable: bool,
      enableUnixSocket: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      usePing: bool,
      ...
    },
    etcd: {
      advertiseClientUrls: [string],
      certFile: path | null,
      clientCertAuth: bool,
      dataDir: path,
      discovery: string,
      enable: bool,
      extraConf: { _: string },
      initialAdvertisePeerUrls: [string],
      initialCluster: [string],
      initialClusterState: string,
      initialClusterToken: string,
      keyFile: path | null,
      listenClientUrls: [string],
      listenPeerUrls: [string],
      name: string,
      openFirewall: bool,
      package: Derivation,
      peerCertFile: path | null,
      peerClientCertAuth: bool,
      peerKeyFile: path | null,
      peerTrustedCaFile: path | null,
      trustedCaFile: path | null,
      ...
    },
    etebase-server: {
      customIni: { ... },
      dataDir: string,
      database: { ... },
      enable: bool,
      host: { ... },
      openFirewall: bool,
      package: Derivation,
      port: { ... } | null,
      secretFile: { ... },
      settings: { ... },
      unixSocket: string | null,
      user: string,
      ...
    },
    eternal-terminal: {
      enable: bool,
      logSize: int,
      port: { ... },
      silent: bool,
      verbosity: string,
      ...
    },
    etesync-dav: {
      apiUrl: string,
      enable: bool,
      host: string,
      openFirewall: bool,
      port: { ... },
      sslCertificate: path | null,
      sslCertificateKey: path | null,
      ...
    },
    ethercalc: { ... },
    evcc: {
      enable: bool,
      environmentFile: path | null,
      extraArgs: [string],
      package: Derivation,
      settings: { ... } | null,
      ...
    },
    evdevremapkeys: {
      enable: bool,
      settings: { ... },
      ...
    },
    evremap: {
      enable: bool,
      settings: { ... },
      ...
    },
    exhibitor: { ... },
    exim: {
      config: string,
      enable: bool,
      group: string,
      package: Derivation,
      queueRunnerInterval: string,
      spoolDir: path,
      user: string,
      ...
    },
    expressvpn: {
      enable: bool,
      ...
    },
    factorio: {
      admins: [string],
      allowedPlayers: [string],
      autosave-interval: int | null,
      bind: string,
      configFile: path,
      description: string | null,
      enable: bool,
      extraArgs: [string],
      extraSettings: { ... },
      extraSettingsFile: path | null,
      game-name: string | null,
      game-password: string | null,
      lan: bool,
      loadLatestSave: bool,
      mods: [Derivation],
      mods-dat: path | null,
      nonBlockingSaving: bool,
      openFirewall: bool,
      package: Derivation,
      password: string | null,
      port: { ... },
      public: bool,
      requireUserVerification: bool,
      saveName: string,
      stateDirName: string,
      token: string | null,
      username: string | null,
      ...
    },
    fail2ban: {
      banaction: string,
      banaction-allports: string,
      bantime: string,
      bantime-increment: {
        enable: bool,
        factor: { ... },
        formula: { ... },
        maxtime: { ... },
        multipliers: { ... },
        overalljails: { ... },
        rndtime: { ... },
        ...
      },
      daemonConfig: { ... },
      daemonSettings: { _: { ... } },
      enable: bool,
      extraPackages: [Derivation],
      extraSettings: { ... },
      ignoreIP: [string],
      jails: { _: { ... } },
      maxretry: { ... },
      package: Derivation,
      packageFirewall: Derivation,
      ...
    },
    fakeroute: {
      enable: bool,
      route: [string],
      ...
    },
    fanout: {
      bufferSize: int,
      enable: bool,
      fanoutDevices: int,
      ...
    },
    fastnetmon-advanced: {
      bgpPeers: { _: { ... } },
      enable: bool,
      enableAdvancedTrafficPersistence: bool,
      hostgroups: { _: { ... } },
      settings: { ... },
      traffic_db: {
        settings: { ... },
        ...
      },
      ...
    },
    fcgiwrap: {
      enable: { ... },
      group: { ... },
      instances: { _: { ... } },
      preforkProcesses: { ... },
      socketAddress: { ... },
      socketType: { ... },
      user: { ... },
      ...
    },
    fcron: {
      allow: [string],
      deny: [string],
      enable: bool,
      maxSerialJobs: int,
      queuelen: int | null,
      systab: string,
      ...
    },
    fedimintd: { _: { ... } },
    fediwall: {
      enable: bool,
      hostName: string,
      nginx: { ... },
      package: Derivation,
      settings: { ... },
      ...
    },
    felix: {
      bundles: [Derivation],
      enable: bool,
      group: string,
      user: string,
      ...
    },
    ferm: {
      config: string,
      enable: bool,
      package: Derivation,
      ...
    },
    ferretdb: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    fider: {
      dataDir: string,
      database: {
        url: string,
        ...
      },
      enable: bool,
      environment: { _: string },
      environmentFiles: [path],
      package: Derivation,
      ...
    },
    filebeat: {
      enable: bool,
      inputs: { _: { ... } },
      modules: { _: { ... } },
      package: Derivation,
      settings: { ... },
      ...
    },
    filebrowser: {
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    filesender: { ... },
    firebird: {
      baseDir: string,
      enable: bool,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    firefly-iii: {
      dataDir: path,
      enable: bool,
      enableNginx: bool,
      group: string,
      package: Derivation,
      poolConfig: { _: { ... } },
      settings: { ... },
      user: string,
      virtualHost: string,
      ...
    },
    firefly-iii-data-importer: {
      dataDir: path,
      enable: bool,
      enableNginx: bool,
      group: string,
      package: Derivation,
      poolConfig: { _: { ... } },
      settings: { ... },
      user: string,
      virtualHost: string,
      ...
    },
    firefox: {
      syncserver: { ... },
      ...
    },
    firefox-syncserver: {
      database: {
        createLocally: bool,
        host: string,
        name: { ... },
        user: string,
        ...
      },
      enable: bool,
      logLevel: string,
      package: Derivation,
      secrets: path,
      settings: { ... },
      singleNode: {
        capacity: { ... },
        enable: bool,
        enableNginx: bool,
        enableTLS: bool,
        hostname: string,
        url: string,
        ...
      },
      ...
    },
    fireqos: {
      config: string,
      enable: bool,
      ...
    },
    firewalld: {
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      packages: [Derivation],
      services: { _: { ... } },
      settings: { ... },
      zones: { _: { ... } },
      ...
    },
    firezone: {
      gateway: {
        apiUrl: { ... },
        enable: bool,
        enableTelemetry: bool,
        logLevel: string,
        name: string,
        package: Derivation,
        tokenFile: path,
        ...
      },
      gui-client: {
        allowedUsers: { ... },
        enable: bool,
        logLevel: string,
        name: string,
        package: Derivation,
        ...
      },
      headless-client: {
        apiUrl: { ... },
        enable: bool,
        enableTelemetry: bool,
        logLevel: string,
        name: string,
        package: Derivation,
        tokenFile: path,
        ...
      },
      relay: {
        apiUrl: { ... },
        enable: bool,
        enableTelemetry: bool,
        highestPort: { ... },
        logLevel: string,
        lowestPort: { ... },
        name: string,
        openFirewall: bool,
        package: Derivation,
        port: { ... },
        publicIpv4: { ... },
        publicIpv6: { ... },
        tokenFile: path,
        ...
      },
      server: {
        api: { ... },
        clusterHosts: { ... },
        domain: { ... },
        enable: bool,
        enableLocalDB: bool,
        nginx: { ... },
        openClusterFirewall: bool,
        provision: { ... },
        settings: { ... },
        settingsSecret: { ... },
        smtp: { ... },
        web: { ... },
        ...
      },
      ...
    },
    flannel: {
      backend: { ... },
      enable: bool,
      etcd: {
        caFile: { ... },
        certFile: { ... },
        endpoints: { ... },
        keyFile: { ... },
        prefix: string,
        ...
      },
      extraNetworkConfig: { ... },
      iface: string | null,
      kubeconfig: path | null,
      network: string,
      nodeName: string | null,
      package: Derivation,
      publicIp: string | null,
      storageBackend: string,
      subnetLen: int,
      subnetMax: string | null,
      subnetMin: string | null,
      ...
    },
    flaresolverr: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    flarum: {
      adminEmail: string,
      adminUser: string,
      baseUrl: string,
      createDatabaseLocally: bool,
      database: { _: { ... } },
      domain: string,
      enable: bool,
      forumTitle: string,
      group: string,
      initialAdminPassword: string,
      package: Derivation,
      stateDir: path,
      user: string,
      ...
    },
    flashpolicyd: { ... },
    flatpak: {
      enable: bool,
      extraPortals: [Derivation],
      package: Derivation,
      ...
    },
    flexget: {
      config: string,
      enable: bool,
      homeDir: path,
      interval: string,
      package: Derivation,
      systemScheduler: bool,
      user: string,
      ...
    },
    flood: {
      enable: bool,
      extraArgs: [string],
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    fluent-bit: {
      configurationFile: path,
      enable: bool,
      graceLimit: { ... } | string | null,
      package: Derivation,
      settings: { ... },
      ...
    },
    fluentd: {
      config: string,
      enable: bool,
      package: Derivation,
      plugins: [path],
      ...
    },
    fluidd: {
      enable: bool,
      hostName: string,
      nginx: { ... },
      package: Derivation,
      ...
    },
    foldingAtHome: { ... },
    foldingathome: {
      config: { ... },
      daemonNiceLevel: { ... },
      enable: bool,
      extraArgs: [string],
      nickname: string | null,
      package: Derivation,
      team: int,
      user: string | null,
      ...
    },
    forgejo: {
      appName: { ... },
      cookieSecure: { ... },
      customDir: string,
      database: {
        createDatabase: bool,
        host: string,
        name: string,
        password: { ... },
        passwordFile: { ... },
        path: string,
        port: { ... },
        socket: { ... },
        type: string,
        user: string,
        ...
      },
      disableRegistration: { ... },
      domain: { ... },
      dump: {
        age: string,
        backupDir: string,
        enable: bool,
        file: { ... },
        interval: string,
        type: string,
        ...
      },
      enable: bool,
      enableUnixSocket: { ... },
      extraConfig: { ... },
      group: string,
      httpAddress: { ... },
      httpPort: { ... },
      lfs: {
        contentDir: string,
        enable: bool,
        ...
      },
      log: {
        level: { ... },
        rootPath: { ... },
        ...
      },
      mailerPasswordFile: { ... },
      package: Derivation,
      repositoryRoot: string,
      rootUrl: { ... },
      secrets: { ... },
      settings: { ... },
      ssh: {
        clonePort: { ... },
        enable: { ... },
        ...
      },
      stateDir: string,
      staticRootPath: { ... },
      useWizard: bool,
      user: string,
      ...
    },
    foundationdb: {
      backupProcesses: int,
      class: string | null,
      dataDir: path,
      enable: bool,
      extraReadWritePaths: [path],
      group: string,
      listenAddress: string,
      listenPortStart: { ... },
      locality: { ... },
      logDir: path,
      logSize: string,
      maxLogSize: string,
      memory: string,
      openFirewall: bool,
      package: Derivation,
      pidfile: path,
      publicAddress: string,
      restartDelay: int,
      serverProcesses: int,
      storageMemory: string,
      tls: { ... },
      traceFormat: string,
      user: string,
      ...
    },
    fourStore: { ... },
    fourStoreEndpoint: { ... },
    fprintd: {
      enable: bool,
      package: Derivation,
      tod: {
        driver: Derivation,
        enable: bool,
        ...
      },
      ...
    },
    fprot: { ... },
    frab: { ... },
    fractalart: {
      enable: bool,
      height: int | null,
      width: int | null,
      ...
    },
    freeciv: {
      enable: bool,
      openFirewall: bool,
      settings: { ... },
      ...
    },
    freefall: {
      devices: [string],
      enable: bool,
      package: Derivation,
      ...
    },
    freenet: {
      enable: bool,
      nice: { ... },
      ...
    },
    freeradius: {
      configDir: path,
      debug: bool,
      enable: bool,
      package: Derivation,
      ...
    },
    freeswitch: {
      configDir: { _: path },
      configTemplate: path,
      enable: bool,
      enableReload: bool,
      package: Derivation,
      ...
    },
    freshrss: {
      api: {
        enable: bool,
        ...
      },
      authType: string,
      baseUrl: string,
      dataDir: string,
      database: {
        host: { ... },
        name: { ... },
        passFile: { ... },
        port: { ... },
        tableprefix: { ... },
        type: string,
        user: { ... },
        ...
      },
      defaultUser: string,
      enable: bool,
      extensions: [Derivation],
      language: string,
      package: Derivation,
      passwordFile: path | null,
      pool: string | null,
      user: string,
      virtualHost: string,
      webserver: string,
      ...
    },
    frigate: {
      checkConfig: bool,
      enable: bool,
      hostname: string,
      package: Derivation,
      preCheckConfig: string,
      settings: { ... },
      vaapiDriver: string | null,
      ...
    },
    froide-govplan: {
      dataDir: string,
      enable: bool,
      hostName: string,
      package: Derivation,
      secretKeyFile: path | null,
      settings: { ... },
      ...
    },
    frp: {
      enable: { ... },
      instances: { _: { ... } },
      package: Derivation,
      role: { ... },
      settings: { ... },
      ...
    },
    frr: {
      babel: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      babeld: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      bfd: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      bfdd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      bgp: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      bgpd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      config: string,
      configFile: path | null,
      eigrp: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      eigrpd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      fabric: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      fabricd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      isis: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      isisd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      ldp: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      ldpd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      mgmt: {
        config: { ... },
        configFile: { ... },
        enable: { ... },
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      mgmtd: {
        extraOptions: { ... },
        options: { ... },
        ...
      },
      nhrp: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      nhrpd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      openFilesLimit: { ... },
      ospf: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      ospf6: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      ospf6d: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      ospfd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      pathd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      pbr: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      pbrd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      pim: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      pim6d: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      pimd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      rip: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      ripd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      ripng: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      ripngd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      sharp: {
        config: { ... },
        configFile: { ... },
        enable: bool,
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      sharpd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      static: {
        config: { ... },
        configFile: { ... },
        enable: { ... },
        extraOptions: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      staticd: {
        extraOptions: { ... },
        options: { ... },
        ...
      },
      vrrpd: {
        enable: bool,
        extraOptions: { ... },
        options: { ... },
        ...
      },
      zebra: {
        config: { ... },
        configFile: { ... },
        enable: { ... },
        extraOptions: { ... },
        options: { ... },
        vtyListenAddress: { ... },
        vtyListenPort: { ... },
        ...
      },
      ...
    },
    fstrim: {
      enable: bool,
      interval: string,
      ...
    },
    fusionInventory: {
      enable: bool,
      extraConfig: string,
      servers: [string],
      ...
    },
    fwupd: {
      blacklistDevices: { ... },
      blacklistPlugins: { ... },
      daemonSettings: { ... },
      disabledDevices: { ... },
      disabledPlugins: { ... },
      enable: bool,
      enableTestRemote: { ... },
      extraRemotes: [string],
      extraTrustedKeys: [path],
      package: Derivation,
      uefiCapsuleSettings: { ... },
      ...
    },
    g3proxy: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    g810-led: {
      earlySetup: bool,
      enable: bool,
      package: Derivation,
      profile: string | null,
      ...
    },
    galene: {
      certFile: path | null,
      dataDir: path,
      enable: bool,
      group: string,
      groupsDir: path,
      httpAddress: string,
      httpPort: { ... },
      insecure: bool,
      keyFile: path | null,
      package: Derivation,
      recordingsDir: path,
      stateDir: path,
      staticDir: path,
      turnAddress: string,
      user: string,
      ...
    },
    gammu-smsd: {
      backend: {
        files: { ... },
        service: string,
        sql: { ... },
        ...
      },
      device: {
        connection: string,
        group: string,
        path: path,
        pin: { ... },
        synchronizeTime: bool,
        ...
      },
      enable: bool,
      extraConfig: {
        gammu: string,
        smsd: string,
        ...
      },
      log: {
        file: string,
        format: string,
        ...
      },
      user: string,
      ...
    },
    gancio: {
      enable: bool,
      nginx: { ... },
      package: Derivation,
      plugins: [Derivation],
      settings: { ... },
      user: string,
      userLocale: { _: { ... } },
      ...
    },
    garage: {
      enable: bool,
      environmentFile: path | null,
      extraEnvironment: { _: string },
      logLevel: string,
      package: Derivation,
      settings: { ... },
      ...
    },
    gateone: { ... },
    gatus: {
      configFile: path,
      enable: bool,
      environmentFile: path | null,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    gdomap: {
      enable: bool,
      ...
    },
    gemstash: {
      enable: bool,
      openFirewall: bool,
      settings: { ... },
      ...
    },
    geoclue2: {
      appConfig: { _: { ... } },
      enable: bool,
      enable3G: bool,
      enableCDMA: bool,
      enableDemoAgent: bool,
      enableModemGPS: bool,
      enableNmea: bool,
      enableStatic: bool,
      enableWifi: bool,
      geoProviderUrl: string,
      package: Derivation,
      staticAccuracy: { ... },
      staticAltitude: int | float,
      staticLatitude: { ... },
      staticLongitude: { ... },
      submissionNick: string,
      submissionUrl: string,
      submitData: bool,
      whitelistedAgents: [string],
      ...
    },
    geoip-updater: { ... },
    geoipupdate: {
      enable: bool,
      interval: string,
      settings: { ... },
      ...
    },
    gerbil: {
      environmentFile: path | null,
      port: { ... },
      ...
    },
    gerrit: {
      builtinPlugins: [string],
      enable: bool,
      jvmHeapLimit: string,
      jvmOpts: [string],
      jvmPackage: Derivation,
      listenAddress: string,
      package: Derivation,
      plugins: [Derivation],
      replicationSettings: { _: { ... } },
      serverId: string,
      settings: { _: { ... } },
      ...
    },
    geth: { _: { ... } },
    getty: {
      autologinOnce: bool,
      autologinUser: string | null,
      extraArgs: [string],
      greetingLine: string,
      helpLine: string,
      loginOptions: string | null,
      loginProgram: path,
      serialSpeed: { ... },
      ...
    },
    ghostunnel: {
      enable: bool,
      package: Derivation,
      servers: { _: { ... } },
      ...
    },
    gitDaemon: {
      basePath: string,
      enable: bool,
      exportAll: bool,
      group: string,
      listenAddress: string,
      options: string,
      package: Derivation,
      port: { ... },
      repositories: [string],
      user: string,
      ...
    },
    gitea: {
      appName: string,
      camoHmacKeyFile: string | null,
      captcha: {
        enable: bool,
        requireForExternalRegistration: bool,
        requireForLogin: bool,
        secretFile: { ... },
        siteKey: { ... },
        type: string,
        url: { ... },
        ...
      },
      cookieSecure: { ... },
      customDir: string,
      database: {
        createDatabase: bool,
        host: string,
        name: string,
        password: string,
        passwordFile: { ... },
        path: string,
        port: { ... },
        socket: { ... },
        type: string,
        user: string,
        ...
      },
      disableRegistration: { ... },
      domain: { ... },
      dump: {
        backupDir: string,
        enable: bool,
        file: { ... },
        interval: string,
        type: string,
        ...
      },
      enable: bool,
      enableUnixSocket: { ... },
      extraConfig: string | null,
      group: string,
      httpAddress: { ... },
      httpPort: { ... },
      lfs: {
        contentDir: string,
        enable: bool,
        ...
      },
      log: {
        level: { ... },
        rootPath: { ... },
        ...
      },
      mailerPasswordFile: string | null,
      metricsTokenFile: string | null,
      minioAccessKeyId: string | null,
      minioSecretAccessKey: string | null,
      package: Derivation,
      repositoryRoot: string,
      rootUrl: { ... },
      settings: { ... },
      ssh: {
        clonePort: { ... },
        enable: { ... },
        ...
      },
      stateDir: string,
      staticRootPath: { ... },
      useWizard: { ... },
      user: string,
      ...
    },
    gitea-actions-runner: {
      instances: { _: { ... } },
      package: Derivation,
      ...
    },
    github-runner: { ... },
    github-runners: { _: { ... } },
    gitlab: {
      backup: {
        keepTime: int,
        path: string,
        skip: { ... },
        startAt: { ... },
        uploadOptions: { ... },
        ...
      },
      backupPath: string,
      databaseCreateLocally: bool,
      databaseHost: string,
      databaseName: string,
      databasePasswordFile: path | null,
      databasePool: int,
      databaseUsername: string,
      enable: bool,
      extraConfig: { ... },
      extraDatabaseConfig: { ... },
      extraEnv: { _: string },
      extraGitlabRb: string,
      extraShellConfig: { ... },
      group: string,
      host: string,
      https: bool,
      initialRootEmail: string,
      initialRootPasswordFile: path | null,
      logrotate: {
        enable: bool,
        extraConfig: { ... },
        frequency: string,
        keep: int,
        ...
      },
      packages: {
        gitaly: Derivation,
        gitlab: Derivation,
        gitlab-shell: Derivation,
        gitlab-workhorse: Derivation,
        pages: Derivation,
        ...
      },
      pages: {
        enable: bool,
        settings: { ... },
        ...
      },
      pagesExtraArgs: { ... },
      port: { ... },
      puma: {
        threadsMax: int,
        threadsMin: int,
        workers: int,
        ...
      },
      redisUrl: string,
      registry: {
        certFile: path,
        defaultForProjects: bool,
        enable: bool,
        externalAddress: string,
        externalPort: { ... },
        host: string,
        issuer: string,
        keyFile: path,
        package: Derivation,
        port: { ... },
        serviceName: string,
        ...
      },
      satelliteDir: { ... },
      secrets: {
        activeRecordDeterministicKeyFile: { ... },
        activeRecordPrimaryKeyFile: { ... },
        activeRecordSaltFile: { ... },
        dbFile: { ... },
        jwsFile: { ... },
        otpFile: { ... },
        secretFile: { ... },
        ...
      },
      sidekiq: {
        concurrency: { ... },
        memoryKiller: { ... },
        ...
      },
      smtp: {
        address: string,
        authentication: { ... },
        domain: string,
        enable: bool,
        enableStartTLSAuto: bool,
        opensslVerifyMode: string,
        passwordFile: { ... },
        port: { ... },
        tls: bool,
        username: { ... },
        ...
      },
      stateDir: string,
      statePath: string,
      user: string,
      workhorse: {
        config: { ... },
        ...
      },
      ...
    },
    gitlab-runner: {
      checkInterval: { ... },
      clear-docker-cache: {
        dates: string,
        enable: bool,
        flags: { ... },
        package: { ... },
        ...
      },
      concurrent: { ... },
      configFile: path | null,
      configOptions: { ... },
      enable: bool,
      extraPackages: [Derivation],
      gracefulTermination: bool,
      gracefulTimeout: string,
      package: Derivation,
      packages: [Derivation],
      prometheusListenAddress: { ... },
      sentryDSN: { ... },
      services: { _: { ... } },
      sessionServer: {
        advertiseAddress: { ... },
        listenAddress: { ... },
        sessionTimeout: { ... },
        ...
      },
      settings: { ... },
      workDir: { ... },
      ...
    },
    gitolite: {
      adminPubkey: string,
      commonHooks: [path],
      dataDir: string,
      description: string,
      enable: bool,
      enableGitAnnex: bool,
      extraGitoliteRc: string,
      group: string,
      user: string,
      ...
    },
    gitwatch: { _: { ... } },
    gitweb: {
      extraConfig: string,
      gitwebConfigFile: path,
      gitwebTheme: bool,
      projectroot: path,
      ...
    },
    glance: {
      enable: bool,
      environmentFile: path | null,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    glances: {
      enable: bool,
      extraArgs: [string],
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    glitchtip: {
      celery: {
        extraArgs: { ... },
        ...
      },
      database: {
        createLocally: bool,
        ...
      },
      enable: bool,
      environmentFiles: [path],
      group: string,
      gunicorn: {
        extraArgs: { ... },
        ...
      },
      listenAddress: string,
      package: Derivation,
      port: { ... },
      redis: {
        createLocally: bool,
        ...
      },
      settings: { ... },
      stateDir: path,
      user: string,
      ...
    },
    globalprotect: {
      csdWrapper: path | null,
      enable: bool,
      settings: { ... },
      ...
    },
    glpiAgent: {
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      stateDir: string,
      ...
    },
    glusterfs: {
      enable: bool,
      enableGlustereventsd: bool,
      extraFlags: [string],
      killMode: string,
      logLevel: string,
      stopKillTimeout: string,
      tlsSettings: { ... },
      useRpcbind: bool,
      ...
    },
    gmediarender: {
      audioDevice: string | null,
      audioSink: string | null,
      enable: bool,
      friendlyName: string | null,
      initialVolume: int | null,
      package: Derivation,
      port: { ... } | null,
      uuid: string | null,
      ...
    },
    gnome: {
      at-spi2-core: {
        enable: bool,
        ...
      },
      core-apps: {
        enable: bool,
        ...
      },
      core-developer-tools: {
        enable: bool,
        ...
      },
      core-os-services: {
        enable: bool,
        ...
      },
      core-shell: {
        enable: bool,
        ...
      },
      core-utilities: {
        enable: bool,
        ...
      },
      evolution-data-server: {
        enable: bool,
        plugins: { ... },
        ...
      },
      games: {
        enable: bool,
        ...
      },
      gcr-ssh-agent: {
        enable: bool,
        package: Derivation,
        ...
      },
      glib-networking: {
        enable: bool,
        ...
      },
      gnome-browser-connector: {
        enable: bool,
        ...
      },
      gnome-initial-setup: {
        enable: bool,
        ...
      },
      gnome-keyring: {
        enable: bool,
        ...
      },
      gnome-online-accounts: {
        enable: bool,
        ...
      },
      gnome-online-miners: { ... },
      gnome-remote-desktop: {
        enable: bool,
        ...
      },
      gnome-settings-daemon: {
        enable: bool,
        ...
      },
      gnome-software: {
        enable: bool,
        ...
      },
      gnome-user-share: {
        enable: bool,
        ...
      },
      localsearch: {
        enable: bool,
        ...
      },
      rygel: {
        enable: bool,
        package: Derivation,
        ...
      },
      sushi: {
        enable: bool,
        ...
      },
      tinysparql: {
        enable: bool,
        ...
      },
      tracker: {
        enable: bool,
        subcommandPackages: { ... },
        ...
      },
      tracker-miners: {
        enable: bool,
        ...
      },
      ...
    },
    gns3-server: {
      auth: {
        enable: bool,
        passwordFile: { ... },
        user: { ... },
        ...
      },
      dynamips: {
        enable: bool,
        package: Derivation,
        ...
      },
      enable: bool,
      log: {
        debug: bool,
        file: { ... },
        ...
      },
      package: Derivation,
      settings: { ... },
      ssl: {
        certFile: { ... },
        enable: bool,
        keyFile: { ... },
        ...
      },
      ubridge: {
        enable: bool,
        package: Derivation,
        ...
      },
      vpcs: {
        enable: bool,
        package: Derivation,
        ...
      },
      ...
    },
    gnunet: {
      enable: bool,
      extraOptions: string,
      fileSharing: {
        quota: int,
        ...
      },
      load: {
        hardNetUpBandwidth: int,
        maxNetDownBandwidth: int,
        maxNetUpBandwidth: int,
        ...
      },
      package: Derivation,
      tcp: {
        port: { ... },
        ...
      },
      udp: {
        port: { ... },
        ...
      },
      ...
    },
    go-autoconfig: {
      enable: bool,
      settings: { ... },
      ...
    },
    go-camo: {
      enable: bool,
      extraOptions: [string],
      keyFile: path,
      listen: string | null,
      sslCert: path | null,
      sslKey: path | null,
      sslListen: string | null,
      ...
    },
    go-csp-collector: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    go-httpbin: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    go-neb: {
      baseUrl: string,
      bindAddress: string,
      config: { ... } | null,
      enable: bool,
      secretFile: path | null,
      ...
    },
    go-shadowsocks2: {
      server: {
        enable: bool,
        listenAddress: string,
        ...
      },
      ...
    },
    go2rtc: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    goatcounter: {
      address: string,
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      port: { ... },
      proxy: bool,
      ...
    },
    gobgpd: {
      enable: bool,
      settings: { ... },
      ...
    },
    gocd-agent: {
      agentConfig: string,
      enable: bool,
      environment: { _: string },
      extraGroups: [string],
      extraOptions: [string],
      goServer: string,
      group: string,
      initialJavaHeapSize: string,
      maxJavaHeapMemory: string,
      packages: [Derivation],
      startupOptions: [string],
      user: string,
      workDir: string,
      ...
    },
    gocd-server: {
      enable: bool,
      environment: { _: string },
      extraGroups: [string],
      extraOptions: [string],
      group: string,
      initialJavaHeapSize: string,
      listenAddress: string,
      maxJavaHeapMemory: string,
      packages: [Derivation],
      port: { ... },
      sslPort: { ... },
      startupOptions: [string],
      user: string,
      workDir: string,
      ...
    },
    godns: {
      enable: bool,
      loadCredential: [string],
      package: Derivation,
      settings: { ... },
      ...
    },
    goeland: {
      enable: bool,
      schedule: string,
      settings: { ... },
      stateDir: path,
      ...
    },
    gogoclient: { ... },
    gokapi: {
      enable: bool,
      environment: { ... },
      mutableSettings: bool,
      package: Derivation,
      settings: { ... },
      settingsFile: string | null,
      ...
    },
    gollum: {
      address: string,
      allowUploads: string | null,
      branch: string,
      emoji: bool,
      enable: bool,
      extraConfig: string,
      group: string,
      h1-title: bool,
      local-time: { ... },
      math: bool,
      mathjax: { ... },
      no-edit: bool,
      package: Derivation,
      port: { ... },
      stateDir: path,
      user: string,
      user-icons: string | null,
      ...
    },
    gonic: {
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    goss: {
      enable: bool,
      environment: { _: string },
      package: Derivation,
      settings: { ... },
      ...
    },
    gotenberg: {
      bindIP: string | null,
      bodyLimit: string | null,
      chromium: {
        autoStart: bool,
        disableJavascript: bool,
        disableRoutes: bool,
        maxQueueSize: { ... },
        package: Derivation,
        ...
      },
      downloadFrom: {
        allowList: { ... },
        denyList: { ... },
        disable: bool,
        maxRetries: { ... },
        ...
      },
      enable: bool,
      enableBasicAuth: bool,
      environmentFile: path | null,
      extraArgs: [string],
      extraFontPackages: [Derivation],
      libreoffice: {
        autoStart: bool,
        disableRoutes: bool,
        maxQueueSize: { ... },
        package: Derivation,
        restartAfter: { ... },
        ...
      },
      logLevel: string,
      package: Derivation,
      pdfEngines: {
        convert: { ... },
        disableRoutes: bool,
        merge: { ... },
        readMetadata: { ... },
        writeMetadata: { ... },
        ...
      },
      port: { ... },
      rootPath: string,
      timeout: string | null,
      ...
    },
    gotify: {
      enable: bool,
      environment: { _: { ... } },
      environmentFiles: [path],
      package: Derivation,
      port: { ... },
      stateDirectoryName: string,
      ...
    },
    gotosocial: {
      enable: bool,
      environmentFile: path | null,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      setupPostgresqlDB: bool,
      ...
    },
    govee2mqtt: {
      enable: bool,
      environmentFile: path,
      group: string,
      package: Derivation,
      user: string,
      ...
    },
    goxlr-utility: {
      autoStart: {
        xdg: bool,
        ...
      },
      enable: bool,
      package: Derivation,
      ...
    },
    gpm: {
      enable: bool,
      protocol: string,
      ...
    },
    gpsd: {
      debugLevel: int,
      device: { ... },
      devices: [string],
      enable: bool,
      extraArgs: [string],
      listenany: bool,
      nowait: bool,
      port: { ... },
      readonly: bool,
      ...
    },
    grafana: {
      addr: { ... },
      analytics: {
        reporting: { ... },
        ...
      },
      auth: {
        anonymous: { ... },
        azuread: { ... },
        disableLoginForm: { ... },
        google: { ... },
        ...
      },
      certFile: { ... },
      certKey: { ... },
      dataDir: path,
      database: {
        connMaxLifetime: { ... },
        host: { ... },
        name: { ... },
        password: { ... },
        passwordFile: { ... },
        path: { ... },
        type: { ... },
        user: { ... },
        ...
      },
      declarativePlugins: [path] | null,
      domain: { ... },
      enable: bool,
      extraOptions: { ... },
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      protocol: { ... },
      provision: {
        alerting: { ... },
        dashboards: { ... },
        datasources: { ... },
        enable: bool,
        notifiers: { ... },
        ...
      },
      rootUrl: { ... },
      security: {
        adminPassword: { ... },
        adminPasswordFile: { ... },
        adminUser: { ... },
        secretKey: { ... },
        secretKeyFile: { ... },
        ...
      },
      server: {
        serveFromSubPath: { ... },
        ...
      },
      settings: { ... },
      smtp: {
        enable: { ... },
        fromAddress: { ... },
        password: { ... },
        passwordFile: { ... },
        user: { ... },
        ...
      },
      socket: { ... },
      staticRootPath: { ... },
      users: {
        allowOrgCreate: { ... },
        allowSignUp: { ... },
        autoAssignOrg: { ... },
        autoAssignOrgRole: { ... },
        ...
      },
      ...
    },
    grafana-agent: { ... },
    grafana-image-renderer: {
      chromium: { ... },
      enable: bool,
      provisionGrafana: bool,
      settings: { ... },
      verbose: { ... },
      ...
    },
    grafana-to-ntfy: {
      enable: bool,
      package: Derivation,
      settings: {
        bauthPass: path,
        bauthUser: string,
        ntfyBAuthPass: path,
        ntfyBAuthUser: { ... },
        ntfyUrl: string,
        ...
      },
      ...
    },
    grafana_reporter: {
      addr: string,
      enable: bool,
      grafana: {
        addr: string,
        port: { ... },
        protocol: string,
        ...
      },
      port: { ... },
      templateDir: string | path,
      ...
    },
    graphical-desktop: {
      enable: bool,
      ...
    },
    graphite: {
      api: { ... },
      beacon: { ... },
      carbon: {
        aggregationRules: { ... },
        blacklist: { ... },
        config: string,
        enableAggregator: bool,
        enableCache: bool,
        enableRelay: bool,
        relayRules: { ... },
        rewriteRules: { ... },
        storageAggregation: { ... },
        storageSchemas: { ... },
        whitelist: { ... },
        ...
      },
      dataDir: path,
      pager: { ... },
      seyren: {
        enable: bool,
        extraConfig: { ... },
        graphiteUrl: string,
        mongoUrl: string,
        port: { ... },
        seyrenUrl: string,
        ...
      },
      web: {
        enable: bool,
        extraConfig: string,
        listenAddress: string,
        port: { ... },
        ...
      },
      ...
    },
    grav: {
      enable: bool,
      maxUploadSize: string,
      package: Derivation,
      phpPackage: Derivation,
      pool: string,
      root: path,
      systemSettings: { ... },
      virtualHost: string | null,
      ...
    },
    graylog: {
      dataDir: string,
      elasticsearchHosts: [string],
      enable: bool,
      extraConfig: string,
      isMaster: bool,
      messageJournalDir: string,
      mongodbUri: string,
      nodeIdFile: string,
      package: Derivation,
      passwordSecret: string,
      plugins: [Derivation],
      rootPasswordSha2: string,
      rootUsername: string,
      user: string,
      ...
    },
    greenclip: {
      enable: bool,
      package: Derivation,
      ...
    },
    greetd: {
      enable: bool,
      greeterManagesPlymouth: bool,
      package: Derivation,
      restart: bool,
      settings: { ... },
      useTextGreeter: bool,
      vt: { ... },
      ...
    },
    grocy: {
      dataDir: string,
      enable: bool,
      extraConfig: string,
      hostName: string,
      nginx: {
        enableSSL: bool,
        ...
      },
      package: Derivation,
      phpfpm: {
        settings: { ... },
        ...
      },
      settings: {
        calendar: { ... },
        culture: string,
        currency: string,
        entryPage: string,
        ...
      },
      ...
    },
    gsignond: { ... },
    guacamole-client: {
      enable: bool,
      enableWebserver: bool,
      logbackXml: path | null,
      package: Derivation,
      settings: { ... },
      userMappingXml: path | null,
      ...
    },
    guacamole-server: {
      enable: bool,
      extraEnvironment: { _: string },
      host: string,
      logbackXml: path | null,
      package: Derivation,
      port: { ... },
      userMappingXml: path | null,
      ...
    },
    guix: {
      enable: bool,
      extraArgs: [string],
      gc: {
        dates: string,
        enable: bool,
        extraArgs: { ... },
        ...
      },
      group: string,
      nrBuildUsers: { ... },
      package: Derivation,
      publish: {
        enable: bool,
        extraArgs: { ... },
        generateKeyPair: bool,
        port: { ... },
        user: string,
        ...
      },
      stateDir: path,
      storeDir: path,
      substituters: {
        authorizedKeys: { ... },
        urls: { ... },
        ...
      },
      ...
    },
    gvfs: {
      enable: bool,
      package: Derivation,
      ...
    },
    gvpe: {
      configFile: path | null,
      configText: string | null,
      customIFSetup: string,
      enable: bool,
      ipAddress: string | null,
      nodename: string | null,
      subnet: string | null,
      ...
    },
    h2o: {
      defaultHTTPListenPort: { ... },
      defaultTLSListenPort: { ... },
      defaultTLSRecommendations: string | null,
      enable: bool,
      group: string,
      hosts: { _: { ... } },
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    hadoop: {
      containerExecutorCfg: { _: { ... } },
      coreSite: { _: { ... } },
      coreSiteInternal: { _: { ... } },
      extraConfDirs: [path],
      gatewayRole: {
        enable: bool,
        enableHbaseCli: bool,
        ...
      },
      hbase: {
        master: { ... },
        package: Derivation,
        regionServer: { ... },
        rest: { ... },
        rootdir: string,
        thrift: { ... },
        zookeeperQuorum: { ... },
        ...
      },
      hbaseSite: { _: { ... } },
      hbaseSiteDefault: { _: { ... } },
      hbaseSiteInternal: { _: { ... } },
      hdfs: {
        datanode: { ... },
        httpfs: { ... },
        journalnode: { ... },
        namenode: { ... },
        zkfc: { ... },
        ...
      },
      hdfsSite: { _: { ... } },
      hdfsSiteDefault: { _: { ... } },
      hdfsSiteInternal: { _: { ... } },
      httpfsSite: { _: { ... } },
      log4jProperties: path,
      mapredSite: { _: { ... } },
      mapredSiteDefault: { _: { ... } },
      package: Derivation,
      yarn: {
        nodemanager: { ... },
        resourcemanager: { ... },
        ...
      },
      yarnSite: { _: { ... } },
      yarnSiteDefault: { _: { ... } },
      yarnSiteInternal: { _: { ... } },
      ...
    },
    haka: { ... },
    handheld-daemon: {
      adjustor: {
        enable: bool,
        loadAcpiCallModule: bool,
        package: { ... },
        ...
      },
      enable: bool,
      package: Derivation,
      ui: {
        enable: bool,
        package: Derivation,
        ...
      },
      user: string,
      ...
    },
    hans: {
      clients: { _: { ... } },
      server: {
        enable: bool,
        extraConfig: string,
        ip: string,
        passwordFile: string,
        respondToSystemPings: bool,
        ...
      },
      ...
    },
    haproxy: {
      config: string | null,
      enable: bool,
      group: string,
      package: Derivation,
      user: string,
      ...
    },
    hardware: {
      argonone: {
        enable: bool,
        package: Derivation,
        ...
      },
      bolt: {
        enable: bool,
        package: Derivation,
        ...
      },
      deepcool-digital-linux: {
        enable: bool,
        extraArgs: { ... },
        package: Derivation,
        ...
      },
      dell-bios-fan-control: {
        enable: bool,
        package: Derivation,
        ...
      },
      lcd: {
        client: { ... },
        server: { ... },
        serverHost: string,
        serverPort: int,
        ...
      },
      openrgb: {
        enable: bool,
        motherboard: { ... },
        package: Derivation,
        server: { ... },
        startupProfile: { ... },
        ...
      },
      pommed: {
        configFile: { ... },
        enable: bool,
        ...
      },
      ...
    },
    harmonia: {
      cache: {
        enable: bool,
        settings: { ... },
        signKeyPath: { ... },
        signKeyPaths: { ... },
        ...
      },
      daemon: {
        dbPath: string,
        enable: bool,
        logLevel: string,
        socketPath: string,
        storeDir: string,
        ...
      },
      enable: bool,
      package: Derivation,
      settings: { ... },
      signKeyPath: path | null,
      signKeyPaths: [path],
      ...
    },
    haste-server: {
      enable: bool,
      openFirewall: bool,
      settings: { ... } | null,
      ...
    },
    hatsu: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    haveged: {
      enable: bool,
      refill_threshold: int,
      ...
    },
    haven: {
      blastrRelays: [string],
      enable: bool,
      environmentFile: path | null,
      importRelays: [string],
      package: Derivation,
      settings: { ... },
      ...
    },
    hbase: { ... },
    hbase-standalone: {
      dataDir: path,
      enable: bool,
      group: string,
      logDir: path,
      package: Derivation,
      settings: { _: { ... } },
      user: string,
      ...
    },
    hdapsd: {
      enable: bool,
      ...
    },
    hddfancontrol: {
      disks: { ... },
      enable: bool,
      extraArgs: { ... },
      logVerbosity: { ... },
      package: Derivation,
      pwmPaths: { ... },
      settings: { _: { ... } },
      smartctl: { ... },
      ...
    },
    headphones: {
      configFile: path,
      dataDir: path,
      enable: bool,
      group: string,
      host: string,
      port: { ... },
      user: string,
      ...
    },
    headscale: {
      address: string,
      configFile: path,
      derp: {
        autoUpdate: { ... },
        auto_update_enable: { ... },
        paths: { ... },
        updateFrequency: { ... },
        urls: { ... },
        ...
      },
      enable: bool,
      ephemeralNodeInactivityTimeout: { ... },
      group: string,
      logLevel: { ... },
      openIdConnect: {
        clientId: { ... },
        clientSecretFile: { ... },
        domainMap: { ... },
        issuer: { ... },
        ...
      },
      package: Derivation,
      port: { ... },
      serverUrl: { ... },
      settings: { ... },
      tls: {
        certFile: { ... },
        keyFile: { ... },
        letsencrypt: { ... },
        ...
      },
      user: string,
      ...
    },
    healthchecks: {
      dataDir: string,
      enable: bool,
      group: string,
      listenAddress: string,
      package: Derivation,
      port: { ... },
      settings: { ... },
      settingsFile: path | null,
      user: string,
      ...
    },
    heapster: {
      enable: bool,
      extraOpts: string,
      package: Derivation,
      sink: string,
      source: string,
      ...
    },
    heartbeat: {
      enable: bool,
      extraConfig: string,
      name: string,
      package: Derivation,
      stateDir: string,
      tags: [string],
      ...
    },
    hebbot: {
      botPasswordFile: path,
      enable: bool,
      package: Derivation,
      settings: { ... },
      templates: {
        project: path,
        report: path,
        section: path,
        ...
      },
      ...
    },
    hedgedoc: {
      configuration: { ... },
      configureNginx: bool,
      enable: bool,
      environmentFile: path | null,
      groups: { ... },
      package: Derivation,
      settings: { ... },
      workDir: { ... },
      ...
    },
    heisenbridge: {
      address: string,
      debug: bool,
      enable: bool,
      extraArgs: [string],
      homeserver: string,
      identd: {
        enable: bool,
        port: { ... },
        ...
      },
      namespaces: { ... },
      owner: string | null,
      package: Derivation,
      port: { ... },
      registrationUrl: string,
      ...
    },
    hercules-ci-agent: {
      baseDirectory: { ... },
      concurrentTasks: { ... },
      enable: bool,
      extraOptions: { ... },
      package: Derivation,
      patchNix: { ... },
      settings: { ... },
      tomlFile: path,
      user: { ... },
      ...
    },
    hickory-dns: {
      configFile: path,
      debug: bool,
      enable: bool,
      package: Derivation,
      quiet: bool,
      settings: { ... },
      ...
    },
    hitch: {
      backend: string,
      ciphers: string,
      enable: bool,
      extraConfig: string,
      frontend: string | { ... },
      group: string,
      ocsp-stapling: {
        enabled: bool,
        ...
      },
      pem-files: [path],
      user: string,
      ...
    },
    hledger-web: {
      allow: string,
      baseUrl: string | null,
      capabilities: { ... },
      enable: bool,
      extraOptions: [string],
      host: string,
      journalFiles: [string],
      port: { ... },
      serveApi: bool,
      stateDir: path,
      ...
    },
    hockeypuck: {
      enable: bool,
      port: { ... },
      settings: { ... },
      ...
    },
    hologram-agent: {
      dialAddress: string,
      enable: bool,
      httpPort: string,
      ...
    },
    hologram-server: {
      awsAccount: string,
      awsDefaultRole: string,
      cacheTimeoutSeconds: int,
      enable: bool,
      enableLdapRoles: bool,
      groupClassAttr: string,
      ldapBaseDN: string,
      ldapBindDN: string,
      ldapBindPassword: string,
      ldapHost: string,
      ldapInsecure: bool,
      ldapUserAttr: string,
      listenAddress: string,
      roleAttr: string,
      statsAddress: string,
      ...
    },
    home-assistant: {
      applyDefaultConfig: { ... },
      autoExtraComponents: { ... },
      blueprints: {
        automation: { ... },
        script: { ... },
        template: { ... },
        ...
      },
      config: { ... } | null,
      configDir: path,
      configWritable: bool,
      customComponents: [{ ... }],
      customLovelaceModules: [Derivation],
      defaultIntegrations: [string],
      enable: bool,
      extraArgs: [string],
      extraComponents: [string],
      extraPackages: { ... } -> { ... },
      lovelaceConfig: { ... } | null | null,
      lovelaceConfigFile: path | null,
      lovelaceConfigWritable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    homeassistant-satellite: { ... },
    homebox: {
      database: {
        createLocally: bool,
        ...
      },
      enable: bool,
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    homebridge: {
      enable: bool,
      environmentFile: string | null,
      group: string,
      openFirewall: bool,
      pluginPath: string,
      settings: { ... },
      uiSettings: { ... },
      user: string,
      userStoragePath: string,
      ...
    },
    homed: {
      enable: bool,
      promptOnFirstBoot: bool,
      settings: {
        Home: { ... },
        ...
      },
      ...
    },
    homepage-dashboard: {
      allowedHosts: string,
      bookmarks: { ... },
      customCSS: string,
      customJS: string,
      docker: { ... },
      enable: bool,
      environmentFile: { ... },
      environmentFiles: [path],
      kubernetes: { ... },
      listenPort: { ... },
      openFirewall: bool,
      package: Derivation,
      proxmox: { ... },
      services: { ... },
      settings: { ... },
      widgets: { ... },
      ...
    },
    homer: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      virtualHost: {
        caddy: { ... },
        domain: string,
        nginx: { ... },
        ...
      },
      ...
    },
    honk: {
      enable: bool,
      extraCSS: path | null,
      extraJS: path | null,
      host: string,
      package: Derivation,
      passwordFile: path,
      port: { ... },
      servername: string,
      username: string,
      ...
    },
    hoogle: {
      enable: bool,
      extraOptions: [string],
      haskellPackages: { ... },
      home: string,
      host: string,
      packages: { ... } -> { ... },
      port: { ... },
      ...
    },
    hostapd: {
      channel: { ... },
      countryCode: { ... },
      driver: { ... },
      enable: bool,
      extraConfig: { ... },
      group: { ... },
      hwMode: { ... },
      interface: { ... },
      logLevel: { ... },
      noScan: { ... },
      package: Derivation,
      radios: { _: { ... } },
      ssid: { ... },
      wpa: { ... },
      wpaPassphrase: { ... },
      ...
    },
    hound: {
      config: { ... },
      enable: bool,
      extraGroups: { ... },
      group: string,
      home: path,
      listen: string,
      package: Derivation,
      settings: { ... } | null,
      user: string,
      ...
    },
    howdy: {
      control: string,
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    hqplayerd: {
      auth: {
        password: { ... },
        username: { ... },
        ...
      },
      config: string | null,
      enable: bool,
      licenseFile: path | null,
      openFirewall: bool,
      ...
    },
    htpdate: {
      enable: bool,
      extraOptions: string,
      proxy: string,
      servers: [string],
      ...
    },
    httpd: {
      adminAddr: string | null,
      configFile: path,
      customLogFormat: string,
      documentRoot: { ... },
      enable: bool,
      enableMellon: bool,
      enablePHP: bool,
      enablePerl: bool,
      enableSSL: { ... },
      enableUserDir: { ... },
      extraConfig: string,
      extraModules: [{ ... }],
      extraSubservices: { ... },
      globalRedirect: { ... },
      group: string,
      hostName: { ... },
      listen: { ... },
      logDir: path,
      logFormat: string,
      logLevel: string | null,
      logPerVirtualHost: bool,
      maxClients: { ... },
      maxRequestsPerChild: { ... },
      mpm: string,
      multiProcessingModule: string,
      package: Derivation,
      phpOptions: string,
      phpPackage: Derivation,
      robotsEntries: { ... },
      servedDirs: { ... },
      servedFiles: { ... },
      serverAliases: { ... },
      sslCiphers: string,
      sslProtocols: string,
      sslServerCert: { ... },
      sslServerChain: { ... },
      sslServerKey: { ... },
      stateDir: { ... },
      user: string,
      virtualHosts: { _: { ... } },
      ...
    },
    https-dns-proxy: {
      address: string,
      enable: bool,
      extraArgs: [string],
      port: { ... },
      preferIPv4: bool,
      provider: {
        ips: { ... },
        kind: string,
        url: string,
        ...
      },
      ...
    },
    hydra: {
      buildMachinesFiles: [path],
      dbi: string,
      debugServer: bool,
      enable: bool,
      extraConfig: string,
      extraEnv: { _: string },
      gcRootsDir: path,
      hydraURL: string,
      listenHost: string,
      logo: path | null,
      maxServers: int,
      maxSpareServers: int,
      minSpareServers: int,
      minimumDiskFree: int,
      minimumDiskFreeEvaluator: int,
      notificationSender: string,
      package: Derivation,
      port: { ... },
      smtpHost: string | null,
      tracker: string,
      useSubstitutes: bool,
      ...
    },
    hydron: { ... },
    hylafax: {
      areaCode: string | null,
      autostart: bool,
      commonModemConfig: { _: { ... } },
      countryCode: string | null,
      enable: bool,
      faxcron: {
        enable: { ... },
        infoDays: { ... },
        logDays: { ... },
        rcvDays: { ... },
        ...
      },
      faxqConfig: { _: { ... } },
      faxqclean: {
        archiving: string,
        docqMinutes: { ... },
        doneqMinutes: { ... },
        enable: { ... },
        ...
      },
      hfaxdConfig: { _: { ... } },
      internationalPrefix: string | null,
      longDistancePrefix: string | null,
      modems: { _: { ... } },
      package: Derivation,
      sendmailPath: path,
      spoolAreaPath: path,
      spoolExtraInit: string,
      userAccessFile: path,
      ...
    },
    hypridle: {
      enable: bool,
      package: Derivation,
      ...
    },
    hyprwhspr-rs: {
      enable: bool,
      environmentFile: string | null,
      package: Derivation,
      ...
    },
    i2p: {
      enable: bool,
      ...
    },
    i2pd: {
      address: string | null,
      addressbook: {
        defaulturl: string,
        subscriptions: { ... },
        ...
      },
      bandwidth: int | null,
      dataDir: string | null,
      enable: bool,
      enableIPv4: bool,
      enableIPv6: bool,
      exploratory: {
        inbound: { ... },
        outbound: { ... },
        ...
      },
      extIp: string | null,
      family: string | null,
      floodfill: bool,
      ifname: string | null,
      ifname4: string | null,
      ifname6: string | null,
      inTunnels: { _: { ... } },
      limits: {
        coreSize: int,
        ntcpHard: int,
        ntcpSoft: int,
        ntcpThreads: int,
        openFiles: int,
        transittunnels: int,
        ...
      },
      logCLFTime: bool,
      logLevel: string,
      nat: bool,
      netid: int,
      notransit: bool,
      ntcp: bool,
      ntcp2: {
        enable: bool,
        port: { ... },
        published: bool,
        ...
      },
      ntcpProxy: string | null,
      outTunnels: { _: { ... } },
      package: Derivation,
      port: { ... } | null,
      precomputation: {
        elgamal: bool,
        ...
      },
      proto: {
        bob: { ... },
        http: { ... },
        httpProxy: { ... },
        i2cp: { ... },
        i2pControl: { ... },
        sam: { ... },
        socksProxy: { ... },
        ...
      },
      reseed: {
        file: { ... },
        floodfill: { ... },
        proxy: { ... },
        urls: { ... },
        verify: bool,
        zipfile: { ... },
        ...
      },
      share: int,
      ssu: bool,
      ssu2: {
        enable: bool,
        port: { ... },
        published: bool,
        ...
      },
      trust: {
        enable: bool,
        family: { ... },
        hidden: bool,
        routers: { ... },
        ...
      },
      upnp: {
        enable: bool,
        name: string,
        ...
      },
      websocket: {
        address: string,
        enable: bool,
        name: string,
        port: { ... },
        ...
      },
      yggdrasil: {
        address: { ... },
        enable: bool,
        ...
      },
      ...
    },
    icecast: {
      admin: {
        password: string,
        user: string,
        ...
      },
      enable: bool,
      extraConf: string,
      extraConfig: string,
      group: { ... },
      hostname: string | null,
      listen: {
        address: string,
        port: { ... },
        ...
      },
      logDir: { ... },
      user: { ... },
      ...
    },
    icecream: {
      daemon: {
        cacheLimit: { ... },
        enable: bool,
        extraArgs: { ... },
        hostname: { ... },
        maxProcesses: { ... },
        netName: string,
        nice: { ... },
        noRemote: bool,
        openBroadcast: bool,
        openFirewall: bool,
        package: Derivation,
        schedulerHost: { ... },
        user: string,
        ...
      },
      scheduler: {
        enable: bool,
        extraArgs: { ... },
        netName: { ... },
        openFirewall: bool,
        openTelnet: bool,
        package: Derivation,
        persistentClientConnection: bool,
        port: { ... },
        ...
      },
      ...
    },
    icingaweb2: {
      authentications: { ... } | null,
      enable: bool,
      generalConfig: { ... } | null,
      groupBackends: { ... } | null,
      libraryPaths: { _: Derivation },
      modulePackages: { _: Derivation },
      modules: {
        doc: { ... },
        migrate: { ... },
        monitoring: { ... },
        setup: { ... },
        test: { ... },
        translation: { ... },
        ...
      },
      pool: string,
      resources: { ... } | null,
      roles: { ... } | null,
      timezone: string,
      virtualHost: string | null,
      ...
    },
    ifm: {
      dataDir: string,
      enable: bool,
      listenAddress: string,
      port: { ... },
      settings: { _: { ... } },
      ...
    },
    ihaskell: {
      enable: bool,
      extraPackages: { ... } -> { ... },
      ...
    },
    ihatemoney: { ... },
    iio-niri: {
      enable: bool,
      extraArgs: [string],
      niriUnit: string,
      package: Derivation,
      ...
    },
    illum: {
      enable: bool,
      ...
    },
    imaginary: {
      address: string,
      enable: bool,
      port: { ... },
      settings: { ... },
      ...
    },
    immich: {
      accelerationDevices: [string] | null,
      database: {
        createDB: bool,
        enable: bool,
        enableVectorChord: { ... },
        enableVectors: { ... },
        host: string,
        name: string,
        port: { ... },
        user: string,
        ...
      },
      enable: bool,
      environment: { ... },
      group: string,
      host: string,
      machine-learning: {
        enable: bool,
        environment: { ... },
        ...
      },
      mediaLocation: path,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      redis: {
        enable: bool,
        host: string,
        port: { ... },
        ...
      },
      secretSettings: { ... },
      secretsFile: string | null,
      settings: { ... },
      user: string,
      ...
    },
    immich-kiosk: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    immich-public-proxy: {
      enable: bool,
      immichUrl: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    immichframe: {
      enable: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    inadyn: {
      configFile: path | null,
      enable: bool,
      group: string,
      interval: string,
      logLevel: string,
      settings: { ... },
      user: string,
      ...
    },
    incron: {
      allow: [string] | null,
      deny: [string] | null,
      enable: bool,
      extraPackages: [Derivation],
      systab: string,
      ...
    },
    infinoted: {
      certificateChain: path | null,
      certificateFile: path | null,
      enable: bool,
      extraConfig: string,
      group: string,
      keyFile: path | null,
      package: Derivation,
      passwordFile: path | null,
      plugins: [string],
      port: { ... },
      rootDirectory: path,
      securityPolicy: string,
      user: string,
      ...
    },
    influxdb: {
      dataDir: path,
      enable: bool,
      extraConfig: { ... },
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    influxdb2: {
      enable: bool,
      package: Derivation,
      provision: {
        enable: bool,
        initialSetup: { ... },
        organizations: { ... },
        users: { ... },
        ...
      },
      settings: { ... },
      ...
    },
    infnoise: {
      enable: bool,
      fillDevRandom: bool,
      ...
    },
    input-remapper: {
      enable: bool,
      enableUdevRules: bool,
      package: Derivation,
      serviceWantedBy: [string],
      ...
    },
    inputplumber: {
      enable: bool,
      package: Derivation,
      ...
    },
    inspircd: {
      config: string,
      enable: bool,
      package: Derivation,
      ...
    },
    interception-tools: {
      enable: bool,
      plugins: [Derivation],
      udevmonConfig: string | path,
      ...
    },
    intune: {
      enable: bool,
      ...
    },
    invidious: {
      address: string,
      database: {
        createLocally: bool,
        host: { ... },
        passwordFile: { ... },
        port: { ... },
        ...
      },
      domain: string | null,
      enable: bool,
      extraSettingsFile: string | null,
      hmacKeyFile: path | null,
      http3-ytproxy: {
        enable: bool,
        package: Derivation,
        ...
      },
      nginx: {
        enable: bool,
        ...
      },
      package: Derivation,
      port: { ... },
      serviceScale: int,
      settings: { ... },
      sig-helper: {
        enable: bool,
        listenAddress: string,
        package: Derivation,
        ...
      },
      ...
    },
    invidious-router: {
      address: string,
      enable: bool,
      nginx: {
        domain: string,
        enable: bool,
        extraDomains: { ... },
        ...
      },
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    invoiceplane: {
  sites: { ... },
  webserver: string,
  ...
},
    iodine: {
      clients: { _: { ... } },
      server: {
        domain: string,
        enable: bool,
        extraConfig: string,
        ip: string,
        passwordFile: string,
        ...
      },
      ...
    },
    iodined: {
      client: { ... },
      domain: string,
      enable: bool,
      extraConfig: string,
      ip: string,
      ...
    },
    iperf3: {
      affinity: { ... } | null,
      authorizedUsersFile: path | null,
      bind: string | null,
      debug: bool,
      enable: bool,
      extraFlags: [string],
      forceFlush: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      rsaPrivateKey: path | null,
      verbose: bool,
      ...
    },
    ipfs: {
      apiAddress: { ... },
      autoMigrate: bool,
      autoMount: bool,
      dataDir: string,
      defaultMode: string,
      emptyRepo: bool,
      enable: bool,
      enableGC: bool,
      extraConfig: { ... },
      extraFlags: [string],
      gatewayAddress: { ... },
      group: string,
      ipfsMountDir: { ... },
      ipnsMountDir: { ... },
      localDiscovery: bool,
      package: Derivation,
      serviceFdlimit: int | null,
      startWhenNeeded: bool,
      swarmAddress: { ... },
      user: string,
      ...
    },
    ipfs-cluster: {
      consensus: string,
      dataDir: string,
      enable: bool,
      initPeers: [string],
      openSwarmPort: bool,
      secretFile: path | null,
      ...
    },
    ipp-usb: {
      enable: bool,
      ...
    },
    iptsd: {
      config: { ... },
      enable: bool,
      ...
    },
    ircdHybrid: {
      adminEmail: string,
      certificate: path | null,
      description: string,
      enable: bool,
      extraIPs: [string],
      extraPort: string,
      rsaKey: path | null,
      serverName: string,
      sid: string,
      ...
    },
    irkerd: {
      enable: bool,
      listenAddress: string,
      nick: string,
      openPorts: bool,
      ...
    },
    irqbalance: {
      enable: bool,
      package: Derivation,
      ...
    },
    isso: {
      enable: bool,
      settings: { ... },
      ...
    },
    ivpn: {
      enable: bool,
      ...
    },
    jack: {
      alsa: {
        enable: bool,
        support32Bit: bool,
        ...
      },
      jackd: {
        enable: bool,
        extraOptions: { ... },
        package: Derivation,
        session: string,
        ...
      },
      loopback: {
        config: string,
        dmixConfig: string,
        enable: bool,
        index: int,
        session: string,
        ...
      },
      ...
    },
    jackett: {
      dataDir: string,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    jboss: {
      deployDir: string,
      enable: bool,
      libUrl: string,
      logDir: string,
      serverDir: string,
      tempDir: string,
      useJK: bool,
      user: string,
      ...
    },
    jellyfin: {
      cacheDir: path,
      configDir: path,
      dataDir: path,
      enable: bool,
      forceEncodingConfig: bool,
      group: string,
      hardwareAcceleration: {
        device: { ... },
        enable: bool,
        type: string,
        ...
      },
      logDir: path,
      openFirewall: bool,
      package: Derivation,
      transcoding: {
        deleteSegments: bool,
        enableHardwareEncoding: bool,
        enableIntelLowPowerEncoding: bool,
        enableSubtitleExtraction: bool,
        enableToneMapping: bool,
        encodingPreset: string,
        h264Crf: { ... },
        h265Crf: { ... },
        hardwareDecodingCodecs: { ... },
        hardwareEncodingCodecs: { ... },
        maxConcurrentStreams: { ... },
        threadCount: { ... },
        throttleTranscoding: bool,
        ...
      },
      user: string,
      ...
    },
    jellyseerr: {
      configDir: path,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    jenkins: {
      enable: bool,
      environment: { _: string },
      extraGroups: [string],
      extraJavaOptions: [string],
      extraOptions: [string],
      group: string,
      home: path,
      javaPackage: Derivation,
      jobBuilder: {
        accessToken: string,
        accessTokenFile: string,
        accessUser: string,
        enable: bool,
        jsonJobs: { ... },
        nixJobs: { ... },
        yamlJobs: string,
        ...
      },
      listenAddress: string,
      package: Derivation,
      packages: [Derivation],
      plugins: { _: Derivation },
      port: { ... },
      prefix: string,
      user: string,
      withCLI: bool,
      ...
    },
    jenkinsSlave: {
      enable: bool,
      group: string,
      home: path,
      javaPackage: Derivation,
      user: string,
      ...
    },
    jibri: {
      config: { ... },
      enable: bool,
      finalizeScript: path,
      ignoreCert: bool,
      xmppEnvironments: { _: { ... } },
      ...
    },
    jicofo: {
      bridgeMuc: string,
      componentPasswordFile: string,
      config: { ... },
      enable: bool,
      userDomain: string,
      userName: string,
      userPasswordFile: string,
      xmppDomain: string | null,
      xmppHost: string,
      ...
    },
    jigasi: {
      bridgeMuc: string,
      componentPasswordFile: string,
      config: { _: string },
      defaultJvbRoomName: string,
      enable: bool,
      environmentFile: path | null,
      userDomain: string,
      userName: string,
      userPasswordFile: string,
      xmppDomain: string | null,
      xmppHost: string,
      ...
    },
    jira: { ... },
    jirafeau: {
      adminPasswordSha256: string,
      dataDir: path,
      enable: bool,
      extraConfig: string,
      hostName: string,
      maxUploadSizeMegabytes: int,
      maxUploadTimeout: string,
      nginxConfig: { ... },
      package: Derivation,
      poolConfig: { _: { ... } },
      ...
    },
    jitsi-meet: {
      caddy: {
        enable: bool,
        ...
      },
      config: { ... },
      enable: bool,
      excalidraw: {
        enable: bool,
        port: { ... },
        ...
      },
      extraConfig: string,
      hostName: string,
      interfaceConfig: { ... },
      jibri: {
        enable: bool,
        ...
      },
      jicofo: {
        enable: bool,
        ...
      },
      jigasi: {
        enable: bool,
        ...
      },
      nginx: {
        enable: bool,
        ...
      },
      prosody: {
        allowners_muc: bool,
        enable: bool,
        lockdown: bool,
        ...
      },
      secureDomain: {
        authentication: string,
        enable: bool,
        ...
      },
      videobridge: {
        enable: bool,
        passwordFile: { ... },
        ...
      },
      ...
    },
    jitsi-videobridge: {
      apis: { ... },
      colibriRestApi: bool,
      config: { ... },
      enable: bool,
      extraProperties: { _: string },
      nat: {
        harvesterAddresses: { ... },
        localAddress: { ... },
        publicAddress: { ... },
        ...
      },
      openFirewall: bool,
      xmppConfigs: { _: { ... } },
      ...
    },
    jitterentropy-rngd: {
      enable: bool,
      package: Derivation,
      ...
    },
    jmusicbot: {
      enable: bool,
      package: Derivation,
      stateDir: path,
      ...
    },
    jotta-cli: {
      enable: bool,
      options: [string],
      package: Derivation,
      ...
    },
    journalbeat: {
      enable: bool,
      extraConfig: string,
      name: string,
      package: Derivation,
      stateDir: string,
      tags: [string],
      ...
    },
    journald: {
      audit: bool | string,
      console: string,
      enableHttpGateway: bool,
      extraConfig: string,
      forwardToSyslog: bool,
      gateway: {
        cert: { ... },
        enable: bool,
        key: { ... },
        merge: bool,
        port: { ... },
        system: bool,
        trust: { ... },
        user: bool,
        ...
      },
      rateLimitBurst: int,
      rateLimitInterval: string,
      remote: {
        enable: bool,
        listen: string,
        output: string,
        port: { ... },
        settings: { ... },
        ...
      },
      storage: string,
      upload: {
        enable: bool,
        settings: { ... },
        ...
      },
      ...
    },
    journaldriver: {
      applicationCredentials: path | null,
      enable: bool,
      googleCloudProject: string | null,
      logLevel: string,
      logName: string | null,
      logStream: string | null,
      ...
    },
    journalwatch: {
      accuracy: string,
      enable: bool,
      extraConfig: string,
      filterBlocks: [{ ... }],
      interval: string,
      mailBinary: path,
      mailFrom: string,
      mailTo: string | null,
      package: Derivation,
      priority: { ... },
      ...
    },
    joycond: {
      enable: bool,
      package: Derivation,
      ...
    },
    jupyter: {
      command: string,
      enable: bool,
      extraEnvironmentVariables: { _: { ... } },
      extraPackages: [Derivation],
      group: string,
      ip: string,
      kernels: { _: { ... } },
      notebookConfig: string,
      notebookDir: string,
      package: Derivation,
      password: string,
      port: { ... },
      user: string,
      ...
    },
    jupyterhub: {
      authentication: string,
      enable: bool,
      extraConfig: string,
      host: string,
      jupyterhubEnv: Derivation,
      jupyterlabEnv: Derivation,
      kernels: { _: { ... } },
      port: { ... },
      spawner: string,
      stateDirectory: string,
      ...
    },
    k3s: {
      agentToken: string,
      agentTokenFile: path | null,
      autoDeployCharts: { _: { ... } },
      charts: { _: { ... } },
      clusterInit: bool,
      configPath: path | null,
      containerdConfigTemplate: string | null,
      disable: [string],
      disableAgent: bool,
      docker: { ... },
      enable: bool,
      environmentFile: path | null,
      extraFlags: string | { ... },
      extraKubeProxyConfig: { _: { ... } },
      extraKubeletConfig: { _: { ... } },
      gracefulNodeShutdown: {
        enable: bool,
        shutdownGracePeriod: string,
        shutdownGracePeriodCriticalPods: string,
        ...
      },
      images: [Derivation],
      manifests: { _: { ... } },
      nodeIP: string | null,
      nodeLabel: [string],
      nodeName: string | null,
      nodeTaint: [string],
      package: Derivation,
      role: string,
      selinux: bool,
      serverAddr: string,
      token: string,
      tokenFile: path | null,
      ...
    },
    kanata: {
      enable: bool,
      keyboards: { _: { ... } },
      package: Derivation,
      ...
    },
    kanboard: {
      dataDir: string,
      domain: string,
      enable: bool,
      group: string,
      nginx: { ... },
      package: Derivation,
      phpfpm: {
        settings: { ... },
        ...
      },
      settings: { _: { ... } },
      user: string,
      ...
    },
    kanidm: {
      client: {
        enable: bool,
        settings: { ... },
        ...
      },
      clientSettings: { ... },
      enableClient: bool,
      enablePam: bool,
      enableServer: bool,
      package: Derivation,
      provision: {
        acceptInvalidCerts: bool,
        adminPasswordFile: { ... },
        autoRemove: bool,
        enable: bool,
        extraJsonFile: { ... },
        groups: { ... },
        idmAdminPasswordFile: { ... },
        instanceUrl: string,
        persons: { ... },
        systems: { ... },
        ...
      },
      server: {
        enable: bool,
        settings: { ... },
        ...
      },
      serverSettings: { ... },
      unix: {
        enable: bool,
        settings: { ... },
        sshIntegration: bool,
        ...
      },
      unixSettings: { ... },
      ...
    },
    kapacitor: {
      alerta: {
        enable: bool,
        environment: string,
        origin: string,
        token: string,
        url: string,
        ...
      },
      bind: string,
      dataDir: path,
      defaultDatabase: {
        enable: bool,
        password: string,
        url: string,
        username: string,
        ...
      },
      enable: bool,
      extraConfig: string,
      group: string,
      loadDirectory: path | null,
      port: { ... },
      taskSnapshotInterval: string,
      user: string,
      ...
    },
    karakeep: {
      browser: {
        enable: bool,
        exe: string,
        port: { ... },
        ...
      },
      enable: bool,
      environmentFile: path | null,
      extraEnvironment: { _: string },
      meilisearch: {
        enable: bool,
        experimental_dumpless_upgrade: bool,
        ...
      },
      package: Derivation,
      ...
    },
    karma: {
      configFile: path,
      enable: bool,
      environment: { _: string },
      extraOptions: [string],
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    kasmweb: {
      datastorePath: string,
      defaultAdminPassword: string,
      defaultGuacToken: string,
      defaultManagerToken: string,
      defaultRegistrationToken: string,
      defaultUserPassword: string,
      enable: bool,
      listenAddress: string,
      listenPort: { ... },
      networkSubnet: string,
      postgres: {
        password: string,
        user: string,
        ...
      },
      redisPassword: string,
      sslCertificate: path | null,
      sslCertificateKey: path | null,
      ...
    },
    kavita: {
      dataDir: string,
      enable: bool,
      ipAdresses: { ... },
      package: Derivation,
      port: { ... },
      settings: { ... },
      tokenKeyFile: path,
      user: string,
      ...
    },
    kbfs: {
      enable: bool,
      enableRedirector: bool,
      extraFlags: [string],
      mountPoint: string,
      ...
    },
    kea: {
      ctrl-agent: { ... },
      dhcp-ddns: { ... },
      dhcp4: { ... },
      dhcp6: { ... },
      package: Derivation,
      ...
    },
    keepalived: {
      enable: bool,
      enableScriptSecurity: bool,
      extraConfig: string,
      extraGlobalDefs: string,
      openFirewall: bool,
      package: Derivation,
      secretFile: path | null,
      snmp: {
        enable: bool,
        enableChecker: bool,
        enableKeepalived: bool,
        enableRfc: bool,
        enableRfcV2: bool,
        enableRfcV3: bool,
        enableTraps: bool,
        socket: { ... },
        ...
      },
      vrrpInstances: { _: { ... } },
      vrrpScripts: { _: { ... } },
      ...
    },
    kerberos_server: {
      enable: bool,
      extraKDCArgs: [string],
      realms: { ... },
      settings: { ... },
      ...
    },
    keter: {
      bundle: {
        appName: string,
        domain: string,
        executable: path,
        publicScript: string,
        secretScript: string,
        ...
      },
      enable: bool,
      globalKeterConfig: { ... },
      keterPackage: Derivation,
      keterRoot: string,
      package: Derivation,
      root: string,
      ...
    },
    keybase: {
      enable: bool,
      ...
    },
    keycloak: {
      bindAddress: { ... },
      database: {
        caCert: { ... },
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        type: string,
        useSSL: bool,
        username: string,
        ...
      },
      enable: bool,
      extraConfig: { ... },
      forceBackendUrlToFrontendUrl: { ... },
      frontendUrl: { ... },
      httpPort: { ... },
      httpsPort: { ... },
      initialAdminPassword: string | null,
      package: Derivation,
      plugins: [path],
      realmFiles: [path],
      settings: { ... },
      sslCertificate: path | null,
      sslCertificateKey: path | null,
      themes: { _: Derivation },
      ...
    },
    keyd: {
      enable: bool,
      ids: { ... },
      keyboards: { _: { ... } },
      package: Derivation,
      settings: { ... },
      ...
    },
    kimai: {
      sites: { _: { ... } },
      webserver: string,
      ...
    },
    kippo: { ... },
    kismet: {
      dataDir: path,
      enable: bool,
      extraConfig: string,
      group: string,
      httpd: {
        address: string,
        enable: bool,
        port: { ... },
        ...
      },
      logTypes: [string],
      package: Derivation,
      serverDescription: string,
      serverName: string,
      settings: { _: { ... } },
      user: string,
      ...
    },
    kiwix-serve: {
      address: string,
      enable: bool,
      extraArgs: [string],
      library: { _: path },
      libraryPath: path | null,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    klipper: {
      apiSocket: path | null,
      configDir: path,
      configFile: path | null,
      enable: bool,
      extraSettings: string,
      firmwares: { _: { ... } },
      group: string | null,
      inputTTY: path,
      logFile: path | null,
      mutableConfig: bool,
      mutableConfigFolder: path,
      octoprintIntegration: bool,
      package: Derivation,
      settings: { _: { ... } },
      user: string | null,
      ...
    },
    kmonad: {
      enable: bool,
      extraArgs: [string],
      keyboards: { _: { ... } },
      package: Derivation,
      ...
    },
    kmscon: {
      autologinUser: string | null,
      enable: bool,
      extraConfig: string,
      extraOptions: string,
      fonts: [{ ... }] | null,
      hwRender: bool,
      package: Derivation,
      useXkbConfig: bool,
      ...
    },
    knot: {
      checkConfig: bool,
      enable: bool,
      enableXDP: bool,
      extraArgs: [string],
      extraConfig: { ... },
      keyFiles: [path],
      package: Derivation,
      settings: { ... },
      settingsFile: path | null,
      ...
    },
    knot-resolver: {
      enable: bool,
      managerPackage: Derivation,
      settings: { ... },
      ...
    },
    komga: {
      enable: bool,
      group: string,
      openFirewall: bool,
      port: { ... },
      settings: { ... },
      stateDir: string,
      user: string,
      ...
    },
    komodo-periphery: {
      allowedIps: [string],
      bindIp: string,
      configFile: path | null,
      containerStatsPollingRate: string,
      disableContainerExec: bool,
      disableTerminals: bool,
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      excludeDiskMounts: [string],
      extraSettings: { ... },
      group: string,
      includeDiskMounts: [string],
      legacyComposeCli: bool,
      logging: {
        level: string,
        otlpEndpoint: string,
        stdio: string,
        ...
      },
      package: Derivation,
      passkeys: [string],
      port: { ... },
      rootDirectory: path,
      ssl: {
        certFile: path,
        enable: bool,
        keyFile: path,
        ...
      },
      statsPollingRate: string,
      user: string,
      ...
    },
    kresd: {
      cacheDir: { ... },
      enable: bool,
      extraConfig: string,
      instances: { ... },
      interfaces: { ... },
      listenDoH: [string],
      listenPlain: [string],
      listenTLS: [string],
      package: Derivation,
      ...
    },
    kthxbye: {
      alertmanager: {
        timeout: string,
        uri: string,
        ...
      },
      enable: bool,
      extendBy: string,
      extendIfExpiringIn: string,
      extendWithPrefix: string,
      extraOptions: [string],
      interval: string,
      listenAddress: string,
      logJSON: bool,
      maxDuration: string | null,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    kubernetes: {
      addonManager: {
        addons: { ... },
        bootstrapAddons: { ... },
        enable: bool,
        ...
      },
      addons: {
        dashboard: { ... },
        dns: { ... },
        ...
      },
      apiserver: {
        address: string,
        admissionControl: { ... },
        advertiseAddress: { ... },
        allowPrivileged: bool,
        apiAudiences: string,
        authorizationMode: { ... },
        authorizationPolicy: { ... },
        basicAuthFile: { ... },
        bindAddress: string,
        clientCaFile: { ... },
        disableAdmissionPlugins: { ... },
        enable: bool,
        enableAdmissionPlugins: { ... },
        etcd: { ... },
        extraOpts: string,
        extraSANs: { ... },
        featureGates: { ... },
        insecureBindAddress: { ... },
        insecurePort: { ... },
        kubeletClientCaFile: { ... },
        kubeletClientCertFile: { ... },
        kubeletClientKeyFile: { ... },
        preferredAddressTypes: { ... },
        proxyClientCertFile: { ... },
        proxyClientKeyFile: { ... },
        publicAddress: { ... },
        runtimeConfig: string,
        securePort: int,
        serviceAccountIssuer: string,
        serviceAccountKeyFile: path,
        serviceAccountSigningKeyFile: path,
        serviceClusterIpRange: string,
        storageBackend: string,
        tlsCertFile: { ... },
        tlsKeyFile: { ... },
        tokenAuthFile: { ... },
        verbosity: { ... },
        webhookConfig: { ... },
        ...
      },
      apiserverAddress: string,
      caFile: path | null,
      clusterCidr: string | null,
      controllerManager: {
        address: string,
        allocateNodeCIDRs: bool,
        bindAddress: string,
        clusterCidr: string,
        enable: bool,
        extraOpts: string,
        featureGates: { ... },
        insecurePort: { ... },
        kubeconfig: { ... },
        leaderElect: bool,
        rootCaFile: { ... },
        securePort: int,
        serviceAccountKeyFile: { ... },
        tlsCertFile: { ... },
        tlsKeyFile: { ... },
        verbosity: { ... },
        ...
      },
      dataDir: path,
      easyCerts: bool,
      etcd: {
        caFile: { ... },
        certFile: { ... },
        keyFile: { ... },
        servers: { ... },
        ...
      },
      featureGates: { _: bool },
      flannel: {
        enable: bool,
        openFirewallPorts: bool,
        ...
      },
      kubeconfig: {
        caFile: { ... },
        certFile: { ... },
        keyFile: { ... },
        server: string,
        ...
      },
      kubelet: {
        address: string,
        allowPrivileged: { ... },
        applyManifests: { ... },
        cadvisorPort: { ... },
        clientCaFile: { ... },
        clusterDns: { ... },
        clusterDomain: string,
        cni: { ... },
        containerRuntime: { ... },
        containerRuntimeEndpoint: string,
        enable: bool,
        extraConfig: { ... },
        extraOpts: string,
        featureGates: { ... },
        healthz: { ... },
        hostname: string,
        kubeconfig: { ... },
        manifests: { ... },
        networkPlugin: { ... },
        nodeIp: { ... },
        port: { ... },
        registerNode: bool,
        seedDockerImages: { ... },
        taints: { ... },
        tlsCertFile: { ... },
        tlsKeyFile: { ... },
        unschedulable: bool,
        verbosity: { ... },
        ...
      },
      lib: { ... },
      masterAddress: string,
      package: Derivation,
      path: [Derivation],
      pki: {
        caCertPathPrefix: string,
        caSpec: { ... },
        certs: { ... },
        cfsslAPIExtraSANs: { ... },
        enable: bool,
        etcClusterAdminKubeconfig: { ... },
        genCfsslAPICerts: bool,
        genCfsslAPIToken: bool,
        genCfsslCACert: bool,
        pkiTrustOnBootstrap: bool,
        ...
      },
      proxy: {
        address: string,
        bindAddress: string,
        enable: bool,
        extraOpts: string,
        featureGates: { ... },
        hostname: string,
        kubeconfig: { ... },
        verbosity: { ... },
        ...
      },
      roles: [string],
      scheduler: {
        address: string,
        enable: bool,
        extraOpts: string,
        featureGates: { ... },
        kubeconfig: { ... },
        leaderElect: bool,
        port: { ... },
        verbosity: { ... },
        ...
      },
      secretsPath: path,
      verbose: { ... },
      ...
    },
    kubo: {
      apiAddress: { ... },
      autoMigrate: bool,
      autoMount: bool,
      dataDir: string,
      defaultMode: string,
      emptyRepo: bool,
      enable: bool,
      enableGC: bool,
      extraConfig: { ... },
      extraFlags: [string],
      gatewayAddress: { ... },
      group: string,
      ipfsMountDir: { ... },
      ipnsMountDir: { ... },
      localDiscovery: bool,
      package: Derivation,
      serviceFdlimit: int | null,
      settings: { ... },
      startWhenNeeded: bool,
      swarmAddress: { ... },
      user: string,
      ...
    },
    labgrid: {
      coordinator: {
        bindAddress: string,
        debug: bool,
        enable: bool,
        openFirewall: bool,
        package: Derivation,
        port: { ... },
        ...
      },
      ...
    },
    lact: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    lambdabot: {
      enable: bool,
      package: Derivation,
      script: string,
      ...
    },
    languagetool: {
      allowOrigin: string | null,
      enable: bool,
      jrePackage: { ... },
      jvmOptions: [string],
      package: Derivation,
      port: { ... },
      public: bool,
      settings: { ... },
      ...
    },
    lanraragi: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      passwordFile: path | null,
      port: { ... },
      redis: {
        passwordFile: { ... },
        port: { ... },
        ...
      },
      ...
    },
    lasuite-docs: {
      backendPackage: Derivation,
      bind: string,
      celery: {
        extraArgs: { ... },
        ...
      },
      collaborationServer: {
        package: Derivation,
        port: { ... },
        settings: { ... },
        ...
      },
      domain: string,
      enable: bool,
      enableNginx: bool,
      environmentFile: path | null,
      frontendPackage: Derivation,
      gunicorn: {
        extraArgs: { ... },
        ...
      },
      postgresql: {
        createLocally: bool,
        ...
      },
      redis: {
        createLocally: bool,
        ...
      },
      s3Url: string,
      secretKeyPath: path | null,
      settings: { ... },
      ...
    },
    lasuite-meet: {
      backendPackage: Derivation,
      bind: string,
      celery: {
        extraArgs: { ... },
        ...
      },
      domain: string,
      enable: bool,
      enableNginx: bool,
      environmentFile: path | null,
      frontendPackage: Derivation,
      gunicorn: {
        extraArgs: { ... },
        ...
      },
      livekit: {
        enable: bool,
        keyFile: path,
        openFirewall: bool,
        settings: { ... },
        ...
      },
      postgresql: {
        createLocally: bool,
        ...
      },
      redis: {
        createLocally: bool,
        ...
      },
      secretKeyPath: path | null,
      settings: { ... },
      ...
    },
    lauti: {
      dataDir: path,
      enable: bool,
      secrets: [path],
      settings: { _: string },
      ...
    },
    lavalink: {
      address: string,
      enable: bool,
      enableHttp2: bool,
      environmentFile: string | null,
      extraConfig: { ... },
      group: string,
      home: string,
      jvmArgs: string,
      openFirewall: bool,
      package: Derivation,
      password: string | null,
      plugins: [{ ... }],
      port: { ... },
      user: string,
      ...
    },
    leaps: {
      address: string,
      enable: bool,
      path: path,
      port: { ... },
      ...
    },
    legit: {
      enable: bool,
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    lemmy: {
      adminPasswordFile: path | null,
      caddy: {
        enable: bool,
        ...
      },
      database: {
        createLocally: bool,
        uri: { ... },
        uriFile: { ... },
        ...
      },
      enable: bool,
      jwtSecretPath: { ... },
      nginx: {
        enable: bool,
        ...
      },
      pictrsApiKeyFile: path | null,
      server: {
        package: Derivation,
        ...
      },
      settings: { ... },
      smtpPasswordFile: path | null,
      ui: {
        package: Derivation,
        port: { ... },
        ...
      },
      ...
    },
    libeufin: {
      bank: {
        createLocalDatabase: bool,
        debug: bool,
        enable: bool,
        initialAccounts: { ... },
        openFirewall: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      nexus: {
        createLocalDatabase: bool,
        debug: bool,
        enable: bool,
        openFirewall: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      settings: { ... },
      ...
    },
    libinput: {
      enable: bool,
      mouse: {
        accelPointsFallback: { ... },
        accelPointsMotion: { ... },
        accelPointsScroll: { ... },
        accelProfile: string,
        accelSpeed: { ... },
        accelStepFallback: { ... },
        accelStepMotion: { ... },
        accelStepScroll: { ... },
        additionalOptions: string,
        buttonMapping: { ... },
        calibrationMatrix: { ... },
        clickMethod: { ... },
        dev: { ... },
        disableWhileTyping: bool,
        horizontalScrolling: bool,
        leftHanded: bool,
        middleEmulation: bool,
        naturalScrolling: bool,
        scrollButton: { ... },
        scrollMethod: string,
        sendEventsMode: string,
        tapping: bool,
        tappingButtonMap: { ... },
        tappingDragLock: bool,
        transformationMatrix: { ... },
        ...
      },
      touchpad: {
        accelPointsFallback: { ... },
        accelPointsMotion: { ... },
        accelPointsScroll: { ... },
        accelProfile: string,
        accelSpeed: { ... },
        accelStepFallback: { ... },
        accelStepMotion: { ... },
        accelStepScroll: { ... },
        additionalOptions: string,
        buttonMapping: { ... },
        calibrationMatrix: { ... },
        clickMethod: { ... },
        dev: { ... },
        disableWhileTyping: bool,
        horizontalScrolling: bool,
        leftHanded: bool,
        middleEmulation: bool,
        naturalScrolling: bool,
        scrollButton: { ... },
        scrollMethod: string,
        sendEventsMode: string,
        tapping: bool,
        tappingButtonMap: { ... },
        tappingDragLock: bool,
        transformationMatrix: { ... },
        ...
      },
      ...
    },
    librechat: {
      credentials: { _: path },
      credentialsFile: path | null,
      dataDir: path,
      enable: bool,
      enableLocalDB: bool,
      env: { ... },
      group: string,
      meilisearch: { ... },
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    libreddit: { ... },
    librenms: {
      dataDir: path,
      database: {
        createLocally: bool,
        database: string,
        host: { ... },
        passwordFile: { ... },
        port: { ... },
        socket: { ... },
        username: string,
        ...
      },
      distributedPoller: {
        distributedBilling: bool,
        enable: bool,
        group: string,
        memcachedHost: string,
        memcachedPort: { ... },
        name: { ... },
        rrdcachedHost: string,
        rrdcachedPort: { ... },
        ...
      },
      enable: bool,
      enableLocalBilling: bool,
      enableOneMinutePolling: bool,
      environmentFile: string | null,
      extraConfig: string | null,
      finalPackage: Derivation,
      group: string,
      hostname: string,
      logDir: path,
      nginx: { ... },
      package: Derivation,
      phpOptions: { _: { ... } },
      pollerThreads: int,
      poolConfig: { _: { ... } },
      settings: { ... },
      useDistributedPollers: bool,
      user: string,
      ...
    },
    librespeed: {
      domain: string | null,
      downloadIPDB: bool,
      enable: bool,
      frontend: {
        contactEmail: string,
        enable: bool,
        pageTitle: string,
        servers: { ... },
        settings: { ... },
        useNginx: bool,
        ...
      },
      package: Derivation,
      secrets: { _: path },
      settings: { _: { ... } },
      tlsCertificate: string | null,
      tlsKey: string | null,
      useACMEHost: string | null,
      ...
    },
    libreswan: {
      configSetup: string,
      connections: { _: string },
      disableRedirects: bool,
      enable: bool,
      policies: { _: string },
      ...
    },
    libretranslate: {
      configureNginx: bool,
      dataDir: path,
      disableWebUI: bool,
      domain: string,
      enable: bool,
      enableApiKeys: bool,
      extraArgs: { _: { ... } },
      group: string,
      host: string,
      package: Derivation,
      port: { ... },
      threads: { ... } | null,
      updateModels: bool,
      user: string,
      ...
    },
    libvirtd: {
      autoSnapshot: {
        calendar: string,
        enable: bool,
        keep: int,
        prefix: string,
        snapshotType: string,
        vms: { ... },
        ...
      },
      ...
    },
    lidarr: {
      dataDir: string,
      enable: bool,
      environmentFiles: [path],
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    lifecycled: {
      awsRegion: string | null,
      cloudwatchGroup: string | null,
      cloudwatchStream: string | null,
      debug: bool,
      enable: bool,
      handler: path,
      instanceId: string | null,
      json: bool,
      noSpot: bool,
      queueCleaner: {
        enable: bool,
        frequency: string,
        parallel: { ... },
        ...
      },
      snsTopic: string | null,
      ...
    },
    lighthouse: {
      beacon: { ... },
      extraArgs: string,
      network: string,
      package: Derivation,
      validator: { ... },
      ...
    },
    lighttpd: {
      cgit: {
        configText: string,
        enable: bool,
        subdir: string,
        ...
      },
      collectd: {
        collectionCgi: path,
        enable: bool,
        ...
      },
      configText: string,
      document-root: path,
      enable: bool,
      enableModules: [string],
      enableUpstreamMimeTypes: bool,
      extraConfig: string,
      gitweb: {
        enable: bool,
        ...
      },
      mod_status: bool,
      mod_userdir: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    limesurvey: {
      config: { ... } | bool,
      database: {
        createLocally: bool,
        dbEngine: string,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        socket: { ... },
        type: string,
        user: string,
        ...
      },
      enable: bool,
      encryptionKey: string | null,
      encryptionKeyFile: path | null,
      encryptionNonce: string | null,
      encryptionNonceFile: path | null,
      httpd: {
        virtualHost: { ... },
        ...
      },
      nginx: {
        virtualHost: { ... },
        ...
      },
      package: Derivation,
      poolConfig: { _: { ... } },
      virtualHost: { ... },
      webserver: string,
      ...
    },
    linkwarden: {
      cacheLocation: path,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        port: { ... },
        user: string,
        ...
      },
      enable: bool,
      enableRegistration: bool,
      environment: { _: string },
      environmentFile: string | null,
      group: string,
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      secretFiles: { _: { ... } },
      storageLocation: path,
      user: string,
      ...
    },
    linux-enable-ir-emitter: {
      device: string,
      enable: bool,
      package: Derivation,
      ...
    },
    linyaps: {
      boxPackage: Derivation,
      enable: bool,
      package: Derivation,
      webStoreInstallerPackage: Derivation,
      ...
    },
    liquidsoap: {
      streams: { _: { ... } },
      ...
    },
    lirc: {
      configs: [string],
      enable: bool,
      extraArguments: [string],
      options: string,
      ...
    },
    listmonk: {
      database: {
        createLocally: bool,
        mutableSettings: bool,
        settings: { ... },
        ...
      },
      enable: bool,
      package: Derivation,
      secretFile: string | null,
      settings: { ... },
      ...
    },
    litellm: {
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      stateDir: path,
      ...
    },
    litestream: {
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      settings: { ... } | null,
      ...
    },
    livebook: {
      enableUserService: bool,
      environment: { _: { ... } },
      environmentFile: path | null,
      extraPackages: [Derivation],
      package: Derivation,
      ...
    },
    livekit: {
      enable: bool,
      ingress: {
        enable: bool,
        environmentFile: { ... },
        openFirewall: { ... },
        package: Derivation,
        settings: { ... },
        ...
      },
      keyFile: path,
      openFirewall: bool,
      package: Derivation,
      redis: {
        createLocally: bool,
        host: { ... },
        port: { ... },
        ...
      },
      settings: { ... },
      ...
    },
    lk-jwt-service: {
      enable: bool,
      keyFile: path,
      livekitUrl: { ... },
      package: Derivation,
      port: { ... },
      ...
    },
    llama-cpp: {
      enable: bool,
      extraFlags: [string],
      host: string,
      model: path | null,
      modelsDir: path | null,
      modelsPreset: { _: { ... } },
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    llama-swap: {
      enable: bool,
      listenAddress: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      tls: {
        certFile: { ... },
        enable: bool,
        keyFile: { ... },
        ...
      },
      ...
    },
    lldap: {
      database: {
        createLocally: bool,
        type: string,
        ...
      },
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      package: Derivation,
      settings: { ... },
      silenceForceUserPassResetWarning: bool,
      ...
    },
    lldpd: {
      enable: bool,
      extraArgs: [string],
      ...
    },
    local-content-share: {
      enable: bool,
      listenAddress: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    localtime: { ... },
    localtimed: {
      enable: bool,
      geoclue2Package: Derivation,
      package: Derivation,
      ...
    },
    locate: {
      enable: bool,
      extraFlags: [string],
      includeStore: { ... },
      interval: string,
      localuser: { ... },
      locate: Derivation,
      output: path,
      package: Derivation,
      period: string,
      pruneBindMounts: bool,
      pruneFS: [string],
      pruneNames: [string],
      prunePaths: [path],
      ...
    },
    logcheck: {
      config: string,
      enable: bool,
      extraGroups: [string],
      extraRulesDirs: [path],
      files: [path],
      ignore: { _: { ... } },
      ignoreCron: { _: { ... } },
      level: string,
      mailTo: string,
      timeOfDay: string,
      user: string,
      ...
    },
    logind: {
      extraConfig: { ... },
      hibernateKey: { ... },
      hibernateKeyLongPress: { ... },
      killUserProcesses: { ... },
      lidSwitch: { ... },
      lidSwitchDocked: { ... },
      lidSwitchExternalPower: { ... },
      powerKey: { ... },
      powerKeyLongPress: { ... },
      rebootKey: { ... },
      rebootKeyLongPress: { ... },
      settings: {
        Login: { ... },
        ...
      },
      suspendKey: { ... },
      suspendKeyLongPress: { ... },
      ...
    },
    logkeys: {
      device: string | null,
      enable: bool,
      ...
    },
    logmein-hamachi: {
      enable: bool,
      ...
    },
    logrotate: {
      allowNetworking: bool,
      checkConfig: bool,
      config: { ... },
      configFile: path,
      enable: bool,
      extraArgs: [string],
      extraConfig: { ... },
      paths: { ... },
      settings: { _: { ... } },
      ...
    },
    logstash: {
      address: string,
      dataDir: string,
      enable: bool,
      enableWeb: { ... },
      extraJvmOptions: string,
      extraSettings: string,
      filterConfig: string,
      filterWorkers: int,
      inputConfig: string,
      listenAddress: string,
      logLevel: string,
      outputConfig: string,
      package: Derivation,
      plugins: [path],
      port: string,
      ...
    },
    loki: {
      configFile: path | null,
      configuration: { ... },
      dataDir: path,
      enable: bool,
      extraFlags: [string],
      group: string,
      package: Derivation,
      user: string,
      ...
    },
    lokinet: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      useLocally: bool,
      ...
    },
    longview: {
      apacheStatusUrl: string,
      apiKey: string,
      apiKeyFile: path | null,
      enable: bool,
      mysqlPassword: string,
      mysqlPasswordFile: path | null,
      mysqlUser: string,
      nginxStatusUrl: string,
      ...
    },
    lorri: {
      enable: bool,
      package: Derivation,
      ...
    },
    lshd: { ... },
    lubelogger: {
      dataDir: string,
      enable: bool,
      environmentFile: path | null,
      group: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { _: string },
      user: string,
      ...
    },
    lvm: {
      boot: {
        thin: { ... },
        vdo: { ... },
        ...
      },
      dmeventd: {
        enable: bool,
        ...
      },
      enable: bool,
      package: Derivation,
      ...
    },
    lxd-image-server: {
      enable: bool,
      group: string,
      nginx: {
        domain: string,
        enable: bool,
        ...
      },
      settings: { ... },
      ...
    },
    mackerel-agent: {
      apiKeyFile: path,
      autoRetirement: bool,
      enable: bool,
      runAsRoot: bool,
      settings: { ... },
      ...
    },
    maddy: {
      config: string | null,
      enable: bool,
      ensureAccounts: [string],
      ensureCredentials: { _: { ... } },
      group: string,
      hostname: string,
      localDomains: [string],
      openFirewall: bool,
      package: Derivation,
      primaryDomain: string,
      secrets: [path],
      tls: {
        certificates: { ... },
        extraConfig: { ... },
        loader: { ... },
        ...
      },
      user: string,
      ...
    },
    magic-wormhole-mailbox-server: {
      enable: bool,
      ...
    },
    magnetico: {
      crawler: {
        address: string,
        extraOptions: { ... },
        maxLeeches: { ... },
        maxNeighbors: { ... },
        port: { ... },
        ...
      },
      enable: bool,
      web: {
        address: string,
        credentials: { ... },
        credentialsFile: { ... },
        extraOptions: { ... },
        port: { ... },
        ...
      },
      ...
    },
    mail: {
      sendmailSetuidWrapper: { ... },
      ...
    },
    mailcatcher: {
      enable: bool,
      http: {
        ip: string,
        path: { ... },
        port: { ... },
        ...
      },
      smtp: {
        ip: string,
        port: { ... },
        ...
      },
      ...
    },
    mailhog: {
      apiPort: { ... },
      enable: bool,
      extraArgs: [string],
      setSendmail: bool,
      smtpPort: { ... },
      storage: string,
      uiPort: { ... },
      user: { ... },
      ...
    },
    mailman: {
      enable: bool,
      enablePostfix: bool,
      extraPythonPackages: { ... },
      hyperkitty: {
        baseUrl: string,
        enable: bool,
        ...
      },
      hyperkittyApiKey: { ... },
      hyperkittyBaseUrl: string,
      ldap: {
        attrMap: { ... },
        bindDn: string,
        bindPasswordFile: string,
        enable: bool,
        groupSearch: { ... },
        serverUri: string,
        superUserGroup: { ... },
        userSearch: { ... },
        ...
      },
      package: { ... },
      packageSet: { ... },
      restApiPassFile: string | null,
      serve: {
        enable: bool,
        uwsgiSettings: { ... },
        virtualRoot: string,
        ...
      },
      settings: { _: { ... } },
      siteOwner: string,
      webHosts: [string],
      webSettings: { ... },
      webUser: string,
      ...
    },
    mailpile: { ... },
    mailpit: {
      instances: { _: { ... } },
      ...
    },
    mainsail: {
      enable: bool,
      hostName: string,
      nginx: { ... },
      package: Derivation,
      ...
    },
    malcontent: {
      enable: bool,
      ...
    },
    mame: {
      emuAddr: string,
      enable: bool,
      hostAddr: string,
      user: string,
      ...
    },
    manticore: {
      enable: bool,
      settings: { ... },
      ...
    },
    marathon: { ... },
    marytts: {
      basePath: path,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      userDictionaries: [path],
      voices: [path],
      ...
    },
    mastodon: {
      activeRecordEncryptionDeterministicKeyFile: string,
      activeRecordEncryptionKeyDerivationSaltFile: string,
      activeRecordEncryptionPrimaryKeyFile: string,
      automaticMigrations: bool,
      configureNginx: bool,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        user: string,
        ...
      },
      elasticsearch: {
        host: { ... },
        passwordFile: { ... },
        port: { ... },
        prefix: { ... },
        preset: string,
        user: { ... },
        ...
      },
      enable: bool,
      enableUnixSocket: bool,
      extraConfig: { ... },
      extraEnvFiles: [path],
      group: string,
      localDomain: string,
      mediaAutoRemove: {
        enable: bool,
        olderThanDays: int,
        startAt: string,
        ...
      },
      otpSecretFile: { ... },
      package: Derivation,
      redis: {
        createLocally: bool,
        enableUnixSocket: bool,
        host: { ... },
        passwordFile: { ... },
        port: { ... },
        ...
      },
      secretKeyBaseFile: string,
      sidekiqPort: { ... },
      sidekiqProcesses: { _: { ... } },
      sidekiqThreads: int,
      smtp: {
        authenticate: bool,
        createLocally: bool,
        fromAddress: string,
        host: string,
        passwordFile: { ... },
        port: { ... },
        user: { ... },
        ...
      },
      streamingPort: { ... },
      streamingProcesses: { ... },
      trustedProxy: string,
      user: string,
      vapidPrivateKeyFile: string,
      vapidPublicKeyFile: string,
      webPort: { ... },
      webProcesses: int,
      webThreads: int,
      ...
    },
    mathics: { ... },
    matomo: {
      enable: bool,
      hostname: string,
      nginx: { ... },
      package: Derivation,
      periodicArchiveProcessing: bool,
      periodicArchiveProcessingUrl: string,
      phpfpmProcessManagerConfig: { ... },
      webServerUser: string | null,
      ...
    },
    matrix-alertmanager: {
      enable: bool,
      homeserverUrl: string,
      matrixRooms: [{ ... }],
      matrixUser: string,
      mention: bool,
      package: Derivation,
      port: { ... },
      secretFile: path,
      tokenFile: path,
      ...
    },
    matrix-appservice-discord: {
      enable: bool,
      environmentFile: path | null,
      localpart: string | null,
      package: Derivation,
      port: { ... },
      serviceDependencies: [string],
      settings: { ... },
      url: string,
      ...
    },
    matrix-appservice-irc: {
      enable: bool,
      localpart: string,
      needBindingCap: bool,
      passwordEncryptionKeyLength: { ... },
      port: { ... },
      registrationUrl: string,
      settings: { ... },
      ...
    },
    matrix-conduit: {
      enable: bool,
      extraEnvironment: { _: string },
      package: Derivation,
      secretFile: path | null,
      settings: { ... },
      ...
    },
    matrix-continuwuity: {
      admin: {
        enable: bool,
        ...
      },
      enable: bool,
      extraEnvironment: { _: string },
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    matrix-hookshot: {
      enable: bool,
      package: Derivation,
      registrationFile: path,
      serviceDependencies: [string],
      settings: { ... },
      ...
    },
    matrix-sliding-sync: { ... },
    matrix-synapse: {
      account_threepid_delegates: {
        email: { ... },
        msisdn: { ... },
        ...
      },
      allow_guest_access: { ... },
      app_service_config_files: { ... },
      bcrypt_rounds: { ... },
      bind_host: { ... },
      bind_port: { ... },
      configFile: path,
      configureRedisLocally: bool,
      create_local_database: { ... },
      dataDir: string,
      database_args: { ... },
      database_name: { ... },
      database_type: { ... },
      database_user: { ... },
      dynamic_thumbnails: { ... },
      enable: bool,
      enableRegistrationScript: bool,
      enable_metrics: { ... },
      enable_registration: { ... },
      enable_registration_captcha: { ... },
      event_cache_size: { ... },
      expire_access_tokens: { ... },
      extraArgs: [string],
      extraConfig: { ... },
      extraConfigFiles: [path],
      extras: [string],
      federation_rc_concurrent: { ... },
      federation_rc_reject_limit: { ... },
      federation_rc_sleep_delay: { ... },
      federation_rc_sleep_limit: { ... },
      federation_rc_window_size: { ... },
      key_refresh_interval: { ... },
      listeners: { ... },
      log: { _: { ... } },
      logConfig: { ... },
      macaroon_secret_key: { ... },
      max_image_pixels: { ... },
      max_upload_size: { ... },
      no_tls: { ... },
      package: Derivation,
      plugins: [Derivation],
      presence: {
        enabled: { ... },
        ...
      },
      public_baseurl: { ... },
      rc_messages_burst_count: { ... },
      rc_messages_per_second: { ... },
      recaptcha_private_key: { ... },
      recaptcha_public_key: { ... },
      redaction_retention_period: { ... },
      registration_shared_secret: { ... },
      report_stats: { ... },
      room_invite_state_types: { ... },
      room_prejoin_state: {
        additional_event_types: { ... },
        disable_default_event_types: { ... },
        ...
      },
      server_name: { ... },
      servers: { ... },
      serviceUnit: string,
      settings: { ... },
      tls_certificate_path: { ... },
      tls_dh_param_path: { ... },
      tls_private_key_path: { ... },
      trusted_third_party_id_servers: { ... },
      turn_shared_secret: { ... },
      turn_uris: { ... },
      turn_user_lifetime: { ... },
      unsecure_port: { ... },
      url_preview_enabled: { ... },
      url_preview_ip_range_blacklist: { ... },
      url_preview_ip_range_whitelist: { ... },
      url_preview_url_blacklist: { ... },
      user_creation_max_duration: { ... },
      verbose: { ... },
      web_client: { ... },
      withJemalloc: bool,
      workers: { _: { ... } },
      ...
    },
    matrix-tuwunel: {
      enable: bool,
      extraEnvironment: { _: string },
      group: string,
      package: Derivation,
      settings: { ... },
      stateDirectory: string,
      user: string,
      ...
    },
    matter-server: {
      enable: bool,
      extraArgs: [string],
      logLevel: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    matterbridge: {
      configFile: string,
      configPath: string | null,
      enable: bool,
      group: string,
      package: Derivation,
      user: string,
      ...
    },
    mattermost: {
      configDir: path,
      dataDir: path,
      database: {
        create: bool,
        driver: string,
        extraConnectionOptions: { ... },
        fromEnvironment: bool,
        host: string,
        name: string,
        password: string,
        peerAuth: bool,
        port: { ... },
        socketPath: path,
        user: string,
        ...
      },
      enable: bool,
      environment: { _: { ... } },
      environmentFile: path | null,
      extraConfig: { ... } | null,
      group: string,
      host: string,
      listenAddress: string,
      localDatabaseCreate: bool,
      localDatabaseName: string,
      localDatabasePassword: string,
      localDatabaseUser: string,
      logDir: path,
      matterircd: {
        enable: bool,
        package: Derivation,
        parameters: { ... },
        ...
      },
      mutableConfig: bool,
      package: Derivation,
      plugins: [{ ... }],
      pluginsBundle: Derivation | null,
      port: { ... },
      preferNixConfig: bool,
      settings: { ... },
      siteName: string,
      siteUrl: string,
      socket: {
        enable: bool,
        export: bool,
        path: path,
        ...
      },
      statePath: path,
      telemetry: {
        enableDiagnostics: bool,
        enableSecurityAlerts: bool,
        ...
      },
      user: string,
      ...
    },
    maubot: {
      configMutable: bool,
      dataDir: string,
      enable: bool,
      extraConfigFile: string,
      package: Derivation,
      plugins: [Derivation],
      pythonPackages: [Derivation],
      settings: { ... },
      ...
    },
    mautrix-discord: {
      dataDir: path,
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      registerToSynapse: bool,
      registrationServiceUnit: string,
      serviceDependencies: [string],
      serviceUnit: string,
      settings: { ... },
      ...
    },
    mautrix-meta: {
      instances: { _: { ... } },
      package: Derivation,
      ...
    },
    mautrix-signal: {
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      registerToSynapse: bool,
      serviceDependencies: [string],
      settings: { ... },
      ...
    },
    mautrix-telegram: {
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      registerToSynapse: bool,
      serviceDependencies: [string],
      settings: { ... },
      ...
    },
    mautrix-whatsapp: {
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      registerToSynapse: bool,
      serviceDependencies: [string],
      settings: { ... },
      ...
    },
    mbpfan: {
      aggressive: bool,
      enable: bool,
      highTemp: { ... },
      lowTemp: { ... },
      maxFanSpeed: { ... },
      maxTemp: { ... },
      minFanSpeed: { ... },
      package: Derivation,
      pollingInterval: { ... },
      settings: { ... },
      verbose: bool,
      ...
    },
    mchprs: {
      dataDir: path,
      declarativeSettings: bool,
      declarativeWhitelist: bool,
      enable: bool,
      maxRuntime: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      whitelist: {
        enable: bool,
        list: { ... },
        ...
      },
      ...
    },
    mealie: {
      credentialsFile: path | null,
      database: {
        createLocally: bool,
        ...
      },
      enable: bool,
      extraOptions: [string],
      listenAddress: string,
      package: Derivation,
      port: { ... },
      settings: { _: { ... } },
      ...
    },
    mediagoblin: {
      createDatabaseLocally: bool,
      domain: string,
      enable: bool,
      package: Derivation,
      paste: {
        port: { ... },
        settings: { ... },
        ...
      },
      pluginPackages: [Derivation],
      settings: { ... },
      ...
    },
    mediamtx: {
      allowVideoAccess: bool,
      enable: bool,
      env: { _: { ... } },
      package: Derivation,
      settings: { ... },
      ...
    },
    mediatomb: {
      customCfg: bool,
      dataDir: path,
      dsmSupport: bool,
      enable: bool,
      group: string,
      interface: string,
      mediaDirectories: [{ ... }],
      openFirewall: bool,
      package: Derivation,
      pcDirectoryHide: bool,
      port: { ... },
      ps3Support: bool,
      serverName: string,
      tg100Support: bool,
      transcoding: bool,
      user: string,
      uuid: string,
      ...
    },
    mediawiki: {
      database: {
        createLocally: bool,
        host: { ... },
        name: string,
        passwordFile: { ... },
        path: { ... },
        port: { ... },
        socket: { ... },
        tablePrefix: { ... },
        type: string,
        user: { ... },
        ...
      },
      enable: bool,
      extensions: { _: { ... } },
      extraConfig: string,
      finalPackage: Derivation,
      httpd: {
        virtualHost: { ... },
        ...
      },
      name: string,
      nginx: {
        hostName: string,
        ...
      },
      package: Derivation,
      passwordFile: path,
      passwordSender: string,
      path: [Derivation],
      phpPackage: Derivation,
      poolConfig: { _: { ... } },
      skins: { _: path },
      uploadsDir: path | null,
      url: string,
      virtualHost: { ... },
      webserver: string,
      ...
    },
    meguca: { ... },
    meilisearch: {
      dumplessUpgrade: { ... },
      enable: bool,
      environment: { ... },
      listenAddress: string,
      listenPort: { ... },
      logLevel: { ... },
      masterKeyEnvironmentFile: { ... },
      masterKeyFile: path | null,
      maxIndexSize: { ... },
      noAnalytics: { ... },
      package: Derivation,
      payloadSizeLimit: { ... },
      settings: { ... },
      ...
    },
    memcached: {
      enable: bool,
      enableUnixSocket: bool,
      extraOptions: [string],
      listen: string,
      maxConnections: { ... },
      maxMemory: { ... },
      port: { ... },
      socket: { ... },
      user: string,
      ...
    },
    meme-bingo-web: {
      address: string,
      baseUrl: string,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    memos: {
      dataDir: path,
      enable: bool,
      environmentFile: path,
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { _: { ... } },
      user: string,
      ...
    },
    merecat: {
      enable: bool,
      settings: { _: { ... } },
      ...
    },
    meshcentral: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    meshtasticd: {
      dataDir: path,
      enable: bool,
      group: string,
      package: Derivation,
      port: { ... },
      settings: { ... } | null,
      user: string,
      ...
    },
    mesos: { ... },
    metabase: {
      enable: bool,
      listen: {
        ip: string,
        port: { ... },
        ...
      },
      openFirewall: bool,
      package: Derivation,
      ssl: {
        enable: bool,
        keystore: { ... },
        port: { ... },
        ...
      },
      ...
    },
    metricbeat: {
      enable: bool,
      modules: { _: { ... } },
      package: Derivation,
      settings: { ... },
      ...
    },
    microbin: {
      dataDir: string,
      enable: bool,
      package: Derivation,
      passwordFile: path | null,
      settings: { ... },
      ...
    },
    microsocks: {
      authOnce: bool,
      authPasswordFile: path | null,
      authUsername: string | null,
      disableLogging: bool,
      enable: bool,
      execWrapper: string | null,
      group: string,
      ip: string,
      outgoingBindIp: string | null,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    mighttpd2: {
      config: string,
      cores: int | null,
      enable: bool,
      routing: string,
      ...
    },
    mihomo: {
      configFile: path,
      enable: bool,
      extraOpts: string | null,
      package: Derivation,
      processesInfo: bool,
      tunMode: bool,
      webui: path | null,
      ...
    },
    mimir: {
      configFile: path | null,
      configuration: { ... },
      enable: bool,
      extraFlags: [string],
      package: Derivation,
      ...
    },
    minecraft-server: {
      dataDir: path,
      declarative: bool,
      enable: bool,
      eula: bool,
      jvmOpts: string,
      openFirewall: bool,
      package: Derivation,
      serverProperties: { _: { ... } },
      whitelist: { _: { ... } },
      ...
    },
    minetest-server: {
      config: { _: { ... } },
      configPath: path | null,
      enable: bool,
      extraArgs: [string],
      gameId: string | null,
      logPath: path | null,
      port: { ... } | null,
      world: path | null,
      ...
    },
    mingetty: { ... },
    minidlna: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    miniflux: {
      adminCredentialsFile: path | null,
      config: { ... },
      createDatabaseLocally: bool,
      enable: bool,
      package: Derivation,
      ...
    },
    minio: {
      accessKey: string,
      browser: bool,
      certificatesDir: path,
      configDir: path,
      consoleAddress: string,
      dataDir: [{ ... }],
      enable: bool,
      listenAddress: string,
      package: Derivation,
      region: string,
      rootCredentialsFile: path | null,
      secretKey: string,
      ...
    },
    miniupnpd: {
      appendConfig: string,
      enable: bool,
      externalInterface: string,
      internalIPs: [string],
      natpmp: bool,
      upnp: bool,
      ...
    },
    mirakurun: {
      allowSmartCardAccess: bool,
      channelSettings: { ... } | null | null,
      enable: bool,
      openFirewall: bool,
      port: { ... } | null,
      serverSettings: { ... },
      tunerSettings: { ... } | null | null,
      unixSocket: path | null,
      ...
    },
    miredo: {
      bindAddress: string | null,
      bindPort: string | null,
      enable: bool,
      interfaceName: string,
      package: Derivation,
      serverAddress: string,
      ...
    },
    misc: {
      nzbget: {
        configFile: { ... },
        dataDir: { ... },
        openFirewall: { ... },
        ...
      },
      ...
    },
    misskey: {
      database: {
        createLocally: bool,
        passwordFile: { ... },
        ...
      },
      enable: bool,
      meilisearch: {
        createLocally: bool,
        keyFile: { ... },
        ...
      },
      package: Derivation,
      redis: {
        createLocally: bool,
        passwordFile: { ... },
        ...
      },
      reverseProxy: {
        enable: bool,
        host: { ... },
        ssl: { ... },
        webserver: { ... },
        ...
      },
      settings: { ... },
      ...
    },
    mjolnir: {
      accessTokenFile: path | null,
      dataPath: path,
      enable: bool,
      homeserverUrl: string,
      managementRoom: string,
      pantalaimon: { ... },
      protectedRooms: [string],
      settings: { ... },
      ...
    },
    mjpg-streamer: {
      enable: bool,
      group: string,
      inputPlugin: string,
      outputPlugin: string,
      user: string,
      ...
    },
    mlmmj: {
      enable: bool,
      group: string,
      listDomain: string,
      mailLists: [string],
      maintInterval: string,
      user: string,
      ...
    },
    mmsd: {
      enable: bool,
      extraArgs: [string],
      ...
    },
    mobilizon: {
      enable: bool,
      nginx: {
        enable: bool,
        ...
      },
      package: Derivation,
      settings: { ... },
      ...
    },
    moinmoin: { ... },
    molly-brown: {
      certPath: path,
      docBase: path,
      enable: bool,
      hostName: string,
      keyPath: path,
      port: { ... },
      settings: { ... },
      ...
    },
    mollysocket: {
      enable: bool,
      environmentFile: path | null,
      logLevel: string,
      settings: { ... },
      ...
    },
    monado: {
      defaultRuntime: bool,
      enable: bool,
      forceDefaultRuntime: bool,
      highPriority: bool,
      package: Derivation,
      ...
    },
    monero: {
      banlist: path | null,
      dataDir: string,
      enable: bool,
      environmentFile: path | null,
      exclusiveNodes: [string],
      extraConfig: string,
      extraNodes: [string],
      limits: {
        download: int,
        syncSize: { ... },
        threads: { ... },
        upload: int,
        ...
      },
      mining: {
        address: string,
        enable: bool,
        threads: { ... },
        ...
      },
      priorityNodes: [string],
      prune: bool,
      rpc: {
        address: string,
        password: { ... },
        port: { ... },
        restricted: bool,
        user: { ... },
        ...
      },
      ...
    },
    monetdb: {
      dataDir: path,
      enable: bool,
      group: string,
      listenAddress: string,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    mongodb: {
      bind_ip: string,
      dbpath: string,
      enable: bool,
      enableAuth: bool,
      extraConfig: string,
      initialRootPassword: { ... },
      initialRootPasswordFile: path | null,
      initialScript: path | null,
      mongoshPackage: Derivation,
      package: Derivation,
      pidFile: string,
      quiet: bool,
      replSetName: string,
      user: string,
      ...
    },
    monica: {
      appKeyFile: path,
      appURL: string,
      config: { _: { ... } },
      dataDir: path,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        user: string,
        ...
      },
      enable: bool,
      group: string,
      hostname: string,
      mail: {
        driver: string,
        encryption: { ... },
        from: string,
        fromName: string,
        host: string,
        passwordFile: { ... },
        port: { ... },
        user: { ... },
        ...
      },
      maxUploadSize: string,
      nginx: { ... },
      phpPackage: Derivation,
      poolConfig: { _: { ... } },
      user: string,
      ...
    },
    monit: {
      config: string,
      enable: bool,
      ...
    },
    moodle: {
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        socket: { ... },
        type: string,
        user: string,
        ...
      },
      enable: bool,
      extraConfig: string,
      initialPassword: string,
      package: Derivation,
      poolConfig: { _: { ... } },
      virtualHost: { ... },
      ...
    },
    moonraker: {
      address: string,
      allowSystemControl: bool,
      analysis: {
        enable: bool,
        ...
      },
      configDir: path | null,
      enable: bool,
      group: string,
      klipperSocket: path,
      package: Derivation | null,
      port: { ... },
      settings: { _: { ... } },
      stateDir: path,
      user: string,
      ...
    },
    moosefs: {
      cgiserver: {
        enable: bool,
        openFirewall: bool,
        settings: { ... },
        ...
      },
      chunkserver: {
        enable: bool,
        hdds: { ... },
        openFirewall: bool,
        settings: { ... },
        ...
      },
      client: {
        enable: bool,
        ...
      },
      master: {
        autoInit: bool,
        enable: bool,
        exports: { ... },
        openFirewall: bool,
        settings: { ... },
        ...
      },
      masterHost: string,
      metalogger: {
        enable: bool,
        settings: { ... },
        ...
      },
      runAsUser: bool,
      ...
    },
    mopidy: {
      configuration: { ... },
      dataDir: string,
      enable: bool,
      extensionPackages: [Derivation],
      extraConfigFiles: [string],
      settings: { _: { ... } },
      ...
    },
    morty: { ... },
    mosquitto: {
      bridges: { _: { ... } },
      dataDir: path,
      enable: bool,
      includeDirs: [path],
      listeners: [{ ... }],
      logDest: [{ ... }],
      logType: [string],
      package: Derivation,
      persistence: bool,
      settings: { ... },
      ...
    },
    movim: {
      dataDir: path,
      database: {
        createLocally: bool,
        name: string,
        type: string,
        user: string,
        ...
      },
      debug: bool,
      domain: string,
      enable: bool,
      group: string,
      h2o: { ... },
      logDir: path,
      minifyStaticFiles: bool | { ... },
      nginx: { ... },
      package: Derivation,
      phpCfg: { _: { ... } },
      phpPackage: Derivation,
      podConfig: { ... },
      poolConfig: { _: { ... } },
      port: { ... },
      precompressStaticFiles: { ... },
      runtimeDir: path,
      secretFile: path | null,
      settings: { _: { ... } },
      user: string,
      verbose: bool,
      ...
    },
    mozillavpn: {
      enable: bool,
      ...
    },
    mpd: {
      credentials: [{ ... }],
      dataDir: path,
      dbFile: { ... },
      enable: bool,
      extraConfig: { ... },
      fluidsynth: bool,
      group: string,
      musicDirectory: { ... },
      network: {
        listenAddress: { ... },
        port: { ... },
        ...
      },
      openFirewall: bool | null,
      playlistDirectory: { ... },
      settings: { ... },
      startWhenNeeded: bool,
      user: string,
      ...
    },
    mpdscribble: {
      enable: bool,
      endpoints: { _: { ... } },
      host: string,
      journalInterval: int,
      passwordFile: string | null,
      port: { ... },
      proxy: string | null,
      verbose: int,
      ...
    },
    mptcpd: {
      enable: bool,
      package: Derivation,
      ...
    },
    mqtt2influxdb: {
      enable: bool,
      environmentFiles: [path],
      influxdb: {
        database: string,
        host: string,
        password: { ... },
        port: { ... },
        ssl: bool,
        username: { ... },
        verify_ssl: bool,
        ...
      },
      mqtt: {
        cafile: { ... },
        certfile: { ... },
        host: string,
        keyfile: { ... },
        password: { ... },
        port: { ... },
        username: { ... },
        ...
      },
      package: Derivation,
      points: [{ ... }],
      ...
    },
    mstpd: {
      enable: bool,
      ...
    },
    mtprotoproxy: {
      adTag: string | null,
      enable: bool,
      extraConfig: { ... },
      port: { ... },
      secureOnly: bool,
      users: { _: string },
      ...
    },
    mtr-exporter: {
      address: string,
      enable: bool,
      extraFlags: [string],
      jobs: [{ ... }],
      mtrFlags: { ... },
      mtrPackage: Derivation,
      package: Derivation,
      port: { ... },
      target: { ... },
      ...
    },
    mullvad-vpn: {
      enable: bool,
      enableEarlyBootBlocking: bool,
      enableExcludeWrapper: bool,
      package: Derivation,
      ...
    },
    multipath: {
      blacklist: string | null,
      blacklist_exceptions: string | null,
      defaults: string | null,
      devices: [{ ... }],
      enable: bool,
      extraConfig: string | null,
      extraConfigFile: string | null,
      overrides: string | null,
      package: Derivation,
      pathGroups: [{ ... }],
      ...
    },
    munge: {
      enable: bool,
      password: path,
      ...
    },
    munin-cron: {
      enable: bool,
      extraCSS: string,
      extraGlobalConfig: string,
      hosts: string,
      ...
    },
    munin-node: {
      disabledPlugins: [string],
      enable: bool,
      extraAutoPlugins: [path],
      extraConfig: string,
      extraPluginConfig: string,
      extraPlugins: { _: path },
      ...
    },
    murmur: {
      allowHtml: bool,
      autobanAttempts: int,
      autobanTime: int,
      autobanTimeframe: int,
      bandwidth: int,
      bonjour: bool,
      clientCertRequired: bool,
      dbus: string,
      enable: bool,
      environmentFile: path | null,
      extraConfig: string,
      group: string,
      hostName: string,
      imgMsgLength: int,
      logDays: int,
      logFile: { ... },
      logToFile: bool,
      openFirewall: bool,
      package: Derivation,
      password: string,
      port: { ... },
      registerHostname: string,
      registerName: string,
      registerPassword: string,
      registerUrl: string,
      sendVersion: bool,
      sslCa: path | null,
      sslCert: path | null,
      sslKey: path | null,
      stateDir: path,
      textMsgLength: int,
      user: string,
      users: int,
      welcometext: string,
      ...
    },
    music-assistant: {
      enable: bool,
      extraOptions: [string],
      package: Derivation,
      providers: [string],
      ...
    },
    mwlib: { ... },
    mx-puppet-discord: { ... },
    mxisd: { ... },
    mycelium: {
      addHostedPublicNodes: bool,
      enable: bool,
      extraArgs: [string],
      keyFile: path | null,
      openFirewall: bool,
      package: Derivation,
      peers: [string],
      ...
    },
    mympd: {
      enable: bool,
      extraGroups: [string],
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    mysql: {
      bind: { ... },
      configFile: path,
      dataDir: path,
      enable: bool,
      ensureDatabases: [string],
      ensureUsers: [{ ... }],
      extraOptions: { ... },
      galeraCluster: {
        clusterAddress: string,
        clusterPassword: string,
        enable: bool,
        localAddress: string,
        localName: string,
        name: string,
        nodeAddresses: { ... },
        package: Derivation,
        sstMethod: string,
        ...
      },
      group: string,
      initialDatabases: [{ ... }],
      initialScript: path | null,
      package: Derivation,
      pidDir: { ... },
      port: { ... },
      replication: {
        masterHost: string,
        masterPassword: string,
        masterPort: { ... },
        masterUser: string,
        role: string,
        serverId: int,
        slaveHost: string,
        ...
      },
      rootPassword: { ... },
      settings: { _: { ... } },
      user: string,
      ...
    },
    mysqlBackup: {
      calendar: string,
      compressionAlg: string,
      compressionLevel: int | null,
      databases: [string],
      enable: bool,
      gzipOptions: string,
      location: path,
      singleTransaction: bool | { ... },
      user: string,
      ...
    },
    n8n: {
      customNodes: [Derivation],
      enable: bool,
      environment: { ... },
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      taskRunners: {
        enable: bool,
        environment: { ... },
        launcherPackage: Derivation,
        runners: { ... },
        ...
      },
      webhookUrl: { ... },
      ...
    },
    nagios: {
      cgiConfigFile: Derivation,
      enable: bool,
      enableWebInterface: bool,
      extraConfig: { _: string },
      mainConfigFile: Derivation | null,
      objectDefs: [path],
      plugins: [Derivation],
      urlPath: { ... },
      validateConfig: bool,
      virtualHost: { ... },
      ...
    },
    namecoind: {
      enable: bool,
      extraNodes: [string],
      generate: bool,
      rpc: {
        address: string,
        allowFrom: { ... },
        certificate: { ... },
        key: { ... },
        password: { ... },
        port: { ... },
        user: { ... },
        ...
      },
      trustedNodes: [string],
      wallet: path,
      ...
    },
    nar-serve: {
      cacheURL: string,
      domain: string,
      enable: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    nats: {
      dataDir: path,
      enable: bool,
      group: string,
      jetstream: bool,
      port: { ... },
      serverName: string,
      settings: { ... },
      user: string,
      validateConfig: bool,
      ...
    },
    navidrome: {
      enable: bool,
      environmentFile: path | null,
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    nbd: {
      server: {
        enable: bool,
        exports: { ... },
        extraOptions: { ... },
        listenAddress: { ... },
        listenPort: { ... },
        ...
      },
      ...
    },
    ncdns: {
      address: string,
      dnssec: {
        enable: bool,
        keys: { ... },
        ...
      },
      enable: bool,
      identity: {
        address: string,
        hostmaster: string,
        hostname: string,
        ...
      },
      port: { ... },
      settings: { ... },
      ...
    },
    ncps: {
      analytics: {
        reporting: { ... },
        ...
      },
      cache: {
        allowDeleteVerb: bool,
        allowPutVerb: bool,
        cdc: { ... },
        dataPath: path,
        database: { ... },
        databaseURL: { ... },
        databaseURLFile: { ... },
        hostName: string,
        lock: { ... },
        lru: { ... },
        maxSize: { ... },
        redis: { ... },
        secretKeyPath: { ... },
        signNarinfo: bool,
        storage: { ... },
        tempPath: path,
        upstream: { ... },
        ...
      },
      dbmatePackage: { ... },
      enable: bool,
      logLevel: string,
      netrcFile: path | null,
      openTelemetry: {
        enable: bool,
        grpcURL: { ... },
        ...
      },
      package: Derivation,
      prometheus: {
        enable: bool,
        ...
      },
      server: {
        addr: string,
        ...
      },
      upstream: {
        caches: { ... },
        publicKeys: { ... },
        ...
      },
      ...
    },
    ndppd: {
      configFile: path | null,
      enable: bool,
      interface: string | null,
      network: string | null,
      proxies: { _: { ... } },
      routeTTL: int,
      ...
    },
    neard: {
      enable: bool,
      settings: { ... },
      ...
    },
    nebula: {
      networks: { _: { ... } },
      ...
    },
    nebula-lighthouse-service: {
      enable: bool,
      settings: { ... },
      user: string,
      ...
    },
    neo4j: {
      bolt: {
        advertisedAddress: string,
        enable: bool,
        listenAddress: string,
        sslPolicy: string,
        tlsLevel: string,
        ...
      },
      boltPort: { ... },
      certDir: path,
      constrainLoadCsv: bool,
      dataDir: path,
      defaultListenAddress: string,
      directories: {
        certificates: path,
        data: path,
        home: path,
        imports: path,
        plugins: path,
        ...
      },
      enable: bool,
      enableBolt: bool,
      enableHttps: bool,
      extraServerConfig: string,
      host: string,
      http: {
        advertisedAddress: string,
        enable: bool,
        listenAddress: string,
        ...
      },
      https: {
        advertisedAddress: string,
        enable: bool,
        listenAddress: string,
        sslPolicy: string,
        ...
      },
      httpsPort: { ... },
      listenAddress: string,
      package: Derivation,
      port: { ... },
      readOnly: bool,
      shell: {
        enable: bool,
        enabled: { ... },
        ...
      },
      ssl: {
        policies: { ... },
        ...
      },
      udc: {
        enabled: { ... },
        ...
      },
      workerCount: { ... },
      ...
    },
    netatalk: {
      enable: bool,
      extmap: string,
      extraConfig: { ... },
      homes: { ... },
      port: { ... },
      settings: { _: { ... } },
      volumes: { ... },
      ...
    },
    netbird: {
      clients: { _: { ... } },
      enable: bool,
      package: Derivation,
      server: {
        coturn: { ... },
        dashboard: { ... },
        domain: string,
        enable: bool,
        enableNginx: bool,
        management: { ... },
        signal: { ... },
        ...
      },
      tunnels: { _: { ... } },
      ui: {
        enable: bool,
        package: Derivation,
        ...
      },
      useRoutingFeatures: string,
      ...
    },
    netbox: {
      dataDir: string,
      enable: bool,
      enableLdap: bool,
      extraConfig: string,
      gunicornArgs: [string],
      keycloakClientSecret: path | null,
      ldapConfigPath: path,
      listenAddress: string,
      package: Derivation,
      plugins: { ... } -> { ... },
      port: { ... },
      secretKeyFile: path,
      settings: { ... },
      unixSocket: string | null,
      ...
    },
    netclient: {
      enable: bool,
      package: Derivation,
      ...
    },
    netdata: {
      claimTokenFile: path | null,
      config: { _: { ... } },
      configDir: { _: path },
      configText: string | null,
      deadlineBeforeStopSec: int,
      enable: bool,
      enableAnalyticsReporting: bool,
      extraNdsudoPackages: [Derivation],
      extraPluginPaths: [path],
      group: string,
      package: Derivation,
      python: {
        enable: bool,
        extraPackages: { ... },
        recommendedPythonPackages: bool,
        ...
      },
      user: string,
      ...
    },
    networkaudiod: {
      enable: bool,
      ...
    },
    networkd-dispatcher: {
      enable: bool,
      extraArgs: [string],
      rules: { _: { ... } },
      ...
    },
    networking: {
      websockify: {
        enable: bool,
        portMap: { ... },
        sslCert: path,
        sslKey: path,
        ...
      },
      ...
    },
    newt: {
      blueprint: { ... },
      enable: bool,
      endpoint: { ... },
      environmentFile: path | null,
      id: { ... },
      logLevel: { ... },
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    nextcloud: {
      appstoreEnable: bool | null,
      autoUpdateApps: {
        enable: bool,
        startAt: { ... },
        ...
      },
      caching: {
        apcu: bool,
        memcached: bool,
        redis: bool,
        ...
      },
      cli: {
        memoryLimit: { ... },
        ...
      },
      config: {
        adminpassFile: { ... },
        adminuser: { ... },
        dbhost: { ... },
        dbname: { ... },
        dbpassFile: { ... },
        dbport: { ... },
        dbtableprefix: { ... },
        dbtype: { ... },
        dbuser: { ... },
        defaultPhoneRegion: { ... },
        extraTrustedDomains: { ... },
        objectstore: { ... },
        overwriteProtocol: { ... },
        trustedProxies: { ... },
        ...
      },
      configureRedis: bool,
      cron: {
        memoryLimit: { ... },
        ...
      },
      database: {
        createLocally: bool,
        ...
      },
      datadir: string,
      enable: bool,
      enableBrokenCiphersForSSE: { ... },
      enableImagemagick: bool,
      extraApps: { _: Derivation },
      extraAppsEnable: bool,
      extraOptions: { ... },
      fastcgiTimeout: int,
      finalPackage: Derivation,
      globalProfiles: { ... },
      home: string,
      hostName: string,
      https: bool,
      imaginary: {
        enable: bool,
        ...
      },
      logLevel: { ... },
      logType: { ... },
      maxUploadSize: string,
      nginx: {
        enableFastcgiRequestBuffering: bool,
        hstsMaxAge: { ... },
        recommendedHttpHeaders: { ... },
        ...
      },
      notify_push: {
        bendDomainToLocalhost: bool,
        dbhost: { ... },
        dbname: { ... },
        dbpassFile: { ... },
        dbport: { ... },
        dbtableprefix: { ... },
        dbtype: { ... },
        dbuser: { ... },
        enable: bool,
        logLevel: string,
        nextcloudUrl: string,
        package: Derivation,
        socketPath: string,
        ...
      },
      occ: Derivation,
      package: Derivation,
      phpExtraExtensions: { ... } -> { ... },
      phpOptions: { _: { ... } },
      phpPackage: Derivation,
      poolConfig: string | null,
      poolSettings: { _: { ... } },
      secretFile: string | null,
      secrets: { _: path },
      settings: { ... },
      skeletonDirectory: { ... },
      webfinger: bool,
      ...
    },
    nextcloud-spreed-signaling: {
      backends: { _: { ... } },
      configureNginx: bool,
      enable: bool,
      group: string,
      hostName: string | null,
      package: Derivation,
      settings: { ... },
      stateDir: path,
      user: string,
      ...
    },
    nextcloud-whiteboard-server: {
      enable: bool,
      secrets: [string],
      settings: { _: string },
      ...
    },
    nextdns: {
      arguments: [string],
      enable: bool,
      ...
    },
    nextjs-ollama-llm-ui: {
      enable: bool,
      hostname: string,
      ollamaUrl: string,
      package: Derivation,
      port: { ... },
      ...
    },
    nexus: {
      enable: bool,
      group: string,
      home: string,
      jdkPackage: Derivation,
      jvmOpts: string,
      listenAddress: string,
      listenPort: { ... },
      package: Derivation,
      user: string,
      ...
    },
    nezha-agent: {
      clientSecretFile: path | null,
      debug: bool,
      disableCommandExecute: { ... },
      disableNat: { ... },
      disableSendQuery: { ... },
      enable: bool,
      extraFlags: { ... },
      genUuid: bool,
      gpu: { ... },
      package: Derivation,
      passwordFile: { ... },
      reportDelay: { ... },
      server: { ... },
      settings: { ... },
      skipConnection: { ... },
      skipProcess: { ... },
      temperature: { ... },
      tls: { ... },
      useIPv6CountryCode: { ... },
      ...
    },
    nfs: {
      extraConfig: string,
      idmapd: {
        settings: { ... },
        ...
      },
      lockdPort: { ... } | null,
      server: {
        createMountPoints: bool,
        enable: bool,
        exports: string,
        extraNfsdConfig: string,
        hostName: { ... },
        lockdPort: { ... },
        mountdPort: { ... },
        nproc: int,
        statdPort: { ... },
        ...
      },
      settings: { _: { ... } },
      statdPort: { ... } | null,
      ...
    },
    nghttpx: {
      backend-address-family: string,
      backends: [{ ... }],
      backlog: int,
      enable: bool,
      extraConfig: string,
      frontends: [{ ... }],
      rlimit-nofile: int,
      single-process: bool,
      single-thread: bool,
      tls: { ... },
      workers: int,
      ...
    },
    nginx: {
      additionalModules: [{ ... }],
      appendConfig: string,
      appendHttpConfig: string,
      clientMaxBodySize: string,
      commonHttpConfig: string,
      config: string,
      defaultHTTPListenPort: { ... },
      defaultListen: [{ ... }],
      defaultListenAddresses: [string],
      defaultMimeTypes: path,
      defaultSSLListenPort: { ... },
      enable: bool,
      enableQuicBPF: bool,
      enableReload: bool,
      eventsConfig: string,
      experimentalZstdSettings: bool,
      gitweb: {
        enable: bool,
        group: string,
        location: string,
        user: string,
        virtualHost: string,
        ...
      },
      group: string,
      httpConfig: string,
      logError: string,
      mapHashBucketSize: { ... } | null,
      mapHashMaxSize: { ... } | null,
      package: Derivation,
      preStart: string,
      prependConfig: string,
      proxyCache: {
        enable: { ... },
        inactive: { ... },
        keysZoneName: { ... },
        keysZoneSize: { ... },
        levels: { ... },
        useTempPath: { ... },
        ...
      },
      proxyCachePath: { _: { ... } },
      proxyResolveWhileRunning: bool,
      proxyTimeout: string,
      recommendedBrotliSettings: bool,
      recommendedGzipSettings: bool,
      recommendedOptimisation: bool,
      recommendedProxySettings: bool,
      recommendedTlsSettings: bool,
      recommendedUwsgiSettings: bool,
      recommendedZstdSettings: { ... },
      resolver: { ... },
      serverNamesHashBucketSize: { ... } | null,
      serverNamesHashMaxSize: { ... } | null,
      serverTokens: bool,
      sslCiphers: string | null,
      sslDhparam: path | bool,
      sslProtocols: string,
      sso: {
        configuration: { ... },
        enable: bool,
        package: Derivation,
        ...
      },
      stateDir: { ... },
      statusPage: bool,
      streamConfig: string,
      tailscaleAuth: {
        enable: bool,
        expectedTailnet: { ... },
        group: string,
        package: Derivation,
        socketPath: path,
        user: string,
        virtualHosts: { ... },
        ...
      },
      typesHashMaxSize: { ... },
      upstreams: { _: { ... } },
      user: string,
      uwsgiResolveWhileRunning: bool,
      uwsgiTimeout: string,
      validateConfigFile: bool,
      virtualHosts: { _: { ... } },
      ...
    },
    ngircd: {
      config: string,
      enable: bool,
      package: Derivation,
      ...
    },
    nifi: {
      enable: bool,
      enableHTTPS: bool,
      group: string,
      initJavaHeapSize: int | null,
      initPasswordFile: path | null,
      initUser: string | null,
      listenHost: string,
      listenPort: { ... },
      maxJavaHeapSize: int | null,
      package: Derivation,
      proxyHost: string | null,
      proxyPort: { ... } | null,
      user: string,
      ...
    },
    nipap: {
      authBackendSettings: { ... },
      enable: bool,
      nipap-www: {
        enable: bool,
        host: { ... },
        package: Derivation,
        port: { ... },
        umask: string,
        unixSocket: { ... },
        workers: int,
        xmlrpcURIFile: { ... },
        ...
      },
      nipapd: {
        database: { ... },
        enable: bool,
        package: Derivation,
        ...
      },
      settings: { ... },
      user: string,
      ...
    },
    nitter: {
      cache: {
        listMinutes: int,
        redisConnections: int,
        redisHost: string,
        redisMaxConnections: int,
        redisPort: { ... },
        rssMinutes: int,
        ...
      },
      config: {
        base64Media: bool,
        enableDebug: bool,
        enableRSS: bool,
        proxy: string,
        proxyAuth: string,
        tokenCount: int,
        ...
      },
      enable: bool,
      guestAccounts: path,
      openFirewall: bool,
      package: Derivation,
      preferences: {
        autoplayGifs: bool,
        bidiSupport: bool,
        hideBanner: bool,
        hidePins: bool,
        hideReplies: bool,
        hideTweetStats: bool,
        hlsPlayback: bool,
        infiniteScroll: bool,
        mp4Playback: bool,
        muteVideos: bool,
        proxyVideos: bool,
        replaceReddit: string,
        replaceTwitter: string,
        replaceYouTube: string,
        squareAvatars: bool,
        stickyProfile: bool,
        theme: string,
        ...
      },
      redisCreateLocally: bool,
      replaceInstagram: { ... },
      server: {
        address: string,
        hostname: string,
        httpMaxConnections: int,
        https: bool,
        port: { ... },
        staticDir: path,
        title: string,
        ...
      },
      sessionsFile: path,
      settings: { ... },
      ...
    },
    nix-serve: {
      bindAddress: string,
      enable: bool,
      extraParams: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      secretKeyFile: string | null,
      ...
    },
    nix-store-gcs-proxy: { _: { ... } },
    nixops-dns: {
      dnsmasq: bool,
      domain: string,
      enable: bool,
      user: string,
      ...
    },
    nixosManual: {
      enable: bool,
      ...
    },
    nixseparatedebuginfod: { ... },
    nixseparatedebuginfod2: {
      cacheExpirationDelay: string,
      enable: bool,
      package: Derivation,
      port: { ... },
      substituter: { ... },
      substituters: [string],
      ...
    },
    nncp: {
      caller: {
        enable: bool,
        extraArgs: { ... },
        ...
      },
      daemon: {
        enable: bool,
        extraArgs: { ... },
        socketActivation: { ... },
        ...
      },
      ...
    },
    nntp-proxy: {
      enable: bool,
      listenAddress: string,
      port: { ... },
      prohibitPosting: bool,
      sslCert: string,
      sslKey: string,
      upstreamMaxConnections: int,
      upstreamPassword: string,
      upstreamPort: { ... },
      upstreamServer: string,
      upstreamUser: string,
      users: { _: { ... } },
      verbosity: string,
      ...
    },
    node-red: {
      configFile: path,
      define: { ... },
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      safe: bool,
      user: string,
      userDir: path,
      withNpmAndGcc: bool,
      ...
    },
    nohang: {
      configPath: string | path,
      enable: bool,
      package: Derivation,
      ...
    },
    nomad: {
      credentials: { _: string },
      dropPrivileges: bool,
      enable: bool,
      enableDocker: bool,
      extraPackages: [Derivation],
      extraSettingsPaths: [path],
      extraSettingsPlugins: [{ ... }],
      package: Derivation,
      settings: { ... },
      ...
    },
    nominatim: {
      database: {
        apiUser: string,
        dbname: string,
        extraConnectionParams: { ... },
        host: string,
        passwordFile: { ... },
        port: { ... },
        superUser: string,
        ...
      },
      enable: bool,
      hostName: string,
      package: Derivation,
      settings: { _: string },
      ui: {
        config: { ... },
        package: Derivation,
        ...
      },
      ...
    },
    nostr-rs-relay: {
      dataDir: path,
      enable: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    novacomd: {
      enable: bool,
      ...
    },
    nscd: {
      config: string,
      enable: bool,
      enableNsncd: bool,
      group: string,
      package: Derivation,
      user: string,
      ...
    },
    nsd: {
      bind8Stats: bool,
      dnssecInterval: string,
      enable: bool,
      extraConfig: string,
      hideVersion: bool,
      identity: string,
      interfaces: [string],
      ipFreebind: bool,
      ipTransparent: bool,
      ipv4: bool,
      ipv4EDNSSize: int,
      ipv6: bool,
      ipv6EDNSSize: int,
      keys: { _: { ... } },
      logTimeAscii: bool,
      nsid: string | null,
      port: { ... },
      ratelimit: {
        enable: bool,
        ipv4PrefixLength: { ... },
        ipv6PrefixLength: { ... },
        ratelimit: int,
        size: int,
        slip: { ... },
        whitelistRatelimit: int,
        ...
      },
      remoteControl: {
        controlCertFile: path,
        controlKeyFile: path,
        enable: bool,
        interfaces: { ... },
        port: { ... },
        serverCertFile: path,
        serverKeyFile: path,
        ...
      },
      reuseport: bool,
      rootServer: bool,
      roundRobin: bool,
      serverCount: int,
      statistics: int | null,
      tcpCount: int,
      tcpQueryCount: int,
      tcpTimeout: int,
      verbosity: int,
      version: string | null,
      xfrdReloadTimeout: int,
      zonefilesCheck: bool,
      zonefilesWrite: int,
      zones: { _: { ... } },
      ...
    },
    ntfy-sh: {
      enable: bool,
      environmentFile: path | null,
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    ntopng: {
      configText: string,
      enable: bool,
      extraConfig: string,
      http-port: { ... },
      httpPort: { ... },
      interfaces: [string],
      redis: {
        address: string,
        createInstance: { ... },
        ...
      },
      ...
    },
    ntp: {
      enable: bool,
      extraConfig: string,
      extraFlags: [string],
      restrictDefault: [string],
      restrictSource: [string],
      servers: [string],
      ...
    },
    ntpd-rs: {
      enable: bool,
      metrics: {
        enable: bool,
        ...
      },
      package: Derivation,
      settings: { ... },
      useNetworkingTimeServers: bool,
      ...
    },
    nullidentdmod: {
      enable: bool,
      userid: string | null,
      ...
    },
    nullmailer: {
      config: {
        adminaddr: { ... },
        allmailfrom: { ... },
        defaultdomain: { ... },
        defaulthost: { ... },
        doublebounceto: { ... },
        helohost: { ... },
        idhost: { ... },
        maxpause: { ... },
        me: { ... },
        pausetime: { ... },
        remotes: { ... },
        sendtimeout: { ... },
        ...
      },
      enable: bool,
      group: string,
      remotesFile: string | null,
      setSendmail: bool,
      user: string,
      ...
    },
    nvme-rs: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    nylon: { _: { ... } },
    nzbget: {
      enable: bool,
      group: string,
      package: Derivation,
      settings: { _: { ... } },
      user: string,
      ...
    },
    nzbhydra2: {
      dataDir: string,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      ...
    },
    oauth2-proxy: {
      approvalPrompt: string,
      azure: {
        resource: string,
        tenant: string,
        ...
      },
      basicAuthPassword: string | null,
      clientID: string | null,
      clientSecret: { ... },
      clientSecretFile: path | null,
      cookie: {
        domain: { ... },
        expire: string,
        httpOnly: bool,
        name: string,
        refresh: { ... },
        secret: { ... },
        secretFile: { ... },
        secure: bool,
        ...
      },
      customTemplatesDir: path | null,
      email: {
        addresses: { ... },
        domains: { ... },
        ...
      },
      enable: bool,
      extraConfig: { _: { ... } },
      github: {
        org: { ... },
        team: { ... },
        ...
      },
      google: {
        adminEmail: string,
        groups: { ... },
        serviceAccountJSON: path,
        ...
      },
      htpasswd: {
        displayForm: bool,
        file: { ... },
        ...
      },
      httpAddress: string,
      keyFile: path | null,
      loginURL: string | null,
      nginx: {
        domain: string,
        proxy: string,
        virtualHosts: { ... },
        ...
      },
      oidcIssuerUrl: string | null,
      package: Derivation,
      passAccessToken: bool,
      passBasicAuth: bool,
      passHostHeader: bool,
      profileURL: string | null,
      provider: string,
      proxyPrefix: string,
      redeemURL: string | null,
      redirectURL: string | null,
      requestLogging: bool,
      reverseProxy: bool,
      scope: string | null,
      setXauthrequest: bool | null,
      signatureKey: string | null,
      skipAuthRegexes: [string],
      tls: {
        certificate: path,
        enable: bool,
        httpsAddress: string,
        key: path,
        ...
      },
      upstream: [string],
      validateURL: string | null,
      ...
    },
    oauth2_proxy: { ... },
    ocis: {
      address: string,
      configDir: path | null,
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      group: string,
      package: Derivation,
      port: { ... },
      stateDir: string,
      url: string,
      user: string,
      ...
    },
    ocserv: {
      config: string,
      enable: bool,
      ...
    },
    ocsinventory-agent: {
      enable: bool,
      interval: string,
      package: Derivation,
      settings: { ... },
      ...
    },
    octoprint: {
      enable: bool,
      extraConfig: { ... },
      group: string,
      host: string | null,
      openFirewall: bool,
      package: Derivation,
      plugins: { ... } -> { ... },
      port: { ... },
      stateDir: path,
      user: string,
      ...
    },
    odoo: {
      addons: [Derivation],
      autoInit: bool,
      autoInitExtraFlags: [string],
      domain: string | null,
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    offlineimap: {
      enable: bool,
      install: bool,
      onCalendar: string,
      package: Derivation,
      path: [path],
      timeoutStartSec: string,
      ...
    },
    ofono: {
      enable: bool,
      plugins: [Derivation],
      ...
    },
    oidentd: {
      enable: bool,
      ...
    },
    oink: {
      apiKeyFile: path,
      domains: [{ ... }],
      enable: bool,
      package: Derivation,
      secretApiKeyFile: path,
      settings: {
        apiKey: { ... },
        interval: { ... },
        secretApiKey: { ... },
        ttl: { ... },
        ...
      },
      ...
    },
    olivetin: {
      enable: bool,
      extraConfigFiles: [path],
      group: string,
      package: Derivation,
      path: [{ ... }],
      settings: { ... },
      user: string,
      ...
    },
    ollama: {
      acceleration: { ... },
      enable: bool,
      environmentVariables: { _: string },
      group: string | null,
      home: string,
      host: string,
      listenAddress: { ... },
      loadModels: [string],
      models: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      rocmOverrideGfx: string | null,
      sandbox: { ... },
      syncModels: bool,
      user: string | null,
      writablePaths: { ... },
      ...
    },
    ombi: {
      dataDir: string,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    omnom: {
      dataDir: path,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      passwordFile: path | null,
      port: { ... },
      settings: { ... },
      user: string,
      ...
    },
    oncall: {
      database: {
        createLocally: bool,
        ...
      },
      enable: bool,
      package: Derivation,
      secretFile: path,
      settings: { ... },
      ...
    },
    onedrive: {
      enable: bool,
      package: Derivation,
      ...
    },
    onlyoffice: {
      enable: bool,
      enableExampleServer: bool,
      examplePort: { ... },
      hostname: string,
      jwtSecretFile: string | null,
      loglevel: string,
      package: Derivation,
      port: { ... },
      postgresHost: string,
      postgresName: string,
      postgresPasswordFile: string | null,
      postgresUser: string,
      rabbitmqUrl: string,
      securityNonceFile: string,
      wopi: bool,
      x2t: Derivation,
      ...
    },
    open-web-calendar: {
      calendarSettings: { ... },
      domain: string,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    open-webui: {
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      stateDir: path,
      ...
    },
    openafsClient: {
      afsdb: bool,
      cache: {
        blocks: int,
        chunksize: { ... },
        directory: string,
        diskless: bool,
        ...
      },
      cellName: string,
      cellServDB: { _: { ... } },
      crypt: bool,
      daemons: int,
      enable: bool,
      fakestat: bool,
      globalCellServDBFile: path | null,
      inumcalc: { ... },
      mountPoint: string,
      packages: {
        module: Derivation,
        programs: Derivation,
        ...
      },
      sparse: bool,
      startDisconnected: bool,
      ...
    },
    openafsServer: {
      advertisedAddresses: [string],
      cellName: string,
      cellServDB: { _: { ... } },
      dottedPrincipals: bool,
      enable: bool,
      package: Derivation,
      roles: {
        backup: { ... },
        database: { ... },
        fileserver: { ... },
        ...
      },
      udpPacketSize: int,
      ...
    },
    openarena: {
      enable: bool,
      extraFlags: [string],
      openPorts: bool,
      package: Derivation,
      ...
    },
    openbao: {
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      settings: { ... },
      ...
    },
    opencloud: {
      address: string,
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      group: string,
      idpWebPackage: Derivation,
      package: Derivation,
      port: { ... },
      settings: { _: { ... } },
      stateDir: string,
      url: string,
      user: string,
      webPackage: Derivation,
      ...
    },
    opendkim: {
      configFile: path | null,
      domains: string,
      enable: bool,
      group: string,
      keyFile: path,
      keyPath: path,
      selector: string,
      settings: { ... },
      socket: string,
      user: string,
      ...
    },
    openfire: { ... },
    opengfw: {
      dir: { ... },
      enable: bool,
      logFile: path | null,
      logFormat: string,
      logLevel: string,
      package: Derivation,
      pcapReplay: path | null,
      rules: [{ ... }],
      rulesFile: path | null,
      settings: { ... },
      settingsFile: path | null,
      user: { ... },
      ...
    },
    openiscsi: {
      discoverPortal: string | null,
      enable: bool,
      enableAutoLoginOut: bool,
      extraConfig: string,
      extraConfigFile: string | null,
      name: string,
      package: Derivation,
      ...
    },
    openldap: {
      configDir: path | null,
      declarativeContents: { _: string },
      enable: bool,
      group: string,
      mutableConfig: bool,
      package: Derivation,
      settings: { ... },
      urlList: [string],
      user: string,
      ...
    },
    openntpd: {
      enable: bool,
      extraConfig: string,
      extraOptions: string,
      servers: [string],
      ...
    },
    opensearch: {
      dataDir: path,
      enable: bool,
      extraCmdLineOptions: [string],
      extraJavaOptions: [string],
      group: string,
      logging: string,
      package: Derivation,
      restartIfChanged: bool,
      settings: { ... },
      user: string,
      ...
    },
    opensmtpd: {
      addSendmailToSystemPath: bool,
      enable: bool,
      extraServerArgs: [string],
      package: Derivation,
      procPackages: [Derivation],
      serverConfiguration: string,
      setSendmail: bool,
      ...
    },
    opensnitch: {
      enable: bool,
      package: Derivation,
      rules: { ... },
      settings: { ... },
      ...
    },
    openssh: {
      allowSFTP: bool,
      authorizedKeysCommand: string,
      authorizedKeysCommandUser: string,
      authorizedKeysFiles: [string],
      authorizedKeysInHomedir: bool,
      banner: string | null,
      challengeResponseAuthentication: { ... },
      ciphers: { ... },
      enable: bool,
      extraConfig: string,
      forwardX11: { ... },
      gatewayPorts: { ... },
      generateHostKeys: bool,
      hostKeys: [{ ... }],
      kbdInteractiveAuthentication: { ... },
      kexAlgorithms: { ... },
      knownHosts: { _: { ... } },
      listenAddresses: [{ ... }],
      logLevel: { ... },
      macs: { ... },
      moduliFile: path,
      openFirewall: bool,
      package: Derivation,
      passwordAuthentication: { ... },
      permitRootLogin: { ... },
      ports: [{ ... }],
      settings: { ... },
      sftpFlags: [string],
      sftpServerExecutable: string,
      startWhenNeeded: bool,
      useDns: { ... },
      ...
    },
    opentelemetry-collector: {
      configFile: path | null,
      enable: bool,
      package: Derivation,
      settings: { ... },
      validateConfigFile: bool,
      ...
    },
    opentracker: {
      enable: bool,
      extraOptions: string,
      package: Derivation,
      ...
    },
    opentsdb: {
      config: string,
      enable: bool,
      group: string,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    openvpn: {
      enable: { ... },
      package: Derivation,
      restartAfterSleep: bool,
      servers: { _: { ... } },
      ...
    },
    openvscode-server: {
      connectionToken: string | null,
      connectionTokenFile: string | null,
      enable: bool,
      extensionsDir: string | null,
      extraArguments: [string],
      extraEnvironment: { _: string },
      extraGroups: [string],
      extraPackages: [Derivation],
      group: string,
      host: string,
      package: Derivation,
      port: { ... },
      serverDataDir: string | null,
      socketPath: string | null,
      telemetryLevel: string | null,
      user: string,
      userDataDir: string | null,
      withoutConnectionToken: bool,
      ...
    },
    openwebrx: {
      enable: bool,
      package: Derivation,
      ...
    },
    opkssh: {
      authorizations: [{ ... }],
      enable: bool,
      group: string,
      package: Derivation,
      providers: { _: { ... } },
      user: string,
      ...
    },
    orangefs: {
      client: {
        enable: bool,
        extraOptions: { ... },
        fileSystems: { ... },
        ...
      },
      server: {
        BMIModules: { ... },
        dataStorageSpace: { ... },
        enable: bool,
        extraConfig: string,
        extraDefaults: string,
        fileSystems: { ... },
        logType: string,
        metadataStorageSpace: { ... },
        servers: { ... },
        ...
      },
      ...
    },
    orca: {
      enable: bool,
      package: Derivation,
      ...
    },
    orthanc: {
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      stateDir: path,
      ...
    },
    osquery: {
      enable: bool,
      flags: { ... },
      package: Derivation,
      settings: { ... },
      ...
    },
    osrm: {
      address: string,
      algorithm: string,
      dataFile: path,
      enable: bool,
      extraFlags: [string],
      port: { ... },
      threads: int,
      ...
    },
    ostinato: {
      enable: bool,
      port: { ... },
      portList: {
        exclude: { ... },
        include: { ... },
        ...
      },
      rateAccuracy: string,
      rpcServer: {
        address: string,
        ...
      },
      ...
    },
    outline: {
      azureAuthentication: { ... },
      cdnUrl: string,
      concurrency: int,
      databaseUrl: string,
      debugOutput: string | null,
      defaultLanguage: string,
      discordAuthentication: { ... },
      enable: bool,
      enableUpdateCheck: bool,
      forceHttps: bool,
      googleAnalyticsId: string | null,
      googleAuthentication: { ... },
      group: string,
      logo: string | null,
      maximumImportSize: int,
      oidcAuthentication: { ... },
      package: Derivation,
      port: { ... },
      publicUrl: string,
      rateLimiter: {
        durationWindow: int,
        enable: bool,
        requests: int,
        ...
      },
      redisUrl: string,
      secretKeyFile: string,
      sentryDsn: string | null,
      sentryTunnel: string | null,
      sequelizeArguments: { ... },
      slackAuthentication: { ... },
      slackIntegration: { ... },
      smtp: { ... },
      sslCertFile: string | null,
      sslKeyFile: string | null,
      storage: { ... },
      user: string,
      utilsSecretFile: string,
      ...
    },
    overseerr: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    owamp: {
      enable: bool,
      ...
    },
    owncast: {
      dataDir: string,
      enable: bool,
      group: string,
      listen: string,
      openFirewall: bool,
      port: { ... },
      rtmp-port: { ... },
      user: string,
      ...
    },
    oxidized: {
      configFile: path | null,
      dataDir: path,
      enable: bool,
      group: string,
      package: Derivation,
      routerDB: path | null,
      user: string,
      ...
    },
    pacemaker: {
      enable: bool,
      package: Derivation,
      ...
    },
    packagekit: {
      backend: { ... },
      enable: bool,
      settings: { _: { ... } },
      vendorSettings: { _: { ... } },
      ...
    },
    pairdrop: {
      enable: bool,
      environment: { ... },
      package: Derivation,
      port: { ... },
      rtcConfig: { ... },
      ...
    },
    paisa: {
      enable: bool,
      host: string,
      mutableSettings: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    pangolin: {
      baseDomain: string | null,
      dashboardDomain: string,
      dataDir: string,
      dnsProvider: string | null,
      enable: bool,
      environmentFile: path | null,
      letsEncryptEmail: string | null,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    pantalaimon-headless: {
      instances: { _: { ... } },
      ...
    },
    pantheon: {
      apps: {
        enable: bool,
        ...
      },
      contractor: {
        enable: bool,
        ...
      },
      files: { ... },
      parental-controls: {
        enable: bool,
        ...
      },
      ...
    },
    paperless: {
      address: string,
      configureNginx: bool,
      configureTika: bool,
      consumptionDir: string,
      consumptionDirIsPublic: bool,
      dataDir: string,
      database: {
        createLocally: bool,
        ...
      },
      domain: string | null,
      enable: bool,
      environmentFile: path | null,
      exporter: {
        directory: string,
        enable: bool,
        onCalendar: { ... },
        settings: { ... },
        ...
      },
      extraConfig: { ... },
      manage: Derivation,
      mediaDir: string,
      openMPThreadingWorkaround: bool,
      package: Derivation,
      passwordFile: path | null,
      port: { ... },
      settings: { ... },
      user: string,
      ...
    },
    paperless-ng: { ... },
    paretosecurity: {
      enable: bool,
      package: Derivation,
      trayIcon: bool,
      users: { _: { ... } },
      ...
    },
    parsedmarc: {
      enable: bool,
      provision: {
        elasticsearch: bool,
        geoIp: bool,
        grafana: { ... },
        localMail: { ... },
        ...
      },
      settings: { ... },
      ...
    },
    parsoid: { ... },
    part-db: {
      enable: bool,
      enableNginx: bool,
      enablePostgresql: bool,
      package: Derivation,
      phpPackage: Derivation,
      poolConfig: { _: { ... } },
      settings: { ... },
      virtualHost: string,
      ...
    },
    passSecretService: {
      enable: bool,
      package: Derivation,
      ...
    },
    patroni: {
      dataDir: path,
      enable: bool,
      environmentFiles: { _: { ... } },
      group: string,
      name: string,
      namespace: string,
      nodeIp: string,
      otherNodesIps: [string],
      postgresqlDataDir: path,
      postgresqlPackage: Derivation,
      postgresqlPort: { ... },
      raft: { ... },
      raftPort: { ... },
      restApiPort: { ... },
      scope: string,
      settings: { ... },
      softwareWatchdog: bool,
      user: string,
      ...
    },
    pcscd: {
      enable: bool,
      extendReaderNames: string | null,
      extraArgs: [string],
      ignoreReaderNames: [{ ... }],
      plugins: [Derivation],
      readerConfig: { ... },
      readerConfigs: [string],
      ...
    },
    pdfding: {
      allowedHosts: [string],
      backup: {
        enable: bool,
        endpoint: { ... },
        schedule: string,
        ...
      },
      consume: {
        enable: bool,
        schedule: string,
        ...
      },
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        type: string,
        user: string,
        ...
      },
      enable: bool,
      envFiles: [path],
      extraEnvironment: { _: string },
      group: string,
      gunicorn: {
        extraArgs: { ... },
        ...
      },
      hostName: string,
      installTestHelpers: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      secretKeyFile: path,
      user: string,
      ...
    },
    pdns-recursor: {
      api: {
        address: string,
        allowFrom: { ... },
        port: { ... },
        ...
      },
      dns: {
        address: { ... },
        allowFrom: { ... },
        port: { ... },
        ...
      },
      dnssecValidation: string,
      enable: bool,
      exportHosts: bool,
      extraConfig: { ... },
      forwardZones: { ... },
      forwardZonesRecurse: { ... },
      luaConfig: string,
      old-settings: { ... },
      resolveNamecoin: bool,
      serveRFC1918: bool,
      settings: { ... },
      yaml-settings: { ... } | null,
      ...
    },
    pdnsd: {
      cacheDir: string,
      enable: bool,
      extraConfig: string,
      globalConfig: string,
      serverConfig: string,
      ...
    },
    pds: {
      enable: bool,
      environmentFiles: [path],
      package: Derivation,
      pdsadmin: { ... },
      settings: { ... },
      ...
    },
    peerflix: {
      downloadDir: path,
      enable: bool,
      stateDir: path,
      ...
    },
    peering-manager: {
      enable: bool,
      enableLdap: bool,
      enableOidc: { ... },
      enableScheduledTasks: bool,
      environmentFile: path | null,
      extraConfig: string,
      ldapConfigPath: path,
      listenAddress: string,
      oidcConfigPath: { ... },
      peeringdbApiKeyFile: path | null,
      plugins: { ... } -> { ... },
      port: { ... },
      secretKeyFile: path,
      settings: { ... },
      ...
    },
    peertube: {
      configureNginx: bool,
      dataDirs: [path],
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        user: string,
        ...
      },
      enable: bool,
      enableWebHttps: bool,
      group: string,
      listenHttp: { ... },
      listenWeb: { ... },
      localDomain: string,
      package: Derivation,
      redis: {
        createLocally: bool,
        enableUnixSocket: bool,
        host: { ... },
        passwordFile: { ... },
        port: { ... },
        ...
      },
      secrets: {
        secretsFile: { ... },
        ...
      },
      serviceEnvironmentFile: path | null,
      settings: { ... },
      smtp: {
        createLocally: bool,
        passwordFile: { ... },
        ...
      },
      user: string,
      ...
    },
    peertube-runner: {
      enable: bool,
      enabledJobTypes: [string],
      group: string,
      instancesToRegister: { _: { ... } },
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    perses: {
      enable: bool,
      extraOptions: [string],
      listenAddress: string,
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    persistent-evdev: {
      devices: { _: string },
      enable: bool,
      ...
    },
    pfix-srsd: {
      configurePostfix: bool,
      domain: string,
      enable: bool,
      secretsFile: path,
      ...
    },
    pgadmin: {
      emailServer: {
        address: string,
        enable: bool,
        passwordFile: path,
        port: { ... },
        sender: string,
        useSSL: bool,
        useTLS: bool,
        username: { ... },
        ...
      },
      enable: bool,
      initialEmail: string,
      initialPasswordFile: path,
      minimumPasswordLength: int,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { _: { ... } },
      ...
    },
    pgbackrest: {
      commands: {
        annotate: { ... },
        archive-get: { ... },
        archive-push: { ... },
        backup: { ... },
        check: { ... },
        expire: { ... },
        help: { ... },
        info: { ... },
        repo-get: { ... },
        repo-ls: { ... },
        restore: { ... },
        server: { ... },
        server-ping: { ... },
        stanza-create: { ... },
        stanza-delete: { ... },
        stanza-upgrade: { ... },
        start: { ... },
        stop: { ... },
        verify: { ... },
        version: { ... },
        ...
      },
      enable: bool,
      repos: { _: { ... } },
      settings: { ... },
      stanzas: { _: { ... } },
      ...
    },
    pgbouncer: {
      adminUsers: { ... },
      authDbname: { ... },
      authFile: { ... },
      authHbaFile: { ... },
      authQuery: { ... },
      authType: { ... },
      authUser: { ... },
      databases: { ... },
      defaultPoolSize: { ... },
      enable: bool,
      extraConfig: { ... },
      group: string,
      homeDir: path,
      ignoreStartupParameters: { ... },
      listenAddress: { ... },
      listenPort: { ... },
      logFile: { ... },
      maxClientConn: { ... },
      maxDbConnections: { ... },
      maxUserConnections: { ... },
      openFilesLimit: int,
      openFirewall: bool,
      package: Derivation,
      peers: { ... },
      poolMode: { ... },
      settings: { ... },
      statsUsers: { ... },
      syslog: {
        enable: { ... },
        syslogFacility: { ... },
        syslogIdent: { ... },
        ...
      },
      tls: {
        client: { ... },
        server: { ... },
        ...
      },
      user: string,
      users: { ... },
      verbose: { ... },
      ...
    },
    pghero: {
      enable: bool,
      environment: { _: string },
      environmentFiles: [path],
      extraArgs: [string],
      extraGroups: [string],
      listenAddress: string,
      package: Derivation,
      settings: { ... },
      ...
    },
    pgmanage: {
      allowCustomConnections: bool,
      connections: { _: string },
      enable: bool,
      localOnly: bool,
      logLevel: string,
      loginGroup: string | null,
      loginTimeout: int,
      package: Derivation,
      port: { ... },
      sqlRoot: string,
      superOnly: bool,
      tls: { ... },
      ...
    },
    pgpkeyserver-lite: {
      enable: bool,
      hkpAddress: string,
      hkpPort: { ... },
      hostname: string,
      package: Derivation,
      ...
    },
    pgscv: {
      enable: bool,
      logLevel: string,
      package: Derivation,
      settings: { ... },
      ...
    },
    photonvision: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      ...
    },
    photoprism: {
      address: string,
      databasePasswordFile: path | null,
      enable: bool,
      group: string,
      importPath: string,
      originalsPath: path,
      package: Derivation,
      passwordFile: path | null,
      port: { ... },
      settings: { _: string },
      storagePath: path,
      user: string,
      ...
    },
    photoview: {
      dataDir: path,
      database: {
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        type: string,
        user: string,
        ...
      },
      enable: bool,
      group: string,
      host: string,
      mediaPath: path,
      package: Derivation,
      port: { ... },
      secretsFile: path | null,
      settings: {
        disableFaceRecognition: bool,
        disableRawProcessing: bool,
        disableVideoEncoding: bool,
        mapboxToken: { ... },
        videoEncoder: { ... },
        ...
      },
      user: string,
      ...
    },
    phpfpm: {
      extraConfig: string | null,
      phpIni: { ... },
      phpOptions: string,
      phpPackage: Derivation,
      poolConfigs: { ... },
      pools: { _: { ... } },
      settings: { _: { ... } },
      ...
    },
    phylactery: {
      enable: bool,
      host: string,
      library: path,
      package: Derivation,
      port: { ... },
      ...
    },
    physlock: {
      allowAnyUser: bool,
      disableSysRq: bool,
      enable: bool,
      lockMessage: string,
      lockOn: {
        extraTargets: { ... },
        hibernate: bool,
        suspend: bool,
        ...
      },
      muteKernelMessages: bool,
      ...
    },
    picom: {
      activeOpacity: { ... },
      backend: string,
      enable: bool,
      experimentalBackends: { ... },
      fade: bool,
      fadeDelta: { ... },
      fadeExclude: [string],
      fadeSteps: [{ ... }],
      inactiveOpacity: { ... },
      menuOpacity: { ... },
      opacityRules: [string],
      package: Derivation,
      refreshRate: { ... },
      settings: { _: { ... } },
      shadow: bool,
      shadowExclude: [string],
      shadowOffsets: [int],
      shadowOpacity: { ... },
      vSync: bool | string,
      wintypes: { ... },
      ...
    },
    picosnitch: {
      enable: bool,
      ...
    },
    pict-rs: {
      address: string,
      dataDir: path,
      enable: bool,
      package: Derivation,
      port: { ... },
      repoPath: path | null,
      storePath: path | null,
      ...
    },
    pid-fan-controller: {
      enable: bool,
      package: Derivation,
      settings: {
        fans: { ... },
        heatSources: { ... },
        interval: int,
        ...
      },
      ...
    },
    pihole-ftl: {
      configDirectory: path,
      enable: bool,
      group: string,
      lists: [{ ... }],
      logDirectory: path,
      macvendorURL: string,
      openFirewallDHCP: bool,
      openFirewallDNS: bool,
      openFirewallWebserver: bool,
      package: Derivation,
      pihole: Derivation,
      piholePackage: Derivation,
      privacyLevel: { ... },
      queryLogDeleter: {
        age: int,
        enable: bool,
        interval: string,
        ...
      },
      settings: { ... },
      stateDirectory: path,
      useDnsmasqConfig: bool,
      user: string,
      webserverEnabled: bool,
      ...
    },
    pihole-web: {
      enable: bool,
      hostName: string,
      package: Derivation,
      ports: [{ ... }],
      ...
    },
    pinchflat: {
      enable: bool,
      extraConfig: { _: { ... } },
      group: string,
      logLevel: string,
      mediaDir: path,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      secretsFile: path | null,
      selfhosted: bool,
      user: string,
      ...
    },
    pingvin-share: { ... },
    pinnwand: {
      enable: bool,
      port: { ... },
      settings: { ... },
      ...
    },
    pipewire: {
      alsa: {
        enable: bool,
        support32Bit: bool,
        ...
      },
      audio: {
        enable: bool,
        ...
      },
      config: { ... },
      configPackages: [Derivation],
      enable: bool,
      extraConfig: {
        client: { ... },
        client-rt: { ... },
        jack: { ... },
        pipewire: { ... },
        pipewire-pulse: { ... },
        ...
      },
      extraLv2Packages: [Derivation],
      jack: {
        enable: bool,
        ...
      },
      media-session: { ... },
      package: Derivation,
      pulse: {
        enable: bool,
        ...
      },
      raopOpenFirewall: bool,
      socketActivation: bool,
      systemWide: bool,
      wireplumber: {
        configPackages: { ... },
        enable: bool,
        extraConfig: { ... },
        extraLv2Packages: { ... },
        extraScripts: { ... },
        package: Derivation,
        ...
      },
      ...
    },
    piwik: {
      enable: bool,
      nginx: { ... } | null,
      phpfpmProcessManagerConfig: { ... },
      webServerUser: string | null,
      ...
    },
    pixelfed: {
      dataDir: string,
      database: {
        automaticMigrations: bool,
        createLocally: bool,
        name: string,
        type: string,
        ...
      },
      domain: string,
      enable: bool,
      group: string,
      maxUploadSize: string,
      nginx: { ... },
      package: Derivation,
      phpPackage: Derivation,
      poolConfig: { _: { ... } },
      redis: {
        createLocally: bool,
        ...
      },
      runtimeDir: string,
      schedulerInterval: string,
      secretFile: path,
      settings: { _: { ... } },
      user: string,
      ...
    },
    pixiecore: {
      apiServer: string,
      cmdLine: string,
      debug: bool,
      dhcpNoBind: bool,
      enable: bool,
      extraArguments: [string],
      initrd: string,
      kernel: string,
      listen: string,
      mode: string,
      openFirewall: bool,
      port: { ... },
      quick: string,
      statusPort: { ... },
      ...
    },
    plantuml-server: {
      allowPlantumlInclude: { ... },
      enable: bool,
      graphvizPackage: Derivation,
      group: string,
      home: path,
      httpAuthorization: string | null,
      listenHost: string,
      listenPort: { ... },
      package: Derivation,
      packages: {
        jdk: Derivation,
        jetty: Derivation,
        ...
      },
      plantumlLimitSize: int,
      plantumlStats: bool,
      user: string,
      ...
    },
    plausible: {
      adminUser: {
        activate: { ... },
        email: { ... },
        name: { ... },
        passwordFile: { ... },
        ...
      },
      database: {
        clickhouse: { ... },
        postgres: { ... },
        ...
      },
      enable: bool,
      mail: {
        email: string,
        smtp: { ... },
        ...
      },
      package: Derivation,
      releaseCookiePath: { ... },
      server: {
        baseUrl: string,
        disableRegistration: string,
        listenAddress: string,
        port: { ... },
        secretKeybaseFile: { ... },
        ...
      },
      ...
    },
    playerctld: {
      enable: bool,
      package: Derivation,
      ...
    },
    pleroma: {
      configs: [string],
      enable: bool,
      group: string,
      package: Derivation,
      secretConfigFile: string,
      stateDir: string,
      user: string,
      ...
    },
    plex: {
      accelerationDevices: [string],
      dataDir: string,
      enable: bool,
      extraPlugins: [path],
      extraScanners: [path],
      group: string,
      managePlugins: { ... },
      openFirewall: bool,
      package: Derivation,
      user: string,
      ...
    },
    plexpy: { ... },
    plikd: {
      enable: bool,
      openFirewall: bool,
      settings: { ... },
      ...
    },
    pocket-id: {
      credentials: { _: path },
      dataDir: path,
      enable: bool,
      environmentFile: path,
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    podgrab: {
      dataDirectory: path,
      enable: bool,
      group: string,
      passwordFile: string | null,
      port: { ... },
      user: string,
      ...
    },
    polaris: {
      enable: bool,
      extraGroups: [string],
      group: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      user: string,
      ...
    },
    polipo: { ... },
    pomerium: {
      configFile: path | null,
      enable: bool,
      secretsFile: path | null,
      settings: { ... },
      useACMEHost: string | null,
      ...
    },
    porn-vault: {
      autoStart: bool,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    portunus: {
      dex: {
        enable: bool,
        oidcClients: { ... },
        port: { ... },
        ...
      },
      domain: string,
      enable: bool,
      group: string,
      ldap: {
        group: string,
        package: Derivation,
        searchUserName: string,
        suffix: string,
        tls: bool,
        user: string,
        ...
      },
      package: Derivation,
      port: { ... },
      seedPath: path | null,
      seedSettings: { _: { ... } },
      stateDir: path,
      user: string,
      ...
    },
    postfix: {
      aliasFiles: { _: path },
      aliasMapType: string,
      canonical: string,
      config: { ... },
      destination: { ... },
      dnsBlacklistOverrides: string,
      dnsBlacklists: [string],
      domain: { ... },
      enable: bool,
      enableHeaderChecks: bool,
      enableSmtp: bool,
      enableSubmission: bool,
      enableSubmissions: bool,
      extraAliases: string,
      extraConfig: { ... },
      extraHeaderChecks: string,
      extraMasterConf: string,
      group: string,
      headerChecks: [{ ... }],
      hostname: { ... },
      localRecipients: [string] | null,
      lookupMX: { ... },
      mapFiles: { _: path },
      masterConfig: { _: { ... } },
      networks: { ... },
      networksStyle: { ... },
      origin: { ... },
      package: Derivation,
      postmasterAlias: string,
      recipientDelimiter: { ... },
      relayDomains: { ... },
      relayHost: { ... },
      relayPort: { ... },
      rootAlias: string,
      setSendmail: bool,
      setgidGroup: string,
      settings: {
        main: { ... },
        master: { ... },
        ...
      },
      sslCACert: { ... },
      sslCert: { ... },
      sslKey: { ... },
      submissionOptions: { _: string },
      submissionsOptions: { _: string },
      tlsTrustedAuthorities: { ... },
      transport: string,
      useDane: { ... },
      useSrs: bool,
      user: string,
      virtual: string,
      virtualMapType: string,
      ...
    },
    postfix-tlspol: {
      configurePostfix: bool,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    postfixadmin: { ... },
    postgres-websockets: {
      enable: bool,
      environment: { ... },
      jwtSecretFile: path | null,
      pgpassFile: path | null,
      ...
    },
    postgresql: {
      authentication: string,
      checkConfig: bool,
      dataDir: path,
      enable: bool,
      enableJIT: bool,
      enableTCPIP: bool,
      ensureDatabases: [string],
      ensureUsers: [{ ... }],
      extensions: { ... } -> { ... },
      extraConfig: { ... },
      extraPlugins: { ... } -> { ... },
      finalPackage: Derivation,
      identMap: string,
      initdbArgs: [string],
      initialScript: path | null,
      logLinePrefix: { ... },
      package: Derivation,
      port: { ... },
      recoveryConfig: { ... },
      settings: { ... },
      superUser: string,
      systemCallFilter: { _: { ... } },
      ...
    },
    postgresqlBackup: {
      backupAll: bool,
      compression: string,
      compressionLevel: { ... },
      databases: [string],
      enable: bool,
      location: path,
      period: { ... },
      pgdumpAllOptions: string,
      pgdumpOptions: string,
      startAt: { ... } | string,
      ...
    },
    postgresqlWalReceiver: {
      receivers: { _: { ... } },
      ...
    },
    postgrest: {
      enable: bool,
      jwtSecretFile: path | null,
      pgpassFile: path | null,
      settings: { ... },
      ...
    },
    postgrey: {
      IPv4CIDR: { ... },
      IPv6CIDR: { ... },
      autoWhitelist: { ... } | null,
      delay: { ... },
      enable: bool,
      greylistAction: string,
      greylistHeader: string,
      greylistText: string,
      inetAddr: { ... },
      inetPort: { ... },
      lookupBySubnet: bool,
      maxAge: { ... },
      privacy: bool,
      retryWindow: string | { ... },
      socket: { ... },
      whitelistClients: [path],
      whitelistRecipients: [path],
      ...
    },
    postsrsd: {
      configurePostfix: bool,
      domain: { ... },
      domains: { ... },
      enable: bool,
      excludeDomains: { ... },
      forwardPort: { ... },
      group: string,
      package: Derivation,
      reversePort: { ... },
      secretsFile: path,
      separator: { ... },
      settings: { ... },
      socketPath: { ... },
      timeout: { ... },
      user: string,
      ...
    },
    power-profiles-daemon: {
      enable: bool,
      package: Derivation,
      ...
    },
    powerdns: {
      enable: bool,
      extraConfig: string,
      secretFile: path | null,
      ...
    },
    powerdns-admin: {
      config: string,
      enable: bool,
      extraArgs: [string],
      saltFile: path | null,
      secretKeyFile: path | null,
      ...
    },
    powerstation: {
      enable: bool,
      package: Derivation,
      ...
    },
    pppd: {
      enable: bool,
      package: Derivation,
      peers: { _: { ... } },
      ...
    },
    pptpd: {
      clientIpRange: string,
      enable: bool,
      extraPppdOptions: string,
      extraPptpdOptions: string,
      maxClients: int,
      serverIp: string,
      ...
    },
    prayer: { ... },
    prefect: {
      baseUrl: string | null,
      dataDir: path,
      database: string,
      databaseHost: string,
      databaseName: string,
      databasePasswordFile: string | null,
      databasePort: string,
      databaseUser: string,
      enable: bool,
      host: string,
      package: Derivation,
      port: { ... },
      workerPools: { _: { ... } },
      ...
    },
    preload: { ... },
    pretalx: {
      celery: {
        enable: bool,
        extraArgs: { ... },
        ...
      },
      database: {
        createLocally: bool,
        ...
      },
      enable: bool,
      environmentFiles: [path],
      finalPackage: Derivation,
      group: string,
      gunicorn: {
        extraArgs: { ... },
        ...
      },
      nginx: {
        domain: string,
        enable: bool,
        ...
      },
      package: Derivation,
      plugins: [Derivation],
      settings: { ... },
      user: string,
      ...
    },
    pretix: {
      celery: {
        extraArgs: { ... },
        ...
      },
      database: {
        createLocally: bool,
        ...
      },
      enable: bool,
      environmentFile: path | null,
      group: string,
      gunicorn: {
        extraArgs: { ... },
        ...
      },
      nginx: {
        domain: string,
        enable: bool,
        ...
      },
      package: Derivation,
      plugins: [Derivation],
      settings: { ... },
      user: string,
      ...
    },
    prey: { ... },
    printing: {
      allowFrom: [string],
      bindirCmds: string,
      browsed: {
        enable: bool,
        package: Derivation,
        ...
      },
      browsedConf: string,
      browsing: bool,
      clientConf: string,
      cups-pdf: {
        enable: bool,
        instances: { ... },
        ...
      },
      cupsFilesConf: { ... },
      cupsdConf: { ... },
      defaultShared: bool,
      drivers: [path],
      enable: bool,
      extraConf: string,
      extraFilesConf: string,
      gutenprint: { ... },
      listenAddresses: [string],
      logLevel: string,
      openFirewall: bool,
      package: Derivation,
      snmpConf: string,
      startWhenNeeded: bool,
      stateless: bool,
      tempDir: path,
      webInterface: bool,
      ...
    },
    private-gpt: { ... },
    privatebin: {
      dataDir: path,
      enable: bool,
      enableNginx: bool,
      group: string,
      package: Derivation,
      poolConfig: { _: { ... } },
      settings: { ... },
      user: string,
      virtualHost: string,
      ...
    },
    privoxy: {
      actionsFiles: { ... },
      certsLifetime: string,
      enable: bool,
      enableEditActions: { ... },
      enableTor: bool,
      extraConfig: { ... },
      filterFiles: { ... },
      inspectHttps: bool,
      listenAddress: { ... },
      settings: { ... },
      userActions: string,
      userFilters: string,
      ...
    },
    prometheus: {
      alertmanager: {
        checkConfig: bool,
        clusterPeers: { ... },
        configText: { ... },
        configuration: { ... },
        enable: bool,
        environmentFile: { ... },
        extraFlags: { ... },
        group: { ... },
        listenAddress: string,
        logFormat: { ... },
        logLevel: string,
        openFirewall: bool,
        package: Derivation,
        port: { ... },
        user: { ... },
        webExternalUrl: { ... },
        ...
      },
      alertmanager-ntfy: {
        enable: bool,
        extraConfigFiles: { ... },
        package: Derivation,
        settings: { ... },
        ...
      },
      alertmanagerGotify: {
        bindAddress: string,
        debug: bool,
        defaultPriority: int,
        dispatchErrors: bool,
        enable: bool,
        environmentFile: { ... },
        extendedDetails: bool,
        gotifyEndpoint: { ... },
        messageAnnotation: string,
        metrics: { ... },
        openFirewall: bool,
        package: Derivation,
        port: { ... },
        priorityAnnotation: string,
        timeout: { ... },
        titleAnnotation: string,
        webhookPath: string,
        ...
      },
      alertmanagerIrcRelay: {
        enable: bool,
        extraFlags: { ... },
        package: Derivation,
        settings: { ... },
        ...
      },
      alertmanagerNotificationQueueCapacity: int,
      alertmanagerTimeout: { ... },
      alertmanagerURL: { ... },
      alertmanagerWebhookLogger: {
        enable: bool,
        extraFlags: { ... },
        package: Derivation,
        ...
      },
      alertmanagers: [{ ... }],
      checkConfig: bool | string,
      configText: string | null,
      enable: bool,
      enableAgentMode: bool,
      enableReload: bool,
      environmentFile: { ... },
      exporters: { ... },
      extraFlags: [string],
      globalConfig: { ... },
      listenAddress: string,
      package: Derivation,
      port: { ... },
      pushgateway: {
        enable: bool,
        extraFlags: { ... },
        log: { ... },
        package: Derivation,
        persistMetrics: bool,
        persistence: { ... },
        stateDir: string,
        web: { ... },
        ...
      },
      remoteRead: [{ ... }],
      remoteWrite: [{ ... }],
      retentionTime: string | null,
      ruleFiles: [path],
      rules: [string],
      sachet: {
        address: string,
        configuration: { ... },
        enable: bool,
        port: { ... },
        ...
      },
      scrapeConfigs: [{ ... }],
      stateDir: string,
      webConfigFile: path | null,
      webExternalUrl: string | null,
      xmpp-alerts: {
        configuration: { ... },
        enable: bool,
        settings: { ... },
        ...
      },
      ...
    },
    prometheus2: { ... },
    promtail: {
      configFile: path | null,
      configuration: { ... } | null,
      enable: bool,
      extraFlags: [string],
      ...
    },
    prosody: {
      admins: [string],
      allowRegistration: bool,
      authentication: string,
      c2sRequireEncryption: bool,
      checkConfig: bool,
      dataDir: path,
      disco_items: [{ ... }],
      enable: bool,
      extraConfig: string,
      extraModules: [string],
      extraPluginPaths: [path],
      group: string,
      httpFileShare: { ... },
      httpInterfaces: [string],
      httpPorts: [{ ... }],
      httpsInterfaces: [string],
      httpsPorts: [{ ... }],
      log: string,
      modules: {
        admin_adhoc: bool,
        admin_telnet: bool,
        announce: bool,
        blocklist: bool,
        bookmarks: bool,
        bosh: bool,
        carbons: bool,
        cloud_notify: bool,
        csi: bool,
        dialback: bool,
        disco: bool,
        groups: bool,
        http_files: bool,
        legacyauth: bool,
        limits: bool,
        mam: bool,
        motd: bool,
        pep: bool,
        ping: bool,
        private: bool,
        proxy65: bool,
        register: bool,
        roster: bool,
        saslauth: bool,
        server_contact_info: bool,
        smacks: bool,
        time: bool,
        tls: bool,
        uptime: bool,
        vcard: bool,
        vcard_legacy: bool,
        version: bool,
        watchregistrations: bool,
        websocket: bool,
        welcome: bool,
        ...
      },
      muc: [{ ... }],
      package: Derivation,
      s2sInsecureDomains: [string],
      s2sRequireEncryption: bool,
      s2sSecureAuth: bool,
      s2sSecureDomains: [string],
      ssl: { ... },
      uploadHttp: { ... },
      user: string,
      virtualHosts: { _: { ... } },
      xmppComplianceSuite: bool,
      ...
    },
    prosody-filer: {
      enable: bool,
      settings: { ... },
      ...
    },
    protonmail-bridge: {
      enable: bool,
      logLevel: string | null,
      package: Derivation,
      path: [path],
      ...
    },
    prowlarr: {
      dataDir: string,
      enable: bool,
      environmentFiles: [path],
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    psd: {
      enable: bool,
      resyncTimer: string,
      ...
    },
    public-inbox: {
      enable: bool,
      http: {
        args: { ... },
        enable: bool,
        mounts: { ... },
        port: { ... },
        ...
      },
      imap: {
        args: { ... },
        cert: { ... },
        enable: bool,
        key: { ... },
        port: { ... },
        ...
      },
      inboxes: { _: { ... } },
      mda: {
        args: { ... },
        enable: bool,
        ...
      },
      nntp: {
        args: { ... },
        cert: { ... },
        enable: bool,
        key: { ... },
        port: { ... },
        ...
      },
      openFirewall: bool,
      package: Derivation,
      path: [Derivation],
      postfix: {
        enable: bool,
        ...
      },
      settings: { ... },
      spamAssassinRules: path | null,
      ...
    },
    pufferpanel: {
      enable: bool,
      environment: { _: string },
      environmentFile: path | null,
      extraGroups: [string],
      extraPackages: [Derivation],
      package: Derivation,
      ...
    },
    pulseaudio: {
      configFile: path | null,
      daemon: {
        config: { ... },
        logLevel: string,
        ...
      },
      enable: bool,
      extraClientConf: string,
      extraConfig: string,
      extraModules: [Derivation],
      package: Derivation,
      support32Bit: bool,
      systemWide: bool,
      tcp: {
        anonymousClients: { ... },
        enable: bool,
        openFirewall: bool,
        port: { ... },
        ...
      },
      zeroconf: {
        discovery: { ... },
        publish: { ... },
        ...
      },
      ...
    },
    pykms: {
      enable: bool,
      extraArgs: [string],
      listenAddress: string,
      logLevel: string,
      memoryLimit: string,
      openFirewallPort: bool,
      package: Derivation,
      port: { ... },
      verbose: { ... },
      ...
    },
    pyload: {
      credentialsFile: path | null,
      downloadDirectory: path,
      enable: bool,
      group: string,
      listenAddress: string,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    pyroscope: {
      configFile: path | null,
      enable: bool,
      extraFlags: [string],
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    qbittorrent: {
      enable: bool,
      extraArgs: [string],
      group: string,
      openFirewall: bool,
      package: Derivation,
      profileDir: path,
      serverConfig: { ... },
      torrentingPort: { ... } | null,
      user: string,
      webuiPort: { ... } | null,
      ...
    },
    qdrant: {
      enable: bool,
      package: Derivation,
      settings: { ... } | null,
      webUIPackage: Derivation,
      ...
    },
    qemuGuest: {
      enable: bool,
      package: Derivation,
      ...
    },
    quagga: { ... },
    quake3-server: {
      baseq3: Derivation | path,
      enable: bool,
      extraConfig: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    quassel: {
      certificateFile: string | null,
      dataDir: string,
      enable: bool,
      interfaces: [string],
      package: Derivation,
      portNumber: { ... },
      requireSSL: bool,
      user: string | null,
      ...
    },
    qui: {
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      secretFile: path,
      settings: { ... },
      user: string,
      ...
    },
    quicktun: { _: { ... } },
    quickwit: {
      dataDir: path,
      enable: bool,
      extraFlags: [string],
      group: string,
      package: Derivation,
      restartIfChanged: bool,
      settings: { ... },
      user: string,
      ...
    },
    quorum: { ... },
    r53-ddns: {
      domain: string,
      enable: bool,
      environmentFile: string,
      hostname: string,
      interval: string,
      ttl: int,
      zoneID: string,
      ...
    },
    rabbitmq: {
      config: string,
      configItems: { _: string },
      cookie: { ... },
      dataDir: path,
      enable: bool,
      listenAddress: string,
      managementPlugin: {
        enable: bool,
        port: { ... },
        ...
      },
      package: Derivation,
      pluginDirs: [path],
      plugins: [string],
      port: { ... },
      unsafeCookie: string,
      ...
    },
    racoon: { ... },
    radarr: {
      dataDir: string,
      enable: bool,
      environmentFiles: [path],
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    radicale: {
      config: string,
      enable: bool,
      extraArgs: [string],
      package: Derivation | null,
      rights: { _: { ... } },
      settings: { _: { ... } },
      ...
    },
    radicle: {
      checkConfig: bool,
      ci: {
        adapters: { ... },
        broker: { ... },
        ...
      },
      configFile: Derivation,
      enable: bool,
      httpd: {
        aliases: { ... },
        enable: bool,
        extraArgs: { ... },
        listenAddress: string,
        listenPort: { ... },
        nginx: { ... },
        package: Derivation,
        ...
      },
      node: {
        extraArgs: { ... },
        listenAddress: string,
        listenPort: { ... },
        openFirewall: bool,
        ...
      },
      package: Derivation,
      privateKeyFile: path | string,
      publicKey: path | string,
      settings: { ... },
      ...
    },
    radvd: {
      config: string,
      debugLevel: { ... },
      enable: bool,
      package: Derivation,
      ...
    },
    railcar: { ... },
    ratbagd: {
      enable: bool,
      package: Derivation,
      ...
    },
    rathole: {
      credentialsFile: path,
      enable: bool,
      package: Derivation,
      role: string,
      settings: { ... },
      ...
    },
    rauc: {
      bootloader: string,
      bundleFormats: [string],
      client: {
        enable: bool,
        ...
      },
      compatible: string,
      dataDir: path,
      enable: bool,
      mark-good: {
        enable: bool,
        ...
      },
      package: Derivation,
      settings: { _: { ... } },
      slots: { _: { ... } },
      ...
    },
    rdnssd: {
      enable: bool,
      ...
    },
    reaction: {
      checkConfig: bool,
      enable: bool,
      loglevel: string | null,
      package: Derivation,
      runAsRoot: bool,
      settings: { ... },
      settingsFiles: [path],
      stopForFirewall: bool,
      ...
    },
    readarr: {
      dataDir: string,
      enable: bool,
      environmentFiles: [path],
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    readeck: {
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      settings: { ... },
      ...
    },
    realm: {
      config: { ... },
      enable: bool,
      package: Derivation,
      ...
    },
    realmd: {
      enable: bool,
      package: Derivation,
      ...
    },
    rebuilderd: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    recyclarr: {
      command: string,
      configuration: { ... },
      enable: bool,
      group: string,
      package: Derivation,
      schedule: string,
      user: string,
      ...
    },
    redis: {
      appendFsync: { ... },
      appendOnly: { ... },
      appendOnlyFilename: { ... },
      bind: { ... },
      databases: { ... },
      dbFilename: { ... },
      dbpath: { ... },
      enable: { ... },
      extraConfig: { ... },
      logLevel: { ... },
      logfile: { ... },
      masterAuth: { ... },
      maxclients: { ... },
      openFirewall: { ... },
      package: Derivation,
      pidFile: { ... },
      port: { ... },
      requirePass: { ... },
      requirePassFile: { ... },
      save: { ... },
      servers: { _: { ... } },
      settings: { ... },
      slaveOf: { ... },
      slowLogLogSlowerThan: { ... },
      slowLogMaxLen: { ... },
      syslog: { ... },
      unixSocket: { ... },
      unixSocketPerm: { ... },
      user: { ... },
      vmOverCommit: bool,
      ...
    },
    redlib: {
      address: string,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { ... },
      ...
    },
    redmine: {
      address: string,
      components: {
        breezy: bool,
        cvs: bool,
        ghostscript: bool,
        git: bool,
        imagemagick: bool,
        mercurial: bool,
        minimagick_font_path: string,
        subversion: bool,
        ...
      },
      database: {
        createLocally: bool,
        host: string,
        name: string,
        password: { ... },
        passwordFile: { ... },
        port: { ... },
        socket: { ... },
        type: string,
        user: string,
        ...
      },
      enable: bool,
      extraConfig: { ... },
      extraEnv: string,
      group: string,
      package: Derivation,
      plugins: { _: path },
      port: { ... },
      settings: { ... },
      stateDir: path,
      themes: { _: path },
      user: string,
      ...
    },
    redshift: {
      brightness: {
        day: string,
        night: string,
        ...
      },
      enable: bool,
      executable: string,
      extraOptions: [string],
      latitude: { ... },
      longitude: { ... },
      package: Derivation,
      provider: string,
      temperature: {
        day: { ... },
        night: { ... },
        ...
      },
      ...
    },
    redsocks: {
      chroot: string | null,
      enable: bool,
      log: string,
      log_debug: bool,
      log_info: bool,
      redsocks: [{ ... }],
      ...
    },
    remark42: {
      dataDir: path,
      enable: bool,
      environmentFile: path | null,
      listenAddress: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      remarkUrl: string,
      settings: { _: string },
      sites: [string],
      ...
    },
    renovate: {
      credentials: { _: path },
      enable: bool,
      environment: { _: { ... } },
      package: Derivation,
      runtimePackages: [Derivation],
      schedule: string | null,
      settings: { ... },
      validateSettings: bool,
      ...
    },
    replay-sorcery: { ... },
    reposilite: {
      database: { ... },
      enable: bool,
      extraArgs: [string],
      group: string,
      keyPasswordFile: path | null,
      openFirewall: bool,
      package: Derivation,
      plugins: [Derivation],
      settings: { ... },
      useACMEHost: string | null,
      user: string,
      workingDirectory: path,
      ...
    },
    resilio: {
      apiKey: string,
      checkForUpdates: bool,
      deviceName: string,
      directoryRoot: string,
      downloadLimit: { ... },
      enable: bool,
      enableWebUI: bool,
      encryptLAN: bool,
      httpListenAddr: string,
      httpListenPort: { ... },
      httpLogin: string,
      httpPass: string,
      listeningPort: { ... },
      package: Derivation,
      sharedFolders: [{ ... }],
      storagePath: path,
      uploadLimit: { ... },
      useUpnp: bool,
      ...
    },
    resolved: {
      dnsDelegates: { _: { ... } },
      dnsovertls: { ... },
      dnssec: { ... },
      domains: { ... },
      enable: bool,
      extraConfig: { ... },
      fallbackDns: { ... },
      llmnr: { ... },
      settings: {
        Resolve: { ... },
        ...
      },
      ...
    },
    restic: {
      backups: { _: { ... } },
      server: {
        appendOnly: bool,
        dataDir: path,
        enable: bool,
        extraFlags: { ... },
        htpasswd-file: { ... },
        listenAddress: string,
        package: Derivation,
        privateRepos: bool,
        prometheus: bool,
        ...
      },
      ...
    },
    restya-board: { ... },
    rethinkdb: {
      dbpath: { ... },
      enable: bool,
      group: { ... },
      pidpath: { ... },
      user: { ... },
      ...
    },
    riak: { ... },
    riemann: {
      config: string,
      configFile: string,
      configFiles: [path],
      enable: bool,
      extraClasspathEntries: [string],
      extraJavaOpts: [string],
      ...
    },
    riemann-dash: {
      config: string,
      dataDir: string,
      enable: bool,
      ...
    },
    riemann-tools: {
      enableHealth: bool,
      extraArgs: [string],
      riemannHost: string,
      ...
    },
    rimgo: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    ringboard: {
      wayland: {
        enable: bool,
        package: Derivation,
        ...
      },
      x11: {
        enable: bool,
        package: Derivation,
        ...
      },
      ...
    },
    rippleDataApi: { ... },
    rippled: { ... },
    rke2: {
      agentToken: string,
      agentTokenFile: path | null,
      autoDeployCharts: { _: { ... } },
      charts: { _: { ... } },
      cisHardening: bool,
      cni: string | null,
      configPath: path | null,
      containerdConfigTemplate: string | null,
      disable: [string],
      enable: bool,
      environmentFile: path | null,
      extraFlags: string | { ... },
      extraKubeProxyConfig: { _: { ... } },
      extraKubeletConfig: { _: { ... } },
      gracefulNodeShutdown: {
        enable: bool,
        shutdownGracePeriod: string,
        shutdownGracePeriodCriticalPods: string,
        ...
      },
      images: [Derivation],
      manifests: { _: { ... } },
      nodeIP: string | null,
      nodeLabel: [string],
      nodeName: string | null,
      nodeTaint: [string],
      package: Derivation,
      role: string,
      selinux: bool,
      serverAddr: string,
      token: string,
      tokenFile: path | null,
      ...
    },
    rkvm: {
      client: {
        enable: bool,
        settings: { ... },
        ...
      },
      enable: bool,
      package: Derivation,
      server: {
        enable: bool,
        settings: { ... },
        ...
      },
      ...
    },
    rmfakecloud: {
      enable: bool,
      environmentFile: path | null,
      extraSettings: { _: string },
      logLevel: string,
      package: Derivation,
      port: { ... },
      storageUrl: string,
      ...
    },
    rmilter: { ... },
    robustirc-bridge: {
      enable: bool,
      extraFlags: [string],
      ...
    },
    roon-bridge: {
      enable: bool,
      group: string,
      openFirewall: bool,
      user: string,
      ...
    },
    roon-server: {
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      user: string,
      ...
    },
    rosenpass: {
      defaultDevice: string | null,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    roundcube: {
      configureNginx: bool,
      database: {
        dbname: string,
        host: string,
        password: string,
        passwordFile: path,
        username: string,
        ...
      },
      dicts: [Derivation],
      enable: bool,
      extraConfig: string,
      hostName: string,
      maxAttachmentSize: int,
      package: Derivation,
      plugins: [string],
      ...
    },
    routedns: {
      configFile: path,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    routinator: {
      enable: bool,
      extraArgs: [string],
      extraServerArgs: [string],
      package: Derivation,
      settings: { ... },
      ...
    },
    rpcbind: {
      enable: bool,
      ...
    },
    rqbit: {
      downloadDir: path,
      enable: bool,
      group: string,
      httpHost: string,
      httpPort: { ... },
      openFirewall: bool,
      package: Derivation,
      peerPort: { ... },
      user: string,
      ...
    },
    rshim: {
      backend: string | null,
      config: { _: { ... } },
      device: string | null,
      enable: bool,
      index: int | null,
      log-level: { ... },
      package: Derivation,
      ...
    },
    rsnapshot: {
      cronIntervals: { _: string },
      enable: bool,
      enableManualRsnapshot: bool,
      extraConfig: string,
      ...
    },
    rspamd: {
      bindSocket: { ... },
      bindUISocket: { ... },
      debug: bool,
      enable: bool,
      extraArgs: [string],
      extraConfig: string,
      group: string,
      localLuaRules: path | null,
      locals: { _: { ... } },
      overrides: { _: { ... } },
      package: Derivation,
      postfix: {
        config: { ... },
        enable: bool,
        ...
      },
      socketActivation: { ... },
      user: string,
      workers: { _: { ... } },
      ...
    },
    rspamd-trainer: {
      enable: bool,
      secrets: [path],
      settings: { ... },
      ...
    },
    rss-bridge: {
      config: { ... },
      dataDir: string,
      enable: bool,
      group: string,
      package: Derivation,
      pool: string | null,
      user: string,
      virtualHost: string | null,
      webserver: string | null,
      whitelist: { ... },
      ...
    },
    rss2email: {
      config: { _: { ... } },
      enable: bool,
      feeds: { _: { ... } },
      interval: string,
      to: string,
      ...
    },
    rstudio-server: {
      enable: bool,
      listenAddr: string,
      package: Derivation,
      rserverExtraConfig: string,
      rsessionExtraConfig: string,
      serverWorkingDir: string,
      ...
    },
    rsync: {
      enable: bool,
      jobs: { _: { ... } },
      package: Derivation,
      ...
    },
    rsyncd: {
      address: { ... },
      enable: bool,
      extraConfig: { ... },
      group: { ... },
      motd: { ... },
      port: { ... },
      settings: { ... },
      socketActivated: bool,
      user: { ... },
      ...
    },
    rsyslogd: {
      defaultConfig: string,
      enable: bool,
      extraConfig: string,
      extraParams: [string],
      ...
    },
    rtorrent: {
      configText: string,
      dataDir: string,
      dataPermissions: string,
      downloadDir: string,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      rpcSocket: string,
      user: string,
      ...
    },
    rtsp-simple-server: { ... },
    rumno: {
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      ...
    },
    rustdesk-server: {
      enable: bool,
      extraRelayArgs: [string],
      extraSignalArgs: [string],
      openFirewall: bool,
      package: Derivation,
      relay: {
        enable: bool,
        extraArgs: { ... },
        ...
      },
      relayIP: { ... },
      signal: {
        enable: bool,
        extraArgs: { ... },
        relayHosts: { ... },
        ...
      },
      ...
    },
    rustus: {
      cors: [string],
      disable_health_access_logs: bool,
      enable: bool,
      host: string,
      info_storage: { ... },
      log_level: string,
      max_body_size: string,
      port: { ... },
      remove_parts: bool,
      storage: { ... },
      tus_extensions: [string],
      url: string,
      ...
    },
    rutorrent: {
      dataDir: string,
      enable: bool,
      group: string,
      hostName: string,
      nginx: {
        enable: bool,
        exposeInsecureRPC2mount: bool,
        ...
      },
      plugins: [{ ... }],
      poolSettings: { _: { ... } },
      rpcSocket: string,
      user: string,
      ...
    },
    sabnzbd: {
      allowConfigWrite: bool,
      configFile: path | null,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      secretFiles: [path],
      settings: { ... },
      stateDir: string,
      user: string,
      ...
    },
    safeeyes: {
      enable: bool,
      ...
    },
    salt: {
      master: {
        configuration: { ... },
        enable: bool,
        ...
      },
      minion: {
        configuration: { ... },
        enable: bool,
        ...
      },
      ...
    },
    samba: {
      configText: { ... },
      defaultShare: { ... },
      enable: bool,
      enableNmbd: bool,
      enableWinbindd: bool,
      extraConfig: { ... },
      invalidUsers: { ... },
      nmbd: {
        enable: bool,
        extraArgs: { ... },
        ...
      },
      nsswins: bool,
      openFirewall: bool,
      package: Derivation,
      securityType: { ... },
      settings: { ... },
      shares: { ... },
      smbd: {
        enable: bool,
        extraArgs: { ... },
        ...
      },
      syncPasswordsByPam: { ... },
      usershares: {
        enable: bool,
        group: string,
        ...
      },
      winbindd: {
        enable: bool,
        extraArgs: { ... },
        ...
      },
      ...
    },
    samba-wsdd: {
      discovery: bool,
      domain: string | null,
      enable: bool,
      extraOptions: [string],
      hoplimit: int | null,
      hostname: string | null,
      interface: string | null,
      listen: string,
      openFirewall: bool,
      workgroup: string | null,
      ...
    },
    saned: {
      enable: bool,
      extraConfig: string,
      ...
    },
    sanoid: {
      datasets: { _: { ... } },
      enable: bool,
      extraArgs: [string],
      interval: string,
      package: Derivation,
      settings: { _: { ... } },
      templates: { _: { ... } },
      ...
    },
    saslauthd: {
      config: string,
      enable: bool,
      mechanism: string,
      package: Derivation,
      ...
    },
    saunafs: {
      chunkserver: {
        enable: bool,
        hdds: { ... },
        openFirewall: bool,
        settings: { ... },
        ...
      },
      client: {
        enable: bool,
        ...
      },
      master: {
        enable: bool,
        exports: { ... },
        openFirewall: bool,
        settings: { ... },
        ...
      },
      masterHost: string,
      metalogger: {
        enable: bool,
        settings: { ... },
        ...
      },
      sfsUser: string,
      ...
    },
    scanservjs: {
      enable: bool,
      extraActions: [string],
      extraConfig: string,
      extraDevicesConfig: string,
      runAfterScan: string,
      settings: { ... },
      stateDir: string,
      ...
    },
    schleuder: {
      enable: bool,
      enablePostfix: bool,
      extraSettingsFile: path | null,
      listDefaults: { ... },
      lists: [string],
      settings: { ... },
      ...
    },
    scion: {
      bypassBootstrapWarning: bool,
      enable: bool,
      package: Derivation,
      scion-control: {
        enable: bool,
        settings: { ... },
        ...
      },
      scion-daemon: {
        enable: bool,
        settings: { ... },
        ...
      },
      scion-dispatcher: {
        enable: bool,
        settings: { ... },
        ...
      },
      scion-ip-gateway: {
        config: { ... },
        enable: bool,
        trafficConfig: { ... },
        ...
      },
      scion-router: {
        enable: bool,
        settings: { ... },
        ...
      },
      stateless: bool,
      ...
    },
    scollector: {
      bosunHost: string,
      collectors: { _: { ... } },
      enable: bool,
      extraConfig: string,
      extraOpts: [string],
      group: string,
      package: Derivation,
      user: string,
      ...
    },
    screego: {
      enable: bool,
      environmentFile: path | null,
      openFirewall: bool,
      settings: { _: string },
      ...
    },
    scrutiny: {
      collector: {
        enable: bool,
        package: Derivation,
        schedule: string,
        settings: { ... },
        ...
      },
      enable: bool,
      influxdb: {
        enable: bool,
        ...
      },
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    scx: {
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      scheduler: string,
      ...
    },
    sdrplayApi: {
      enable: bool,
      ...
    },
    searx: {
      configFile: path,
      configureNginx: bool,
      configureUwsgi: bool,
      domain: string,
      enable: bool,
      environmentFile: path | null,
      faviconsSettings: { _: { ... } },
      limiterSettings: { _: { ... } },
      package: Derivation,
      redisCreateLocally: bool,
      runInUwsgi: bool,
      settings: { ... },
      settingsFile: path,
      uwsgiConfig: { ... },
      ...
    },
    seatd: {
      enable: bool,
      group: string,
      logLevel: string,
      user: string,
      ...
    },
    seeks: { ... },
    self-deploy: {
      branch: string,
      enable: bool,
      nixArgs: { ... },
      nixAttribute: string | null,
      nixFile: path,
      repository: path | string,
      sshKeyFile: path | null,
      startAt: string | { ... },
      switchCommand: string,
      ...
    },
    selfoss: {
      database: {
        host: string,
        name: string,
        password: { ... },
        port: { ... },
        type: string,
        user: string,
        ...
      },
      enable: bool,
      extraConfig: string,
      pool: string,
      user: string,
      ...
    },
    send: {
      baseUrl: string | null,
      dataDir: path,
      enable: bool,
      environment: { _: { ... } },
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      redis: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        ...
      },
      ...
    },
    serviio: {
      dataDir: path,
      enable: bool,
      openFirewall: bool,
      ...
    },
    sftpgo: {
      dataDir: path,
      enable: bool,
      extraArgs: [string],
      extraReadWriteDirs: [path],
      group: string,
      loadDataFile: path | null,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    shadowsocks: {
      enable: bool,
      encryptionMethod: string,
      extraConfig: { ... },
      fastOpen: bool,
      localAddress: string | { ... },
      mode: string,
      package: Derivation,
      password: string | null,
      passwordFile: path | null,
      plugin: string | null,
      pluginOpts: string,
      port: { ... },
      ...
    },
    shairport-sync: {
      arguments: string,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { _: { ... } },
      user: string,
      ...
    },
    sharkey: {
      enable: bool,
      environmentFiles: [path],
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      setupMeilisearch: bool,
      setupPostgresql: bool,
      setupRedis: bool,
      ...
    },
    shellhub-agent: {
      enable: bool,
      keepAliveInterval: int,
      package: Derivation,
      preferredHostname: string,
      privateKey: path,
      server: string,
      tenantId: string,
      ...
    },
    shellinabox: { ... },
    shibboleth-sp: {
      configFile: path,
      enable: bool,
      fastcgi: {
        enable: bool,
        shibAuthorizerPort: { ... },
        shibResponderPort: { ... },
        ...
      },
      ...
    },
    shiori: {
      address: string,
      databaseUrl: string | null,
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      port: { ... },
      webRoot: string,
      ...
    },
    shoko: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      plugins: [Derivation],
      webui: Derivation | null,
      ...
    },
    shorewall: {
      configs: { _: string },
      enable: bool,
      package: Derivation,
      ...
    },
    shorewall6: {
      configs: { _: string },
      enable: bool,
      package: Derivation,
      ...
    },
    shout: { ... },
    sickbeard: {
      configFile: path,
      dataDir: path,
      enable: bool,
      group: string,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    signald: { ... },
    sillytavern: {
      configFile: path,
      enable: bool,
      group: string,
      listen: bool | null,
      listenAddressIPv4: string | null,
      listenAddressIPv6: string | null,
      package: Derivation,
      port: { ... } | null,
      user: string,
      whitelist: bool | null,
      ...
    },
    silverbullet: {
      enable: bool,
      envFile: path | null,
      extraArgs: [string],
      group: string,
      listenAddress: string,
      listenPort: { ... },
      openFirewall: bool,
      package: Derivation,
      spaceDir: path,
      user: string,
      ...
    },
    simplesamlphp: { ... },
    sing-box: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    siproxd: { ... },
    sitespeed-io: {
      dataDir: string,
      enable: bool,
      package: Derivation,
      period: string,
      runs: [{ ... }],
      user: string,
      ...
    },
    sks: {
      dataDir: path,
      enable: bool,
      extraDbConfig: string,
      hkpAddress: [string],
      hkpPort: { ... },
      package: Derivation,
      webroot: path | null,
      ...
    },
    skydns: {
      address: string,
      domain: string,
      enable: bool,
      etcd: {
        caCert: { ... },
        machines: { ... },
        tlsKey: { ... },
        tlsPem: { ... },
        ...
      },
      extraConfig: { _: string },
      nameservers: [string],
      package: Derivation,
      ...
    },
    slimserver: {
      dataDir: path,
      enable: bool,
      package: Derivation,
      ...
    },
    slskd: {
      domain: string | null,
      enable: bool,
      environmentFile: path,
      group: string,
      nginx: { ... },
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    slurm: {
      client: {
        enable: bool,
        ...
      },
      clusterName: string,
      controlAddr: string | null,
      controlMachine: string | null,
      dbdserver: {
        configFile: { ... },
        dbdHost: string,
        enable: bool,
        extraConfig: string,
        storagePass: { ... },
        storagePassFile: { ... },
        storageUser: string,
        ...
      },
      enableSrunX11: bool,
      enableStools: bool,
      etcSlurm: path,
      extraCgroupConfig: string,
      extraConfig: string,
      extraConfigPaths: [path],
      extraPlugstackConfig: string,
      mpi: {
        PmixCliTmpDirBase: string,
        extraMpiConfig: string,
        ...
      },
      nodeName: [string],
      package: Derivation,
      partitionName: [string],
      procTrackType: string,
      rest: {
        enable: bool,
        environment: { ... },
        options: string,
        ...
      },
      server: {
        enable: bool,
        flags: { ... },
        ...
      },
      stateSaveLocation: string,
      user: string,
      ...
    },
    smartd: {
      autodetect: bool,
      defaults: {
        autodetected: string,
        monitored: string,
        ...
      },
      devices: [{ ... }],
      enable: bool,
      extraOptions: [string],
      notifications: {
        mail: { ... },
        systembus-notify: { ... },
        test: bool,
        wall: { ... },
        x11: { ... },
        ...
      },
      ...
    },
    smartdns: {
      bindPort: { ... },
      enable: bool,
      settings: { _: { ... } },
      ...
    },
    smokeping: {
      alertConfig: string,
      cgiUrl: string,
      config: string | null,
      databaseConfig: string,
      enable: bool,
      extraConfig: string,
      host: string | null,
      hostName: string,
      imgUrl: string,
      linkStyle: string,
      mailHost: string,
      owner: string,
      ownerEmail: string,
      package: Derivation,
      port: { ... },
      presentationConfig: string,
      presentationTemplate: string,
      probeConfig: string,
      sendmail: path | null,
      smokeMailTemplate: string,
      targetConfig: string,
      user: string,
      webService: bool,
      ...
    },
    snapper: {
      cleanupInterval: string,
      configs: { _: { ... } },
      filters: string | null,
      persistentTimer: bool,
      snapshotInterval: string,
      snapshotRootOnBoot: bool,
      ...
    },
    snapraid: {
      contentFiles: [string],
      dataDisks: { _: string },
      enable: bool,
      exclude: [string],
      extraConfig: string,
      parityFiles: [string],
      scrub: {
        interval: string,
        olderThan: int,
        plan: int,
        ...
      },
      sync: {
        interval: string,
        ...
      },
      touchBeforeSync: bool,
      ...
    },
    snapserver: {
      buffer: { ... },
      codec: { ... },
      controlPort: { ... },
      enable: bool,
      http: {
        docRoot: { ... },
        enable: { ... },
        listenAddress: { ... },
        port: { ... },
        ...
      },
      listenAddress: { ... },
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      sampleFormat: { ... },
      send: { ... },
      settings: { ... },
      streamBuffer: { ... },
      streams: { ... },
      tcp: {
        enable: { ... },
        listenAddress: { ... },
        port: { ... },
        ...
      },
      ...
    },
    snipe-it: {
      appKeyFile: path,
      appURL: string,
      config: { _: { ... } },
      dataDir: path,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        user: string,
        ...
      },
      enable: bool,
      group: string,
      hostName: string,
      mail: {
        backupNotificationAddress: string,
        driver: string,
        encryption: { ... },
        from: { ... },
        host: string,
        passwordFile: { ... },
        port: { ... },
        replyTo: { ... },
        user: { ... },
        ...
      },
      maxUploadSize: string,
      nginx: { ... },
      poolConfig: { _: { ... } },
      user: string,
      ...
    },
    sniproxy: {
      config: string,
      enable: bool,
      group: string,
      logDir: { ... },
      user: string,
      ...
    },
    snips-sh: {
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      settings: { ... },
      stateDir: path,
      ...
    },
    snmpd: {
      configFile: path,
      configText: string,
      enable: bool,
      listenAddress: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    snowflake-proxy: {
      broker: string | null,
      capacity: int | null,
      enable: bool,
      extraFlags: [string],
      relay: string | null,
      stun: string | null,
      ...
    },
    soft-serve: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    softether: {
      dataDir: path,
      enable: bool,
      package: Derivation,
      vpnbridge: {
        enable: bool,
        ...
      },
      vpnclient: {
        down: string,
        enable: bool,
        up: string,
        ...
      },
      vpnserver: {
        enable: bool,
        ...
      },
      ...
    },
    sogo: {
      configReplaces: { _: string },
      ealarmsCredFile: string | null,
      enable: bool,
      extraConfig: string,
      language: string,
      timezone: string,
      vhostName: string,
      ...
    },
    soju: {
      acceptProxyIP: [string],
      adminSocket: {
        enable: bool,
        ...
      },
      configFile: path,
      enable: bool,
      enableMessageLogging: bool,
      extraConfig: string,
      hostName: string,
      httpOrigins: [string],
      listen: [string],
      package: Derivation,
      tlsCertificate: path | null,
      tlsCertificateKey: path | null,
      ...
    },
    solanum: {
      config: string,
      enable: bool,
      motd: string | null,
      openFilesLimit: int,
      ...
    },
    sonarr: {
      dataDir: string,
      enable: bool,
      environmentFiles: [path],
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    sonic-server: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    sourcehut: { ... },
    spacecookie: {
      address: string,
      enable: bool,
      hostname: { ... },
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      root: { ... },
      settings: { ... },
      ...
    },
    spamassassin: {
      config: string,
      debug: bool,
      enable: bool,
      initPreConf: string | path,
      ...
    },
    spark: {
      confDir: path,
      logDir: path,
      master: {
        bind: string,
        enable: bool,
        extraEnvironment: { ... },
        restartIfChanged: bool,
        ...
      },
      package: Derivation,
      worker: {
        enable: bool,
        extraEnvironment: { ... },
        master: string,
        restartIfChanged: bool,
        workDir: path,
        ...
      },
      ...
    },
    speechd: {
      clients: { ... },
      config: string | null,
      enable: bool,
      modules: { ... },
      package: Derivation,
      ...
    },
    speedify: {
      enable: bool,
      package: Derivation,
      ...
    },
    speedtest-tracker: {
      dataDir: path,
      enable: bool,
      enableNginx: bool,
      group: string,
      package: Derivation,
      poolConfig: { _: { ... } },
      settings: { ... },
      user: string,
      virtualHost: string,
      ...
    },
    spice-autorandr: {
      enable: bool,
      package: Derivation,
      ...
    },
    spice-vdagentd: {
      enable: bool,
      ...
    },
    spice-webdavd: {
      enable: bool,
      package: Derivation,
      ...
    },
    spiped: {
      config: { _: { ... } },
      enable: bool,
      ...
    },
    spire: {
      agent: {
        configFile: path,
        enable: bool,
        expandEnv: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      server: {
        configFile: path,
        enable: bool,
        expandEnv: bool,
        openFirewall: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      ...
    },
    spoolman: {
      enable: bool,
      environment: { ... },
      listen: string,
      openFirewall: bool,
      port: { ... },
      ...
    },
    spotifyd: {
      config: string,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    squeezelite: {
      enable: bool,
      extraArguments: string,
      pulseAudio: bool,
      ...
    },
    squid: {
      configText: string | null,
      enable: bool,
      extraConfig: string,
      package: Derivation,
      proxyAddress: string | null,
      proxyPort: { ... },
      validateConfig: bool,
      ...
    },
    ssh-agent-switcher: {
      enable: bool,
      package: Derivation,
      ...
    },
    sshd: {
      enable: bool,
      ...
    },
    sshguard: {
      attack_threshold: int,
      blacklist_file: path,
      blacklist_threshold: int | null,
      blocktime: int,
      detection_time: int,
      enable: bool,
      services: [string],
      whitelist: [string],
      ...
    },
    sshwifty: {
      enable: bool,
      package: Derivation,
      settings: { ... } | null,
      sharedKeyFile: path | null,
      socks5PasswordFile: path | null,
      ...
    },
    sslh: {
      appendConfig: { ... },
      enable: bool,
      listenAddress: [string],
      listenAddresses: [string],
      method: string,
      port: { ... },
      settings: { ... },
      timeout: { ... },
      transparent: { ... },
      verbose: { ... },
      ...
    },
    sslmate-agent: {
      enable: bool,
      ...
    },
    ssm-agent: {
      enable: bool,
      package: Derivation,
      ...
    },
    ssmtp: { ... },
    sssd: {
      config: string,
      enable: bool,
      environmentFile: path | null,
      kcm: bool,
      settings: { _: { ... } },
      sshAuthorizedKeysIntegration: bool,
      subIDsIntegration: bool,
      ...
    },
    stalwart: {
      credentials: { _: string },
      dataDir: path,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    stalwart-mail: { ... },
    stargazer: {
      allowCgiUser: bool,
      certLifetime: string,
      certOrg: string,
      connectionLogging: bool,
      debugMode: bool,
      enable: bool,
      genCerts: bool,
      group: string,
      ipLog: bool,
      ipLogPartial: bool,
      listen: [string],
      regenCerts: bool,
      requestTimeout: { ... },
      responseTimeout: { ... },
      routes: [{ ... }],
      store: path,
      user: string,
      ...
    },
    stash: {
      dataDir: path,
      enable: bool,
      group: string,
      jwtSecretKeyFile: path,
      mutablePlugins: bool,
      mutableScrapers: bool,
      mutableSettings: bool,
      openFirewall: bool,
      package: Derivation,
      passwordFile: path | null,
      plugins: [Derivation],
      scrapers: [Derivation],
      sessionStoreKeyFile: path,
      settings: { ... },
      user: string,
      username: string | null,
      ...
    },
    static-web-server: {
      configuration: { ... },
      enable: bool,
      listen: string,
      root: path,
      ...
    },
    statsd: { ... },
    step-ca: {
      address: string,
      enable: bool,
      intermediatePasswordFile: path | null,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      settings: { _: { ... } },
      ...
    },
    stirling-pdf: {
      enable: bool,
      environment: { _: { ... } },
      environmentFiles: [path],
      package: Derivation,
      ...
    },
    stratis: {
      enable: bool,
      ...
    },
    strfry: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    strongswan: {
      ca: { _: { ... } },
      connections: { _: { ... } },
      enable: bool,
      enabledPlugins: [string],
      managePlugins: bool,
      secrets: [string],
      setup: { _: string },
      ...
    },
    strongswan-swanctl: {
      enable: bool,
      includes: [path],
      package: Derivation,
      strongswan: {
        extraConfig: string,
        ...
      },
      swanctl: {
        authorities: { ... },
        connections: { ... },
        pools: { ... },
        secrets: { ... },
        ...
      },
      ...
    },
    stubby: {
      authenticationMode: { ... },
      enable: bool,
      fallbackProtocols: { ... },
      idleTimeout: { ... },
      listenAddresses: { ... },
      logLevel: string | null,
      queryPaddingBlocksize: { ... },
      roundRobinUpstreams: { ... },
      settings: { _: { ... } },
      subnetPrivate: { ... },
      upstreamServers: { ... },
      ...
    },
    stunnel: {
      clients: { _: { ... } },
      enable: bool,
      enableInsecureSSLv3: bool,
      fipsMode: bool,
      group: string | null,
      logLevel: string,
      servers: { _: { ... } },
      user: string | null,
      ...
    },
    subsonic: {
      contextPath: path,
      defaultMusicFolder: path,
      defaultPlaylistFolder: path,
      defaultPodcastFolder: path,
      enable: bool,
      home: path,
      httpsPort: { ... },
      listenAddress: string,
      maxMemory: int,
      port: { ... },
      transcoders: [path],
      ...
    },
    sundtek: {
      enable: bool,
      ...
    },
    sunshine: {
      applications: { ... },
      autoStart: bool,
      capSysAdmin: bool,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    supergfxd: {
      enable: bool,
      settings: { ... } | null | null,
      ...
    },
    supybot: {
      configFile: path,
      enable: bool,
      extraPackages: { ... } -> { ... },
      plugins: { _: path },
      stateDir: path,
      ...
    },
    suricata: {
      configFile: path,
      disabledRules: [string],
      enable: bool,
      enabledSources: [string],
      package: Derivation,
      settings: { ... },
      ...
    },
    surrealdb: {
      dbPath: string,
      enable: bool,
      extraFlags: [string],
      host: string,
      package: Derivation,
      port: { ... },
      ...
    },
    suwayomi-server: {
      dataDir: path,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    svnserve: {
      enable: bool,
      svnBaseDir: string,
      ...
    },
    swapspace: {
      enable: bool,
      extraArgs: [string],
      installWrapper: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    switcherooControl: {
      enable: bool,
      package: Derivation,
      ...
    },
    sympa: {
      database: {
        createLocally: bool,
        host: { ... },
        name: string,
        passwordFile: { ... },
        port: { ... },
        type: string,
        user: { ... },
        ...
      },
      domains: { _: { ... } },
      enable: bool,
      lang: string,
      listMasters: [string],
      mainDomain: string | null,
      mta: {
        type: string,
        ...
      },
      settings: { _: { ... } },
      settingsFile: { _: { ... } },
      web: {
        enable: bool,
        fcgiProcs: { ... },
        https: bool,
        server: string,
        ...
      },
      ...
    },
    synapse-auto-compressor: {
      enable: bool,
      package: Derivation,
      postgresUrl: string,
      settings: {
        chunk_size: int,
        chunks_to_compress: int,
        levels: { ... },
        ...
      },
      startAt: string | { ... },
      ...
    },
    syncoid: {
      commands: { _: { ... } },
      commonArgs: [string],
      enable: bool,
      group: string,
      interval: string | { ... },
      localSourceAllow: [string],
      localTargetAllow: [string],
      package: Derivation,
      service: { ... },
      sshKey: string | null,
      user: string,
      ...
    },
    syncplay: {
      certDir: path | null,
      chat: bool,
      enable: bool,
      extraArgs: [string],
      group: { ... },
      interfaceIpv4: string,
      interfaceIpv6: string,
      ipv4Only: bool,
      ipv6Only: bool,
      isolateRooms: bool,
      maxChatMessageLength: { ... },
      maxUsernameLength: { ... },
      motd: string | null,
      motdFile: string | null,
      package: Derivation,
      passwordFile: path | null,
      permanentRooms: [string],
      permanentRoomsFile: string | null,
      port: { ... },
      ready: bool,
      roomsDBFile: string | null,
      salt: string | null,
      saltFile: path | null,
      statsDBFile: string | null,
      useACMEHost: string | null,
      user: { ... },
      ...
    },
    syncthing: {
      all_proxy: string | null,
      cert: string | null,
      configDir: path,
      dataDir: path,
      databaseDir: path,
      declarative: {
        cert: { ... },
        devices: { ... },
        extraOptions: { ... },
        folders: { ... },
        key: { ... },
        overrideDevices: bool,
        overrideFolders: bool,
        ...
      },
      devices: { ... },
      enable: bool,
      extraFlags: [string],
      extraOptions: { ... },
      folders: { ... },
      group: string,
      guiAddress: string,
      guiPasswordFile: string | null,
      key: string | null,
      openDefaultPorts: bool,
      options: { ... },
      overrideDevices: bool,
      overrideFolders: bool,
      package: Derivation,
      relay: {
        enable: bool,
        extraOptions: { ... },
        globalRateBps: { ... },
        listenAddress: string,
        perSessionRateBps: { ... },
        pools: { ... },
        port: { ... },
        providedBy: string,
        statusListenAddress: string,
        statusPort: { ... },
        ...
      },
      settings: { ... },
      systemService: bool,
      useInotify: { ... },
      user: string,
      ...
    },
    synergy: {
      client: {
        autoStart: bool,
        enable: bool,
        screenName: string,
        serverAddress: string,
        ...
      },
      server: {
        address: string,
        autoStart: bool,
        configFile: path,
        enable: bool,
        screenName: string,
        tls: { ... },
        ...
      },
      ...
    },
    syslog-ng: {
      configHeader: string,
      enable: bool,
      extraConfig: string,
      extraModulePaths: [string],
      listenToJournal: { ... },
      package: Derivation,
      serviceName: { ... },
      ...
    },
    syslogd: {
      defaultConfig: string,
      enable: bool,
      enableNetworkInput: bool,
      extraConfig: string,
      extraParams: [string],
      tty: string,
      ...
    },
    sysprof: {
      enable: bool,
      ...
    },
    sysstat: {
      collect-args: string,
      collect-frequency: string,
      enable: bool,
      ...
    },
    system-config-printer: {
      enable: bool,
      ...
    },
    system76-scheduler: {
      assignments: { _: { ... } },
      enable: bool,
      exceptions: [string],
      package: Derivation,
      settings: {
        cfsProfiles: { ... },
        processScheduler: { ... },
        ...
      },
      useStockConfig: bool,
      ...
    },
    systembus-notify: {
      enable: bool,
      ...
    },
    systemd-lock-handler: {
      enable: bool,
      package: Derivation,
      ...
    },
    szurubooru: {
      client: {
        package: Derivation,
        ...
      },
      dataDir: path,
      database: {
        host: string,
        name: string,
        passwordFile: path,
        port: { ... },
        user: string,
        ...
      },
      enable: bool,
      group: string,
      openFirewall: bool,
      server: {
        host: string,
        package: Derivation,
        port: { ... },
        settings: { ... },
        threads: int,
        ...
      },
      user: string,
      ...
    },
    tabby: {
      acceleration: string | null,
      enable: bool,
      host: string,
      indexInterval: { ... },
      model: string,
      package: Derivation,
      port: { ... },
      usageCollection: bool,
      ...
    },
    tabbyapi: {
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    tahoe: {
      introducers: { _: { ... } },
      nodes: { _: { ... } },
      ...
    },
    tailscale: {
      authKeyFile: path | null,
      authKeyParameters: { ... },
      derper: {
        configureNginx: bool,
        domain: string,
        enable: bool,
        openFirewall: bool,
        package: Derivation,
        port: { ... },
        stunPort: { ... },
        verifyClients: bool,
        ...
      },
      disableTaildrop: bool,
      disableUpstreamLogging: bool,
      enable: bool,
      extraDaemonFlags: [string],
      extraSetFlags: [string],
      extraUpFlags: [string],
      interfaceName: string,
      openFirewall: bool,
      package: Derivation,
      permitCertUid: string | null,
      port: { ... },
      serve: {
        configFile: { ... },
        enable: bool,
        services: { ... },
        ...
      },
      useRoutingFeatures: string,
      ...
    },
    tailscaleAuth: {
      enable: bool,
      group: string,
      package: Derivation,
      socketPath: path,
      user: string,
      ...
    },
    taler: {
      enable: bool,
      exchange: {
        debug: bool,
        denominationConfig: string,
        enable: bool,
        openFirewall: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      includes: [path],
      merchant: {
        debug: bool,
        enable: bool,
        openFirewall: bool,
        package: Derivation,
        settings: { ... },
        ...
      },
      runtimeDir: string,
      settings: { ... },
      ...
    },
    tandoor-recipes: {
      address: string,
      database: {
        createLocally: bool,
        ...
      },
      enable: bool,
      extraConfig: { ... },
      group: string,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    tang: {
      enable: bool,
      ipAddressAllow: [string],
      listenStream: [string],
      package: Derivation,
      ...
    },
    target: {
      config: { ... },
      enable: bool,
      ...
    },
    tarsnap: {
      archives: { _: { ... } },
      cachedir: { ... },
      enable: bool,
      keyfile: string,
      package: Derivation,
      ...
    },
    taskchampion-sync-server: {
      allowClientIds: [string],
      dataDir: path,
      dynamicUser: bool,
      enable: bool,
      group: string,
      host: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      snapshot: {
        days: { ... },
        versions: { ... },
        ...
      },
      user: string,
      ...
    },
    taskserver: {
      allowedClientIDs: string | { ... },
      ciphers: string | null,
      config: { ... },
      confirmation: bool,
      dataDir: path,
      debug: bool,
      disallowedClientIDs: string | { ... },
      enable: bool,
      extensions: path | null,
      extraConfig: { ... },
      fqdn: string,
      group: string,
      ipLog: bool,
      listenHost: string,
      listenPort: { ... },
      openFirewall: bool,
      organisations: { _: { ... } },
      pki: {
        auto: { ... },
        manual: { ... },
        ...
      },
      queueSize: int,
      requestLimit: int,
      trust: string,
      user: string,
      ...
    },
    tautulli: {
      configFile: string,
      dataDir: string,
      enable: bool,
      group: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      user: string,
      ...
    },
    tayga: {
      dataDir: path,
      enable: bool,
      ipv4: { ... },
      ipv6: { ... },
      log: [string],
      mappings: { _: string },
      package: Derivation,
      tunDevice: string,
      wkpfStrict: bool,
      ...
    },
    tcsd: {
      conformanceCred: path,
      enable: bool,
      endorsementCred: path,
      firmwarePCRs: string,
      group: string,
      kernelPCRs: string,
      platformCred: path,
      stateDir: path,
      user: string,
      ...
    },
    teamspeak3: {
      dataDir: path,
      defaultVoicePort: { ... },
      enable: bool,
      fileTransferIP: string | null,
      fileTransferPort: { ... },
      logPath: path,
      openFirewall: bool,
      openFirewallServerQuery: bool,
      queryHttpPort: { ... },
      queryIP: string | null,
      queryPort: { ... },
      querySshPort: { ... },
      voiceIP: string | null,
      ...
    },
    teamviewer: {
      enable: bool,
      package: Derivation,
      ...
    },
    technitium-dns-server: {
      enable: bool,
      firewallTCPPorts: [int],
      firewallUDPPorts: [int],
      openFirewall: bool,
      package: Derivation,
      ...
    },
    tedicross: { ... },
    tee-supplicant: {
      enable: bool,
      package: Derivation,
      pluginPath: path,
      reeFsParentPath: path,
      trustedApplications: [path],
      ...
    },
    teeworlds: {
      enable: bool,
      environmentFile: path | null,
      extraOptions: [string],
      game: {
        enablePowerups: bool,
        enableReadyMode: bool,
        enableTeamDamage: bool,
        enableVoteKick: bool,
        gameType: string,
        map: string,
        playerSlots: { ... },
        restrictSpectators: bool,
        scoreLimit: { ... },
        swapTeams: bool,
        timeLimit: { ... },
        tournamentMode: string,
        voteKickBanTime: { ... },
        voteKickMinimumPlayers: { ... },
        ...
      },
      motd: string | null,
      name: string,
      openPorts: bool,
      package: Derivation,
      password: string | null,
      port: { ... },
      rconPassword: string | null,
      register: bool,
      server: {
        bindAddr: { ... },
        enableHighBandwidth: bool,
        enableSpamProtection: bool,
        hostName: { ... },
        inactivePenalty: string,
        inactiveTime: { ... },
        kickInactiveSpectators: bool,
        maxClients: { ... },
        maxClientsPerIP: { ... },
        skillLevel: string,
        ...
      },
      ...
    },
    telegraf: {
      enable: bool,
      environmentFiles: [path],
      extraConfig: { ... },
      package: Derivation,
      ...
    },
    telepathy: {
      enable: bool,
      ...
    },
    teleport: {
      diag: {
        addr: string,
        enable: bool,
        port: { ... },
        ...
      },
      enable: bool,
      insecure: {
        enable: bool,
        ...
      },
      package: Derivation,
      settings: { ... },
      ...
    },
    tempo: {
      configFile: path | null,
      enable: bool,
      extraFlags: [string],
      settings: { ... },
      ...
    },
    temporal: {
      dataDir: path,
      enable: bool,
      group: string,
      package: Derivation,
      restartIfChanged: bool,
      settings: { ... },
      user: string,
      ...
    },
    terraria: {
      autoCreatedWorldSize: string,
      banListPath: path | null,
      dataDir: string,
      enable: bool,
      maxPlayers: { ... },
      messageOfTheDay: string | null,
      noUPnP: bool,
      openFirewall: bool,
      package: Derivation,
      password: string | null,
      port: { ... },
      secure: bool,
      worldPath: path | null,
      ...
    },
    tetrd: {
      enable: bool,
      ...
    },
    tftpd: {
      enable: bool,
      path: path,
      ...
    },
    thanos: {
      compact: {
        arguments: { ... },
        compact: { ... },
        consistency-delay: { ... },
        downsampling: { ... },
        enable: bool,
        http-address: { ... },
        log: { ... },
        objstore: { ... },
        retention: { ... },
        startAt: { ... },
        stateDir: string,
        tracing: { ... },
        ...
      },
      downsample: {
        arguments: { ... },
        enable: bool,
        log: { ... },
        objstore: { ... },
        stateDir: string,
        tracing: { ... },
        ...
      },
      package: Derivation,
      query: {
        arguments: { ... },
        enable: bool,
        endpoints: { ... },
        grpc-address: { ... },
        grpc-client-server-name: { ... },
        grpc-client-tls-ca: { ... },
        grpc-client-tls-cert: { ... },
        grpc-client-tls-key: { ... },
        grpc-client-tls-secure: bool,
        grpc-compression: { ... },
        grpc-server-tls-cert: { ... },
        grpc-server-tls-client-ca: { ... },
        grpc-server-tls-key: { ... },
        http-address: { ... },
        log: { ... },
        query: { ... },
        selector-labels: { ... },
        store: { ... },
        tracing: { ... },
        web: { ... },
        ...
      },
      query-frontend: {
        arguments: { ... },
        enable: bool,
        grpc-address: { ... },
        grpc-server-tls-cert: { ... },
        grpc-server-tls-client-ca: { ... },
        grpc-server-tls-key: { ... },
        http-address: { ... },
        log: { ... },
        query-frontend: { ... },
        tracing: { ... },
        ...
      },
      receive: {
        arguments: { ... },
        enable: bool,
        grpc-address: { ... },
        grpc-server-tls-cert: { ... },
        grpc-server-tls-client-ca: { ... },
        grpc-server-tls-key: { ... },
        http-address: { ... },
        labels: { ... },
        log: { ... },
        objstore: { ... },
        receive: { ... },
        remote-write: { ... },
        stateDir: string,
        tracing: { ... },
        tsdb: { ... },
        ...
      },
      rule: {
        alert: { ... },
        alertmanagers: { ... },
        arguments: { ... },
        enable: bool,
        eval-interval: { ... },
        grpc-address: { ... },
        grpc-server-tls-cert: { ... },
        grpc-server-tls-client-ca: { ... },
        grpc-server-tls-key: { ... },
        http-address: { ... },
        labels: { ... },
        log: { ... },
        objstore: { ... },
        query: { ... },
        rule-files: { ... },
        stateDir: string,
        tracing: { ... },
        tsdb: { ... },
        web: { ... },
        ...
      },
      sidecar: {
        arguments: { ... },
        enable: bool,
        grpc-address: { ... },
        grpc-server-tls-cert: { ... },
        grpc-server-tls-client-ca: { ... },
        grpc-server-tls-key: { ... },
        http-address: { ... },
        log: { ... },
        objstore: { ... },
        prometheus: { ... },
        reloader: { ... },
        tracing: { ... },
        tsdb: { ... },
        ...
      },
      store: {
        arguments: { ... },
        block-sync-concurrency: { ... },
        chunk-pool-size: { ... },
        enable: bool,
        grpc-address: { ... },
        grpc-server-tls-cert: { ... },
        grpc-server-tls-client-ca: { ... },
        grpc-server-tls-key: { ... },
        http-address: { ... },
        index-cache-size: { ... },
        log: { ... },
        max-time: { ... },
        min-time: { ... },
        objstore: { ... },
        stateDir: string,
        store: { ... },
        sync-block-duration: { ... },
        tracing: { ... },
        ...
      },
      ...
    },
    thelounge: {
      enable: bool,
      extraConfig: { ... },
      package: Derivation,
      plugins: [Derivation],
      port: { ... },
      private: { ... },
      public: bool,
      ...
    },
    thermald: {
      configFile: path | null,
      debug: bool,
      enable: bool,
      ignoreCpuidCheck: bool,
      package: Derivation,
      ...
    },
    thinkfan: {
      enable: bool,
      extraArgs: [string],
      fans: [{ ... }],
      levels: [{ ... }],
      sensors: [{ ... }],
      settings: { _: { ... } },
      smartSupport: bool,
      ...
    },
    throttled: {
      enable: bool,
      extraConfig: string,
      ...
    },
    tiddlywiki: {
      enable: bool,
      listenOptions: { ... },
      ...
    },
    tigerbeetle: {
      addresses: [string],
      cacheGridSize: { ... },
      clusterId: { ... },
      enable: bool,
      package: Derivation,
      replicaCount: { ... },
      replicaIndex: { ... },
      ...
    },
    tika: {
      configFile: path | null,
      enable: bool,
      enableOcr: bool,
      listenAddress: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    timekpr: {
      adminUsers: [string],
      enable: bool,
      package: Derivation,
      ...
    },
    timesyncd: {
      enable: bool,
      extraConfig: string,
      fallbackServers: [string] | null,
      servers: [string] | null,
      ...
    },
    tinc: {
      networks: { _: { ... } },
      ...
    },
    tinyauth: {
      dataDir: path,
      enable: bool,
      environmentFile: path,
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    tinydns: {
      data: string,
      enable: bool,
      ip: string,
      ...
    },
    tinyproxy: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    tlp: {
      enable: bool,
      extraConfig: string,
      package: Derivation,
      pd: {
        enable: bool,
        package: Derivation,
        ...
      },
      settings: { _: { ... } },
      ...
    },
    tlsrpt: {
      collectd: {
        extraFlags: { ... },
        settings: { ... },
        ...
      },
      configurePostfix: bool,
      enable: bool,
      fetcher: {
        settings: { ... },
        ...
      },
      package: Derivation,
      reportd: {
        extraFlags: { ... },
        settings: { ... },
        ...
      },
      ...
    },
    tmate-ssh-server: {
      advertisedPort: { ... },
      enable: bool,
      host: string,
      keysDir: string | null,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    todesk: {
      enable: bool,
      package: Derivation,
      ...
    },
    tomcat: {
      axis2: {
        enable: bool,
        services: { ... },
        ...
      },
      baseDir: path,
      catalinaOpts: { ... } | string,
      commonLibs: [string],
      enable: bool,
      extraConfigFiles: [path],
      extraEnvironment: [string],
      extraGroups: [string],
      group: string,
      javaOpts: { ... } | string,
      jdk: Derivation,
      logDirs: [path],
      logPerVirtualHost: bool,
      package: Derivation,
      port: { ... },
      purifyOnStart: bool,
      serverXml: string,
      sharedLibs: [string],
      user: string,
      virtualHosts: [{ ... }],
      webapps: [path],
      ...
    },
    tor: {
      client: {
        dns: { ... },
        enable: bool,
        onionServices: { ... },
        privoxy: { ... },
        socksIsolationOptions: { ... },
        socksListenAddress: { ... },
        socksListenAddressFaster: { ... },
        socksPolicy: { ... },
        transparentProxy: { ... },
        ...
      },
      controlPort: { ... },
      controlSocket: {
        enable: bool,
        ...
      },
      enable: bool,
      enableGeoIP: bool,
      extraConfig: { ... },
      hiddenServices: { _: { ... } },
      obfs4Package: Derivation,
      openFirewall: bool,
      package: Derivation,
      relay: {
        accountingMax: { ... },
        accountingStart: { ... },
        address: { ... },
        bandwidthBurst: { ... },
        bandwidthRate: { ... },
        bridgeTransports: { ... },
        contactInfo: { ... },
        enable: bool,
        exitPolicy: { ... },
        isBridge: { ... },
        isExit: { ... },
        nickname: { ... },
        onionServices: { ... },
        port: { ... },
        portSpec: { ... },
        role: string,
        ...
      },
      settings: { ... },
      torsocks: {
        allowInbound: bool,
        enable: bool,
        fasterServer: string,
        onionAddrRange: string,
        server: string,
        socks5Password: { ... },
        socks5Username: { ... },
        ...
      },
      tsocks: {
        config: string,
        enable: bool,
        server: string,
        ...
      },
      ...
    },
    torque: {
      mom: {
        enable: bool,
        serverNode: string,
        ...
      },
      server: {
        enable: bool,
        ...
      },
      ...
    },
    torrentstream: {
      address: string,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    touchegg: {
      enable: bool,
      package: Derivation,
      ...
    },
    tox-node: {
      enable: bool,
      keysFile: string,
      lanDiscovery: bool,
      logType: string,
      motd: string,
      tcpAddresses: [string],
      tcpConnectionLimit: int,
      threads: int,
      udpAddress: string,
      ...
    },
    toxBootstrapd: {
      enable: bool,
      extraConfig: string,
      keysFile: string,
      port: { ... },
      ...
    },
    toxvpn: {
      auto_add_peers: [string],
      enable: bool,
      localip: string,
      port: { ... },
      ...
    },
    tp-auto-kbbl: {
      arguments: [string],
      device: string,
      enable: bool,
      package: Derivation,
      ...
    },
    traccar: {
      enable: bool,
      environmentFile: path | null,
      settings: { ... },
      settingsFile: path | null,
      ...
    },
    traefik: {
      dataDir: path,
      dynamicConfigFile: path | null,
      dynamicConfigOptions: { ... },
      enable: bool,
      environmentFiles: [path],
      group: string,
      package: Derivation,
      staticConfigFile: path | null,
      staticConfigOptions: { ... },
      ...
    },
    trafficserver: {
      cache: string,
      enable: bool,
      hosting: string,
      ipAllow: { ... } | null | null,
      logging: { ... } | null | null,
      parent: string,
      plugins: [{ ... }],
      records: { _: { ... } },
      remap: string,
      sni: { ... } | null | null,
      splitDns: string,
      sslMulticert: string,
      storage: string,
      strategies: { ... } | null | null,
      volume: { ... } | null | null,
      ...
    },
    transfer-sh: {
      enable: bool,
      package: Derivation,
      provider: string,
      secretFile: path | null,
      settings: { ... },
      ...
    },
    transmission: {
      credentialsFile: path,
      downloadDirPermissions: string | null,
      enable: bool,
      extraFlags: [string],
      group: string,
      home: path,
      openFirewall: bool,
      openPeerPorts: bool,
      openRPCPort: bool,
      package: Derivation,
      performanceNetParameters: bool,
      port: { ... },
      settings: { ... },
      user: string,
      webHome: path | null,
      ...
    },
    tremor-rs: {
      enable: bool,
      host: string,
      loggerSettings: { ... },
      port: { ... },
      tremorLibDir: path,
      troyFileList: [path],
      ...
    },
    trezord: {
      emulator: {
        enable: bool,
        port: { ... },
        ...
      },
      enable: bool,
      ...
    },
    trickster: {
      configFile: path | null,
      enable: bool,
      instance-id: int | null,
      log-level: string,
      metrics-port: { ... },
      origin: string,
      origin-type: string,
      origin-url: string,
      package: Derivation,
      profiler-port: { ... } | null,
      proxy-port: { ... },
      ...
    },
    triggerhappy: {
      bindings: [{ ... }],
      enable: bool,
      extraConfig: string,
      user: string,
      ...
    },
    trilium-server: {
      dataDir: string,
      enable: bool,
      environmentFile: path | null,
      host: string,
      instanceName: string,
      nginx: { ... },
      noAuthentication: bool,
      noBackup: bool,
      package: Derivation,
      port: { ... },
      ...
    },
    trust-dns: {
      debug: bool,
      enable: bool,
      package: Derivation,
      quiet: bool,
      settings: { ... },
      ...
    },
    tsidp: {
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      settings: {
        debugAllRequests: bool,
        debugTsnet: bool,
        enableFunnel: bool,
        enableSts: bool,
        hostName: string,
        localPort: { ... },
        logLevel: string,
        port: { ... },
        useLocalTailscaled: bool,
        ...
      },
      ...
    },
    tsmBackup: {
      autoTime: string | null,
      command: string,
      enable: bool,
      servername: string,
      ...
    },
    tt-rss: {
      auth: {
        autoCreate: bool,
        autoLogin: bool,
        ...
      },
      checkForUpdates: { ... },
      database: {
        createLocally: bool,
        host: { ... },
        name: string,
        password: { ... },
        passwordFile: { ... },
        port: { ... },
        type: string,
        user: string,
        ...
      },
      email: {
        digestSubject: string,
        fromAddress: string,
        fromName: string,
        login: string,
        password: string,
        security: string,
        server: string,
        ...
      },
      enable: bool,
      enableGZipOutput: bool,
      extraConfig: string,
      feedCryptKey: string,
      forceArticlePurge: int,
      logDestination: string,
      phpPackage: Derivation,
      pluginPackages: [Derivation],
      plugins: [string],
      pool: string,
      pubSubHubbub: {
        enable: bool,
        hub: string,
        ...
      },
      registration: {
        enable: bool,
        maxUsers: int,
        notifyAddress: string,
        ...
      },
      root: path,
      selfUrlPath: string,
      sessionCookieLifetime: int,
      simpleUpdateMode: bool,
      singleUserMode: bool,
      sphinx: {
        index: { ... },
        server: string,
        ...
      },
      themePackages: [Derivation],
      updateDaemon: {
        commandFlags: string,
        ...
      },
      user: string,
      virtualHost: string | null,
      ...
    },
    tts: {
      servers: { _: { ... } },
      ...
    },
    ttyd: {
      caFile: path | null,
      certFile: path | null,
      checkOrigin: bool,
      clientOptions: { _: string },
      enable: bool,
      enableIPv6: bool,
      enableSSL: bool,
      entrypoint: [string],
      indexFile: path | null,
      interface: string | null,
      keyFile: path | null,
      logLevel: int,
      maxClients: int,
      passwordFile: path | null,
      port: { ... },
      signal: { ... },
      socket: path | null,
      terminalType: string,
      user: string,
      username: string | null,
      writeable: bool | null,
      ...
    },
    tuliprox: {
      apiProxySettings: { ... } | null,
      enable: bool,
      extraArgs: [string],
      mappingSettings: { ... } | null,
      package: Derivation,
      sourceSettings: { ... } | null,
      systemSettings: { ... } | null,
      ...
    },
    tumbler: {
      enable: bool,
      package: { ... },
      ...
    },
    tuned: {
      enable: bool,
      package: Derivation,
      ppdSettings: { ... },
      ppdSupport: bool,
      profiles: { _: { ... } },
      recommend: { _: { ... } },
      settings: { ... },
      ...
    },
    tuptime: {
      enable: bool,
      timer: {
        enable: bool,
        period: string,
        ...
      },
      ...
    },
    turborepo-remote-cache: {
      enable: bool,
      environment: { _: { ... } },
      environmentFile: path | null,
      openFirewall: bool,
      package: Derivation,
      ...
    },
    turn-rs: {
      enable: bool,
      package: Derivation,
      secretFile: path | null,
      settings: { ... },
      ...
    },
    tvheadend: { ... },
    twingate: {
      enable: bool,
      package: Derivation,
      ...
    },
    typesense: {
      apiKeyFile: path,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    tzupdate: {
      enable: bool,
      package: Derivation,
      timer: {
        enable: bool,
        interval: string,
        ...
      },
      ...
    },
    u9fs: {
      enable: bool,
      extraArgs: string,
      listenStreams: [string],
      user: string,
      ...
    },
    udev: {
      enable: bool,
      extraHwdb: string,
      extraRules: string,
      initrdRules: string,
      packages: [path],
      path: [path],
      ...
    },
    udisks2: {
      enable: bool,
      mountOnMedia: bool,
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    udp-over-tcp: {
      package: Derivation,
      tcp2udp: { _: { ... } },
      udp2tcp: { _: { ... } },
      ...
    },
    uhub: { _: { ... } },
    ulogd: {
      enable: bool,
      logLevel: string,
      settings: { _: { ... } },
      ...
    },
    umami: {
      createPostgresqlDatabase: bool,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    umurmur: {
      configFile: path,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    unbound: {
      allowedAccess: { ... },
      checkconf: bool,
      enable: bool,
      enableRootTrustAnchor: bool,
      extraConfig: { ... },
      forwardAddresses: { ... },
      group: string,
      interfaces: { ... },
      localControlSocketPath: string | null,
      package: Derivation,
      resolveLocalQueries: bool,
      settings: { ... },
      stateDir: path,
      user: string,
      ...
    },
    unclutter: {
      enable: bool,
      excluded: [string],
      extraOptions: [string],
      keystroke: bool,
      package: Derivation,
      threeshold: int,
      threshold: int,
      timeout: int,
      ...
    },
    unclutter-xfixes: {
      enable: bool,
      extraOptions: [string],
      package: Derivation,
      threshold: int,
      timeout: int,
      ...
    },
    undervolt: {
      analogioOffset: int | null,
      coreOffset: int | null,
      enable: bool,
      gpuOffset: int | null,
      p1: {
        limit: { ... },
        window: { ... },
        ...
      },
      p2: {
        limit: { ... },
        window: { ... },
        ...
      },
      package: Derivation,
      temp: int | null,
      tempAc: int | null,
      tempBat: int | null,
      turbo: int | null,
      uncoreOffset: int | null,
      useTimer: bool,
      verbose: bool,
      ...
    },
    unifi: {
      dataDir: { ... },
      enable: bool,
      extraJvmOptions: [string],
      initialJavaHeapSize: int | null,
      jrePackage: Derivation,
      maximumJavaHeapSize: int | null,
      mongodbPackage: Derivation,
      openFirewall: bool,
      openPorts: bool,
      unifiPackage: Derivation,
      ...
    },
    unifi-poller: { ... },
    unifi-video: { ... },
    unit: {
      config: string,
      enable: bool,
      group: string,
      logDir: path,
      package: Derivation,
      stateDir: path,
      user: string,
      ...
    },
    unpoller: {
      enable: bool,
      influxdb: {
        db: string,
        disable: bool,
        interval: string,
        pass: path,
        url: string,
        user: string,
        verify_ssl: bool,
        ...
      },
      loki: {
        interval: string,
        pass: path,
        tenant_id: string,
        timeout: string,
        url: string,
        user: string,
        verify_ssl: bool,
        ...
      },
      poller: {
        debug: bool,
        plugins: { ... },
        quiet: bool,
        ...
      },
      prometheus: {
        disable: bool,
        http_listen: string,
        report_errors: bool,
        ...
      },
      unifi: {
        controllers: { ... },
        defaults: { ... },
        dynamic: bool,
        ...
      },
      ...
    },
    upower: {
      allowRiskyCriticalPowerAction: bool,
      criticalPowerAction: string,
      enable: bool,
      enableWattsUpPro: bool,
      ignoreLid: bool,
      noPollBatteries: bool,
      package: Derivation,
      percentageAction: { ... },
      percentageCritical: { ... },
      percentageLow: { ... },
      timeAction: { ... },
      timeCritical: { ... },
      timeLow: { ... },
      usePercentageForPolicy: bool,
      ...
    },
    uptermd: {
      enable: bool,
      extraFlags: [string],
      hostKey: path | null,
      listenAddress: string,
      openFirewall: bool,
      port: { ... },
      ...
    },
    uptime: { ... },
    uptime-kuma: {
      appriseSupport: bool,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    uptimed: {
      enable: bool,
      ...
    },
    urserver: {
      enable: bool,
      ...
    },
    urxvtd: {
      enable: bool,
      package: Derivation,
      ...
    },
    usbguard: {
      IPCAccessControlFiles: { ... },
      IPCAllowedGroups: [string],
      IPCAllowedUsers: [string],
      auditFilePath: { ... },
      dbus: {
        enable: bool,
        ...
      },
      deviceRulesWithPort: bool,
      enable: bool,
      implicitPolicyTarget: string,
      implictPolicyTarget: string,
      insertedDevicePolicy: string,
      package: Derivation,
      presentControllerPolicy: string,
      presentDevicePolicy: string,
      restoreControllerDeviceState: bool,
      ruleFile: path | null,
      rules: string | null,
      ...
    },
    usbmuxd: {
      enable: bool,
      group: string,
      package: Derivation,
      user: string,
      ...
    },
    usbrelayd: {
      broker: string,
      clientName: string,
      enable: bool,
      ...
    },
    userborn: {
      enable: bool,
      package: Derivation,
      passwordFilesLocation: string,
      static: bool,
      ...
    },
    userdbd: {
      enable: bool,
      enableSSHSupport: bool,
      silenceHighSystemUsers: bool,
      ...
    },
    ustreamer: {
      autoStart: bool,
      device: path,
      enable: bool,
      extraArgs: [string],
      listenAddress: string,
      package: Derivation,
      ...
    },
    uvcvideo: {
      dynctrl: {
        enable: bool,
        packages: { ... },
        ...
      },
      ...
    },
    uwsgi: {
      capabilities: [string],
      enable: bool,
      group: string,
      instance: { ... },
      package: Derivation,
      plugins: [string],
      runDir: path,
      user: string,
      ...
    },
    v2ray: {
      config: { ... } | null | null,
      configFile: string | null,
      enable: bool,
      package: Derivation,
      ...
    },
    v2raya: {
      cliPackage: Derivation,
      enable: bool,
      package: Derivation,
      ...
    },
    v4l2-relayd: {
      instances: { _: { ... } },
      ...
    },
    varnish: {
      config: string,
      enable: bool,
      enableConfigCheck: bool,
      extraCommandLine: string,
      extraModules: [Derivation],
      http_address: string | null,
      listen: [{ ... }],
      package: Derivation,
      stateDir: { ... },
      ...
    },
    vault: {
      address: string,
      dev: bool,
      devRootTokenID: string | null,
      enable: bool,
      extraConfig: string,
      extraSettingsPaths: [path],
      listenerExtraConfig: string,
      package: Derivation,
      storageBackend: string,
      storageConfig: string | null,
      storagePath: path | null,
      telemetryConfig: string,
      tlsCertFile: string | null,
      tlsKeyFile: string | null,
      ...
    },
    vault-agent: {
      instances: { _: { ... } },
      ...
    },
    vaultwarden: {
      backupDir: string | null,
      config: { _: { ... } },
      configureNginx: bool,
      configurePostgres: bool,
      dbBackend: string,
      domain: string | null,
      enable: bool,
      environmentFile: [path],
      package: Derivation,
      webVaultPackage: Derivation,
      ...
    },
    vdirsyncer: {
      enable: bool,
      jobs: { _: { ... } },
      package: Derivation,
      ...
    },
    vdr: {
      enable: bool,
      enableLirc: bool,
      extraArguments: [string],
      group: string,
      package: Derivation,
      user: string,
      videoDir: path,
      ...
    },
    vector: {
      enable: bool,
      gracefulShutdownLimitSecs: { ... },
      journaldAccess: bool,
      package: Derivation,
      settings: { ... },
      validateConfig: bool,
      ...
    },
    veilid: {
      enable: bool,
      openFirewall: bool,
      settings: { ... },
      ...
    },
    venus: { ... },
    victorialogs: {
      basicAuthPasswordFile: string | null,
      basicAuthUsername: string | null,
      enable: bool,
      extraOptions: [string],
      listenAddress: string,
      package: Derivation,
      stateDir: string,
      ...
    },
    victoriametrics: {
      basicAuthPasswordFile: path | null,
      basicAuthUsername: string | null,
      checkConfig: bool,
      enable: bool,
      extraOptions: [string],
      listenAddress: string,
      package: Derivation,
      prometheusConfig: { ... },
      retentionPeriod: string | null,
      stateDir: string,
      ...
    },
    victoriatraces: {
      basicAuthPasswordFile: string | null,
      basicAuthUsername: string | null,
      enable: bool,
      extraOptions: [string],
      listenAddress: string,
      package: Derivation,
      retentionPeriod: string,
      stateDir: string,
      ...
    },
    vikunja: {
      address: string,
      database: {
        database: string,
        host: string,
        path: string,
        type: string,
        user: string,
        ...
      },
      enable: bool,
      environmentFiles: [path],
      frontendHostname: string,
      frontendScheme: string,
      package: Derivation,
      port: { ... },
      settings: { ... },
      setupNginx: { ... },
      ...
    },
    virtuoso: { ... },
    vlagent: {
      enable: bool,
      extraArgs: [string],
      openFirewall: bool,
      package: Derivation,
      remoteWrite: {
        basicAuthPasswordFile: { ... },
        basicAuthUsername: { ... },
        maxDiskUsagePerUrl: { ... },
        url: { ... },
        ...
      },
      ...
    },
    vmagent: {
      checkConfig: bool,
      dataDir: { ... },
      enable: bool,
      extraArgs: [string],
      group: { ... },
      openFirewall: bool,
      package: Derivation,
      prometheusConfig: { ... },
      remoteWrite: {
        basicAuthPasswordFile: { ... },
        basicAuthUsername: { ... },
        url: { ... },
        ...
      },
      remoteWriteUrl: string | null,
      user: { ... },
      ...
    },
    vmalert: {
      enable: { ... },
      instances: { _: { ... } },
      package: Derivation,
      rules: { ... },
      settings: { ... },
      ...
    },
    vmwareGuest: { ... },
    vnstat: {
      enable: bool,
      package: Derivation,
      ...
    },
    vsftpd: {
      allowWriteableChroot: bool,
      anonymousMkdirEnable: bool,
      anonymousUmask: string,
      anonymousUploadEnable: bool,
      anonymousUser: bool,
      anonymousUserHome: path,
      anonymousUserNoPassword: bool,
      chrootlocalUser: bool,
      enable: bool,
      enableVirtualUsers: bool,
      extraConfig: string,
      forceLocalDataSSL: bool,
      forceLocalLoginsSSL: bool,
      localRoot: string | null,
      localUsers: bool,
      portPromiscuous: bool,
      rsaCertFile: path | null,
      rsaKeyFile: path | null,
      ssl_sslv2: bool,
      ssl_sslv3: bool,
      ssl_tlsv1: bool,
      userDbPath: string | null,
      userlist: [string],
      userlistDeny: bool,
      userlistEnable: bool,
      userlistFile: path,
      virtualUseLocalPrivs: bool,
      writeEnable: bool,
      ...
    },
    vsmartcard-vpcd: {
      enable: bool,
      hostname: string,
      port: { ... },
      ...
    },
    vwifi: {
      client: {
        enable: bool,
        extraArgs: { ... },
        serverAddress: { ... },
        serverPort: { ... },
        spy: bool,
        ...
      },
      module: {
        enable: bool,
        macPrefix: { ... },
        numRadios: int,
        ...
      },
      package: Derivation,
      server: {
        enable: bool,
        extraArgs: { ... },
        openFirewall: bool,
        ports: { ... },
        vsock: { ... },
        ...
      },
      ...
    },
    waagent: {
      enable: bool,
      extraPackages: [Derivation],
      package: Derivation,
      settings: { ... },
      ...
    },
    wakapi: {
      database: {
        createLocally: bool,
        dialect: { ... },
        name: string,
        user: string,
        ...
      },
      enable: bool,
      environmentFiles: [path],
      package: Derivation,
      passwordSalt: { ... },
      passwordSaltFile: { ... },
      settings: { ... },
      smtpPassword: { ... },
      smtpPasswordFile: { ... },
      stateDir: path,
      ...
    },
    wakeonlan: { ... },
    warpgate: {
      databaseUrlFile: string | null,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    wasabibackend: {
      customConfigFile: path | null,
      dataDir: path,
      enable: bool,
      endpoint: {
        ip: string,
        port: { ... },
        ...
      },
      group: string,
      network: string,
      rpc: {
        ip: string,
        password: string,
        passwordFile: { ... },
        port: { ... },
        user: string,
        ...
      },
      user: string,
      ...
    },
    wastebin: {
      enable: bool,
      package: Derivation,
      secretFile: path | null,
      settings: { ... },
      stateDir: path,
      ...
    },
    watchdogd: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    webdav: {
      configFile: path,
      enable: bool,
      environmentFile: path | null,
      group: string,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    webdav-server-rs: {
      configFile: path,
      debug: bool,
      enable: bool,
      group: string,
      settings: { ... },
      user: string,
      ...
    },
    webhook: {
      enable: bool,
      enableTemplates: bool,
      environment: { _: string },
      extraArgs: [string],
      group: string,
      hooks: { _: { ... } },
      hooksTemplated: { _: string },
      ip: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      urlPrefix: string,
      user: string,
      verbose: bool,
      ...
    },
    weblate: {
      configurePostgresql: bool,
      djangoSecretKeyFile: path,
      enable: bool,
      extraConfig: string,
      localDomain: string,
      package: Derivation,
      smtp: {
        enable: bool,
        from: string,
        host: string,
        passwordFile: { ... },
        port: { ... },
        user: { ... },
        ...
      },
      ...
    },
    weechat: {
      binary: path,
      enable: bool,
      headless: bool,
      package: Derivation,
      root: path,
      sessionName: string,
      ...
    },
    wg-access-server: {
      enable: bool,
      package: Derivation,
      secretsFile: path,
      settings: { ... },
      ...
    },
    wg-netmanager: {
      enable: bool,
      ...
    },
    wgautomesh: {
      enable: bool,
      enableGossipEncryption: bool,
      enablePersistence: bool,
      gossipSecretFile: path,
      logLevel: string,
      openFirewall: bool,
      settings: { ... },
      ...
    },
    whisparr: {
      dataDir: path,
      enable: bool,
      environmentFiles: [path],
      group: string,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      user: string,
      ...
    },
    whitebophir: {
      enable: bool,
      listenAddress: string,
      package: Derivation,
      port: { ... },
      ...
    },
    whoami: {
      enable: bool,
      extraArgs: [string],
      package: Derivation,
      port: { ... },
      ...
    },
    whoogle-search: {
      enable: bool,
      extraEnv: { _: string },
      listenAddress: string,
      port: { ... },
      ...
    },
    wiki-js: {
      enable: bool,
      environmentFile: path | null,
      settings: { ... },
      stateDirectoryName: string,
      ...
    },
    windmill: {
      baseUrl: string,
      database: {
        createLocally: bool,
        name: string,
        url: string,
        urlPath: { ... },
        user: string,
        ...
      },
      enable: bool,
      logLevel: string,
      lspPort: { ... },
      package: Derivation,
      serverPort: { ... },
      ...
    },
    winstone: { ... },
    wivrn: {
      autoStart: bool,
      config: {
        enable: bool,
        json: { ... },
        ...
      },
      defaultRuntime: bool,
      enable: bool,
      extraServerFlags: [string],
      highPriority: bool,
      monadoEnvironment: { ... },
      openFirewall: bool,
      package: Derivation,
      steam: {
        importOXRRuntimes: bool,
        package: Derivation,
        ...
      },
      ...
    },
    wlock: {
      enable: bool,
      package: Derivation,
      ...
    },
    woodpecker-agents: {
      agents: { _: { ... } },
      ...
    },
    woodpecker-server: {
      enable: bool,
      environment: { _: string },
      environmentFile: [path],
      package: Derivation,
      ...
    },
    wordpress: {
      sites: { _: { ... } },
      webserver: string,
      ...
    },
    workout-tracker: {
      address: string,
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      port: { ... },
      settings: { _: string },
      ...
    },
    writefreely: {
      acme: {
        enable: bool,
        ...
      },
      admin: {
        initialPasswordFile: path,
        name: { ... },
        ...
      },
      database: {
        createLocally: bool,
        host: string,
        migrate: bool,
        name: string,
        passwordFile: { ... },
        port: { ... },
        tls: bool,
        type: string,
        user: { ... },
        ...
      },
      enable: bool,
      group: string,
      host: string,
      nginx: {
        enable: bool,
        forceSSL: bool,
        ...
      },
      package: Derivation,
      settings: { ... },
      stateDir: path,
      user: string,
      ...
    },
    wstunnel: {
      clients: { _: { ... } },
      enable: bool,
      servers: { _: { ... } },
      ...
    },
    wymoing: {
      openwakeword: {
        preLoadModels: { ... },
        ...
      },
      ...
    },
    wyoming: {
      faster-whisper: {
        package: Derivation,
        servers: { ... },
        ...
      },
      openwakeword: {
        customModelsDirectories: { ... },
        enable: bool,
        extraArgs: { ... },
        package: Derivation,
        refractorySeconds: { ... },
        threshold: { ... },
        triggerLevel: { ... },
        uri: { ... },
        ...
      },
      piper: {
        package: Derivation,
        servers: { ... },
        ...
      },
      satellite: {
        area: { ... },
        enable: bool,
        extraArgs: { ... },
        group: string,
        microphone: { ... },
        name: string,
        package: Derivation,
        sound: { ... },
        sounds: { ... },
        uri: string,
        user: string,
        vad: { ... },
        ...
      },
      ...
    },
    x2goserver: {
      enable: bool,
      nxagentDefaultOptions: [string],
      package: Derivation,
      settings: { _: { ... } },
      superenicer: {
        enable: bool,
        ...
      },
      ...
    },
    xandikos: {
      address: string,
      enable: bool,
      extraOptions: [string],
      nginx: { ... },
      package: Derivation,
      port: { ... },
      routePrefix: string,
      ...
    },
    xbanish: {
      arguments: string,
      enable: bool,
      ...
    },
    xe-guest-utilities: {
      enable: bool,
      ...
    },
    xfs: {
      enable: bool,
      ...
    },
    xinetd: {
      enable: bool,
      extraDefaults: string,
      services: [{ ... }],
      ...
    },
    xl2tpd: {
      clientIpRange: string,
      enable: bool,
      extraPppdOptions: string,
      extraXl2tpOptions: string,
      serverIp: string,
      ...
    },
    xmr-stak: { ... },
    xmrig: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    xonotic: {
      appendConfig: string | null,
      dataDir: path,
      enable: bool,
      openFirewall: bool,
      package: Derivation,
      prependConfig: string | null,
      settings: { ... },
      ...
    },
    xray: {
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      settingsFile: path | null,
      ...
    },
    xrdp: {
      audio: {
        enable: bool,
        package: Derivation,
        ...
      },
      confDir: path,
      defaultWindowManager: string,
      enable: bool,
      extraConfDirCommands: string,
      openFirewall: bool,
      package: Derivation,
      port: { ... },
      sslCert: string,
      sslKey: string,
      ...
    },
    xscreensaver: {
      enable: bool,
      hooks: { _: string },
      package: Derivation,
      ...
    },
    xserver: {
      autoRepeatDelay: int | null,
      autoRepeatInterval: int | null,
      autorun: bool,
      cmt: {
        enable: bool,
        models: string,
        ...
      },
      config: string,
      defaultDepth: int,
      desktopManager: {
        budgie: { ... },
        cde: { ... },
        cinnamon: { ... },
        deepin: { ... },
        e19: { ... },
        enlightenment: { ... },
        gnome: { ... },
        kodi: { ... },
        lumina: { ... },
        lxqt: { ... },
        mate: { ... },
        pantheon: { ... },
        phosh: { ... },
        plasma5: { ... },
        plasma6: { ... },
        retroarch: { ... },
        runXdgAutostartIfNone: bool,
        session: { ... },
        surf-display: { ... },
        wallpaper: { ... },
        xfce: { ... },
        xfce4-14: { ... },
        xterm: { ... },
        ...
      },
      deviceSection: string,
      digimend: {
        enable: bool,
        ...
      },
      display: int | null,
      displayManager: {
        auto: { ... },
        autoLogin: { ... },
        defaultSession: { ... },
        desktopManagerHandlesLidAndPower: { ... },
        extraSessionFilesPackages: { ... },
        gdm: { ... },
        hiddenUsers: { ... },
        importedVariables: { ... },
        job: { ... },
        lightdm: { ... },
        logToJournal: bool,
        sddm: { ... },
        session: { ... },
        sessionCommands: string,
        sessionData: { ... },
        sessionPackages: { ... },
        setupCommands: string,
        slim: { ... },
        startx: { ... },
        sx: { ... },
        xauthBin: { ... },
        xpra: { ... },
        xserverArgs: { ... },
        xserverBin: path,
        ...
      },
      dpi: int | null,
      drivers: [{ ... }],
      enable: bool,
      enableCtrlAltBackspace: bool,
      enableTCP: bool,
      enableTearFree: bool,
      excludePackages: [Derivation],
      exportConfiguration: bool,
      externallyConfiguredDrivers: [string],
      extraConfig: string,
      extraDisplaySettings: string,
      extraLayouts: { _: { ... } },
      filesSection: string,
      fontPath: string | null,
      gdk-pixbuf: { ... },
      imwheel: {
        enable: bool,
        extraOptions: { ... },
        rules: { ... },
        ...
      },
      inputClassSections: [string],
      layout: string,
      libinput: {
        accelProfile: string,
        accelSpeed: { ... },
        additionalOptions: string,
        buttonMapping: { ... },
        calibrationMatrix: { ... },
        clickMethod: { ... },
        disableWhileTyping: bool,
        enable: bool,
        horizontalScrolling: bool,
        leftHanded: bool,
        middleEmulation: bool,
        mouse: { ... },
        naturalScrolling: bool,
        scrollButton: { ... },
        scrollMethod: string,
        sendEventsMode: string,
        tapping: bool,
        tappingButtonMap: { ... },
        tappingDragLock: bool,
        touchpad: { ... },
        transformationMatrix: { ... },
        ...
      },
      logFile: string | null,
      moduleSection: string,
      modules: [path],
      monitorSection: string,
      multitouch: { ... },
      resolutions: [{ ... }],
      screenSection: string,
      serverFlagsSection: string,
      serverLayoutSection: string,
      startDbusSession: { ... },
      startGnuPGAgent: { ... },
      synaptics: {
        accelFactor: { ... },
        additionalOptions: string,
        buttonsMap: { ... },
        dev: { ... },
        enable: bool,
        fingersMap: { ... },
        horizEdgeScroll: bool,
        horizTwoFingerScroll: bool,
        horizontalScroll: bool,
        maxSpeed: { ... },
        minSpeed: { ... },
        palmDetect: bool,
        palmMinWidth: { ... },
        palmMinZ: { ... },
        scrollDelta: { ... },
        tapButtons: bool,
        twoFingerScroll: bool,
        vertEdgeScroll: bool,
        vertTwoFingerScroll: bool,
        ...
      },
      terminateOnReset: bool,
      tty: { ... },
      updateDbusEnvironment: bool,
      upscaleDefaultCursor: bool,
      useGlamor: { ... },
      useXFS: { ... },
      vaapiDrivers: [Derivation],
      verbose: int | null,
      videoDriver: string | null,
      videoDrivers: [string],
      virtualScreen: { ... } | null,
      wacom: {
        enable: bool,
        ...
      },
      windowManager: {
        2bwm: { ... },
        afterstep: { ... },
        awesome: { ... },
        berry: { ... },
        bspwm: { ... },
        clfswm: { ... },
        cwm: { ... },
        dk: { ... },
        dwm: { ... },
        e16: { ... },
        evilwm: { ... },
        exwm: { ... },
        fluxbox: { ... },
        fvwm: { ... },
        fvwm2: { ... },
        fvwm3: { ... },
        hackedbox: { ... },
        herbstluftwm: { ... },
        hypr: { ... },
        i3: { ... },
        i3-gaps: { ... },
        icewm: { ... },
        jwm: { ... },
        katriawm: { ... },
        leftwm: { ... },
        lwm: { ... },
        metacity: { ... },
        mlvwm: { ... },
        mwm: { ... },
        nimdow: { ... },
        notion: { ... },
        openbox: { ... },
        oxwm: { ... },
        pekwm: { ... },
        qtile: { ... },
        ragnarwm: { ... },
        ratpoison: { ... },
        sawfish: { ... },
        session: { ... },
        smallwm: { ... },
        spectrwm: { ... },
        stumpwm: { ... },
        tinywm: { ... },
        twm: { ... },
        windowlab: { ... },
        windowmaker: { ... },
        wmderland: { ... },
        wmii: { ... },
        xmonad: { ... },
        ...
      },
      xautolock: {
        enable: bool,
        enableNotifier: bool,
        extraOptions: { ... },
        killer: { ... },
        killtime: int,
        locker: string,
        notifier: { ... },
        notify: int,
        nowlocker: { ... },
        time: int,
        ...
      },
      xkb: {
        dir: path,
        extraLayouts: { ... },
        layout: string,
        model: string,
        options: string,
        variant: string,
        ...
      },
      xkbDir: path,
      xkbModel: string,
      xkbOptions: string,
      xkbVariant: string,
      xrandrHeads: [{ ... }],
      ...
    },
    xtreemfs: {
      dir: {
        address: string,
        enable: bool,
        extraConfig: string,
        httpPort: { ... },
        port: { ... },
        replication: { ... },
        syncMode: string,
        uuid: string,
        ...
      },
      enable: bool,
      homeDir: path,
      mrc: {
        address: string,
        enable: bool,
        extraConfig: string,
        httpPort: { ... },
        port: { ... },
        replication: { ... },
        syncMode: string,
        uuid: string,
        ...
      },
      osd: {
        address: string,
        enable: bool,
        extraConfig: string,
        httpPort: { ... },
        port: { ... },
        uuid: string,
        ...
      },
      ...
    },
    yandex-disk: {
      directory: path,
      enable: bool,
      excludes: string,
      password: string,
      user: string | null,
      username: string,
      ...
    },
    yarr: {
      address: string,
      authFilePath: path | null,
      baseUrl: string | null,
      enable: bool,
      environmentFile: path | null,
      package: Derivation,
      port: { ... },
      ...
    },
    yggdrasil: {
      config: { ... },
      denyDhcpcdInterfaces: [string],
      enable: bool,
      extraArgs: [string],
      group: string | null,
      openMulticastPort: bool,
      package: Derivation,
      persistentKeys: bool,
      settings: { ... },
      ...
    },
    yggdrasil-jumper: {
      appendListenAddresses: bool,
      detectWireguard: bool,
      enable: bool,
      extraArgs: [string],
      extraConfig: string,
      logLevel: string,
      package: Derivation,
      retrieveListenAddresses: bool,
      settings: { ... },
      ...
    },
    ympd: {
      enable: bool,
      mpd: {
        host: string,
        port: { ... },
        ...
      },
      webPort: string | { ... },
      ...
    },
    your_spotify: {
      clientPackage: Derivation,
      enable: bool,
      enableLocalDB: bool,
      nginxVirtualHost: string | null,
      package: Derivation,
      settings: { ... },
      spotifySecretFile: path,
      ...
    },
    youtrack: {
      address: string,
      autoUpgrade: bool,
      baseUrl: { ... },
      enable: bool,
      environmentalParameters: { ... },
      extraParams: { ... },
      generalParameters: [string],
      jvmOpts: { ... },
      maxMemory: { ... },
      maxMetaspaceSize: { ... },
      package: Derivation,
      port: { ... },
      statePath: path,
      virtualHost: string | null,
      ...
    },
    ytdl-sub: {
      group: string,
      instances: { _: { ... } },
      package: Derivation,
      user: string,
      ...
    },
    yubikey-agent: {
      enable: bool,
      package: Derivation,
      ...
    },
    zabbixAgent: {
      enable: bool,
      extraConfig: { ... },
      extraPackages: [Derivation],
      listen: {
        ip: string,
        port: { ... },
        ...
      },
      modules: { _: Derivation },
      openFirewall: bool,
      package: Derivation,
      server: string,
      settings: { _: { ... } },
      ...
    },
    zabbixProxy: {
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        socket: { ... },
        type: string,
        user: string,
        ...
      },
      enable: bool,
      extraConfig: { ... },
      extraPackages: [Derivation],
      listen: {
        ip: string,
        port: { ... },
        ...
      },
      modules: { _: Derivation },
      openFirewall: bool,
      package: Derivation,
      server: string,
      settings: { _: { ... } },
      ...
    },
    zabbixServer: {
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        socket: { ... },
        type: string,
        user: string,
        ...
      },
      dbPassword: { ... },
      dbServer: string,
      enable: bool,
      extraConfig: { ... },
      extraPackages: [Derivation],
      listen: {
        ip: string,
        port: { ... },
        ...
      },
      modules: { _: Derivation },
      openFirewall: bool,
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    zabbixWeb: {
      database: {
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        socket: { ... },
        type: string,
        user: string,
        ...
      },
      enable: bool,
      extraConfig: string,
      frontend: string,
      hostname: string,
      httpd: {
        virtualHost: { ... },
        ...
      },
      nginx: {
        virtualHost: { ... },
        ...
      },
      package: Derivation,
      poolConfig: { _: { ... } },
      server: {
        address: string,
        port: { ... },
        ...
      },
      virtualHost: { ... },
      ...
    },
    zammad: {
      dataDir: path,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        passwordFile: { ... },
        port: { ... },
        settings: { ... },
        user: { ... },
        ...
      },
      enable: bool,
      group: string,
      host: string,
      nginx: {
        configure: bool,
        domain: string,
        ...
      },
      openPorts: { ... },
      package: Derivation,
      port: { ... },
      redis: {
        createLocally: bool,
        host: string,
        name: string,
        port: { ... },
        ...
      },
      secretKeyBaseFile: path | null,
      user: string,
      websocketPort: { ... },
      ...
    },
    zapret: {
      blacklist: [string],
      configureFirewall: bool,
      enable: bool,
      httpMode: string,
      httpSupport: bool,
      package: Derivation,
      params: [string],
      qnum: int,
      udpPorts: [string],
      udpSupport: bool,
      whitelist: [string],
      ...
    },
    zeitgeist: {
      enable: bool,
      ...
    },
    zenohd: {
      backends: [Derivation],
      enable: bool,
      env: { _: string },
      extraOptions: [string],
      home: string,
      package: Derivation,
      plugins: [Derivation],
      settings: { ... },
      ...
    },
    zerobin: {
      dataDir: string,
      enable: bool,
      extraConfig: string,
      group: string,
      listenAddress: string,
      listenPort: { ... },
      user: string,
      ...
    },
    zeronet: {
      dataDir: { ... },
      enable: bool,
      fileserverPort: { ... },
      logDir: { ... },
      package: Derivation,
      port: { ... },
      settings: { _: { ... } },
      tor: bool,
      torAlways: bool,
      ...
    },
    zeronsd: {
      servedNetworks: { _: { ... } },
      ...
    },
    zerotierone: {
      enable: bool,
      joinNetworks: [string],
      localConf: { ... },
      package: Derivation,
      port: { ... },
      ...
    },
    zeyple: {
      enable: bool,
      group: string,
      keys: [path],
      rotateLogs: bool,
      settings: { _: { ... } },
      user: string,
      ...
    },
    zfs: {
      autoReplication: {
        enable: bool,
        followDelete: bool,
        host: string,
        identityFilePath: path,
        localFilesystem: string,
        package: Derivation,
        recursive: bool,
        remoteFilesystem: string,
        username: string,
        ...
      },
      autoScrub: {
        enable: bool,
        interval: string,
        pools: { ... },
        randomizedDelaySec: string,
        ...
      },
      autoSnapshot: {
        daily: int,
        enable: bool,
        flags: string,
        frequent: int,
        hourly: int,
        monthly: int,
        weekly: int,
        ...
      },
      expandOnBoot: string | { ... },
      trim: {
        enable: bool,
        interval: string,
        randomizedDelaySec: string,
        ...
      },
      zed: {
        enableMail: bool,
        settings: { ... },
        ...
      },
      ...
    },
    zigbee2mqtt: {
      config: { ... },
      dataDir: path,
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    zipline: {
      database: {
        createLocally: bool,
        ...
      },
      enable: bool,
      environmentFiles: [path],
      package: Derivation,
      settings: { ... },
      ...
    },
    zitadel: {
      enable: bool,
      extraSettingsPaths: [path],
      extraStepsPaths: [path],
      group: string,
      masterKeyFile: path,
      openFirewall: bool,
      package: Derivation,
      settings: { ... },
      steps: { ... },
      tlsMode: string,
      user: string,
      ...
    },
    znapzend: {
      autoCreation: bool,
      enable: bool,
      features: {
        compressed: bool,
        lowmemRecurse: bool,
        oracleMode: bool,
        recvu: bool,
        sendRaw: bool,
        skipIntermediates: bool,
        zfsGetType: bool,
        ...
      },
      logLevel: string,
      logTo: string,
      mailErrorSummaryTo: { ... },
      noDestroy: bool,
      pure: bool,
      zetup: { _: { ... } },
      ...
    },
    znc: {
      confOptions: {
        extraZncConf: string,
        modules: { ... },
        networks: { ... },
        nick: string,
        passBlock: string,
        port: { ... },
        uriPrefix: { ... },
        useSSL: bool,
        userModules: { ... },
        userName: string,
        ...
      },
      config: { _: { ... } },
      configFile: path,
      dataDir: path,
      enable: bool,
      extraFlags: [string],
      group: string,
      modulePackages: [Derivation],
      mutable: bool,
      openFirewall: bool,
      useLegacyConfig: bool,
      user: string,
      zncConf: { ... },
      ...
    },
    zoneminder: {
      cameras: int,
      database: {
        createLocally: bool,
        host: string,
        name: string,
        password: string,
        username: string,
        ...
      },
      enable: bool,
      extraConfig: string,
      hostname: string,
      openFirewall: bool,
      port: { ... },
      storageDir: string | null,
      webserver: string,
      ...
    },
    zookeeper: {
      dataDir: path,
      enable: bool,
      extraCmdLineOptions: [string],
      extraConf: string,
      id: int,
      jre: Derivation,
      logging: string,
      package: Derivation,
      port: { ... },
      preferIPv4: bool,
      purgeInterval: int,
      servers: string,
      ...
    },
    zram-generator: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    zrepl: {
      enable: bool,
      package: Derivation,
      settings: { ... },
      ...
    },
    zwave-js: {
      enable: bool,
      extraFlags: [string],
      package: Derivation,
      port: { ... },
      secretsConfigFile: path,
      serialPort: path,
      settings: { ... },
      ...
    },
    zwave-js-ui: {
      enable: bool,
      package: Derivation,
      serialPort: path,
      settings: { ... },
      ...
    },
    ...
  },
  snapraid: { ... },
  sound: {
    enable: { ... },
    enableOSSEmulation: bool,
    extraConfig: string,
    mediaKeys: { ... },
    ...
  },
  specialisation: { _: {
  configuration: { ... },
  inheritParentConfig: bool,
  ...
} },
  stubby: {
    debugLogging: { ... },
    ...
  },
  swapDevices: [{
  device: string,
  deviceName: string,
  discardPolicy: { ... },
  encrypted: { ... },
  label: string,
  options: { ... },
  priority: { ... },
  randomEncryption: { ... },
  realDevice: path,
  size: { ... },
  ...
}],
  system: {
    activatable: bool,
    activatableSystemBuilderCommands: string,
    activationScripts: { _: string | { ... } },
    autoUpgrade: {
      allowReboot: bool,
      channel: string | null,
      dates: string,
      enable: bool,
      fixedRandomDelay: bool,
      flags: [string],
      flake: string | null,
      operation: string,
      persistent: bool,
      randomizedDelaySec: string,
      rebootWindow: { ... },
      runGarbageCollection: bool,
      upgrade: bool,
      ...
    },
    boot: {
      loader: {
        id: { ... },
        initrdFile: string,
        kernelFile: string,
        ukiFile: string,
        ...
      },
      ...
    },
    build: {
  images: { ... },
  installBootLoader: { ... },
  noFacter: { ... },
  separateActivationScript: Derivation,
  toplevel: Derivation,
  ...
},
    checks: [Derivation],
    configurationRevision: string | null,
    copySystemConfiguration: bool,
    defaultChannel: string,
    disableInstallerTools: bool,
    dryActivationScript: { ... },
    etc: {
      overlay: {
        enable: bool,
        mutable: bool,
        ...
      },
      ...
    },
    extraDependencies: [path],
    extraSystemBuilderCmds: string,
    forbiddenDependenciesRegex: [string],
    forbiddenDependenciesRegexes: [string],
    fsPackages: { ... },
    image: {
      id: string | null,
      version: string | null,
      ...
    },
    includeBuildDependencies: bool,
    modulesTree: [path],
    name: string,
    nixos: {
      codeName: string,
      distroId: string,
      distroName: string,
      extraLSBReleaseArgs: { _: string },
      extraOSReleaseArgs: { _: string },
      label: { ... },
      release: string,
      revision: string | null,
      tags: [string],
      variantName: string | null,
      variant_id: { ... } | null,
      vendorId: string,
      vendorName: string,
      version: string,
      versionSuffix: string,
      ...
    },
    nixos-generate-config: {
      configuration: string,
      desktopConfiguration: [string],
      flake: string,
      ...
    },
    nixos-init: {
      enable: bool,
      package: Derivation,
      ...
    },
    nixosLabel: { ... },
    nixosRevision: string | null,
    nixosVersion: string,
    nixosVersionSuffix: string,
    nssDatabases: {
      group: [string],
      hosts: [string],
      passwd: [string],
      services: [string],
      shadow: [string],
      subgid: [string],
      subuid: [string],
      sudoers: [string],
      ...
    },
    nssHosts: [string],
    nssModules: [path],
    path: { ... },
    preSwitchChecks: { _: string },
    preSwitchChecksScript: path,
    rebuild: {
      enableNg: { ... },
      ...
    },
    replaceDependencies: {
      cutoffPackages: [Derivation],
      replacements: [{ ... }],
      ...
    },
    replaceRuntimeDependencies: [{ ... }],
    requiredKernelConfig: [{ ... }],
    services: { _: { ... } },
    stateVersion: string,
    switch: {
      enable: bool,
      enableNg: { ... },
      inhibitors: { _: string },
      ...
    },
    systemBuilderArgs: { _: { ... } },
    systemBuilderCommands: string,
    tools: {
      nixos-build-vms: {
        enable: bool,
        ...
      },
      nixos-enter: {
        enable: bool,
        ...
      },
      nixos-generate-config: {
        enable: bool,
        ...
      },
      nixos-install: {
        enable: bool,
        ...
      },
      nixos-option: {
        enable: bool,
        ...
      },
      nixos-rebuild: {
        enable: bool,
        ...
      },
      nixos-version: {
        enable: bool,
        ...
      },
      ...
    },
    userActivationScripts: { _: string | { ... } },
    ...
  },
  systemd: {
    additionalUpstreamSystemUnits: [string],
    additionalUpstreamUserUnits: [string],
    automounts: [{ ... }],
    coredump: {
      enable: bool,
      extraConfig: string,
      ...
    },
    ctrlAltDelUnit: string,
    defaultUnit: string,
    enableCgroupAccounting: { ... },
    enableEmergencyMode: bool,
    enableStrictShellChecks: bool,
    enableUnifiedCgroupHierarchy: { ... },
    extraConfig: { ... },
    generator-packages: { ... },
    generatorEnvironment: { _: string },
    generatorPath: [Derivation],
    generators: { _: path },
    globalEnvironment: { _: { ... } | Derivation | null },
    managerEnvironment: { _: { ... } | Derivation | null },
    mounts: [{ ... }],
    network: {
      config: { ... },
      enable: bool,
      links: { _: { ... } },
      netdevs: { _: { ... } },
      networks: { _: { ... } },
      units: { _: { ... } },
      wait-online: {
        anyInterface: bool,
        enable: bool,
        extraArgs: { ... },
        ignoredInterfaces: { ... },
        timeout: { ... },
        ...
      },
      ...
    },
    nspawn: { _: { ... } },
    oomd: {
      enable: bool,
      enableRootSlice: bool,
      enableSystemSlice: bool,
      enableUserServices: bool,
      enableUserSlices: bool,
      extraConfig: { ... },
      settings: {
        OOM: { ... },
        ...
      },
      ...
    },
    package: Derivation,
    packages: [Derivation],
    paths: { _: { ... } },
    repart: {
      enable: bool,
      partitions: { _: { ... } },
      ...
    },
    services: { _: { ... } },
    settings: {
      Manager: { ... },
      ...
    },
    shutdown: { _: path },
    shutdownRamfs: {
      contents: { _: { ... } },
      enable: bool,
      shell: {
        enable: bool,
        ...
      },
      storePaths: [{ ... }],
      ...
    },
    sleep: {
      extraConfig: { ... },
      settings: {
        Sleep: { ... },
        ...
      },
      ...
    },
    slices: { _: { ... } },
    sockets: { _: { ... } },
    suppressedSystemUnits: [string],
    sysupdate: {
      enable: bool,
      reboot: {
        enable: bool,
        timerConfig: { ... },
        ...
      },
      timerConfig: { _: { ... } },
      transfers: { _: { ... } },
      ...
    },
    sysusers: {
      enable: bool,
      ...
    },
    targets: { _: { ... } },
    timers: { _: { ... } },
    tmpfiles: {
      packages: [Derivation],
      rules: [string],
      settings: { _: { ... } },
      ...
    },
    tpm2: {
      enable: bool,
      ...
    },
    units: { _: { ... } },
    user: {
      extraConfig: string,
      generators: { _: path },
      paths: { _: { ... } },
      services: { _: { ... } },
      slices: { _: { ... } },
      sockets: { _: { ... } },
      targets: { _: { ... } },
      timers: { _: { ... } },
      tmpfiles: {
        enable: bool,
        rules: { ... },
        users: { ... },
        ...
      },
      units: { _: { ... } },
      ...
    },
    watchdog: {
      device: { ... },
      kexecTime: { ... },
      rebootTime: { ... },
      runtimeTime: { ... },
      ...
    },
    ...
  },
  time: {
    hardwareClockInLocalTime: bool,
    timeZone: string | null,
    ...
  },
  users: {
    allowNoPasswordLogin: bool,
    defaultUserHome: string,
    defaultUserShell: path | Derivation,
    enforceIdUniqueness: bool,
    extraGroups: { _: { ... } },
    extraUsers: { _: { ... } },
    groups: { _: { ... } },
    ldap: {
      base: string,
      bind: {
        distinguishedName: string,
        password: string,
        passwordFile: string,
        policy: string,
        timeLimit: int,
        ...
      },
      daemon: {
        enable: bool,
        extraConfig: string,
        rootpwmoddn: string,
        rootpwmodpwFile: string,
        ...
      },
      enable: bool,
      extraConfig: string,
      loginPam: bool,
      nsswitch: bool,
      server: string,
      timeLimit: int,
      useTLS: bool,
      ...
    },
    manageLingering: bool,
    motd: string,
    motdFile: path | null,
    mutableUsers: bool,
    mysql: {
      database: string,
      enable: bool,
      host: string,
      nss: { ... },
      pam: { ... },
      passwordFile: path,
      user: string,
      ...
    },
    users: { _: { ... } },
    ...
  },
  virtualisation: {
    appvm: {
      enable: bool,
      user: string,
      ...
    },
    containerd: {
      args: { _: string },
      configFile: path | null,
      enable: bool,
      settings: { ... },
      ...
    },
    containers: {
      cdi: {
        dynamic: { ... },
        ...
      },
      containersConf: {
        cniPlugins: { ... },
        settings: { ... },
        ...
      },
      enable: bool,
      ociSeccompBpfHook: {
        enable: bool,
        ...
      },
      policy: { ... },
      registries: {
        block: { ... },
        insecure: { ... },
        search: { ... },
        ...
      },
      storage: {
        settings: { ... },
        ...
      },
      ...
    },
    cri-o: {
      enable: bool,
      extraPackages: [Derivation],
      logLevel: string,
      networkDir: path | null,
      package: Derivation,
      pauseCommand: string | null,
      pauseImage: string | null,
      runtime: string | null,
      settings: { ... },
      storageDriver: string,
      ...
    },
    diskSize: string | { ... },
    diskSizeAutoSupported: bool,
    docker: {
      autoPrune: {
        dates: string,
        enable: bool,
        flags: { ... },
        persistent: bool,
        randomizedDelaySec: { ... },
        ...
      },
      daemon: {
        settings: { ... },
        ...
      },
      enable: bool,
      enableNvidia: bool,
      enableOnBoot: bool,
      extraOptions: string,
      extraPackages: [Derivation],
      listenOptions: [string],
      liveRestore: { ... },
      logDriver: string,
      package: Derivation,
      rootless: {
        daemon: { ... },
        enable: bool,
        extraPackages: { ... },
        package: Derivation,
        setSocketVariable: bool,
        ...
      },
      socketActivation: { ... },
      storageDriver: string | null,
      ...
    },
    growPartition: bool,
    hypervGuest: {
      enable: bool,
      videoMode: { ... },
      ...
    },
    incus: {
      agent: {
        enable: bool,
        ...
      },
      clientPackage: Derivation,
      enable: bool,
      lxcPackage: Derivation,
      package: Derivation,
      preseed: { ... },
      socketActivation: bool,
      softDaemonRestart: bool,
      startTimeout: { ... },
      ui: {
        enable: bool,
        package: Derivation,
        ...
      },
      ...
    },
    kvmgt: {
      device: string,
      enable: bool,
      vgpus: { _: { ... } },
      ...
    },
    libvirtd: {
      allowedBridges: [string],
      dbus: {
        enable: bool,
        package: Derivation,
        ...
      },
      enable: bool,
      enableKVM: { ... },
      extraConfig: string,
      extraOptions: [string],
      firewallBackend: string,
      hooks: { ... },
      nss: { ... },
      onBoot: string,
      onShutdown: string,
      package: Derivation,
      parallelShutdown: { ... },
      qemu: { ... },
      qemuOvmf: { ... },
      qemuOvmfPackage: { ... },
      qemuPackage: { ... },
      qemuRunAsRoot: { ... },
      qemuSwtpm: { ... },
      qemuVerbatimConfig: { ... },
      shutdownTimeout: { ... },
      sshProxy: bool,
      startDelay: { ... },
      ...
    },
    lxc: {
      bridgeConfig: string,
      defaultConfig: string,
      enable: bool,
      lxcfs: {
        enable: bool,
        ...
      },
      package: Derivation,
      systemConfig: string,
      unprivilegedContainers: bool,
      usernetConfig: string,
      ...
    },
    lxd: { ... },
    multipass: { ... },
    oci-containers: {
      backend: string,
      containers: { _: { ... } },
      ...
    },
    podman: {
      autoPrune: {
        dates: string,
        enable: bool,
        flags: { ... },
        ...
      },
      defaultNetwork: {
        dnsname: { ... },
        extraPlugins: { ... },
        settings: { ... },
        ...
      },
      dockerCompat: bool,
      dockerSocket: {
        enable: bool,
        ...
      },
      enable: bool,
      enableNvidia: bool,
      extraPackages: [Derivation],
      extraRuntimes: [Derivation],
      networkSocket: {
        enable: bool,
        listenAddress: string,
        openFirewall: bool,
        port: { ... },
        server: string,
        tls: { ... },
        ...
      },
      package: Derivation,
      ...
    },
    rkt: { ... },
    rosetta: {
      enable: bool,
      mountPoint: string,
      mountTag: string,
      ...
    },
    spiceUSBRedirection: {
      enable: bool,
      ...
    },
    virtualbox: {
      guest: {
        clipboard: bool,
        dragAndDrop: bool,
        draganddrop: bool,
        enable: bool,
        seamless: bool,
        use3rdPartyModules: bool,
        vboxsf: bool,
        verbose: bool,
        ...
      },
      host: {
        addNetworkInterface: bool,
        enable: bool,
        enableExtensionPack: bool,
        enableHardening: bool,
        enableKvm: bool,
        enableWebService: bool,
        headless: bool,
        package: Derivation,
        ...
      },
      ...
    },
    vmVariant: {
  appstream: { ... },
  assertions: { ... },
  boot: { ... },
  console: { ... },
  containers: { ... },
  docker-containers: { ... },
  documentation: { ... },
  dysnomia: { ... },
  ec2: { ... },
  environment: { ... },
  fileSystems: { ... },
  fonts: { ... },
  gtk: { ... },
  hardware: { ... },
  i18n: { ... },
  ids: { ... },
  image: { ... },
  isSpecialisation: bool,
  jobs: { ... },
  krb5: { ... },
  lib: { ... },
  location: { ... },
  meta: { ... },
  minifyStaticFiles: { ... },
  nesting: { ... },
  networking: { ... },
  nix: { ... },
  nixops: { ... },
  nixpkgs: { ... },
  oci: { ... },
  openstack: { ... },
  passthru: { ... },
  power: { ... },
  powerManagement: { ... },
  programs: { ... },
  qt: { ... },
  qt5: { ... },
  security: { ... },
  services: { ... },
  snapraid: { ... },
  sound: { ... },
  specialisation: { ... },
  stubby: { ... },
  swapDevices: { ... },
  system: { ... },
  systemd: { ... },
  time: { ... },
  users: { ... },
  virtualisation: { ... },
  warnings: { ... },
  xdg: { ... },
  zramSwap: { ... },
  ...
},
    vmVariantWithBootLoader: {
  appstream: { ... },
  assertions: { ... },
  boot: { ... },
  console: { ... },
  containers: { ... },
  docker-containers: { ... },
  documentation: { ... },
  dysnomia: { ... },
  ec2: { ... },
  environment: { ... },
  fileSystems: { ... },
  fonts: { ... },
  gtk: { ... },
  hardware: { ... },
  i18n: { ... },
  ids: { ... },
  image: { ... },
  isSpecialisation: bool,
  jobs: { ... },
  krb5: { ... },
  lib: { ... },
  location: { ... },
  meta: { ... },
  minifyStaticFiles: { ... },
  nesting: { ... },
  networking: { ... },
  nix: { ... },
  nixops: { ... },
  nixpkgs: { ... },
  oci: { ... },
  openstack: { ... },
  passthru: { ... },
  power: { ... },
  powerManagement: { ... },
  programs: { ... },
  qt: { ... },
  qt5: { ... },
  security: { ... },
  services: { ... },
  snapraid: { ... },
  sound: { ... },
  specialisation: { ... },
  stubby: { ... },
  swapDevices: { ... },
  system: { ... },
  systemd: { ... },
  time: { ... },
  users: { ... },
  virtualisation: { ... },
  warnings: { ... },
  xdg: { ... },
  zramSwap: { ... },
  ...
},
    vmware: {
      guest: {
        enable: bool,
        headless: bool,
        package: Derivation,
        ...
      },
      host: {
        enable: bool,
        extraConfig: string,
        extraPackages: { ... },
        package: Derivation,
        ...
      },
      ...
    },
    vswitch: {
      enable: bool,
      ipsec: { ... },
      package: Derivation,
      resetOnStart: bool,
      ...
    },
    waydroid: {
      enable: bool,
      package: Derivation,
      ...
    },
    xen: {
      boot: {
        bios: { ... },
        builderVerbosity: string,
        efi: { ... },
        params: { ... },
        ...
      },
      bootParams: [string],
      bridge: {
        address: { ... },
        forwardDns: { ... },
        name: { ... },
        prefixLength: { ... },
        ...
      },
      debug: bool,
      dom0Resources: {
        maxMemory: { ... },
        maxVCPUs: { ... },
        memory: { ... },
        ...
      },
      domains: {
        extraConfig: string,
        ...
      },
      efi: {
        bootBuilderVerbosity: string,
        path: path,
        ...
      },
      enable: bool,
      package: Derivation,
      package-qemu: Derivation,
      qemu: {
        package: Derivation,
        pidFile: path,
        ...
      },
      qemu-package: Derivation,
      store: {
        path: path,
        settings: { ... },
        type: string,
        ...
      },
      stored: path,
      trace: bool,
      ...
    },
    ...
  },
  warnings: [string],
  xdg: {
    autostart: {
      enable: bool,
      ...
    },
    icons: {
      enable: bool,
      fallbackCursorThemes: [string],
      ...
    },
    menus: {
      enable: bool,
      ...
    },
    mime: {
      addedAssociations: { _: { ... } },
      defaultApplications: { _: { ... } },
      enable: bool,
      removedAssociations: { _: { ... } },
      ...
    },
    portal: {
      config: { _: { ... } },
      configPackages: [Derivation],
      enable: bool,
      extraPortals: [Derivation],
      gtkUsePortal: { ... },
      lxqt: {
        enable: bool,
        styles: { ... },
        ...
      },
      wlr: {
        enable: bool,
        settings: { ... },
        ...
      },
      xdgOpenUsePortal: bool,
      ...
    },
    sounds: {
      enable: bool,
      ...
    },
    terminal-exec: {
      enable: bool,
      package: Derivation,
      settings: { _: { ... } },
      ...
    },
    ...
  },
  zramSwap: {
    algorithm: string | string,
    enable: bool,
    memoryMax: int | null,
    memoryPercent: { ... },
    numDevices: { ... },
    priority: int,
    swapDevices: int,
    writebackDevice: path | null,
    ...
  },
  ...
};

val config :: NixosConfig;
val lib :: Lib;
val pkgs :: Pkgs;
val options :: { ... };
val modulesPath :: path;
